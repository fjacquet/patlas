apiVersion: v1
kind: Service
metadata:
  name: {{ include "patlas.fullname" . }}
  labels:
    {{- include "patlas.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "patlas.selectorLabels" . | nindent 4 }}
