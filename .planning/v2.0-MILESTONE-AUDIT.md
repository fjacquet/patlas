---
milestone: v2.0
name: Offline-Capable, Redesigned, Better Deck
audited: 2026-05-20
status: tech_debt
method: inline-execution (no per-phase GSD VERIFICATION/SUMMARY/VALIDATION artifacts — phases 12–18 were executed inline with atomic commits per the project's inline-execution preference; verification evidence is the CI gate battery + visual render verification)
scores:
  requirements: 14/14 implemented (test/build/visual evidence)
  gates: 6/6 green (typecheck · biome · supply-chain · bundle-size · build · 506 tests/80 files)
  browser_uat: 0/2 run (offline boot, web-layout) — outstanding
gaps: {}
tech_debt:
  - area: verification
    items:
      - "Browser UAT not run: PWA offline cold-boot (PWA-02) and the web-layout visual (NAV-01/UIX-*) are verified structurally + by unit tests, not by a live Playwright pass."
  - area: pptx
    items:
      - "DR slide is bare when no scenario is selected (could show a stretched-cluster reservation summary)."
  - area: ui-icons
    items:
      - "UIX-03 icons are hand-authored inline SVG placeholders; swap for the icons-project render_icon exact set once that MCP is enabled (icon prop is the stable seam)."
  - area: method
    items:
      - "No per-phase GSD artifacts (VERIFICATION/SUMMARY/VALIDATION) for 12–18 — Nyquist status MISSING for all v2.0 phases by construction (inline execution). Code is test-gated instead."
---

# v2.0 Milestone Audit — Offline-Capable, Redesigned, Better Deck

**Audited:** 2026-05-20 · **Status:** `tech_debt` (all requirements implemented & test-verified; no blockers; browser UAT + minor polish outstanding)

## Method note

Phases 12–18 were executed **inline** (atomic commits, ROADMAP/STATE tracking) rather than via `/gsd-execute-phase`, so there are no per-phase `VERIFICATION.md` / `SUMMARY.md` / `VALIDATION.md` files to aggregate. This audit instead cross-references `REQUIREMENTS.md` against three evidence sources: (1) the implementing commits, (2) the CI gate battery, (3) visual render verification of the PPTX (`soffice → PDF`). The `.planning/phases/` directory still holds the v1.0 (01–11) artifacts.

## Gate battery (2026-05-20, fresh)

| Gate | Result |
|------|--------|
| `tsc --noEmit` (app + test) | ✓ no errors |
| `biome check .` | ✓ 288 files clean |
| `check:supply-chain` | ✓ OK (ADR-0001 SW envelope enforced) |
| `check:bundle-size` | ✓ index 290.5 KiB / vendor-tanstack 17 KiB ≤ gates |
| `vite build` | ✓ emits `sw.js` + `manifest.webmanifest` + 21-entry precache |
| `vitest run` | ✓ **506 tests, 80/80 files** |

## Requirements coverage (14/14 implemented)

| REQ | Status | Evidence (commit · code) |
|-----|--------|--------------------------|
| GOV-01 ADR-0001 SW exception + PITFALLS rewrite | ✓ satisfied | `feat(12-01)` · `docs/adr/0001-privacy-invariant.md` Amendment (v2.0) |
| GOV-02 supply-chain gate narrowed+strengthened+tests | ✓ satisfied | `feat(12-01)` · `scripts/check-supply-chain.mjs` + `tests/check-supply-chain.test.ts` |
| PWA-01 installable manifest (scope /vatlas/) | ✓ satisfied | `feat(13-01)` · build emits `manifest.webmanifest` scope `/vatlas/` |
| PWA-02 fully offline (precache-only SW) | ✓ structural | `feat(13-01)` · `dist/sw.js` precache 21; **browser cold-boot UAT pending** |
| PWA-03 SW guard-first, no runtimeCaching | ✓ satisfied | `feat(13-01)` · `src/sw.ts` + gate assertion + `tests/pwa-sw.test.ts` |
| PWA-04 smart auto-update | ✓ satisfied | `feat(13-01)` · `src/pwa/registerSW.ts`+`updatePolicy.ts` + tests |
| NAV-01 vertical nav (now LEFT, one column) | ✓ satisfied | `feat(14-01)`/`fix(14-01)`/`feat(15-02)` · `App.tsx`; **visual UAT pending** |
| UIX-01 KPI-tile dashboard | ✓ satisfied | `feat(15-01)` · `StatTile`/`TileSection` |
| UIX-02 cluster detail + per-cluster tile/rows | ✓ satisfied | `feat(15-01)`+`feat(15-02)` · `ClusterDetail` tiles + cluster table rows |
| UIX-03 same-origin inline-SVG icons | ✓ satisfied (placeholder) | `feat(15-01)` · `src/components/icons/`; exact-set swap deferred |
| PLN-01 planning visual return | ✓ satisfied | `feat(16-01)` · `PlannedEstatePanel`+`plannedChartOptions`+test |
| PPT-01 crisp/correctly-sized charts | ✓ satisfied | `feat(17-01)`+`fix(18-03)` · native pptxgenjs shapes (resvg-no-font root cause) |
| PPT-02 dropped facts surfaced | ✓ satisfied | `feat(17-01)`+`feat(18-01..03)` · all slides enriched, visually verified |
| PPT-03 pptx tests | ✓ satisfied | `feat(17-01)` · `builder.test`/`chartSvg.test`/`plannedChartOptions.test` |

**Beyond the requirements:** Phase 18 (not REQ-tracked) rebuilt the entire deck to vsizer parity (brand-free), fixed the resvg-no-font chart-text root cause (all deck visuals now native pptxgenjs), reworked the web layout (one left column + cluster table rows), and centralized stretched management.

## Integration / E2E (inline check)

- Production build succeeds end-to-end; app shell + PWA wired; privacy invariant intact and **extended into the SW scope** (CSP + runtime guard + CI denylist + guard-first `sw.ts`).
- Export pipeline verified: PPTX regenerated from the canary fixture and rendered (soffice→PDF) — title, overview, cluster, storage, network, EOS, planned, inventory all render with correct text/labels.
- No cross-phase wiring breaks found; the single-`useMemo` (`useEstateView`) + inputs-only store invariants held (no second memo introduced by PLN-01).

## Audit findings (fixed during this audit)

- **`tests/pwa-sw.test.ts` failed to collect** ("URL must be of scheme file" — fragile `readFileSync(new URL(import.meta.url))`) → fixed to `resolve(process.cwd(), …)` (`fix(13-01)`); all 80 test files now pass.

## Outstanding before close (tech debt — no blockers)

1. **Consolidated browser UAT (Playwright):** PWA offline cold-boot (PWA-02), the single-left-column + cluster-table layout (NAV-01/UIX-02), planning visual (PLN-01), regenerated deck on real 3-cluster data. Artifacts under `.playwright-mcp/` only (Vite-reload trap).
2. **DR slide** bare when no scenario — optional stretched-cluster reservation summary.
3. **Icon exact-set swap** (UIX-03) once the icons-project `render_icon` MCP is enabled.

## Verdict

All 14 v2.0 requirements are implemented and test/build/visual-verified; no critical gaps or broken flows. Status is `tech_debt` (not `passed`) solely because the live browser UAT has not been run and minor polish (DR slide, icon swap) remains. Safe to `/gsd-complete-milestone v2.0` accepting the tracked debt, or run the UAT first.
