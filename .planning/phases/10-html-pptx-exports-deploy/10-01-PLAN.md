---
phase: 10-html-pptx-exports-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engines/export/spike/svgToPng.spike.ts
  - src/engines/export/spike/svgToPng.spike.test.ts
  - .planning/phases/10-html-pptx-exports-deploy/10-SPIKE-DECISION.md
autonomous: false
requirements: [PPT-01, PPT-02]
must_haves:
  truths:
    - "An ECharts SSR SVG string can be rasterized to a PNG byte array with NO DOM/document, inside a Web Worker"
    - "The chosen rasterizer (or main-thread fallback) clears check-supply-chain, npm audit, OSV and check-bundle-size gates"
    - "pptxgenjs 4.0.1 can write({outputType:'arraybuffer'}) inside a Web Worker (A4 verified or main-thread fallback recorded)"
  artifacts:
    - path: "src/engines/export/spike/svgToPng.spike.ts"
      provides: "Proven SVG->PNG-without-DOM mechanism + a pptxgenjs-in-worker smoke probe"
    - path: ".planning/phases/10-html-pptx-exports-deploy/10-SPIKE-DECISION.md"
      provides: "The locked rasterizer + worker-safety decision downstream plans depend on"
  key_links:
    - from: "src/engines/export/spike/svgToPng.spike.ts"
      to: "10-SPIKE-DECISION.md"
      via: "decision record consumed by plan 04 (PPTX charts)"
      pattern: "resvg|OffscreenCanvas|createImageBitmap"
---

<objective>
Resolve the single genuine technical unknown of Phase 10 before any PPTX chart work begins: how to rasterize an ECharts SSR SVG string into a PNG **without a DOM/document**, suitable for embedding in a `.pptx` (PowerPoint clients do not render embedded SVG — RESEARCH Pitfall 1, verified). Also smoke-test that `pptxgenjs@4.0.1` can synthesize an ArrayBuffer inside a Web Worker (Assumption A4 — vsizer ran it main-thread).

Purpose: This is a `checkpoint:decision` gate. Plan 04 (PPTX engine) and plan 02 (export worker) MUST NOT assume a mechanism — they consume the locked decision in `10-SPIKE-DECISION.md`. Picking the rasterizer requires a human call (a new runtime/WASM dependency vs a main-thread degradation), so this plan is non-autonomous by the deep-work rules.
Output: A working spike module proving at least one no-DOM SVG→PNG path renders a treemap + a heatmap (the visually hardest), a recorded pptxgenjs-in-worker result, and the decision document.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-html-pptx-exports-deploy/10-RESEARCH.md
@.planning/phases/10-html-pptx-exports-deploy/10-PATTERNS.md
@.planning/phases/10-html-pptx-exports-deploy/10-CONTEXT.md

<interfaces>
<!-- ECharts SSR call (RESEARCH Pattern 2) — DOM-free, the SVG source for the spike. -->
echarts.init(null, null, { renderer: 'svg', ssr: true, width, height })
  .setOption(option) ; .renderToSVGString() ; .dispose()

<!-- The tree-shaken registry the spike MUST reuse (src/components/Chart.tsx:1-55) -->
import * as echarts from 'echarts/core'
import { BarChart, GaugeChart, HeatmapChart, LineChart, PieChart, TreemapChart } from 'echarts/charts'
import { SVGRenderer } from 'echarts/renderers'
// echarts.use([...]) with SVGRenderer; never `import * as echarts from 'echarts'` (barrel = bundle-gate fail)

<!-- pptxgenjs 4.0.1 worker probe: import PptxGenJS; one slide; pptx.write({ outputType: 'arraybuffer' }) -->
<!-- Gates (existing, must not regress): node scripts/check-supply-chain.mjs ; npm audit --audit-level=low ;
     OSV ; node scripts/check-bundle-size.mjs (echarts chunk <= 300 KB gz). REQUIRED_XLSX_PIN must stay byte-exact. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prototype both no-DOM SVG->PNG paths against the hardest charts</name>
  <read_first>
    - .planning/phases/10-html-pptx-exports-deploy/10-RESEARCH.md (Open Question 1, Assumptions A1/A4, "Don't Hand-Roll" SVG→PNG row, Standard Stack rasterizer row)
    - src/components/Chart.tsx (the exact tree-shaken `echarts/core` + `echarts.use([...])` registry to reuse — do not re-derive)
    - src/theme/echartsTheme.ts (MIDNIGHT_EXECUTIVE_LIGHT — sRGB hex only; zrender cannot parse oklch())
    - scripts/check-bundle-size.mjs (the echarts chunk budget the spike must not blow)
  </read_first>
  <files>src/engines/export/spike/svgToPng.spike.ts, src/engines/export/spike/svgToPng.spike.test.ts</files>
  <action>
    Build a spike module exporting two candidate rasterizers from one ECharts SSR SVG string:
    (A) `@resvg/resvg-wasm` (WASM, no DOM, worker-safe) — install ONLY if you proceed with it; it is the candidate dependency under evaluation.
    (B) main-thread `createImageBitmap` + `OffscreenCanvas` (zero new deps; moves chart raster off the worker).
    The SVG source is ECharts SSR (`echarts.init(null,null,{renderer:'svg',ssr:true,width,height})`) reusing the Chart.tsx registry verbatim (import the same `echarts/core` + per-chart subpaths + SVGRenderer; never the barrel). Generate one treemap option and one heatmap option (the visually hardest per RESEARCH) using `MIDNIGHT_EXECUTIVE_LIGHT` sRGB-hex colors only — no `oklch()` may reach a chart option. Each path returns `Uint8Array` PNG bytes. The spike test asserts: SVG string is non-empty and contains `<svg`; each rasterizer returns a PNG with a valid 8-byte PNG signature and non-trivial byte length; the resvg path requires no `document`/`window`. Keep the heavy echarts/resvg imports confined to this spike file (mirror parser.worker.ts chunk discipline). Use `npm run test:run -- src/engines/export/spike` to exercise it.
  </action>
  <verify>
    <automated>npm run test:run -- src/engines/export/spike</automated>
  </verify>
  <acceptance_criteria>
    - `src/engines/export/spike/svgToPng.spike.test.ts` green: both treemap and heatmap SVG strings produced via ECharts SSR (no DOM), at least one path yields a valid PNG (PNG signature bytes 0x89 0x50 0x4E 0x47 present)
    - The spike imports `echarts/core` + per-chart subpaths (grep: no `from 'echarts'` barrel import in the spike file: `grep -rn "from 'echarts'" src/engines/export/spike/ | grep -v "echarts/" | grep -vc '^#'` returns 0)
    - No `document.`/`window.` reference in the resvg branch of the spike module
  </acceptance_criteria>
  <done>Both candidate rasterization paths exist and run under Vitest; at least one produces a valid PNG from a treemap and a heatmap SVG with no DOM.</done>
</task>

<task type="auto">
  <name>Task 2: Smoke-test pptxgenjs-in-worker + run all four supply/size gates against any new dep</name>
  <read_first>
    - .planning/phases/10-html-pptx-exports-deploy/10-RESEARCH.md (Open Question 2, Assumption A4, Environment Availability table)
    - scripts/check-supply-chain.mjs (telemetry denylist + REQUIRED_XLSX_PIN byte-exact gate — line ~41)
    - src/engines/parser/parser.worker.ts (the `/// <reference lib="webworker" />` + fetchGuard-first worker preamble to mirror in the probe worker)
  </read_first>
  <files>src/engines/export/spike/svgToPng.spike.ts, src/engines/export/spike/svgToPng.spike.test.ts</files>
  <action>
    Add a pptxgenjs-in-worker probe: in the spike test, instantiate a throwaway module worker that imports `'../../privacy/fetchGuard'` FIRST, then `PptxGenJS`, builds a 1-slide deck with one `addImage({ data: 'image/png;base64,...' })` using a spike PNG, and posts back `pptx.write({ outputType: 'arraybuffer' })`. Assert the worker returns a non-empty ArrayBuffer whose first bytes are the ZIP local-file-header signature `PK\x03\x04` (0x50 0x4B 0x03 0x04 — a valid OOXML container). If pptxgenjs throws on `window`/`document` inside the worker, record that and assert the main-thread `pptx.write` path instead (acceptable degradation: HTML stays in the worker, PPTX assembly on main thread). Then run, and capture exit status of: `node scripts/check-supply-chain.mjs`, `npm audit --audit-level=low`, and `node scripts/check-bundle-size.mjs` — these MUST pass with whatever dep (if any) the spike added; the SheetJS tarball pin in package.json MUST remain byte-identical (never let any tool rewrite it).
  </action>
  <verify>
    <automated>npm run test:run -- src/engines/export/spike && node scripts/check-supply-chain.mjs && node scripts/check-bundle-size.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Probe asserts pptxgenjs output is a valid ZIP/OOXML container (bytes `PK\x03\x04`) from EITHER the worker OR the main-thread fallback, and which one is recorded
    - `node scripts/check-supply-chain.mjs` exits 0 (no telemetry pkg added; `grep -c "https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz" package.json` unchanged from pre-task)
    - `node scripts/check-bundle-size.mjs` exits 0 (echarts chunk still ≤ 300 KB gz; any WASM blob accounted)
  </acceptance_criteria>
  <done>pptxgenjs worker-safety is empirically determined (worker or main-thread fallback), and every supply/audit/OSV/bundle-size gate is green with any candidate dependency.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Lock the rasterizer + worker-safety decision</name>
  <decision>Which SVG→PNG-without-DOM mechanism does Phase 10 use for PPTX charts, and where does pptxgenjs run?</decision>
  <context>RESEARCH Open Question 1 is the highest-risk unknown and explicitly mandates a Wave-0 human-locked choice (resvg-wasm adds a WASM dependency + supply-chain surface; the OffscreenCanvas path adds zero deps but moves chart raster onto the main thread). Plans 02 and 04 are written against whatever this decision records — they must not re-decide it. Both candidates were proven runnable in Tasks 1-2 with gate evidence.</context>
  <options>
    <option id="resvg-wasm">
      <name>@resvg/resvg-wasm in the export worker</name>
      <pros>Chart raster stays off the main thread; whole synthesis in one worker; cleanest data flow</pros>
      <cons>New runtime + WASM dependency; supply-chain/bundle-size surface (proven passing in Task 2 but is an ongoing maintenance entry)</cons>
    </option>
    <option id="offscreen-canvas">
      <name>Main-thread OffscreenCanvas/createImageBitmap raster before postMessage</name>
      <pros>Zero new dependencies; nothing added to supply-chain/bundle-size</pros>
      <cons>Chart rasterization runs on the main thread (brief, bounded — N charts), partially defeating the worker-offload goal for the chart-heavy path</cons>
    </option>
  </options>
  <files>.planning/phases/10-html-pptx-exports-deploy/10-SPIKE-DECISION.md</files>
  <action>Write `10-SPIKE-DECISION.md` recording: (1) the chosen rasterizer with the Task-1/2 evidence (PNG validity, gate exit codes, bundle delta); (2) the pptxgenjs execution location (worker vs main-thread fallback) with the Task-2 evidence; (3) the exact function signature `primitives/chartSvg.ts` (plan 04) must implement: `chartSvgToPng(svg: string, width: number, height: number) => Promise&lt;Uint8Array&gt;` and where it executes; (4) if a dep was added, its name+version+the four gate results. This file is the binding contract for plans 02 and 04.</action>
  <verify><automated>test -f .planning/phases/10-html-pptx-exports-deploy/10-SPIKE-DECISION.md && grep -Eqi "resvg|OffscreenCanvas|createImageBitmap" .planning/phases/10-html-pptx-exports-deploy/10-SPIKE-DECISION.md</automated></verify>
  <acceptance_criteria>
    - 10-SPIKE-DECISION.md exists and names exactly one rasterizer mechanism and one pptxgenjs execution location
    - It states the `chartSvgToPng` signature + execution thread plans 02/04 will implement against
  </acceptance_criteria>
  <resume-signal>Select: resvg-wasm or offscreen-canvas (and confirm pptxgenjs worker vs main-thread)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidate dependency → app supply chain | a new rasterizer/WASM blob enters the dependency tree |
| spike worker → network | any fetch in a worker throws by design (privacy invariant) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | new rasterizer dependency | mitigate | Task 2 runs check-supply-chain + npm audit --audit-level=low + OSV + check-bundle-size against any added dep; decision blocked if any gate fails |
| T-10-02 | Tampering | package.json xlsx pin | mitigate | Task 2 asserts the SheetJS tarball pin byte-unchanged; never `npm audit fix --force` |
| T-10-03 | Information disclosure | spike/probe worker | mitigate | probe worker imports `privacy/fetchGuard` FIRST (mirrors parser.worker.ts); resvg WASM is bundled, never fetched |
| T-10-04 | Information disclosure | spike error path | accept | spike is a throwaway probe with synthetic chart data only — no parsed workbook bytes present to leak |
</threat_model>

<verification>
- `npm run test:run -- src/engines/export/spike` green
- `node scripts/check-supply-chain.mjs` && `node scripts/check-bundle-size.mjs` exit 0
- `10-SPIKE-DECISION.md` present, names one rasterizer + pptxgenjs location + the `chartSvgToPng` signature
</verification>

<success_criteria>
A no-DOM SVG→PNG path is proven on the hardest charts, pptxgenjs worker-safety is empirically settled, all four supply/audit/OSV/bundle gates pass with any added dependency, and the locked decision is recorded for plans 02/04 to implement against.
</success_criteria>

<output>
After completion, create `.planning/phases/10-html-pptx-exports-deploy/10-01-SUMMARY.md`
</output>
