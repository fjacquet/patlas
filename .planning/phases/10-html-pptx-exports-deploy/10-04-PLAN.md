---
phase: 10-html-pptx-exports-deploy
plan: 04
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - src/engines/export/pptx/theme.ts
  - src/engines/export/pptx/format.ts
  - src/engines/export/pptx/format.test.ts
  - src/engines/export/pptx/primitives/colors.ts
  - src/engines/export/pptx/primitives/kpiCard.ts
  - src/engines/export/pptx/primitives/progressBar.ts
  - src/engines/export/pptx/primitives/chartSvg.ts
  - src/engines/export/pptx/primitives/chartSvg.test.ts
  - src/engines/export/pptx/slides/titleSlide.ts
  - src/engines/export/pptx/slides/overviewSlide.ts
  - src/engines/export/pptx/slides/clusterSlide.ts
  - src/engines/export/pptx/slides/contentionAnnex.ts
  - src/engines/export/pptx/slides/eosSlide.ts
  - src/engines/export/pptx/slides/drSimSlide.ts
  - src/engines/export/pptx/slides/trendsSlide.ts
  - src/engines/export/pptx/slides/inventorySlide.ts
  - src/engines/export/pptx/builder.ts
  - src/engines/export/pptx/builder.test.ts
requirements: [PPT-01, PPT-02, PPT-03, PPT-04]
autonomous: true
must_haves:
  truths:
    - "buildPptx produces a valid OOXML ArrayBuffer that opens in PowerPoint with no Repair errors"
    - "Charts are embedded as rasterized PNG (never SVG) so PowerPoint renders them"
    - "One slide per cluster always (D-01); slide list is the fixed PPT-03 order; trends slide omitted at <2 snapshots (D-09)"
    - "Numbers are locale-formatted: FR ,/U+00A0 (after U+202F substitution), EN ,/. (PPT-04)"
  artifacts:
    - path: "src/engines/export/pptx/builder.ts"
      provides: "Pure composition root assembling all slides -> ArrayBuffer"
      exports: ["buildPptx"]
    - path: "src/engines/export/pptx/format.ts"
      provides: "Locale-parameterized number formatter (FR U+202F->U+00A0 net-new transform)"
      exports: ["pptxNumber", "pptxSafeFormat"]
    - path: "src/engines/export/pptx/primitives/chartSvg.ts"
      provides: "chart->PNG via the Wave-0-locked rasterizer (never SVG into pptx)"
      exports: ["chartSvgToPng"]
  key_links:
    - from: "src/engines/export/pptx/builder.ts"
      to: "src/engines/export/buildExportView.ts (plan 02)"
      via: "consumes EstateView + trends; one clusterSlide per cluster"
      pattern: "clusterSlide|EstateView"
    - from: "src/engines/export/pptx/primitives/chartSvg.ts"
      to: ".planning/phases/10-html-pptx-exports-deploy/10-SPIKE-DECISION.md (plan 01)"
      via: "implements the locked chartSvgToPng signature/execution thread"
      pattern: "chartSvgToPng"
---

<objective>
Build the PPTX deck engine: ported-fresh vsizer-shape modules (`theme`, `format`, `primitives`, `builder`, the 4 ported slides) + 4 net-new slides (eos/drSim/trends/inventory), with **every chart rasterized to PNG** via the Wave-0-locked mechanism (PowerPoint cannot render embedded SVG — RESEARCH Pitfall 1). One slide per cluster always (D-01); fixed PPT-03 slide order; trends slide omitted at <2 snapshots (D-09); locale-parameterized numbers with the net-new FR U+202F→U+00A0 substitution (PPT-04, Moderate-8).

Purpose: Closes PPT-01..04. Depends on plan 01 (the locked rasterizer + pptxgenjs execution decision in `10-SPIKE-DECISION.md`) and plan 02 (`buildExportView`, `chartToSvg`, `pptx` namespace). `src/engines/export/` does not exist in this repo — every "port" file is built FRESH against the cited in-repo analog, never copied from a vsizer path.
Output: A full `src/engines/export/pptx/` tree, a golden-PPTX structural snapshot test, and a both-locale `format.ts` test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-html-pptx-exports-deploy/10-RESEARCH.md
@.planning/phases/10-html-pptx-exports-deploy/10-PATTERNS.md
@.planning/phases/10-html-pptx-exports-deploy/10-CONTEXT.md
@.planning/phases/10-html-pptx-exports-deploy/10-UI-SPEC.md
@.planning/phases/10-html-pptx-exports-deploy/10-01-SUMMARY.md
@.planning/phases/10-html-pptx-exports-deploy/10-SPIKE-DECISION.md
@.planning/phases/10-html-pptx-exports-deploy/10-02-SUMMARY.md

<interfaces>
<!-- From plan 01 (10-SPIKE-DECISION.md): the LOCKED chartSvgToPng signature + execution thread.
     chartSvgToPng(svg: string, width: number, height: number) => Promise<Uint8Array>  (PNG bytes)
     plus whether pptxgenjs runs in the worker or main thread (plan 05 wires accordingly). -->
<!-- From plan 02: buildExportView(...) => { view: EstateView, trends: TrendSeries|null }; chartToSvg(opt,w,h):string; pptx i18n ns -->

<!-- pptxgenjs 4.0.1: new PptxGenJS(); slide.addImage({ data:'image/png;base64,...' });
     pptx.write({ outputType:'arraybuffer' }). NEVER addImage an image/svg+xml data URI (PowerPoint renders blank). -->

<!-- Port analogs (fresh build, NO in-repo path to copy):
     builder.ts  ← shape of src/engines/aggregation/estateView.ts:64-95 (pure composition root, typed params, no React/Zustand/Zod)
     theme.ts / primitives/* ← shape of src/theme/echartsTheme.ts (palette-const module; Midnight Executive hex, NO '#' prefix; brand-free PPT-02/D-03)
     format.ts ← shape of src/utils/format.ts:1-20 (every fmt* takes locale, toLocaleString(locale,…), em-dash on non-finite).
       NET-NEW: FR U+202F→U+00A0 substitution (Moderate-8) is NOT in src/utils/format.ts — add it here only.
     slides/* ← shape of src/components/storage/DatastoreDetail.tsx (screen-fit presenter = slide contract) -->

<!-- PPT-03 fixed slide order: title · overview · per-cluster (one each, D-01, no cap) ·
     CPU-Ready annex (conditional) · EOS · DR · trends (omit if <2 snapshots, D-09) · inventory summary -->
<!-- D-03 title slide: vCenter name(s) · clusterCount · hostCount · vmCount · captured {date} ·
     'Generated by vatlas — factual estate synthesis'. Numbers via format.ts locale. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: theme + locale-parameterized format (FR U+202F->U+00A0) + primitives + chartSvg raster</name>
  <read_first>
    - src/theme/echartsTheme.ts (palette-const module shape for theme.ts/primitives; sRGB hex)
    - src/utils/format.ts (lines 1-20 — every formatter takes `locale`, uses toLocaleString(locale,…), em-dash on non-finite; mirror exactly)
    - .planning/phases/10-html-pptx-exports-deploy/10-SPIKE-DECISION.md (the LOCKED chartSvgToPng signature + execution thread + any added dep)
    - .planning/phases/10-html-pptx-exports-deploy/10-RESEARCH.md (Pitfall 1 PowerPoint-no-SVG, Pitfall 4 FR-only format bug, §Code Examples PPTX chart embed)
  </read_first>
  <files>src/engines/export/pptx/theme.ts, src/engines/export/pptx/format.ts, src/engines/export/pptx/format.test.ts, src/engines/export/pptx/primitives/colors.ts, src/engines/export/pptx/primitives/kpiCard.ts, src/engines/export/pptx/primitives/progressBar.ts, src/engines/export/pptx/primitives/chartSvg.ts, src/engines/export/pptx/primitives/chartSvg.test.ts</files>
  <behavior>
    - `pptxNumber(12345.6, 'en')` → `12,345.6`; `pptxNumber(12345.6, 'fr')` → FR grouping with U+00A0 (after U+202F substitution), `,` decimal
    - A non-finite input returns the em-dash sentinel `—` (never `0`/`NaN`)
    - `chartSvgToPng(svg, w, h)` returns PNG bytes (valid PNG signature) using exactly the Wave-0-locked mechanism; it never returns or accepts an SVG data URI
    - theme/primitives expose Midnight Executive hex with NO `#` prefix (pptxgenjs convention), brand-free
  </behavior>
  <action>
    Build FRESH (no vsizer path exists): `theme.ts` + `primitives/{colors,kpiCard,progressBar}.ts` as palette-const/helper modules shaped like `echartsTheme.ts` — Midnight Executive hex without `#` (pptxgenjs convention), brand-free (PPT-02/D-03), no React. `format.ts` mirroring `src/utils/format.ts`'s locale-param pattern (`toLocaleString(locale,…)`, em-dash `—` on non-finite) and ADDING the net-new FR transform: after FR grouping, substitute U+202F → U+00A0 (Moderate-8 — this transform is absent from `src/utils/format.ts`, add it here only); export `pptxNumber(n, locale)` and `pptxSafeFormat`. `primitives/chartSvg.ts` implements EXACTLY the `chartSvgToPng(svg, width, height): Promise<Uint8Array>` signature and execution thread locked in `10-SPIKE-DECISION.md` (consume the spike module / chosen dep) plus an `addChartImage(slide, pngBytes, layout)` helper that calls `slide.addImage({ data: 'image/png;base64,' + base64(pngBytes), ... })` — it must NEVER pass an `image/svg+xml` data URI (PowerPoint renders blank — Pitfall 1). Tests: `format.test.ts` asserts EN `12,345.6` and FR U+00A0-grouped `,`-decimal and em-dash on non-finite; `chartSvg.test.ts` rasterizes one ECharts SSR SVG (via plan-02 `chartToSvg`) and asserts a valid PNG signature and that the addImage payload mime is `image/png`.
  </action>
  <verify>
    <automated>npm run test:run -- src/engines/export/pptx/format src/engines/export/pptx/primitives/chartSvg && node scripts/check-supply-chain.mjs && node scripts/check-bundle-size.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `format.test.ts` green for both locales; FR output contains U+00A0 (` `) and NOT U+202F (` `); non-finite → `—`
    - `chartSvg.test.ts` asserts PNG signature bytes and `image/png` mime (grep: no `image/svg+xml` literal in chartSvg.ts → `grep -c "svg+xml" src/engines/export/pptx/primitives/chartSvg.ts` returns 0)
    - theme/primitives hex constants carry no leading `#` (pptxgenjs convention)
    - supply-chain + bundle-size gates exit 0 (any Wave-0 dep still passing)
  </acceptance_criteria>
  <done>Locale-correct numbers (incl. the net-new FR substitution), brand-free Midnight Executive theme/primitives, and a PowerPoint-safe chart→PNG path exist.</done>
</task>

<task type="auto">
  <name>Task 2: Eight slide modules (4 ported-fresh + 4 net-new) — screen-fit, factual</name>
  <read_first>
    - src/components/storage/DatastoreDetail.tsx (the screen-fit presenter = slide contract; one viewport = one 16:9 slide)
    - src/types/estate.ts (ClusterAggregate / OperationalInsights / EosProjection / DrSimResult / TrendSeries / GlobalSummary shapes)
    - .planning/phases/10-html-pptx-exports-deploy/10-UI-SPEC.md (PPTX Deck Visual Contract: D-01 one-per-cluster always, D-02 low text density, D-03 title slide)
    - src/engines/export/pptx/primitives/chartSvg.ts (the Task-1 PNG raster + addChartImage helper)
  </read_first>
  <files>src/engines/export/pptx/slides/titleSlide.ts, src/engines/export/pptx/slides/overviewSlide.ts, src/engines/export/pptx/slides/clusterSlide.ts, src/engines/export/pptx/slides/contentionAnnex.ts, src/engines/export/pptx/slides/eosSlide.ts, src/engines/export/pptx/slides/drSimSlide.ts, src/engines/export/pptx/slides/trendsSlide.ts, src/engines/export/pptx/slides/inventorySlide.ts</files>
  <action>
    Build 8 slide modules, each a pure `addXSlide(pptx, data, strings, locale)` function shaped like the DatastoreDetail screen-fit presenter (low text density per D-02, factual-only per HTM-05/PPT, numbers via Task-1 `pptxNumber`, em-dash sentinel). Ported-fresh: `titleSlide` (D-03 estate identity/freshness line + `Generated by vatlas — factual estate synthesis`, brand-free), `overviewSlide` (estate KPI headline), `clusterSlide` (one cluster — chart via `addChartImage`/`chartSvgToPng`), `contentionAnnex` (CPU-Ready, emitted only when readiness data present — conditional). Net-new: `eosSlide` (P7 `EosProjection` buckets), `drSimSlide` (P6 DR scenario physical impact), `trendsSlide` (P8 `TrendSeries`), `inventorySlide` (inventory summary). All chart visuals go through `chartSvgToPng` PNG — never an SVG data URI. Strings come from the `pptx` namespace (passed in; engines stay pure — no i18next import). No editorial verbs, no traffic-light.
  </action>
  <verify>
    <automated>npm run typecheck && npm run test:run -- src/engines/export/pptx</automated>
  </verify>
  <acceptance_criteria>
    - All 8 slide modules export a typed pure `add*Slide` function; `npm run typecheck` passes
    - No slide imports React/Zustand/i18next (`grep -lE "from 'react'|from 'zustand'|i18next" src/engines/export/pptx/slides/*.ts` returns nothing)
    - No slide constructs an `image/svg+xml` payload (`grep -rc "svg+xml" src/engines/export/pptx/slides/ | grep -v ':0' | wc -l` is 0)
    - Factual-only: `grep -riE "recommend|should|poor\b|good\b|critical|warning" src/engines/export/pptx/slides/*.ts` returns nothing
  </acceptance_criteria>
  <done>All 8 PPT-03 slide modules exist as pure presenters with PowerPoint-safe PNG charts and factual-only content.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: builder.ts composition root + golden-PPTX structural snapshot</name>
  <read_first>
    - src/engines/aggregation/estateView.ts (lines 64-95 — the pure composition-root shape builder.ts must match: typed params, no React/Zustand/Zod, single-pass JSDoc)
    - src/engines/export/buildExportView.ts (plan 02 — the { view, trends } the builder consumes)
    - .planning/phases/10-html-pptx-exports-deploy/10-CONTEXT.md (D-01 one-slide-per-cluster always, D-09 trends omission)
    - .planning/phases/10-html-pptx-exports-deploy/10-RESEARCH.md (golden-PPTX structural snapshot test guidance; A4 worker/main-thread per spike decision)
  </read_first>
  <files>src/engines/export/pptx/builder.ts, src/engines/export/pptx/builder.test.ts</files>
  <behavior>
    - `buildPptx(view, trends, strings, locale): Promise<ArrayBuffer>` emits slides in the fixed PPT-03 order
    - Exactly one cluster slide per cluster in `view.clusters` — a 50-cluster view yields 50 cluster slides (D-01, no cap)
    - The trends slide is present iff `trends` is non-null (D-09)
    - The contention/CPU-Ready annex appears only when readiness data is present (conditional, PPT-03)
    - Output is a valid OOXML container (ZIP `PK\x03\x04` signature) — opens without Repair
  </behavior>
  <action>
    Create `builder.ts` exporting `buildPptx(view, trends, strings, locale)` shaped like `estateView.ts`'s pure composition root (typed params, no React/Zustand/Zod, single-pass JSDoc): `new PptxGenJS()`, then dispatch slides in fixed PPT-03 order — title → overview → one `clusterSlide` per `view.clusters` entry (D-01, no top-N cap) → `contentionAnnex` if readiness present → eos → drSim → trends (only if `trends` non-null — D-09) → inventory; return `pptx.write({ outputType: 'arraybuffer' })`. Honor the pptxgenjs execution-thread decision from `10-SPIKE-DECISION.md` (the function stays pure either way; plan 05 wires it into the worker or main thread accordingly). `builder.test.ts` is the golden-PPTX STRUCTURAL snapshot: from a synthetic multi-cluster view assert (a) output bytes start with `PK\x03\x04`; (b) slide count = 2 + clusterCount + (readiness?1:0) + 3 + (trends?1:0) [title+overview + N clusters + annex? + eos+drSim+inventory + trends?]; (c) a 50-cluster fixture yields 50 cluster slides (D-01); (d) a 1-snapshot view (trends null) has NO trends slide (D-09); run once per locale (EN+FR) asserting locale-formatted numbers reached the deck.
  </action>
  <verify>
    <automated>npm run test:run -- src/engines/export/pptx/builder</automated>
  </verify>
  <acceptance_criteria>
    - Test asserts buildPptx output begins with ZIP signature `PK\x03\x04` (valid OOXML, opens without Repair)
    - Test asserts a 50-cluster fixture produces exactly 50 cluster slides (D-01) and a trends-null view produces no trends slide (D-09)
    - Golden structural snapshot runs for both `'en'` and `'fr'` and is committed
    - `grep -lE "from 'react'|from 'zustand'" src/engines/export/pptx/builder.ts` returns nothing (pure)
  </acceptance_criteria>
  <done>One pure call assembles the full PPT-03 deck in fixed order, one slide per cluster always, trends conditional, as a valid OOXML ArrayBuffer with a both-locale golden snapshot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-derived names → slide text | VM/cluster names placed into pptxgenjs text runs |
| Wave-0 rasterizer dep → supply chain | the locked rasterizer is part of the dependency tree |
| generated .pptx → recipient's PowerPoint | the deck is opened on an untrusted machine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-13 | Tampering | malicious name injected into a slide | mitigate | pptxgenjs writes text as OOXML text runs (not markup); names pass as plain `text:` data, never a raw XML escape hatch |
| T-10-14 | Information disclosure | rasterizer/font dep adding telemetry or fetching | mitigate | Wave-0 dep already cleared check-supply-chain/audit/OSV/bundle-size in plan 01; Task 1 re-runs the supply-chain + bundle-size gates |
| T-10-15 | Tampering | SVG data URI silently rendering blank (data-integrity of the deliverable) | mitigate | chartSvg never emits `image/svg+xml`; grep gate + golden snapshot; PNG-only path (Pitfall 1) |
| T-10-16 | Repudiation | corrupt OOXML triggering PowerPoint "Repair" (deliverable unusable) | mitigate | builder.test.ts asserts a valid `PK\x03\x04` ZIP/OOXML container signature |
</threat_model>

<verification>
- `npm run test:run -- src/engines/export/pptx` green (format both-locale + golden structural snapshot)
- `npm run typecheck` passes
- `node scripts/check-supply-chain.mjs` && `node scripts/check-bundle-size.mjs` exit 0
</verification>

<success_criteria>
`buildPptx` produces a valid OOXML ArrayBuffer with the fixed PPT-03 slide list, one slide per cluster always (D-01), trends conditional (D-09), PowerPoint-renderable PNG charts (never SVG), and locale-correct numbers in both EN and FR (incl. the net-new FR U+202F→U+00A0 substitution).
</success_criteria>

<output>
After completion, create `.planning/phases/10-html-pptx-exports-deploy/10-04-SUMMARY.md`
</output>
