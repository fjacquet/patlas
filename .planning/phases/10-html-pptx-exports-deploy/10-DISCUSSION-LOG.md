# Phase 10: HTML + PPTX Exports & Deploy - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-18
**Phase:** 10-html-pptx-exports-deploy
**Areas discussed:** PPTX deck shape, Export trigger UX, Locale & multi-snapshot in exports

> Gray area "HTML report IA & depth" was offered but NOT selected by the user → left to ROADMAP's fixed HTM-04 section list + size budget (Claude's discretion within those).

---

## PPTX deck shape

### Per-cluster scaling
| Option | Description | Selected |
|--------|-------------|----------|
| Top-N + rollup slide (Recommended) | Top-N clusters get a slide each, remainder summarized on one table slide | |
| One slide per cluster, always | Faithful 1:1 (vsizer); 50-cluster estate = 50+ slides | ✓ |
| You decide per estate size | Claude picks a threshold heuristic | |

### Deck vs HTML relationship
| Option | Description | Selected |
|--------|-------------|----------|
| Tighter executive subset (Recommended) | Low-density slides per PPT-03; HTML carries detail | ✓ |
| Mirror the HTML report | Deck depth tracks the report section-for-section | |

### Title slide
| Option | Description | Selected |
|--------|-------------|----------|
| Estate identity + freshness (Recommended) | vCenter(s), capture date, counts, "Generated by vatlas", brand-free | ✓ |
| Minimal | "VMware Estate Report" + capture date only | |
| You decide | Claude composes within brand-free/no-editorial constraint | |

**User's choice:** 1:1 per cluster always; executive-subset density; factual estate-identity title slide.
**Notes:** User explicitly rejected the top-N cap — long decks for large estates are acceptable.

---

## Export trigger UX

### Control style & placement
| Option | Description | Selected |
|--------|-------------|----------|
| One 'Export' menu in the header (Recommended) | Single header control, menu with HTML/PPTX | |
| Two buttons in the header | Separate Export HTML / Export PPTX header buttons | ✓ |
| Dedicated Export view (ViewToggle segment) | New view with both options + preview | |

### Worker progress feedback
| Option | Description | Selected |
|--------|-------------|----------|
| Sonner toast: progress → done/error (Recommended) | Reuse mounted `<Toaster/>`; control disabled+spinner | |
| Inline progress bar on the control | Determinate/indeterminate bar on the button | |
| You decide | Claude picks the lightest adequate pattern | ✓ |

### Filename convention
| Option | Description | Selected |
|--------|-------------|----------|
| vatlas_{vCenter}_{captureDate}.{ext} (Recommended) | Sanitized vCenter token, ISO date | ✓ |
| vatlas_estate_{exportTimestamp}.{ext} | Generation timestamp, no vCenter | |
| You decide | Claude picks a collision-resistant sanitized convention | |

**User's choice:** Two header buttons; progress = Claude's discretion; `vatlas_{vCenter}_{captureDate}.{ext}`.
**Notes:** Progress feedback delegated to Claude — documented choice is the sonner-toast + disabled-button-spinner pattern (no new surface).

---

## Locale & multi-snapshot in exports

### Export locale
| Option | Description | Selected |
|--------|-------------|----------|
| Active locale only (Recommended) | Renders in the current FR/EN; one artifact = one language | ✓ |
| Self-contained FR/EN toggle in the HTML | Both languages embedded; doubles text vs budget | |
| Always both — two files | One FR + one EN file per export | |

### Multi-snapshot scope
| Option | Description | Selected |
|--------|-------------|----------|
| Merged estate + trends section (Recommended) | Export the merged P4 EstateView the app shows + trends | |
| Active snapshot only + trends | Body = active snapshot's estate; trends span all snapshots | ✓ |
| You decide | Claude aligns to the shipped EstateView surface | |

### Single snapshot behavior
| Option | Description | Selected |
|--------|-------------|----------|
| Omit the trends section entirely (Recommended) | No trends section/slide with one snapshot | ✓ |
| Keep section with a factual 'single snapshot' line | Section present, explicit message | |
| You decide | Claude picks per factual-only principle | |

**User's choice:** Active locale only; **active snapshot's** estate (not merged) + cross-snapshot trends; omit trends entirely when single snapshot.
**Notes:** "Active snapshot only" is a deliberate divergence from the dashboard's merged multi-vCenter view — flagged in CONTEXT.md D-08 as planner-binding.

## Claude's Discretion

- Worker progress feedback (D-06) — documented as sonner-toast + disabled-button-spinner.
- HTML report IA & depth — not selected for discussion; follows ROADMAP HTM-04 section list within the <5/<15 MB budget; per-cluster depth and P9 Storage/Network section placement at Claude's discretion (researcher to size against a realistic large estate).

## Deferred Ideas

None — discussion stayed within phase scope. HTML in-file FR/EN toggle and dual-file export were considered and rejected (not deferred); PDF export remains out of scope per CLAUDE.md.
