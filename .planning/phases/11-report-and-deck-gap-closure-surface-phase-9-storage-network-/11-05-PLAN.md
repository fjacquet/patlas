---
phase: 11-report-and-deck-gap-closure
plan: 05
type: execute
wave: 2
depends_on: ["11-01", "11-02"]
files_modified:
  - src/engines/export/pptx/slides/storageSlide.ts
  - src/engines/export/pptx/slides/networkSlide.ts
  - src/engines/export/pptx/slides/plannedSlide.ts
  - src/engines/export/pptx/builder.ts
  - src/engines/export/pptx/builder.test.ts
autonomous: true
requirements: [STG-01, STG-02, STG-03, STG-04, STG-05, NET-01, NET-02, NET-03, NET-04, NET-05, ALR-01, ALR-02, ALR-03, ALR-04, ALR-05, VSR-01, VSR-02, VSR-03, VSR-04, VSR-05, PLN-03, PLN-04]
must_haves:
  truths:
    - "The PPTX deck contains a Storage slide (KPI row + the storageTreemap chart panel + factual flagged-count KPI)"
    - "The PPTX deck contains a Network slide (KPI-only, no chart — D-08)"
    - "The PPTX deck contains a planned-vs-measured slide (F-1 export side)"
    - "Slides compose ONLY _layout.ts primitives; no per-slide raw addText/addShape"
  artifacts:
    - path: "src/engines/export/pptx/slides/storageSlide.ts"
      provides: "addStorageSlide — KPI row + treemap panel + flagged-count KPI"
      contains: "addStorageSlide"
    - path: "src/engines/export/pptx/slides/networkSlide.ts"
      provides: "addNetworkSlide — KPI-only, no chart"
      contains: "addNetworkSlide"
    - path: "src/engines/export/pptx/slides/plannedSlide.ts"
      provides: "addPlannedSlide — measured-vs-planned KPI row"
      contains: "addPlannedSlide"
    - path: "src/engines/export/pptx/builder.ts"
      provides: "the three new slides composed in the fixed PPT-03 order"
      contains: "addStorageSlide"
  key_links:
    - from: "src/engines/export/pptx/builder.ts"
      to: "png('storageTreemap')"
      via: "the existing shared-PNG png() helper resolving the 11-01 storageTreemap key"
      pattern: "png\\('storageTreemap'\\)"
    - from: "src/engines/export/pptx/slides/storageSlide.ts"
      to: "view.storage.estate + view.flags.counts"
      via: "addKpiRow cards (provisioned/inUse/capacity + flagged count)"
      pattern: "view\\.flags\\.counts"
---

<objective>
F-2 (deck side) + F-1 (deck side): add three new PPTX slides — **Storage** (KPI row + the 11-01 `storageTreemap` chart panel + a factual flagged-datastores count KPI), **Network** (KPI-only, no chart per D-08), and **Planned** (measured-vs-planned KPI row, F-1) — composed only from `_layout.ts` primitives, and wire them into `builder.ts` at a coherent spot in the fixed PPT-03 order.

Purpose: an entire shipped Phase 9 reaches neither shareable artifact and the planned re-aggregation never exports. "The report is the product" — the deck must carry the executive subset (D-02: tighter executive slides; exhaustive detail lives in the HTML report via plan 11-06).
Output: storageSlide.ts, networkSlide.ts, plannedSlide.ts + builder.ts wiring + golden-structure test extension.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-report-and-deck-gap-closure-surface-phase-9-storage-network-/11-CONTEXT.md
@.planning/phases/11-report-and-deck-gap-closure-surface-phase-9-storage-network-/11-PATTERNS.md
@.planning/phases/11-report-and-deck-gap-closure-surface-phase-9-storage-network-/11-01-SUMMARY.md
@.planning/phases/11-report-and-deck-gap-closure-surface-phase-9-storage-network-/11-02-SUMMARY.md

<interfaces>
_layout.ts primitives: addHeader(s, title) → y ; addKpiRow(s, cards, y) → y2 ; addChartPanel(s, png, box, caption) ; addNote(s, text, y) ; exports CONTENT_W, M (src/engines/export/pptx/slides/_layout.ts)
eosSlide.ts is the KPI+chart analog (src/engines/export/pptx/slides/eosSlide.ts:9-37) ; drSimSlide.ts is the KPI+chart+null-guard analog (drSimSlide.ts:12-49)
pptxNumber(n, locale), ExportLocale (src/engines/export/pptx/format.ts) ; ExportStrings (src/engines/export/types.ts)
StorageByX.estate: { provisionedMib; inUseMib; capacityMib; usedMib; freeMib } (storageByX.ts:57-63) — branded MiB
ThresholdFlags.counts: { fs: number; ds: number; lu: number } (thresholdFlags.ts:50)
NetworkRollup: { vswitches[]; dvswitches[]; portgroups[]; vmPortgroupCount } (network.ts:53-59)
EstateView.plannedView: { globals: GlobalSummary; clusters: ClusterAggregate[] } | null (estate.ts:494)
GlobalSummary safe fields: vmCount, hostCount, clusterCount, vcpuPerPcpu (builder.ts:56-60 usage)
builder.ts: fixed-order call sequence :53-99 ; png(k) = shared?.get(k) helper :51 ; the per-cluster loop :79-86 ; addEosSlide/addDrSimSlide call-sites :93-94
Branded MiB→GiB: Math.round(Number(x) / 1024) (drSimSlide.ts:34, chartBundle.ts:127) — never * 1.048576
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: addStorageSlide — KPI row + treemap panel + factual flagged-count KPI (D-04/D-06/D-07)</name>
  <files>src/engines/export/pptx/slides/storageSlide.ts</files>
  <read_first>
    - src/engines/export/pptx/slides/eosSlide.ts (the exact module shape to replicate — import set, signature, `addHeader`→`addKpiRow`→`addChartPanel` sequence, the `{ x: M, y: y2, w: CONTENT_W, h: 7.15 - y2 }` chart box)
    - src/engines/export/pptx/slides/drSimSlide.ts:34 (the branded `Math.round(Number(x) / 1024)` MiB→GiB conversion to copy)
    - src/engines/export/pptx/slides/_layout.ts (the primitives + `CONTENT_W`/`M` exports; the gold factual rule painted by `addHeader` :34-41 — that IS the D-06 gold marker, do NOT add a second colour)
    - src/engines/aggregation/storageByX.ts:57-63 (StorageByX.estate field shape) + src/engines/aggregation/thresholdFlags.ts:43-51 (ThresholdFlags.counts)
    - .planning/phases/11-report-and-deck-gap-closure-surface-phase-9-storage-network-/11-PATTERNS.md § storageSlide.ts (the verbatim skeleton + the D-06 flag-KPI card spec)
    - .planning/phases/11-report-and-deck-gap-closure-surface-phase-9-storage-network-/11-02-SUMMARY.md (the resolved `storage.*` i18n keys available)
    - CLAUDE.md § Conventions/Gotchas (factual-only — no "recommend/should/poor/good/over/risk/warning"; gold marker only, no traffic-light; branded units never `* 1.048576`)
  </read_first>
  <action>
    Create src/engines/export/pptx/slides/storageSlide.ts copying eosSlide.ts's module shape verbatim. Signature: `export function addStorageSlide(pptx: PptxGenJS, view: EstateView, chartPng: Uint8Array | undefined, strings: ExportStrings, locale: ExportLocale): void`. Body: `const s = pptx.addSlide()`; `const y = addHeader(s, strings['storage.title'] ?? 'Storage')`; `const y2 = addKpiRow(s, [...], y)` with cards: provisioned = `{ label: strings['storage.provisioned'] ?? 'Provisioned (GiB)', value: pptxNumber(Math.round(Number(view.storage.estate.provisionedMib) / 1024), locale) }`, in-use = same pattern on `view.storage.estate.inUseMib` with `strings['storage.inUse']`, capacity = same on `view.storage.estate.capacityMib` with `strings['storage.capacity']`, and the D-06 factual flagged KPI `{ label: strings['storage.flagged'] ?? 'Flagged datastores', value: pptxNumber(view.flags.counts.ds + view.flags.counts.lu, locale) }` (count only — the gold rule from `addHeader` is the only marker; NO traffic-light colour, NO verb anywhere). Then `addChartPanel(s, chartPng, { x: M, y: y2, w: CONTENT_W, h: 7.15 - y2 }, strings['storage.byCluster'] ?? 'Storage by cluster')` — `chartPng` is the `storageTreemap` shared PNG passed from builder.ts (Task 4). Use `Math.round(Number(x) / 1024)` for every MiB→GiB (drSimSlide.ts:34) — NEVER `* 1.048576`, NEVER `/1000`. Compose ONLY `_layout.ts` primitives — no `s.addText`/`s.addShape`. Imports match eosSlide.ts exactly (Biome auto-organizes).
  </action>
  <verify>
    <automated>npx tsc -b 2>&1 | tail -5 && npx @biomejs/biome check src/engines/export/pptx/slides/storageSlide.ts</automated>
  </verify>
  <acceptance_criteria>
    - storageSlide.ts exports `addStorageSlide` (`grep -c "export function addStorageSlide" src/engines/export/pptx/slides/storageSlide.ts` is 1)
    - It references `view.flags.counts` and `view.storage.estate` (both grep > 0)
    - It calls `addHeader`, `addKpiRow`, `addChartPanel` and imports them from `./_layout` (no `s.addText`/`s.addShape` — `grep -cE 's\.addText|s\.addShape' src/engines/export/pptx/slides/storageSlide.ts` is 0)
    - No `1.048576` and no editorial verb (`grep -iE 'recommend|should|poor|good|warning|risk' src/engines/export/pptx/slides/storageSlide.ts` is 0)
    - MiB→GiB uses `Math.round(Number(` … `/ 1024)` form (grep > 0)
    - `npx tsc -b` exits 0; biome check clean on the file
  </acceptance_criteria>
  <done>addStorageSlide is a _layout-only slide: header + 4-card KPI row (provisioned/inUse/capacity + factual flagged count) + the storageTreemap chart panel; branded units; factual-only.</done>
</task>

<task type="auto">
  <name>Task 2: addNetworkSlide — KPI-only, NO chart (D-08)</name>
  <files>src/engines/export/pptx/slides/networkSlide.ts</files>
  <read_first>
    - src/engines/export/pptx/slides/inventorySlide.ts (the KPI-row analog) + src/engines/export/pptx/slides/drSimSlide.ts:21-24 (the no-chart `addHeader`+content idiom; D-08 = NO addChartPanel for network)
    - src/engines/export/pptx/slides/_layout.ts (`addHeader`, `addKpiRow` — do NOT import/call `addChartPanel`)
    - src/engines/aggregation/network.ts:53-59 (NetworkRollup field shape; arrays empty when optional network sheets absent — `pptxNumber(0)` renders `0`, factual-degrade, never fabricate/em-dash a real zero)
    - .planning/phases/11-report-and-deck-gap-closure-surface-phase-9-storage-network-/11-PATTERNS.md § networkSlide.ts (the verbatim skeleton — header + one KPI row, NO chart, NO PNG param)
    - .planning/phases/11-report-and-deck-gap-closure-surface-phase-9-storage-network-/11-02-SUMMARY.md (the resolved `network.*` i18n keys)
  </read_first>
  <action>
    Create src/engines/export/pptx/slides/networkSlide.ts. Signature: `export function addNetworkSlide(pptx: PptxGenJS, view: EstateView, strings: ExportStrings, locale: ExportLocale): void` — NO `chartPng` parameter (D-08: network has no chart). Body: `const s = pptx.addSlide()`; `const y = addHeader(s, strings['network.title'] ?? 'Network')`; `addKpiRow(s, [...], y)` with four cards: `{ label: strings['network.vswitches'] ?? 'vSwitches', value: pptxNumber(view.network.vswitches.length, locale) }`, dvswitches → `view.network.dvswitches.length` / `strings['network.dvswitches']`, portgroups → `view.network.portgroups.length` / `strings['network.portgroups']`, vnetwork → `view.network.vmPortgroupCount` / `strings['network.vnetwork']`. Import ONLY `addHeader, addKpiRow` from `./_layout` — do NOT import or call `addChartPanel`. Compose only `_layout.ts` primitives. `pptxNumber(0)` for an absent optional network sheet renders `0` — that is correct, do not special-case or em-dash it.
  </action>
  <verify>
    <automated>npx tsc -b 2>&1 | tail -5 && npx @biomejs/biome check src/engines/export/pptx/slides/networkSlide.ts</automated>
  </verify>
  <acceptance_criteria>
    - networkSlide.ts exports `addNetworkSlide` (grep is 1)
    - It does NOT reference `addChartPanel` and does NOT take a `chartPng`/PNG param (`grep -c "addChartPanel" src/engines/export/pptx/slides/networkSlide.ts` is 0; `grep -c "chartPng" ...` is 0)
    - It references `view.network.vswitches`, `view.network.dvswitches`, `view.network.portgroups`, `view.network.vmPortgroupCount`
    - No `s.addText`/`s.addShape`; no editorial verb
    - `npx tsc -b` exits 0; biome check clean
  </acceptance_criteria>
  <done>addNetworkSlide is a header + single KPI-row slide (vSwitch/dvSwitch/portgroup/vNetwork counts), no chart, _layout-only, factual-degrade on absent network sheets.</done>
</task>

<task type="auto">
  <name>Task 3: addPlannedSlide — measured-vs-planned KPI row (F-1 deck side, D-02/D-03)</name>
  <files>src/engines/export/pptx/slides/plannedSlide.ts</files>
  <read_first>
    - src/engines/export/pptx/slides/eosSlide.ts (KPI module shape) + src/engines/export/pptx/slides/drSimSlide.ts:21-24 (the `=== null` → `addNote` guard idiom to copy for the plannedView null branch)
    - src/types/estate.ts:486-494 (the `plannedView` shape + doc — `{ globals; clusters } | null`, null only in EMPTY_VIEW; copy the guard anyway per PATTERNS)
    - src/engines/export/pptx/builder.ts:56-60 (which GlobalSummary fields are read safely — `vmCount`, `hostCount`, `clusterCount`; `vcpuPerPcpu` via Number())
    - .planning/phases/11-report-and-deck-gap-closure-surface-phase-9-storage-network-/11-PATTERNS.md § plannedSlide.ts (the verbatim skeleton — header, null-guard `addNote`, measured-vs-planned KPI row; faithful-minimal, executive-tight, one screen-fit slide)
    - .planning/phases/11-report-and-deck-gap-closure-surface-phase-9-storage-network-/11-02-SUMMARY.md (the resolved `planned.*` i18n keys)
  </read_first>
  <action>
    Create src/engines/export/pptx/slides/plannedSlide.ts. Signature: `export function addPlannedSlide(pptx: PptxGenJS, view: EstateView, strings: ExportStrings, locale: ExportLocale): void`. Body: `const s = pptx.addSlide()`; `const y = addHeader(s, strings['planned.title'] ?? 'Planned vs measured estate')`; if `view.plannedView === null` then `addNote(s, strings['planned.none'] ?? '—', y); return` (copy drSimSlide.ts:21-24 guard). Otherwise `addKpiRow(s, [...], y)` with measured-vs-planned cards: `{ label: strings['planned.vcpuMeasured'] ?? 'vCPU:pCPU measured', value: pptxNumber(Number(view.globals.vcpuPerPcpu), locale) }`, `{ label: strings['planned.vcpuPlanned'] ?? 'vCPU:pCPU planned', value: pptxNumber(Number(view.plannedView.globals.vcpuPerPcpu), locale) }`, `{ label: strings['planned.vmMeasured'] ?? 'VMs measured', value: pptxNumber(view.globals.vmCount, locale) }`, `{ label: strings['planned.vmPlanned'] ?? 'VMs planned', value: pptxNumber(view.plannedView.globals.vmCount, locale) }`. Keep it executive-tight — header + one KPI row, one screen-fit slide (D-02/D-03 faithful-minimal; the per-cluster delta lives in the HTML report per 11-06, NOT on the deck). Import `addHeader, addKpiRow, addNote` from `./_layout`. No `s.addText`/`s.addShape`. No editorial verb. No `* 1.048576` (vcpuPerPcpu is a ratio, vmCount a count — no unit conversion).
  </action>
  <verify>
    <automated>npx tsc -b 2>&1 | tail -5 && npx @biomejs/biome check src/engines/export/pptx/slides/plannedSlide.ts</automated>
  </verify>
  <acceptance_criteria>
    - plannedSlide.ts exports `addPlannedSlide` (grep is 1)
    - It references `view.plannedView` (null-guard + render read, grep ≥ 2) and `view.globals`
    - It has the `=== null` → `addNote` early return
    - No `addChartPanel` (planned slide has no chart), no `s.addText`/`s.addShape`, no editorial verb
    - `npx tsc -b` exits 0; biome check clean
  </acceptance_criteria>
  <done>addPlannedSlide is a header + null-guard + measured-vs-planned KPI-row slide, _layout-only, executive-tight, factual-only.</done>
</task>

<task type="auto">
  <name>Task 4: Wire the three slides into builder.ts at a coherent PPT-03 position + extend the golden-structure test</name>
  <files>src/engines/export/pptx/builder.ts, src/engines/export/pptx/builder.test.ts</files>
  <read_first>
    - src/engines/export/pptx/builder.ts (the file being modified — the fixed-order sequence :53-99, the `png(k) = shared?.get(k)` helper :51, the per-cluster loop :79-86, the `addEosSlide`/`addDrSimSlide` call-sites :93-94, the import block :20-27)
    - src/engines/export/pptx/slides/{storageSlide,networkSlide,plannedSlide}.ts (the three signatures from Tasks 1-3)
    - .planning/phases/11-report-and-deck-gap-closure-surface-phase-9-storage-network-/11-PATTERNS.md § builder.ts (D-05 placement: Storage+Network after the per-cluster loop, before the contention annex; planned slide with the other re-aggregation slides e.g. after addDrSimSlide; the exact call-site shapes)
    - .planning/phases/11-report-and-deck-gap-closure-surface-phase-9-storage-network-/11-01-SUMMARY.md (confirms the `storageTreemap` shared key the worker auto-rasterizes — builder resolves it via `png('storageTreemap')`, no worker edit needed for PPTX path)
    - src/engines/export/buildExportView.test.ts:16-45 (the fixture-factory + the existing builder golden-structure test pattern; builder.test.ts runs wasm-free with `opts.charts` omitted → "—" panels per builder.ts:11-13)
  </read_first>
  <action>
    Modify src/engines/export/pptx/builder.ts: add three imports alongside builder.ts:20-27 — `import { addNetworkSlide } from './slides/networkSlide'`, `import { addPlannedSlide } from './slides/plannedSlide'`, `import { addStorageSlide } from './slides/storageSlide'` (Biome auto-organizes alphabetically — let it). D-05 placement: immediately AFTER the `for (const cluster of view.clusters) { addClusterSlide(...) }` loop (builder.ts:79-86) and BEFORE the conditional contention annex (builder.ts:88-91), insert `addStorageSlide(pptx, view, png('storageTreemap'), strings, locale)` then `addNetworkSlide(pptx, view, strings, locale)` (no PNG arg — D-08). Place `addPlannedSlide(pptx, view, strings, locale)` with the other re-aggregation slides — directly after the existing `addDrSimSlide(pptx, view.drSim, png('drBar'), strings, locale)` line (builder.ts:94). Do NOT change the title/overview/per-cluster/eos/drSim/trends/inventory call shapes or order; only insert the three new calls at the two specified spots. `png('storageTreemap')` resolves the 11-01 shared key (the worker's generic `Object.entries(optBundle.shared)` raster loop rasterizes it automatically — no export.worker.ts edit on the PPTX path; that file is owned by plan 11-06's HTML branch only). Then extend src/engines/export/pptx/builder.test.ts (the existing golden-structure test): add assertions that a built deck (wasm-free, `opts.charts` omitted) contains a Storage slide, a Network slide, and a Planned slide — assert via the same slide-count / slide-presence mechanism the existing test uses for eos/drSim (mirror the existing assertion idiom; if the test counts slides, assert the count increased by exactly 3 for a non-empty view, or assert the new slide titles are present using the strings bag the test already constructs).
  </action>
  <verify>
    <automated>npx tsc -b 2>&1 | tail -5 && npx vitest run src/engines/export/pptx/builder.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - builder.ts contains `addStorageSlide(pptx, view, png('storageTreemap'), strings, locale)`, `addNetworkSlide(pptx, view, strings, locale)`, `addPlannedSlide(pptx, view, strings, locale)` (each grep ≥ 1)
    - `grep -c "png('storageTreemap')" src/engines/export/pptx/builder.ts` is 1
    - The three new `import { add*Slide }` lines are present
    - Storage+Network calls appear textually after the `for (const cluster of view.clusters)` loop and before `addContentionAnnex`; addPlannedSlide appears after `addDrSimSlide`
    - export.worker.ts is NOT modified by this plan (`git diff --name-only` does not list src/engines/export/export.worker.ts)
    - `npx tsc -b` exits 0; `npx vitest run src/engines/export/pptx/builder.test.ts` exits 0 with the new Storage/Network/Planned slide assertions passing
    - `npx @biomejs/biome check src/engines/export/pptx/builder.ts src/engines/export/pptx/builder.test.ts` clean
  </acceptance_criteria>
  <done>builder.ts emits Storage (with storageTreemap PNG) + Network + Planned slides in the fixed PPT-03 order at the D-05 positions; the golden-structure test asserts their presence; tsc + vitest + biome green; no worker edit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-memory EstateView → downloaded PPTX file | Already-on-screen P9 view-models shaped into deck slides. No new input crosses; output is a user-initiated local download. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-08 | Information disclosure | Storage/Network/Planned slides exposing cluster/datastore counts | accept | Identical data already rendered on screen (StorageView/NetworkView/PlanningView) and in the per-cluster slides; the new slides expose nothing the deliverable does not already carry. No PII increase. Active snapshot + active locale only (D-07/D-08). |
| T-11-09 | Tampering | editorial fabrication on the flag KPI | mitigate | D-06 flagged KPI is a raw `counts.ds + counts.lu` integer with the gold `addHeader` rule only; acceptance criteria grep-gate the no-verb/no-traffic-light denylist; slides compose only `_layout.ts` (no per-slide raw drawing). |
| T-11-10 | Tampering | engine purity | mitigate | Slides are pure transforms (no React/DOM/Zustand/Zod); tsc-enforced; builder golden-structure test stays wasm-free. |

Phase 11 adds no network/auth/input surface; this plan only shapes already-in-memory view-models into a user-downloaded file under the existing fetchGuard/CSP privacy invariant.
</threat_model>

<verification>
- `npx tsc -b` exits 0
- `npx vitest run src/engines/export/pptx/builder.test.ts` exits 0 (Storage/Network/Planned slides asserted present)
- `npx @biomejs/biome check` clean on all five files
- `node scripts/check-bundle-size.mjs` passes (no new echarts registration — 11-01 confirmed)
- export.worker.ts unmodified by this plan (owned by 11-06)
</verification>

<success_criteria>
F-2 deck side + F-1 deck side closed: the PPTX deck carries a Storage slide (KPI + treemap + factual flagged count), a Network slide (KPI-only), and a Planned slide (measured-vs-planned), composed only from `_layout.ts`, in the fixed PPT-03 order at the D-05 positions, executive-tight, factual-only, golden-structure-tested.
</success_criteria>

<output>
After completion, create `.planning/phases/11-report-and-deck-gap-closure-surface-phase-9-storage-network-/11-05-SUMMARY.md`
</output>
