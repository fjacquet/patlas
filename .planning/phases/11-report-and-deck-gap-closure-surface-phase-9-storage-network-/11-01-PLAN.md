---
phase: 11-report-and-deck-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engines/export/chartBundle.ts
  - src/engines/export/chartBundle.test.ts
autonomous: true
requirements: [STG-01, STG-02, STG-03]
must_haves:
  truths:
    - "buildChartBundle(view,...).shared.storageTreemap is a treemap EChartsOption"
    - "The treemap data maps view.storage.byCluster rows to {name,value} (provisioned consumption)"
  artifacts:
    - path: "src/engines/export/chartBundle.ts"
      provides: "storageTreemap pure option builder threaded into the shared bundle"
      contains: "storageTreemap"
    - path: "src/engines/export/chartBundle.test.ts"
      provides: "Vitest coverage for the storageTreemap builder"
      contains: "storageTreemap"
  key_links:
    - from: "src/engines/export/chartBundle.ts buildChartBundle"
      to: "shared.storageTreemap"
      via: "shared record entry keyed 'storageTreemap'"
      pattern: "storageTreemap:\\s*storageTreemap"
---

<objective>
Add the D-07 Storage treemap as a pure `EChartsOption` builder in `chartBundle.ts` and thread it into the `shared` option bundle under the key `storageTreemap`. This is the single source of truth the Wave-2 PPTX slide (`png('storageTreemap')`) and HTML report (`optBundle.shared.storageTreemap`) both consume.

Purpose: F-2 — the Storage analytics shipped in Phase 9 reach neither shareable artifact. The treemap is THE P9 storage visual on both surfaces (user explicitly chose treemap, D-07). Building it once here keeps `chartBundle` pure and avoids re-derivation.
Output: `storageTreemap` option builder + its `shared` bundle entry + Vitest coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-report-and-deck-gap-closure-surface-phase-9-storage-network-/11-CONTEXT.md
@.planning/phases/11-report-and-deck-gap-closure-surface-phase-9-storage-network-/11-PATTERNS.md

<interfaces>
StorageConsumptionGroup (src/engines/aggregation/storageByX.ts:30-35):
  { key: string; provisionedMib: MiB; inUseMib: MiB }
StorageByX.byCluster: StorageConsumptionGroup[] (src/engines/aggregation/storageByX.ts:46-51)
EstateView.storage: StorageByX (src/types/estate.ts:511)
ChartOptionBundle.shared: Record<string, EChartsOption> (chartBundle.ts:151-154)
buildChartBundle assembles `shared` at chartBundle.ts:170-173 (`eosBar: eosBar(view, L)` is the exact thread-in analog)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the storageTreemap pure option builder + thread it into the shared bundle</name>
  <files>src/engines/export/chartBundle.ts</files>
  <read_first>
    - src/engines/export/chartBundle.ts (the file being modified — study `eosBar(view, L)` at :88-110 as the private-builder analog and the `shared` assembly at :170-173 as the thread-in analog; the inline `ChartLabels`/`LABELS` map at :22-61 is the locale-string pattern)
    - src/components/storage/StorageView.tsx:85-99 (the proven on-screen treemap series shape — `{ type:'treemap', breadcrumb:{show:false}, data: groups.map(g => ({ name:g.key, value:g.provisionedMib as number })) }`)
    - src/engines/aggregation/storageByX.ts:30-64 (StorageConsumptionGroup + StorageByX field shapes)
    - .planning/phases/11-report-and-deck-gap-closure-surface-phase-9-storage-network-/11-PATTERNS.md § chartBundle.ts (the exact builder skeleton + thread-in line)
    - CLAUDE.md § Gotcha 4 (TreemapChart already registered in renderCharts.ts:26-47 and Chart.tsx:42 — NO echarts.use edit; no bundle-budget impact)
  </read_first>
  <action>
    In src/engines/export/chartBundle.ts add a private builder `function storageTreemap(view: EstateView, _L: ChartLabels): EChartsOption` placed alongside `eosBar`/`drBar` (after `eosBar`, before `drBar`). It returns `{ tooltip: {}, series: [{ type: 'treemap', breadcrumb: { show: false }, data: view.storage.byCluster.map((g) => ({ name: g.key, value: g.provisionedMib as number })) }] }` — the SAME series shape `StorageView.tsx:85-99` renders for the consumption lens, so the exported visual IS the on-screen visual. `g.key` is the cluster name (user data, not locale) so no `ChartLabels` field is needed; keep the `_L` param unused with the leading-underscore convention for signature symmetry with `eosBar`/`drBar`. In `buildChartBundle` (chartBundle.ts:170-173) add the line `storageTreemap: storageTreemap(view, L),` to the `shared` record literal, directly after the `eosBar: eosBar(view, L),` entry — mirroring exactly how `eosBar` is threaded. Do NOT touch `echarts.use(...)` anywhere (TreemapChart is already registered per CLAUDE.md Gotcha 4 — verified). Do NOT add a `ChartLabels` field. No raw `* 1.048576` or `/1000` anywhere (no unit conversion needed — provisionedMib passes straight through as the treemap value, identical to the on-screen path).
  </action>
  <behavior>
    - buildChartBundle(view, null, 'en').shared.storageTreemap.series[0].type === 'treemap'
    - The treemap data length equals view.storage.byCluster.length and each entry is { name, value } sourced from g.key / g.provisionedMib
    - shared.storageTreemap.series[0].breadcrumb.show === false
  </behavior>
  <verify>
    <automated>npx tsc -b 2>&1 | tail -5 && npx @biomejs/biome check src/engines/export/chartBundle.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "storageTreemap" src/engines/export/chartBundle.ts` returns ≥ 3 (builder declaration + the `shared` thread-in entry + at minimum one reference)
    - `npx tsc -b` exits 0 (no errors in final 5 lines of output)
    - `npx @biomejs/biome check src/engines/export/chartBundle.ts` reports no errors
    - No occurrence of `echarts.use` added to chartBundle.ts (`grep -c "echarts.use" src/engines/export/chartBundle.ts` is 0)
    - No occurrence of the literal `1.048576` in chartBundle.ts changes
  </acceptance_criteria>
  <done>chartBundle.ts exports a `storageTreemap`-keyed treemap EChartsOption in `buildChartBundle(...).shared`, typechecks clean, lint clean, zero echarts-registration edits.</done>
</task>

<task type="auto">
  <name>Task 2: Vitest coverage for the storageTreemap builder</name>
  <files>src/engines/export/chartBundle.test.ts</files>
  <read_first>
    - src/engines/export/buildExportView.test.ts:16-45 (the `host()`/`vm()` fixture-factory + `buildEstateView(mergeSnapshotsToEstate([snap]), …)` pattern to produce a real EstateView for the test)
    - src/engines/export/chartBundle.ts (the builder under test, including the new storageTreemap entry)
    - src/engines/aggregation/storageByX.ts:30-64 (assert against StorageConsumptionGroup shape)
    - CLAUDE.md § Architecture (engines/ Vitest-gated ≥75%)
  </read_first>
  <action>
    Create src/engines/export/chartBundle.test.ts (if `chartBundle.test.ts` already exists, extend it instead of overwriting). Using the `buildExportView.test.ts:16-45` fixture-factory idiom (`host()`/`vm()` builders → `buildEstateView(mergeSnapshotsToEstate([snap]), …)`), build a real `EstateView` whose `storage.byCluster` has at least two groups. Add a `describe('storageTreemap', ...)` with `it` cases asserting: (1) `buildChartBundle(view, null, 'en').shared.storageTreemap.series[0].type === 'treemap'`; (2) the treemap `data` array length equals `view.storage.byCluster.length`; (3) each `data[i]` is `{ name: view.storage.byCluster[i].key, value: view.storage.byCluster[i].provisionedMib }`; (4) `series[0].breadcrumb.show === false`. Mirror the `describe/it` structure already in `buildExportView.test.ts`. The treemap is locale-independent (cluster names are data) so no FR-locale case is required, but a `buildChartBundle(view, null, 'fr')` smoke assertion that `shared.storageTreemap` is still present is acceptable.
  </action>
  <verify>
    <automated>npx vitest run src/engines/export/chartBundle.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/engines/export/chartBundle.test.ts` exits 0 with all assertions passing
    - The test file references `storageTreemap`, `treemap`, and `byCluster`
    - `npx @biomejs/biome check src/engines/export/chartBundle.test.ts` reports no errors
    - The new storageTreemap builder lines are covered (the test exercises `buildChartBundle` which calls the builder)
  </acceptance_criteria>
  <done>chartBundle.test.ts asserts the storageTreemap option type, data mapping, and breadcrumb config against a real fixture EstateView; vitest green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-memory EstateView → exported chart option | Already-parsed, already-on-screen data is shaped into a chart option. No new untrusted input crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information disclosure | storageTreemap node names (user cluster names) | accept | Cluster names are already rendered on screen (StorageView) and into the HTML report/PPTX per-cluster slides; the treemap exposes nothing the deliverable does not already carry. No PII beyond existing surface. |
| T-11-02 | Tampering | chartBundle purity | mitigate | Builder is a pure function returning `EChartsOption` only — no React/DOM/Zustand/Zod (enforced by tsc + the engines purity convention). Vitest gate ≥75% (Task 2). |

Phase 11 adds no network, no auth, no new input parsing, no new external surface — it shapes already-in-memory view-models under the existing fetchGuard/CSP privacy invariant. Threat surface is "do not leak more than the on-screen views already do" — satisfied (identical data, identical series shape as `StorageView`).
</threat_model>

<verification>
- `npx tsc -b` exits 0
- `npx vitest run src/engines/export/chartBundle.test.ts` exits 0
- `npx @biomejs/biome check src/engines/export/chartBundle.ts src/engines/export/chartBundle.test.ts` clean
- `node scripts/check-bundle-size.mjs` passes (TreemapChart already registered — no budget impact, verify regardless)
</verification>

<success_criteria>
`buildChartBundle(view, …).shared.storageTreemap` is a treemap `EChartsOption` mapping `view.storage.byCluster` to `{name,value}`, pure, Vitest-covered, typecheck + lint + bundle-size clean. The `storageTreemap` key is the contract Wave-2 plans 11-05 (PPTX) and 11-06 (HTML) consume.
</success_criteria>

<output>
After completion, create `.planning/phases/11-report-and-deck-gap-closure-surface-phase-9-storage-network-/11-01-SUMMARY.md`
</output>
