---
phase: 11-report-and-deck-gap-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/i18n/locales/en/report.json
  - src/i18n/locales/fr/report.json
  - src/i18n/locales/en/pptx.json
  - src/i18n/locales/fr/pptx.json
autonomous: true
requirements: [STG-04, STG-05, NET-01, NET-02, NET-03, NET-04, NET-05]
must_haves:
  truths:
    - "Storage / Network / planned i18n keys resolve in EN and FR for both report and pptx string bags"
    - "keyParity.test.ts stays green (EN↔FR identical paths)"
    - "New dotted keys are collision-free (or identical-by-intent) across report.json and pptx.json"
  artifacts:
    - path: "src/i18n/locales/en/report.json"
      provides: "EN report Storage/Network/planned section + table-header strings"
      contains: "storage"
    - path: "src/i18n/locales/fr/report.json"
      provides: "FR report Storage/Network/planned strings (Gio / PRA conventions)"
      contains: "storage"
    - path: "src/i18n/locales/en/pptx.json"
      provides: "EN slide-chrome Storage/Network/planned strings"
      contains: "storage"
    - path: "src/i18n/locales/fr/pptx.json"
      provides: "FR slide-chrome Storage/Network/planned strings"
      contains: "storage"
  key_links:
    - from: "src/i18n/locales/{en,fr}/report.json + pptx.json"
      to: "the flattened export string bag (useExport.ts:77-84)"
      via: "flattenBundle dotted keys consumed by renderReport.tsx / slide modules via strings['storage.title'] ?? fallback"
      pattern: "\"storage\""
---

<objective>
Add the EN+FR i18n keys the Wave-2 Storage/Network/planned slides and report sections resolve at render time, in BOTH `report.json` and `pptx.json`, with EN↔FR path parity. These are the localized chrome strings (titles, KPI labels, table headers) — numbers come from `pptxNumber`/`fmtInt`, never pre-formatted in strings.

Purpose: F-2 + F-1 — the slides/sections use `strings['storage.title'] ?? 'fallback'`; the keys must exist in both locale files for both export bags or FR exports fall back to EN literals and `keyParity.test.ts` fails. The CRITICAL flatten-collision gotcha (useExport.ts:77-84: report+pptx flatten into ONE prefix-less dotted-key bag, pptx clobbers report) is resolved by keeping the Storage/Network/planned dotted paths structurally identical across both files (intended — same dotted key resolves the same string regardless of which file wins the spread).
Output: matched `storage`/`network`/`planned` nested blocks in all four locale files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-report-and-deck-gap-closure-surface-phase-9-storage-network-/11-CONTEXT.md
@.planning/phases/11-report-and-deck-gap-closure-surface-phase-9-storage-network-/11-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the storage/network/planned nested key blocks to EN + FR report.json and pptx.json</name>
  <files>src/i18n/locales/en/report.json, src/i18n/locales/fr/report.json, src/i18n/locales/en/pptx.json, src/i18n/locales/fr/pptx.json</files>
  <read_first>
    - src/i18n/locales/en/report.json + src/i18n/locales/fr/report.json (the files being modified — study the existing nested `eos` / `dr` blocks as the structural analog for the new blocks)
    - src/i18n/locales/en/pptx.json + src/i18n/locales/fr/pptx.json (same — existing `eos` / `dr` nested blocks)
    - src/hooks/useExport.ts:50-58 + :77-84 (the `{ ...flattenBundle(report), ...flattenBundle(pptx) }` prefix-less flatten — the collision rule that mandates identical dotted paths across the two files)
    - src/i18n/keyParity.test.ts (the recursive EN↔FR deep key-diff gate — every added EN key must land in FR with the identical path)
    - .planning/phases/11-report-and-deck-gap-closure-surface-phase-9-storage-network-/11-PATTERNS.md § i18n (FR conventions: `Gio` for GiB, `PRA` for DR; no pre-formatted numbers; no editorial verbs; PR #4 added matching dotted paths to both files)
    - CLAUDE.md § Conventions (i18n keys land in BOTH en/ and fr/; no pre-formatted numbers in strings; no editorial verbs "recommend/should/poor/good")
  </read_first>
  <action>
    Add three nested blocks — `storage`, `network`, `planned` — to ALL FOUR files (`en/report.json`, `fr/report.json`, `en/pptx.json`, `fr/pptx.json`), mirroring the existing nested `eos`/`dr` block structure. The dotted paths MUST be structurally identical across report.json and pptx.json (collision-free-by-intent: the same dotted key resolves the same string regardless of which file wins the flatten spread — useExport.ts:77-84). Keys to add (each with EN value and FR value; FR uses `Gio` for GiB, `PRA` for DR, no pre-formatted numbers, no editorial verbs):

    `storage`: `title` (EN "Storage" / FR "Stockage"), `byCluster` (EN "Storage by cluster" / FR "Stockage par cluster" — the treemap panel caption), `provisioned` (EN "Provisioned (GiB)" / FR "Provisionné (Gio)"), `inUse` (EN "In use (GiB)" / FR "Utilisé (Gio)"), `capacity` (EN "Capacity (GiB)" / FR "Capacité (Gio)"), `flagged` (EN "Flagged datastores" / FR "Datastores signalés" — factual, NO verb), `colCluster` (EN "Cluster" / FR "Cluster"), `colProvisioned` (EN "Provisioned" / FR "Provisionné"), `colInUse` (EN "In use" / FR "Utilisé"), `vsanShared` (EN "Shared across N clusters" — the literal contains `{{n}}` placeholder, FR "Partagé sur {{n}} clusters").

    `network`: `title` (EN "Network" / FR "Réseau"), `vswitches` (EN "vSwitches" / FR "vSwitches"), `dvswitches` (EN "dvSwitches" / FR "dvSwitches"), `portgroups` (EN "Portgroups" / FR "Groupes de ports"), `vnetwork` (EN "VM adjacencies" / FR "Adjacences VM").

    `planned`: `title` (EN "Planned vs measured estate" / FR "Parc planifié vs mesuré"), `none` (EN "—" / FR "—"), `vcpuMeasured` (EN "vCPU:pCPU measured" / FR "vCPU:pCPU mesuré"), `vcpuPlanned` (EN "vCPU:pCPU planned" / FR "vCPU:pCPU planifié"), `vmMeasured` (EN "VMs measured" / FR "VM mesurées"), `vmPlanned` (EN "VMs planned" / FR "VM planifiées"), `clusterDelta` (EN "Per-cluster delta" / FR "Écart par cluster"), `colCluster` (EN "Cluster" / FR "Cluster"), `colMeasured` (EN "Measured" / FR "Mesuré"), `colPlanned` (EN "Planned" / FR "Planifié").

    Add the SAME dotted paths to all four files (pptx.json may carry shorter slide-chrome variants only if a key would visually overflow a slide — but keep paths identical; differing VALUES are allowed, differing PATHS are not). Insert each block respecting JSON nesting and the existing alphabetical/grouping order of the file (Biome will not reformat JSON — keep valid JSON, trailing-comma-free). Do NOT put any number, unit-converted value, or editorial verb inside a string value. The FR `flagged`/`planned` strings must contain NO "recommandé/devrait/insuffisant/risque" (factual-only invariant).
  </action>
  <verify>
    <automated>npx vitest run src/i18n/keyParity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/i18n/keyParity.test.ts` exits 0 (EN↔FR parity green for report.json AND pptx.json)
    - All four files contain a `"storage"`, `"network"`, and `"planned"` block (`grep -l '"storage"' src/i18n/locales/en/report.json src/i18n/locales/fr/report.json src/i18n/locales/en/pptx.json src/i18n/locales/fr/pptx.json` lists all 4)
    - `node -e "JSON.parse(require('fs').readFileSync('src/i18n/locales/fr/report.json'))"` and the same for the other 3 files succeed (valid JSON)
    - The `storage`/`network`/`planned` dotted paths are identical between report.json and pptx.json within each locale (diff the key trees, not the values)
    - No string value contains a digit-formatted number, `1.048576`, or any of `recommend|should|poor|good|recommandé|devrait|insuffisant` (case-insensitive grep over the added blocks returns 0)
    - `npx @biomejs/biome check src/i18n/locales/en/report.json src/i18n/locales/fr/report.json src/i18n/locales/en/pptx.json src/i18n/locales/fr/pptx.json` reports no errors
  </acceptance_criteria>
  <done>All four locale files carry matched `storage`/`network`/`planned` blocks with EN↔FR path parity and report↔pptx path-collision-free-by-intent; keyParity green; valid JSON; factual-only strings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static i18n config → exported document chrome | Hand-authored localized chrome strings only. No user input, no dynamic content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Tampering | i18n string values introducing editorial verdicts | mitigate | Acceptance criteria grep-gate the factual-only denylist (EN+FR); no number/verb in any value. |
| T-11-04 | Information disclosure | i18n config | accept | Static chrome strings contain no estate data, no PII — they are templates the engines fill at render via formatters. |

Phase 11 adds no network/auth/input-parsing surface. This plan only adds static localization config consumed under the existing privacy invariant.
</threat_model>

<verification>
- `npx vitest run src/i18n/keyParity.test.ts` exits 0
- All four locale files valid JSON, carry matched `storage`/`network`/`planned` blocks
- `npx @biomejs/biome check` clean on the four files
- Factual-only denylist grep returns 0 over the added blocks
</verification>

<success_criteria>
The Storage/Network/planned i18n keys exist in EN+FR for both report.json and pptx.json with identical dotted paths (collision-free-by-intent across the flattened bag), keyParity green, factual-only, no pre-formatted numbers. This is the chrome contract Wave-2 plans 11-05 (PPTX slides) and 11-06 (HTML sections) resolve via `strings['storage.title'] ?? 'fallback'`.
</success_criteria>

<output>
After completion, create `.planning/phases/11-report-and-deck-gap-closure-surface-phase-9-storage-network-/11-02-SUMMARY.md`
</output>
