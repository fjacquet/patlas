---
phase: 11-report-and-deck-gap-closure
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/planning/PlannedEstatePanel.tsx
  - src/components/planning/PlanningView.tsx
  - src/i18n/locales/en/alloc.json
  - src/i18n/locales/fr/alloc.json
autonomous: true
requirements: [PLN-03, PLN-04]
must_haves:
  truths:
    - "PlanningView renders a planned-vs-measured estate panel reading view.plannedView (F-1: the dead projection becomes a live feature on screen)"
    - "The panel is a pure useEstateView prop-consumer — no second useMemo, no @/engines import in components"
    - "view.plannedView has at least one non-test reader (F-1 resolved on the screen side)"
  artifacts:
    - path: "src/components/planning/PlannedEstatePanel.tsx"
      provides: "Thin view-prop presenter of view.plannedView vs view.globals"
      contains: "view.plannedView"
    - path: "src/components/planning/PlanningView.tsx"
      provides: "PlannedEstatePanel mounted in the planning surface"
      contains: "PlannedEstatePanel"
  key_links:
    - from: "src/components/planning/PlanningView.tsx"
      to: "src/components/planning/PlannedEstatePanel.tsx"
      via: "panel mounted with the existing single useEstateView `view` prop"
      pattern: "<PlannedEstatePanel"
    - from: "src/components/planning/PlannedEstatePanel.tsx"
      to: "view.plannedView"
      via: "direct prop read (mirrors DrSimPanel reading view.plannedDrSim)"
      pattern: "view\\.plannedView"
---

<objective>
Resolve F-1: render `view.plannedView` (PLN-03/04) on screen in `PlanningView` as a planned-vs-measured estate panel, mirroring the existing `DrSimPanel` thin-presenter idiom. `view.plannedView` is currently computed every `buildEstateView` pass and consumed by zero non-test readers — this makes it a live feature (the user chose "render + export", not "remove").

Purpose: F-1 — the planned-ratio re-aggregated estate surface was specified (PLN-03/04) but never surfaced. This plan closes the screen side; plans 11-05 (PPTX) and 11-06 (HTML) carry it into the exports. The component MUST stay a pure `useEstateView` prop-consumer — no second `useMemo`, no `@/engines` import (CLAUDE.md hard invariant).
Output: a `PlannedEstatePanel` mounted in `PlanningView`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-report-and-deck-gap-closure-surface-phase-9-storage-network-/11-CONTEXT.md
@.planning/phases/11-report-and-deck-gap-closure-surface-phase-9-storage-network-/11-PATTERNS.md

<interfaces>
EstateView.plannedView: { globals: GlobalSummary; clusters: ClusterAggregate[] } | null (src/types/estate.ts:494) — null only in EMPTY_VIEW
EstateView.globals: GlobalSummary (src/types/estate.ts:457) — the MEASURED estate
GlobalSummary safe fields used elsewhere: vmCount, hostCount, clusterCount, vcpuPerPcpu (read usage at renderReport.tsx:93-101, DrSimPanel.tsx:173-179)
ClusterAggregate has: .cluster (name), .vmCount, .hostCount, .vcpuPerPcpu (read usage at renderReport.tsx:125-141)
DrSimPanel pure-presenter template: DrSimPanelProps { view: EstateView; ... } (DrSimPanel.tsx:6-19); reads view.drSim/view.plannedDrSim directly (DrSimPanel.tsx:97); `Stat` row idiom (DrSimPanel.tsx:49-56)
PlanningView single useEstateView call already exists (PlanningView.tsx:53: `const view = useEstateView(mode)`)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PlannedEstatePanel — a thin view-prop presenter of plannedView vs globals</name>
  <files>src/components/planning/PlannedEstatePanel.tsx</files>
  <read_first>
    - src/components/dr/DrSimPanel.tsx (the canonical thin presenter — CONTEXT D-03 "mirror this idiom exactly": the `DrSimPanelProps { view: EstateView; ... }` prop shape :6-19, the `Stat` component :49-56, the direct `view.drSim`/`view.plannedDrSim` reads :97, the `drSim === null` null-guard :342-343, the `summary(g: GlobalSummary)` projection :173-179)
    - src/components/planning/PlanningView.tsx (where this mounts — the single `useEstateView(mode)` at :53; the `flex flex-col gap-6` container :82; the `mt-12` structural break :86; the `ErrorBoundary FallbackComponent={PlanningError}` wrap :81)
    - src/types/estate.ts:457 + :486-494 (GlobalSummary; the `plannedView` doc-comment confirming it is `null` only in EMPTY_VIEW but always present otherwise — copy the null-guard anyway for safety per PATTERNS)
    - .planning/phases/11-report-and-deck-gap-closure-surface-phase-9-storage-network-/11-PATTERNS.md § PlanningView.tsx (the invariants: pure prop-consumer, no second useMemo, no @/engines import, reuse the `Stat` row idiom, `font-mono tabular-nums`, light/dark twins)
    - CLAUDE.md § Architecture + Conventions (no second useMemo anywhere; no `@/engines` import in components; factual-only, no editorial verbs; toggles reuse the ThemeToggle fieldset idiom — N/A here, no toggle)
  </read_first>
  <action>
    Create src/components/planning/PlannedEstatePanel.tsx as a function component `PlannedEstatePanel({ view }: { view: EstateView })`, structured exactly like `DrSimPanel`'s presenter half. It reads ONLY off the `view` prop — `view.globals` (the MEASURED estate) and `view.plannedView` (the PLANNED re-aggregation). NO `useEstateView`, NO `useMemo`, NO `@/engines` import (CLAUDE.md hard invariant — every number comes straight off the prop, exactly as `DrSimPanel` reads `view.drSim`/`view.plannedDrSim`). Use `useTranslation('alloc')` for chrome (the namespace `PlanningView` already uses) and `useTranslation()` `i18n.language` for the locale, mirroring `DrSimPanel.tsx:94-96`. Copy the `Stat` row component verbatim from `DrSimPanel.tsx:49-56` (label + `font-mono text-sm tabular-nums` value, light/dark twins). Render: a `<section className="panel flex flex-col gap-4">` with an `<h2>` title `t('plannedEstate.title')`; if `view.plannedView === null` render the `t('plannedEstate.none')` factual note and return (copy the `DrSimPanel.tsx:342-343` null-guard idiom); otherwise an estate-level measured-vs-planned `Stat` block (measured = `view.globals.vmCount`/`.vcpuPerPcpu`; planned = `view.plannedView.globals.vmCount`/`.vcpuPerPcpu` — format every number via `fmtInt(Number(x), loc)` from `@/utils/format`, NEVER pre-format, NEVER a raw `* 1.048576`); plus a per-cluster delta list (D-03 "expected shape" — for each `view.plannedView.clusters` entry, a row of cluster name + measured-vs-planned `vcpuPerPcpu` matched against the same-named `view.globals`/`view.clusters` cluster; keep it within one screen-fit block, light/dark twins, factual-only — NO "recommend/should/poor/good"). Use the `alloc` i18n keys `plannedEstate.title`, `plannedEstate.none`, `plannedEstate.vmMeasured`, `plannedEstate.vmPlanned`, `plannedEstate.vcpuMeasured`, `plannedEstate.vcpuPlanned`, `plannedEstate.clusterDelta`, `plannedEstate.colCluster`, `plannedEstate.colMeasured`, `plannedEstate.colPlanned` (added to alloc.json in Task 3). The component is a pure presenter — no local state, no effects.
  </action>
  <verify>
    <automated>npx tsc -b 2>&1 | tail -5 && npx @biomejs/biome check src/components/planning/PlannedEstatePanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "view.plannedView" src/components/planning/PlannedEstatePanel.tsx` returns ≥ 2 (null-guard + render read)
    - `grep -c "useMemo" src/components/planning/PlannedEstatePanel.tsx` returns 0
    - `grep -c "@/engines" src/components/planning/PlannedEstatePanel.tsx` returns 0
    - `grep -c "useEstateView" src/components/planning/PlannedEstatePanel.tsx` returns 0 (it consumes the prop, never calls the hook)
    - No occurrence of `1.048576` in the file; no editorial verb (`grep -iE 'recommend|should|poor|good' src/components/planning/PlannedEstatePanel.tsx` returns 0)
    - `npx tsc -b` exits 0; `npx @biomejs/biome check src/components/planning/PlannedEstatePanel.tsx` clean
  </acceptance_criteria>
  <done>PlannedEstatePanel is a pure `{ view }`-prop presenter of measured-vs-planned estate + per-cluster delta, mirroring DrSimPanel, with the null-guard, no hooks-recompute, no engine import, factual-only.</done>
</task>

<task type="auto">
  <name>Task 2: Mount PlannedEstatePanel in PlanningView</name>
  <files>src/components/planning/PlanningView.tsx</files>
  <read_first>
    - src/components/planning/PlanningView.tsx (the file being modified — the `flex flex-col gap-6` container :82, the existing `PlannedRatiosControl` + `mt-12` break + `DrSimPanel` mount :83-97, the `ErrorBoundary` wrap :81, the single `view = useEstateView(mode)` :53)
    - src/components/planning/PlannedEstatePanel.tsx (the component created in Task 1 — its `{ view }` prop shape)
    - .planning/phases/11-report-and-deck-gap-closure-surface-phase-9-storage-network-/11-PATTERNS.md § PlanningView.tsx (mount it inside the existing container, structurally separated like the `mt-12` break, inside the existing ErrorBoundary; pass the existing `view` — no new hook call)
  </read_first>
  <action>
    In src/components/planning/PlanningView.tsx add `import { PlannedEstatePanel } from './PlannedEstatePanel'` (Biome auto-organizes imports alphabetically). Inside the existing `<div className="flex flex-col gap-6">` (PlanningView.tsx:82), mount `<PlannedEstatePanel view={view} />` passing the EXISTING `view` from the single `useEstateView(mode)` call at :53 — do NOT add a second `useEstateView`/`useMemo`. Place it structurally separated like the existing `mt-12` DR break — e.g. a `<div className="mt-12"><PlannedEstatePanel view={view} /></div>` block after the `PlannedRatiosControl` and before or after the DR `mt-12` block (planner's discretion within the existing planning surface — keep it inside the existing `ErrorBoundary FallbackComponent={PlanningError}` at :81). Do not change the `!snapshot` empty-state branch, the `mode`/`drMode`/`applyPlannedToDr` state, or the DrSimPanel mount.
  </action>
  <verify>
    <automated>npx tsc -b 2>&1 | tail -5 && npx @biomejs/biome check src/components/planning/PlanningView.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<PlannedEstatePanel" src/components/planning/PlanningView.tsx` returns 1
    - `grep -c "useEstateView" src/components/planning/PlanningView.tsx` returns exactly 1 (the single pre-existing call — no second one added)
    - `grep -c "useMemo" src/components/planning/PlanningView.tsx` returns 0
    - `<PlannedEstatePanel` is rendered with `view={view}` (the existing hook result, not a new call)
    - `npx tsc -b` exits 0; `npx @biomejs/biome check src/components/planning/PlanningView.tsx` clean
  </acceptance_criteria>
  <done>PlannedEstatePanel is mounted in PlanningView with the existing single `view`, inside the existing ErrorBoundary, single-useEstateView invariant intact.</done>
</task>

<task type="auto">
  <name>Task 3: Add the plannedEstate alloc i18n keys (EN + FR)</name>
  <files>src/i18n/locales/en/alloc.json, src/i18n/locales/fr/alloc.json</files>
  <read_first>
    - src/i18n/locales/en/alloc.json + src/i18n/locales/fr/alloc.json (the files being modified — study the existing `planned.*` block as the structural + FR-terminology analog; PlanningView already uses `useTranslation('alloc')` with `planned.emptyHeading`/`planned.emptyBody`)
    - src/i18n/keyParity.test.ts (the EN↔FR deep-diff gate — every added EN key lands in FR with the identical path)
    - .planning/phases/11-report-and-deck-gap-closure-surface-phase-9-storage-network-/11-PATTERNS.md § i18n (FR conventions: no pre-formatted numbers, no editorial verbs; FR app terminology)
    - CLAUDE.md § Conventions (i18n keys in BOTH en/ and fr/; no pre-formatted numbers; no editorial verbs)
  </read_first>
  <action>
    Add a `plannedEstate` nested block to BOTH src/i18n/locales/en/alloc.json and src/i18n/locales/fr/alloc.json with identical key paths: `title` (EN "Planned vs measured estate" / FR "Parc planifié vs mesuré"), `none` (EN "—" / FR "—"), `vmMeasured` (EN "VMs measured" / FR "VM mesurées"), `vmPlanned` (EN "VMs planned" / FR "VM planifiées"), `vcpuMeasured` (EN "vCPU:pCPU measured" / FR "vCPU:pCPU mesuré"), `vcpuPlanned` (EN "vCPU:pCPU planned" / FR "vCPU:pCPU planifié"), `clusterDelta` (EN "Per-cluster delta" / FR "Écart par cluster"), `colCluster` (EN "Cluster" / FR "Cluster"), `colMeasured` (EN "Measured" / FR "Mesuré"), `colPlanned` (EN "Planned" / FR "Planifié"). Mirror the existing `planned` block's nesting + JSON style (no trailing commas, valid JSON). No number, unit value, or editorial verb in any string. These are the keys `PlannedEstatePanel` (Task 1) resolves via `t('plannedEstate.*')`.
  </action>
  <verify>
    <automated>npx vitest run src/i18n/keyParity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/i18n/keyParity.test.ts` exits 0 (alloc.json EN↔FR parity green)
    - Both `src/i18n/locales/en/alloc.json` and `src/i18n/locales/fr/alloc.json` contain a `"plannedEstate"` block (`grep -l '"plannedEstate"' src/i18n/locales/en/alloc.json src/i18n/locales/fr/alloc.json` lists both)
    - `node -e "JSON.parse(require('fs').readFileSync('src/i18n/locales/fr/alloc.json'))"` and EN equivalent succeed (valid JSON)
    - The EN and FR `plannedEstate` key trees are identical (paths match)
    - No string value contains a digit-formatted number, `1.048576`, or `recommend|should|poor|good|recommandé|devrait` (grep over the block returns 0)
  </acceptance_criteria>
  <done>alloc.json EN+FR carry a matched `plannedEstate` block; keyParity green; valid JSON; factual-only — the chrome `PlannedEstatePanel` resolves.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-memory EstateView prop → rendered DOM | Already-on-screen estate data presented in a new on-screen panel. No new untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06 | Information disclosure | PlannedEstatePanel rendering cluster names / counts | accept | Same data already rendered by DrSimPanel/PlannedRatiosControl on the same screen; no new surface, no PII beyond the existing planning view. React text nodes escape content. |
| T-11-07 | Tampering | single-useMemo / engine-purity invariant | mitigate | Acceptance criteria grep-gate: 0 `useMemo`, 0 `@/engines`, 0 second `useEstateView` — the panel is a pure prop-consumer (tsc-enforced types). |

Phase 11 adds no network/auth/input surface; this plan only mounts a presenter over already-in-memory view-models under the existing fetchGuard/CSP privacy invariant.
</threat_model>

<verification>
- `npx tsc -b` exits 0
- `npx vitest run src/i18n/keyParity.test.ts` exits 0
- `npx @biomejs/biome check src/components/planning/PlannedEstatePanel.tsx src/components/planning/PlanningView.tsx` clean
- `view.plannedView` has a non-test reader (PlannedEstatePanel); no second useMemo / @/engines import
</verification>

<success_criteria>
F-1 screen side closed: `PlanningView` renders a `PlannedEstatePanel` reading `view.plannedView` vs `view.globals` (measured-vs-planned + per-cluster delta), a pure single-useEstateView prop-consumer, factual-only, EN+FR. `view.plannedView` is no longer a dead projection on screen. Plans 11-05/11-06 carry it into the exports.
</success_criteria>

<output>
After completion, create `.planning/phases/11-report-and-deck-gap-closure-surface-phase-9-storage-network-/11-04-SUMMARY.md`
</output>
