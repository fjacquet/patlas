---
phase: 11-report-and-deck-gap-closure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [STG-01, NET-01, DTL-01, ALR-01, VSR-01, PLN-03, PLN-04]
must_haves:
  truths:
    - "REQUIREMENTS.md traceability rows for shipped Phases 4/5/9/10 read Complete (not Pending)"
    - "HTM/PPT/DEP rows are labelled Phase 10 (not the stale Phase 7)"
    - "PLN-03/PLN-04 read Complete (consistent with the F-1 render+export outcome)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Accurate traceability table so /gsd-audit-milestone 1.0 can pass"
      contains: "Phase 10"
  key_links:
    - from: ".planning/REQUIREMENTS.md traceability table"
      to: "/gsd-audit-milestone 1.0 re-audit"
      via: "Source-A traceability rows matching shipped + SUMMARY requirements_completed reality"
      pattern: "Phase 10 \\| Complete"
---

<objective>
Reconcile the stale `REQUIREMENTS.md` traceability table so a re-audit (`/gsd-audit-milestone 1.0`) passes. Mechanical doc edit only — flip the stale `Pending` rows for shipped Phases 4/5/9/10 to `Complete`, correct the HTM/PPT/DEP rows mislabeled `Phase 7` → `Phase 10`, and confirm PLN-03/PLN-04 read `Complete` (consistent with the F-1 render+export closure landing in plans 11-04/05/06).

Purpose: The v1.0 audit (`gaps_found`) flagged the traceability as stale: ~46 rows still `Pending` despite shipping; HTM/PPT/DEP rows mislabeled `Phase 7` (they are Phase 10). This blocks a clean re-audit even after F-1/F-2 close. No discussion needed (CONTEXT bookkeeping decision — Claude's discretion, mechanical).
Output: an accurate traceability table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/11-report-and-deck-gap-closure-surface-phase-9-storage-network-/11-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip stale Pending rows to Complete and correct the mislabeled HTM/PPT/DEP phase</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines ~250-339 — the traceability table being edited; note PLN-03/04 at :275-276 ALREADY read Complete, so they are verified-not-flipped)
    - .planning/v1.0-MILESTONE-AUDIT.md (the `tech_debt` milestone-wide note: "Phases 4/5/9/10 rows still 'Pending' despite shipping (~46 rows); HTM/PPT/DEP rows mislabeled 'Phase 7' (they are Phase 10)")
    - .planning/phases/11-report-and-deck-gap-closure-surface-phase-9-storage-network-/11-CONTEXT.md § Bookkeeping (the mechanical edit scope)
    - CLAUDE.md § Gotchas (the grep-gate / security-hook "absence comment" trap — do not write a comment literally naming a forbidden token; this is a doc edit so phrase any explanatory note plainly)
  </read_first>
  <action>
    Edit `.planning/REQUIREMENTS.md` traceability table (the `| ID | Phase | Status |` rows ~lines 250-333) mechanically:
    (1) Phase 4 rows — `MVC-01..04`, `STR-01..03` (lines 257-263): change `Pending` → `Complete`.
    (2) Phase 5 rows — `RCI-01..05` (lines 264-268): change `Pending` → `Complete`.
    (3) Phase 9 rows — `STG-01..05`, `NET-01..05`, `DTL-01..03`, `ALR-01..05`, `VSR-01..05` (lines 300-322): change `Pending` → `Complete` (shipped on-screen P9; Phase 11 surfaces them to report+deck — both states are now Complete once 11-05/11-06 land; this reconcile records the shipped reality, the F-2 closure plans deliver the deliverable side).
    (4) HTM/PPT/DEP rows — `HTM-01..05`, `PPT-01..04`, `DEP-01..02` (lines 323-333): change the Phase column from `Phase 7` → `Phase 10` AND `Pending` → `Complete` (they shipped in Phase 10 — exports + deploy).
    (5) Verify `PLN-03`/`PLN-04` (lines 275-276) already read `Complete` — leave as-is (the F-1 render+export in plans 11-04/05/06 makes that accurate; do NOT flip them back, do NOT duplicate).
    Do NOT touch already-`Complete` rows (Phases 2/3/6/7/8 and the `~~retired~~`/struck rows — those carry deliberate strike-through annotations; leave verbatim). Do NOT renumber, reorder, or change the `Coverage:` summary block at the bottom unless a count it states is now wrong (if the prose still reads correctly leave it; this is a status-column edit, not a re-count). Use plain Markdown table cells — no inline HTML, no comment naming any grep-gated token.
  </action>
  <verify>
    <automated>grep -c "Phase 7 | Pending" .planning/REQUIREMENTS.md; grep -c "Phase 9 | Pending" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 9 | Pending" .planning/REQUIREMENTS.md` returns 0 (all P9 rows now Complete)
    - `grep -c "Phase 4 | Pending" .planning/REQUIREMENTS.md` returns 0
    - `grep -c "Phase 5 | Pending" .planning/REQUIREMENTS.md` returns 0
    - No `HTM-`/`PPT-`/`DEP-` row still reads `Phase 7` (`grep -E '\| (HTM|PPT|DEP)-[0-9]+ \| Phase 7' .planning/REQUIREMENTS.md` returns nothing)
    - Every `HTM-`/`PPT-`/`DEP-` row reads `| Phase 10 | Complete |`
    - `PLN-03` and `PLN-04` rows read `Complete` (unchanged, verified present)
    - The struck/retired rows (lines ~269-282 with `~~` or "Retired"/"Dropped"/"Re-derived" text) are byte-identical to before (no accidental edit)
    - The file is still valid GitHub-Flavored Markdown (table renders — no broken `|` alignment that drops a column)
  </acceptance_criteria>
  <done>The traceability table records all shipped requirements as Complete with correct phase labels; struck rows untouched; a re-audit Source-A trace matches the SUMMARY `requirements_completed` + integration-WIRED reality.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Pure planning-doc bookkeeping edit. No code, no runtime, no data, no input boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | Tampering | traceability accuracy | mitigate | Edit is constrained to status/phase columns of shipped rows; acceptance criteria assert struck/retired rows are byte-identical and counts are zeroed — no fabricated Complete for unshipped work (all flipped rows are SUMMARY-`requirements_completed`-listed + integration-WIRED per the audit). |

No code, network, auth, or input surface. Proportionate: this is a documentation reconcile with no runtime threat.
</threat_model>

<verification>
- `grep -c "Phase 9 | Pending"`, `"Phase 4 | Pending"`, `"Phase 5 | Pending"` all return 0
- No HTM/PPT/DEP row reads `Phase 7`
- PLN-03/PLN-04 read Complete
- REQUIREMENTS.md is valid Markdown; struck rows untouched
</verification>

<success_criteria>
`REQUIREMENTS.md` traceability accurately records shipped Phases 4/5/9/10 as Complete with correct phase labels (HTM/PPT/DEP → Phase 10), PLN-03/04 Complete. A re-run of `/gsd-audit-milestone 1.0` no longer flags stale traceability.
</success_criteria>

<output>
After completion, create `.planning/phases/11-report-and-deck-gap-closure-surface-phase-9-storage-network-/11-03-SUMMARY.md`
</output>
