---
phase: 11-report-and-deck-gap-closure
plan: 06
type: execute
wave: 2
depends_on: ["11-01", "11-02"]
files_modified:
  - src/engines/export/html/renderReport.tsx
  - src/engines/export/export.worker.ts
  - src/engines/export/html/renderReport.test.tsx
autonomous: true
requirements: [STG-01, STG-02, STG-03, STG-04, STG-05, NET-01, NET-02, NET-03, NET-04, NET-05, DTL-01, DTL-02, DTL-03, ALR-01, ALR-02, ALR-03, ALR-04, ALR-05, VSR-01, VSR-02, VSR-03, VSR-04, VSR-05, PLN-03, PLN-04]
must_haves:
  truths:
    - "The HTML report contains a Storage section (KPI + per-cluster/per-datastore tables + factual flagged count + the storageTreemap chart slot)"
    - "The HTML report contains a Network section (factual count/table rollup)"
    - "The HTML report contains a planned-vs-measured subsection (F-1 export side, per-cluster delta)"
    - "vSAN shared-LUN datastores render as 'shared across N clusters' (factual)"
    - "The exported HTML stays under the <5 MB typical budget on a large estate (top-N inline + remainder folded)"
  artifacts:
    - path: "src/engines/export/html/renderReport.tsx"
      provides: "Storage + Network + planned sections in the fixed HTM-04 order + the storage-treemap chart slot"
      contains: "data-chart-slot=\"storage-treemap\""
    - path: "src/engines/export/export.worker.ts"
      provides: "the storage-treemap HTML slot fed the storageTreemap SSR SVG"
      contains: "storage-treemap"
  key_links:
    - from: "src/engines/export/html/renderReport.tsx"
      to: "view.storage / view.network / view.flags / view.vsan / view.datastoreDetail / view.plannedView"
      via: "Section + Metric + annex-table + the storage-treemap data-chart-slot"
      pattern: "view\\.storage"
    - from: "src/engines/export/export.worker.ts (html branch)"
      to: "the 'storage-treemap' chart slot"
      via: "charts.set('storage-treemap', chartToSvg(optBundle.shared.storageTreemap, ...))"
      pattern: "storage-treemap"
---

<objective>
F-2 (HTML side) + F-1 (HTML side): add the exhaustive Phase 9 detail to the HTML report — dedicated **Storage** section (KPI + per-cluster/per-datastore tables + factual flagged count + the 11-01 `storageTreemap` chart), **Network** section (factual count/table rollup, no chart per D-08), vSAN shared-LUN as "shared across N clusters", and a **planned-vs-measured** subsection (F-1, per-cluster delta) — all in the fixed HTM-04 `data-section` order, sized against the <5 MB typical / <15 MB hard-ceiling budget.

Purpose: "the report is the product" — the HTML report carries the *exhaustive* P9 detail (Phase 10 D-02; the deck stays executive-tight via 11-05). This plan also feeds the single estate-level `storageTreemap` SVG into a fixed HTML chart slot.
Output: renderReport.tsx Storage/Network/planned sections + the export.worker.ts HTML-branch slot wiring + renderReport test extension.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-report-and-deck-gap-closure-surface-phase-9-storage-network-/11-CONTEXT.md
@.planning/phases/11-report-and-deck-gap-closure-surface-phase-9-storage-network-/11-PATTERNS.md
@.planning/phases/11-report-and-deck-gap-closure-surface-phase-9-storage-network-/11-01-SUMMARY.md
@.planning/phases/11-report-and-deck-gap-closure-surface-phase-9-storage-network-/11-02-SUMMARY.md

<interfaces>
renderReport.tsx: <Section id title> :50-61 ; <Metric label value flag?> :63-70 (flag=true paints the gold `.flagged` style — D-06) ; the `.annex-table` <table> pattern :193-216 ; TOP_N_CLUSTERS=16 size lever :29-33 ; exportChartSlots(view) :237-242 ; `data-chart-slot` slot mechanism :143-145 ; fmtInt/fmtPercentWhole from @/utils/format ; loc = bcp47(locale)
StorageByX.estate { provisionedMib; inUseMib; capacityMib; usedMib; freeMib }, .byCluster/.byDatastore StorageConsumptionGroup[] {key;provisionedMib;inUseMib}, .capacityByDatastore StorageCapacityGroup[] {key;name;capacityMib;usedMib;freeMib} (storageByX.ts:30-64)
ThresholdFlags.counts { fs; ds; lu } (thresholdFlags.ts:50)
NetworkRollup { vswitches[]; dvswitches[]; portgroups[]; vmPortgroupCount } (network.ts:53-59)
VsanRelinkResult: attributed / shared-across-N / unrelinkable maps keyed by `naa ?? name` (estate.ts:512-516; read the VsanRelinkResult type for the shared-across-N field name before coding)
EstateView.plannedView { globals: GlobalSummary; clusters: ClusterAggregate[] } | null (estate.ts:494)
export.worker.ts html branch :63-72 — builds `charts: Map<string,string>` from exportChartSlots per-cluster; assembleHtml SLOT_RE splices ANY data-chart-slot id (no assembleHtml change)
chartToSvg(opt, CHART_W, CHART_H) (export.worker.ts:69) ; optBundle.shared.storageTreemap is the 11-01 key
Security (renderReport.tsx:9-16): every user-derived value is a plain JSX text node ({text}); the React raw-markup escape-hatch prop is grep-gated FORBIDDEN in this file
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Storage + Network + planned sections to renderReport.tsx in the fixed HTM-04 order</name>
  <files>src/engines/export/html/renderReport.tsx</files>
  <read_first>
    - src/engines/export/html/renderReport.tsx (the file being modified — the `<Section>`/`<Metric>` components :50-70, the per-cluster section + `data-chart-slot` :123-148, the `.annex-table` table :193-216, the `TOP_N_CLUSTERS` size lever + sort :29-33/:85-87, `exportChartSlots` :237-242, the security doc-comment :9-16 — plain `{text}` nodes only, the raw-markup prop is grep-gated forbidden)
    - src/engines/aggregation/storageByX.ts:30-64 (StorageByX field shapes) + thresholdFlags.ts:43-51 (counts) + network.ts:53-59 (NetworkRollup)
    - src/types/estate.ts:494 + :512-516 (plannedView shape; VsanRelinkResult — read the actual shared-across-N field/key name in the VsanRelinkResult type definition before coding the "shared across N clusters" row)
    - .planning/phases/11-report-and-deck-gap-closure-surface-phase-9-storage-network-/11-PATTERNS.md § renderReport.tsx (the Section + chart-slot idiom to replicate; the `<Metric flag>` gold style for D-06; the budget lever; the security gotcha; the safer plain `<table className="annex-table">` over reusing DataTable.tsx — avoids the inventory:col.<id> + flex-desync gotcha)
    - .planning/phases/11-report-and-deck-gap-closure-surface-phase-9-storage-network-/11-02-SUMMARY.md (the resolved `storage.*`/`network.*`/`planned.*` report.json keys)
    - CLAUDE.md § Gotchas (do NOT reuse DataTable.tsx for report tables — use the plain `.annex-table` `<table>` to sidestep the `inventory:col.<id>` header + flex `<thead>/<tbody>` desync latent bug; do NOT write a doc-comment literally naming the grep-gated raw-markup prop — phrase absence notes plainly like the existing :9-16)
  </read_first>
  <action>
    Modify the `Report` function in src/engines/export/html/renderReport.tsx. Add three new `<Section>`s in the fixed HTM-04 order (insert AFTER the existing `id="per-cluster"` section :148 and BEFORE `id="eos"` :150, so the narrative reads cluster → storage → network → … ; planned subsection placement is planner discretion — either its own `<Section id="planned">` or a subsection inside an existing planning-adjacent section; recommended: a dedicated `<Section id="planned">` placed after `id="dr"`):

    `<Section id="storage" title={strings['storage.title'] ?? 'Storage'}>`: estate `<Metric>`s for provisioned/inUse/capacity (`fmtInt(Math.round(Number(view.storage.estate.provisionedMib) / 1024), loc)` — branded MiB→GiB via `Math.round(.../1024)`, NEVER `* 1.048576`); a factual flagged `<Metric label={strings['storage.flagged'] ?? 'Flagged datastores'} value={fmtInt(view.flags.counts.ds + view.flags.counts.lu, loc)} flag={view.flags.counts.ds + view.flags.counts.lu > 0} />` (the `flag` prop paints the gold `.flagged` style — D-06, no traffic-light, no verb); a per-cluster storage `<table className="annex-table">` (mirror the `.annex-table` pattern :193-211) over `view.storage.byCluster` with columns Cluster / Provisioned / In-use (apply the SAME `TOP_N_CLUSTERS` top-N-inline + remainder-folded budget lever the file already uses at :85-87/:33 — sort `byCluster` desc by provisioned, render top-N rows, fold the rest into a single "+N more" summary row or omit per the existing lever — keep typical estate < 5 MB); a per-datastore `<table className="annex-table">` over `view.storage.byDatastore` (same top-N lever); the vSAN shared-LUN rows from `view.vsan` rendered factually as `strings['storage.vsanShared']` with the `{{n}}` count substituted (read the VsanRelinkResult shared-across-N map's key/length to compute N — factual "shared across N clusters", NEVER a verdict); and the chart slot `<div data-chart-slot="storage-treemap" className="chart-slot" />` (the single estate-level treemap; Task 2 feeds it). Every cluster/datastore name is a plain `{c.key}` / `{d.name}` JSX text node — never the grep-gated raw-markup prop (renderReport.tsx:9-16 security model).

    `<Section id="network" title={strings['network.title'] ?? 'Network'}>`: factual `<Metric>`s — vSwitches `fmtInt(view.network.vswitches.length, loc)`, dvSwitches `view.network.dvswitches.length`, Portgroups `view.network.portgroups.length`, VM adjacencies `view.network.vmPortgroupCount` (each with its `strings['network.*']` label). No chart (D-08). A portgroup `<table className="annex-table">` over `view.network.portgroups` (network/switch/vmCount columns) is acceptable detail; empty arrays render an empty table / `0` metrics (factual-degrade — never fabricate).

    `<Section id="planned" title={strings['planned.title'] ?? 'Planned vs measured estate'}>`: if `view.plannedView === null` render `<p className="factual-note">{strings['planned.none'] ?? '—'}</p>` (mirror the `id="dr"` null branch :160-161); otherwise estate measured-vs-planned `<Metric>`s (measured `view.globals.vcpuPerPcpu`/`vmCount` vs planned `view.plannedView.globals.*`) AND a per-cluster delta `<table className="annex-table">` (D-03 expected shape — cluster name + measured vs planned `vcpuPerPcpu`, joining `view.plannedView.clusters` to same-named `view.clusters`; apply the top-N lever). Factual-only — no editorial verb.

    Add the new section ids to nothing else (assembleHtml SLOT_RE matches any `data-chart-slot`; the HTM-04 order is the textual `<Section>` order). Do NOT reuse `components/inventory/DataTable.tsx` (CLAUDE.md gotcha — use the plain `.annex-table` `<table>`). Do NOT add any `dark:` twin (light-theme-fixed static file, :20-22). Do NOT add a doc-comment that literally names the grep-gated raw-markup prop.
  </action>
  <verify>
    <automated>npx tsc -b 2>&1 | tail -5 && npx @biomejs/biome check src/engines/export/html/renderReport.tsx</automated>
  </verify>
  <acceptance_criteria>
    - renderReport.tsx contains `<Section id="storage"`, `<Section id="network"`, `<Section id="planned"` (each grep ≥ 1)
    - It contains `data-chart-slot="storage-treemap"` (exactly 1)
    - It references `view.storage`, `view.network`, `view.flags.counts`, `view.vsan`, `view.plannedView`
    - The storage flagged Metric uses the `flag={...> 0}` gold-style prop; no traffic-light class, no editorial verb (`grep -iE 'recommend|should|poor|good|warning|risk' src/engines/export/html/renderReport.tsx` returns 0)
    - No `1.048576`; MiB→GiB uses `Math.round(Number(` … `/ 1024)`
    - It does NOT import `components/inventory/DataTable` (`grep -c "inventory/DataTable" src/engines/export/html/renderReport.tsx` is 0)
    - The grep-gated raw-markup prop is still absent from the file (the existing grep gate on renderReport.tsx still passes — no new occurrence, including in comments)
    - `npx tsc -b` exits 0; biome check clean
  </acceptance_criteria>
  <done>renderReport.tsx carries Storage (KPI + per-cluster/per-datastore tables + factual flagged count + treemap slot + vSAN shared-across-N) + Network (factual rollup) + planned (measured-vs-planned + per-cluster delta) sections in the fixed HTM-04 order, budget-aware, plain-text-node-safe, factual-only.</done>
</task>

<task type="auto">
  <name>Task 2: Feed the storage-treemap HTML chart slot in the export worker's HTML branch</name>
  <files>src/engines/export/export.worker.ts</files>
  <read_first>
    - src/engines/export/export.worker.ts:63-72 (the file being modified — the HTML branch that builds `const charts = new Map<string, string>()` from `exportChartSlots(view)` per-cluster then calls `assembleHtml`; `chartToSvg(opt, CHART_W, CHART_H)` :69)
    - .planning/phases/11-report-and-deck-gap-closure-surface-phase-9-storage-network-/11-PATTERNS.md § export.worker.ts + § renderReport.tsx (the single estate-level treemap gets a FIXED slot id "storage-treemap" fed alongside the per-cluster loop; assembleHtml SLOT_RE matches any id — no assembleHtml change)
    - .planning/phases/11-report-and-deck-gap-closure-surface-phase-9-storage-network-/11-01-SUMMARY.md (`optBundle.shared.storageTreemap` is the 11-01 key — present in the shared option bundle on the HTML path too, since chartBundle is path-agnostic)
    - src/engines/export/html/renderReport.tsx (Task 1's `data-chart-slot="storage-treemap"` — the slot id must match exactly)
  </read_first>
  <action>
    In src/engines/export/export.worker.ts, in the `if (req.kind === 'html')` branch (export.worker.ts:63-72), after the existing `for (const slot of exportChartSlots(view)) { ... charts.set(slot.id, ...) }` per-cluster loop and before the `assembleHtml({ view, trends, charts, ... })` call, add a single line feeding the estate-level treemap slot: `charts.set('storage-treemap', chartToSvg(optBundle.shared.storageTreemap, CHART_W, CHART_H))`. The slot id `'storage-treemap'` MUST exactly match the `data-chart-slot="storage-treemap"` in renderReport.tsx (Task 1). Use the SAME `chartToSvg(opt, CHART_W, CHART_H)` call shape the per-cluster loop uses (export.worker.ts:69 — no rasterize, SVG inlined directly on the HTML path). Do NOT touch the `else` PPTX branch (it auto-rasterizes `optBundle.shared` generically — owned by 11-05's contract, no edit needed there). Do NOT change `assembleHtml` (its SLOT_RE splices any `data-chart-slot` id). Guard for absence only if `optBundle.shared.storageTreemap` could be undefined — it is always present per 11-01 (threaded unconditionally into `shared`), so a guard is optional; if added, keep it factual (skip the set, the slot renders empty — never fabricate).
  </action>
  <verify>
    <automated>npx tsc -b 2>&1 | tail -5 && npx @biomejs/biome check src/engines/export/export.worker.ts</automated>
  </verify>
  <acceptance_criteria>
    - export.worker.ts contains `charts.set('storage-treemap'` (exactly 1) inside the `req.kind === 'html'` branch
    - It references `optBundle.shared.storageTreemap`
    - The PPTX `else` branch is unchanged (the `for (const [k, opt] of Object.entries(optBundle.shared))` raster loop is byte-identical to before)
    - assembleHtml is not modified (`git diff --name-only` does not list src/engines/export/html/assembleHtml.ts)
    - `npx tsc -b` exits 0; biome check clean
  </acceptance_criteria>
  <done>The HTML branch feeds the `storage-treemap` slot the storageTreemap SSR SVG with the per-cluster loop; slot id matches renderReport.tsx; PPTX branch + assembleHtml untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the renderReport test for the new sections + a budget assertion</name>
  <files>src/engines/export/html/renderReport.test.tsx</files>
  <read_first>
    - src/engines/export/html/renderReport.test.tsx (the file being modified/extended — the existing `renderReport(input)` string-assertion pattern; if no such test file exists, create it mirroring src/engines/export/buildExportView.test.ts:16-45 fixture-factory + `buildEstateView(mergeSnapshotsToEstate([snap]), …)`)
    - src/engines/export/html/renderReport.tsx (the sections under test — assert `data-section="storage"`, `data-section="network"`, `data-section="planned"` and `data-chart-slot="storage-treemap"` appear in the returned string)
    - .planning/phases/11-report-and-deck-gap-closure-surface-phase-9-storage-network-/11-PATTERNS.md § Engine test (the buildExportView.test.ts fixture idiom; engines/ Vitest-gated ≥75%)
  </read_first>
  <action>
    Create or extend src/engines/export/html/renderReport.test.tsx. Using the `buildExportView.test.ts:16-45` fixture-factory idiom (build a real `EstateView` with ≥2 clusters and non-empty `storage.byCluster`/`network`/`flags`), add `describe`/`it` cases asserting the `renderReport(input)` output string contains: `data-section="storage"`, `data-section="network"`, `data-section="planned"`, and `data-chart-slot="storage-treemap"`; the flagged `<Metric>` renders with the `flagged` class when `view.flags.counts.ds + view.flags.counts.lu > 0`; the planned section renders the `planned.none` factual note when `view.plannedView` is null (use `EMPTY_VIEW` or a null-plannedView fixture) and the measured-vs-planned metrics otherwise. Add a budget-shape assertion: build a large-estate fixture (e.g. 40+ clusters, many datastores) and assert `Buffer.byteLength(renderReport(input), 'utf8')` is under a generous bound that proves the top-N lever folds the remainder (e.g. < 2 MB for the body string before asset inlining — the existing TOP_N_CLUSTERS lever must visibly cap inline rows; assert the rendered string does NOT contain every one of the 40 cluster names inline if the lever folds them, OR assert the body length stays bounded). Mirror the existing test's assertion idiom and the strings bag it constructs.
  </action>
  <verify>
    <automated>npx vitest run src/engines/export/html/renderReport.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/engines/export/html/renderReport.test.tsx` exits 0 with all new assertions passing
    - The test asserts presence of `data-section="storage"`, `data-section="network"`, `data-section="planned"`, `data-chart-slot="storage-treemap"`
    - The test covers the `view.plannedView === null` branch (planned.none) and the non-null branch
    - The test includes a large-estate budget-shape assertion proving the top-N fold (bounded body length or capped inline cluster rows)
    - `npx @biomejs/biome check src/engines/export/html/renderReport.test.tsx` clean
  </acceptance_criteria>
  <done>renderReport.test.tsx asserts the three new sections, the treemap slot, the D-06 flagged style, the plannedView null/non-null branches, and the budget fold; vitest green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-memory EstateView → downloaded HTML file | Already-on-screen P9 view-models serialized into a static HTML report. No new input; output is a user-initiated local download. Chart SVG spliced by assembleHtml (the single sanctioned string-assembly layer). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-11 | Tampering / XSS | user-derived cluster/datastore/VM names in the new report sections | mitigate | Every name is a plain JSX `{text}` node — `renderToStaticMarkup` HTML-escapes it; the raw-markup escape-hatch prop stays absent (grep-gated, asserted in acceptance criteria). Chart SVG is the trusted ECharts-SSR string spliced by assembleHtml, not held in the React tree (renderReport.tsx:9-16 model preserved). |
| T-11-12 | Information disclosure | exhaustive per-datastore/per-cluster detail in the HTML report | accept | This detail is exactly what the P9 on-screen views (StorageView/NetworkView) already show; the report is the explicit deliverable for it (Phase 10 D-02). Active snapshot + active locale only. No PII beyond the existing on-screen surface. |
| T-11-13 | Denial of service | oversized HTML on a very large estate | mitigate | The existing top-N-inline + remainder-folded budget lever (TOP_N_CLUSTERS) is reused for the new tables; Task 3 asserts the bound — keeps typical estate < 5 MB, hard ceiling < 15 MB. |
| T-11-14 | Tampering | editorial fabrication (flag / vSAN / planned) | mitigate | Flagged = raw count + gold `.flagged` style only; vSAN = factual "shared across N clusters"; planned = measured-vs-planned numbers; acceptance criteria grep-gate the no-verb denylist. |

Phase 11 adds no network/auth/input surface; this plan serializes already-in-memory view-models to a user-downloaded file under the existing fetchGuard/CSP privacy invariant — the threat surface is "do not leak more than the on-screen views already do" (satisfied: identical data) plus "no editorial fabrication" (gated) plus the preserved no-raw-markup React-tree invariant.
</threat_model>

<verification>
- `npx tsc -b` exits 0
- `npx vitest run src/engines/export/html/renderReport.test.tsx` exits 0 (3 new sections + treemap slot + null/non-null planned + budget fold asserted)
- `npx @biomejs/biome check` clean on the three files
- The grep-gated raw-markup prop is still absent from renderReport.tsx (existing gate passes)
- assembleHtml.ts + the export.worker.ts PPTX branch unmodified
</verification>

<success_criteria>
F-2 HTML side + F-1 HTML side closed: the HTML report carries an exhaustive Storage section (KPI + per-cluster/per-datastore tables + factual flagged count + the storageTreemap chart + vSAN shared-across-N), a Network section (factual rollup), and a planned-vs-measured subsection (per-cluster delta), in the fixed HTM-04 order, budget-aware (top-N fold), plain-text-node-safe, factual-only, tested.
</success_criteria>

<output>
After completion, create `.planning/phases/11-report-and-deck-gap-closure-surface-phase-9-storage-network-/11-06-SUMMARY.md`
</output>
