---
phase: 07-os-end-of-support-forecast
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - src/types/estate.ts
  - src/engines/aggregation/estateView.ts
  - src/utils/format.ts
  - src/components/Chart.tsx
  - src/components/ViewToggle.tsx
  - src/components/eos/EosView.tsx
  - src/App.tsx
  - src/i18n/locales/en/eos.json
  - src/i18n/locales/fr/eos.json
  - src/i18n/locales/en/inventory.json
  - src/i18n/locales/fr/inventory.json
  - src/i18n/index.ts
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [EOS-01, EOS-02, EOS-03, EOS-04, EOS-05, EOS-06]
must_haves:
  truths:
    - "A user opens the 5th 'eos' ViewToggle segment and sees the as-of line, the catalogue-verified line, a 6-tile bucket strip (overdue / ≤+3 / ≤+6 / ≤+9 / ≤+12 / unknown-OS), a forecast chart, and a reconciliation line"
    - "Clicking any of the five entity buckets opens the shipped P3 DataTable drilled to that bucket's affected VMs; clicking the unknown-OS bucket shows the verbatim raw OS strings + occurrence counts"
    - "ESXi hosts are surfaced classified by build→support state as a separate kind, never summed with VM counts; patch level shows the em-dash sentinel"
    - "When the catalogue lastVerified is >90 days before today, a neutral 'catalogue may be outdated' caption appears; every string exists in both en/ and fr/"
    - "D-00: copy is factual with no editorial verbs, EN/FR parity holds, the em-dash sentinel is used for non-determinable values, the EOS projection composes inside the existing buildEstateView single pass with NO new useMemo, no localStorage of dataset rows, and nothing is fabricated"
    - "D-03: an always-visible 'EOS catalogue verified: YYYY-MM-DD' line plus a neutral '>90 days → catalogue may be outdated' caption render with EN/FR parity"
    - "D-05: the view is the 5th 'eos' ViewToggle segment reusing the shipped `fieldset role=group` + aria-pressed idiom — no new nav component is introduced"
    - "D-07: the 'as of {today}' reference date (the injected reference from buildEosProjection) is surfaced in the EOS view"
    - "D-11: the unknown-OS drill surfaces each verbatim unrecognized OS string plus its occurrence count — an aggregated-count-only display is not acceptable"
    - "D-12: the original raw OS string is preserved verbatim in the unknown-OS drill list display (auto-escaped React text child, never mutated)"
    - "D-09: ESXi hosts are surfaced as a separate entity kind classified by build→support state, never summed with VM counts, with the patch level shown as the em-dash sentinel"
    - "D-08: the entity-bucket drill reuses the shipped P3 DataTable (objectKind 'vm'|'esx') — no new table component is introduced"
  artifacts:
    - path: "src/components/eos/EosView.tsx"
      provides: "Read-only EOS presenter (no computation; reads view.eos via useEstateView)"
      contains: "useEstateView"
    - path: "src/engines/aggregation/estateView.ts"
      provides: "eos projection composed inside the existing buildEstateView single pass — NO new useMemo"
      contains: "buildEosProjection"
    - path: "src/utils/format.ts"
      provides: "fmtDate(iso, locale) with em-dash sentinel on invalid input"
      contains: "export const fmtDate"
    - path: "src/i18n/locales/en/eos.json"
      provides: "EN eos namespace (factual copy, no editorial verbs)"
      contains: "bucket"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/eos/EosView.tsx"
      via: "activeView === 'eos' ? <EosView /> branch"
      pattern: "EosView"
    - from: "src/engines/aggregation/estateView.ts"
      to: "src/engines/eos/bucketEos.ts"
      via: "buildEosProjection inside buildEstateView (single pass, no second memo)"
      pattern: "buildEosProjection"
    - from: "src/components/eos/EosView.tsx"
      to: "src/components/inventory/DataTable.tsx"
      via: "bucket→entity drill, objectKind 'vm'|'esx' (D-08 reuse)"
      pattern: "DataTable"
---

<objective>
Wire the EOS projection into the single `buildEstateView` pass, add the 5th `'eos'` ViewToggle segment, register the ECharts heatmap/calendar series, and ship the read-only `EosView` presenter (bucket strip, forecast chart, P3 DataTable drill, unknown-OS raw-string list, freshness lines) with EN/FR i18n parity. Reconcile the stale REQUIREMENTS.md EOS→"Phase 5" mapping to Phase 7.

Purpose: Closes EOS-01..06 end-to-end and renders the phase goal (the lifecycle conversation) — bucket strip primary, chart secondary, no verdict color (D-00/G1, approved 07-UI-SPEC).
Output: modified `estate.ts`/`estateView.ts`/`format.ts`/`Chart.tsx`/`ViewToggle.tsx`/`App.tsx`, new `EosView.tsx`, new `eos` i18n namespace (en+fr), `nav.eos` added to inventory.json (en+fr), and reconciled REQUIREMENTS.md/ROADMAP.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-os-end-of-support-forecast/07-CONTEXT.md
@.planning/phases/07-os-end-of-support-forecast/07-RESEARCH.md
@.planning/phases/07-os-end-of-support-forecast/07-PATTERNS.md
@.planning/phases/07-os-end-of-support-forecast/07-UI-SPEC.md
@.planning/phases/07-os-end-of-support-forecast/07-01-SUMMARY.md
@.planning/phases/07-os-end-of-support-forecast/07-02-SUMMARY.md

<interfaces>
<!-- From Plan 02: buildEosProjection({ vinfo, vhost, catalogue, today }): EosProjection -->
<!-- From Plan 01: loadEosCatalogue(): EosCatalogue (the parse-once boundary) -->

<!-- src/components/ViewToggle.tsx (EXTEND — proven 2x P5 'hosts'/P6 'planning'): -->
export type AppView = 'dashboard' | 'inventory' | 'hosts' | 'planning'   // → add | 'eos'
const VIEWS = ['dashboard','inventory','hosts','planning'] as const       // → add 'eos'
// ViewToggle reads t(`nav.${view}`, ns 'inventory') — add nav.eos to BOTH en/fr inventory.json.
// Keep <fieldset role="group"> + biome-ignore (CI grep gate asserts literal role="group") + aria-pressed + arrow wraparound VERBATIM.

<!-- src/components/inventory/DataTable.tsx (D-08 reuse — no new table): -->
export interface DataTableProps<T> { data: T[]; columns: ColumnDef<T>[]; headerFor: (id:string)=>string; objectKind: 'vm'|'esx'|'datastore'; defaultColumnVisibility?: VisibilityState }
// VM column defs: src/components/inventory/columns/vmColumns.ts → vmColumns: ColumnDef<VmDisplayRow>[]
// ESX column defs: src/components/inventory/columns/esxColumns.ts → esxColumns: ColumnDef<EsxAggregate>[]

<!-- src/engines/aggregation/estateView.ts (MODIFY — self-pattern): the return object (lines 250-267)
     adds each forward-compat field; EMPTY_VIEW (lines 290-307) gives it a frozen empty value
     (mirror trends:null / plannedView:null / EMPTY_INSIGHTS Object.freeze idiom). -->

<!-- src/utils/format.ts (MODIFY): fmtInt(n, locale='fr-FR') / fmtPercent — locale param + em-dash on bad input. NO date helper exists today; add fmtDate. -->

<!-- src/components/Chart.tsx CAVEAT: the echarts.use([...]) registry (lines 27-36) currently registers
     only Bar/Pie/Gauge. Heatmap/Calendar series are NOT registered yet — must be added (SVG-only, canvas stays excluded — VIZ-01). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EstateView.eos type + buildEstateView compose + fmtDate + ECharts registry</name>
  <read_first>
    - src/types/estate.ts (lines 346-388 — the EstateView interface; the `trends: T|null` / `plannedView: {...}|null` JSDoc idiom to mirror)
    - src/engines/aggregation/estateView.ts (lines 108-126 the vinfo loop, 224-267 the plannedView/return idiom, 270-307 EMPTY_VIEW/EMPTY_INSIGHTS Object.freeze idiom)
    - src/utils/format.ts (lines 14, 20-21 fmtInt — locale param + em-dash sentinel idiom)
    - src/components/Chart.tsx (lines 27-36 the echarts.use registry; lines 43-60 the single-option contract)
    - .planning/phases/07-os-end-of-support-forecast/07-RESEARCH.md §Integration (lines 399-415, exact compose call) + §Pattern 3 (EosProjection shape)
    - .planning/phases/07-os-end-of-support-forecast/07-PATTERNS.md §estateView.ts / §estate.ts / §format.ts / §Forecast timeline chart sections
  </read_first>
  <behavior>
    - `EstateView` gains `eos: EosProjection` (typed; `EMPTY_VIEW.eos` is a frozen empty projection — mirror the `plannedView: null` / `EMPTY_INSIGHTS` Object.freeze idiom)
    - `buildEstateView` composes `eos` via `buildEosProjection({ vinfo: merged.vinfo, vhost: merged.vhost, catalogue: loadEosCatalogue(), today: new Date() })` inside the EXISTING single pass — there is NO second `useMemo` and no second iteration (it rides the existing `for (const vm of merged.vinfo)` operation-class — D-00 single-memo invariant)
    - `fmtDate('2026-05-17', 'en-US')` returns a locale-formatted date; `fmtDate('not-a-date')` returns `'—'` (em-dash sentinel, never `0`/"N/A" — D-00)
    - The ECharts registry includes the Heatmap series + Calendar component so the forecast chart renders as inline SVG (canvas stays excluded — VIZ-01)
  </behavior>
  <action>
    Modify `src/types/estate.ts`: add `eos: EosProjection` to `EstateView` with JSDoc mirroring the `plannedView` note ("Produced inside the single buildEstateView pass — no second useMemo"); export the `EosProjection` interface (the §Pattern 3 shape) — define it here or re-export from `src/engines/eos/bucketEos.ts`, whichever keeps the existing import direction (engines → types) intact; if a cycle would form, define `EosProjection` in `estate.ts` and have `bucketEos.ts` `import type` it. Modify `src/engines/aggregation/estateView.ts`: add `import { buildEosProjection } from '@/engines/eos/bucketEos'` and `import { loadEosCatalogue } from '@/engines/eos/catalogue'` at top; inside `buildEstateView`, in the same loop family as the existing `classifyOsFamily` pass, compute `const eos = buildEosProjection({ vinfo: merged.vinfo, vhost: merged.vhost, catalogue: loadEosCatalogue(), today: new Date() })`; add `eos` to the single `return { ... }` object; add a frozen empty `eos` to `EMPTY_VIEW` (Object.freeze, mirror `trends: null`/`EMPTY_INSIGHTS`). Modify `src/utils/format.ts`: add `export const fmtDate = (iso: string, locale = 'fr-FR'): string` mirroring `fmtInt` exactly (locale param; `Number.isNaN(new Date(iso).getTime()) ? '—' : d.toLocaleDateString(locale, {...})`). Modify `src/components/Chart.tsx`: add `HeatmapChart` (or the calendar-compatible series) + `CalendarComponent` to the existing `echarts.use([...])` array (lines 27-36) — keep the SVG renderer / Midnight theme injection untouched; do NOT add canvas.
  </action>
  <verify>
    <automated>npx vitest run src/engines src/utils && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `rtk grep -c "buildEosProjection" src/engines/aggregation/estateView.ts` ≥1 and `rtk grep -c "useMemo" src/engines/aggregation/estateView.ts` returns 0 (single-memo invariant — D-00)
    - `src/utils/format.ts` exports `fmtDate`; a test/assertion confirms `fmtDate('not-a-date')` === `'—'`
    - `EstateView` has an `eos` member and `EMPTY_VIEW` has a frozen `eos` (`rtk grep -c "eos" src/types/estate.ts` ≥1; `rtk grep -n "eos" src/engines/aggregation/estateView.ts` shows it in both the return object and EMPTY_VIEW)
    - `rtk grep -c "Heatmap\|Calendar" src/components/Chart.tsx` ≥1 and `rtk grep -c "renderer: 'canvas'\|CanvasRenderer" src/components/Chart.tsx` returns 0 (SVG-only — VIZ-01)
    - `npx tsc --noEmit` exits 0; `npx vitest run src/engines src/utils` exits 0
  </acceptance_criteria>
  <done>EstateView carries a typed eos projection composed in the single pass with no new memo; fmtDate added with the em-dash sentinel; ECharts heatmap/calendar registered SVG-only.</done>
</task>

<task type="auto">
  <name>Task 2: 5th 'eos' ViewToggle segment + App branch + EosView presenter + i18n EN/FR</name>
  <read_first>
    - src/components/ViewToggle.tsx (lines 3-5 AppView/VIEWS; lines 28 useTranslation('inventory'); lines 31-49 the role="group"+biome-ignore+arrow-wraparound — keep VERBATIM)
    - src/components/hosts/HostsView.tsx (the primary presenter analog — lines 25-37 shell, 64-98 the plain-table/break-all verbatim-string idiom, 107-114 the Stat label↔value composition)
    - src/components/planning/PlanningView.tsx (lines 21-32 + 81 the scoped ErrorBoundary reading only error.message; lines 64-77 the no-snapshot early return)
    - src/App.tsx (lines 4-9 view imports; lines 38-45 the activeView === 'planning' ternary chain)
    - src/components/inventory/DataTable.tsx (lines 22-31 DataTableProps) + src/components/inventory/columns/{vmColumns,esxColumns}.ts
    - src/i18n/locales/en/inventory.json + src/i18n/index.ts (namespace registration) ; src/i18n/locales/en/hosts.json (the namespace structure analog)
    - .planning/phases/07-os-end-of-support-forecast/07-UI-SPEC.md §Component Inventory + §Copywriting Contract (lines 100-117 — every string, EN, FR parity required) + §Claude's-Discretion Recommendations (Open Items 1/2/3 — segment, cumulative tiles, unknown-OS = drill body)
    - .planning/phases/07-os-end-of-support-forecast/07-PATTERNS.md §ViewToggle.tsx / §EosView.tsx / §App.tsx / §i18n sections
  </read_first>
  <action>
    Modify `src/components/ViewToggle.tsx`: add `'eos'` to the `AppView` union and as the 5th `VIEWS` entry — change ONLY those two lines; keep `<fieldset role="group">`, the `biome-ignore`, `<legend className="sr-only">`, `aria-pressed`, the arrow-wraparound `move()`/`onKeyDown`, and the `bg-accent-500 text-surface-900` active styling verbatim (Open Item 1 — D-05 reuses the shipped fieldset+aria-pressed idiom, no new nav component). Add `"eos": "<EN label>"` / `"<FR label>"` to the `nav` group in BOTH `src/i18n/locales/en/inventory.json` and `src/i18n/locales/fr/inventory.json`. Create `src/i18n/locales/en/eos.json` and `src/i18n/locales/fr/eos.json` with the factual copy from 07-UI-SPEC §Copywriting Contract (heading "OS end-of-support forecast"; as-of line "Forecast computed as of {{today}}"; freshness "EOS catalogue verified: {{date}}"; staleness "Catalogue may be outdated"; bucket labels "Overdue"/"Within 3 months"/.../"Within 12 months"/"Unknown OS"; reconciliation "{{total}} entities — {{bucketed}} bucketed"; "VMs: {{n}} · ESXi hosts: {{m}}"; drill affordance "Show {{label}} entities ({{count}})"; unknown heading "Unrecognized OS strings"; "{{count}} occurrences") — NO editorial verbs, NO pre-formatted numbers/dates in strings (interpolate) — D-00 factual-copy + EN/FR parity. Register the `eos` namespace in `src/i18n/index.ts` next to the existing namespaces. Create `src/components/eos/EosView.tsx`: `useTranslation('eos')`, `const view = useEstateView('active')` (the single memo — presenter only, NO computation), read `view.eos`. Render in a `<main className="flex-1 overflow-y-auto p-8"><div className="flex flex-col gap-6">` shell (verbatim HostsView): (a) the always-visible as-of line from `view.eos.reference.today` (D-07 — surface the injected reference date) + the catalogue-verified line from `view.eos.reference.lastVerified` via `fmtDate(..., i18n.language)` in the neutral `text-sm text-slate-500 dark:text-slate-400` caption idiom, plus the conditional ">90 days" staleness caption (D-03 — always-visible verified line + neutral outdated caption, EN/FR parity); (b) a bucket strip of 6 native `<button>` tiles (overdue / ≤+3 / ≤+6 / ≤+9 / ≤+12 / unknown-OS) using cumulative counts derived from the disjoint partition (Open Item 2), each `h-20`, label `text-sm font-semibold`, count `text-2xl font-mono tabular-nums`, `aria-label="Show {label} entities ({count})"`, `aria-pressed` for the selected tile, neutral `ring-2 ring-primary-500` + `bg-slate-100 dark:bg-surface-700` selected state (NEVER accent, NEVER a status color — no traffic-light tokens); (c) a reconciliation line computed from the disjoint partition with the VM/ESXi-split rendering (D-09 — never one conflated total); (d) the forecast chart via the shipped `<Chart option={...} />` (single option prop; a pure selector off `view.eos`; neutral surface/primary ramp, NOT util traffic-light); (e) the drill region — for the five entity buckets pass the selected bucket's affected rows to the shipped `<DataTable data=... columns={vmColumns|esxColumns} headerFor=... objectKind={'vm'|'esx'} />` (D-08 reuse); for the unknown-OS bucket render `view.eos.rawUnknown` as a plain `<table className="w-full text-left text-sm"><tbody className="font-mono tabular-nums">` with `break-all` cells showing the verbatim raw string + "{{count}} occurrences" (Open Item 3 — this is the drill body; D-11 verbatim string + occurrence count, never aggregated-count-only; D-12 original string preserved verbatim; render the string ONLY as an auto-escaped React text child — no raw-HTML injection sink whatsoever). Wrap the body in the scoped `<ErrorBoundary>` reading only `error.message` and the no-snapshot early-return, both verbatim from PlanningView. Modify `src/App.tsx`: add `import { EosView } from './components/eos/EosView'` next to the existing view imports and a `) : activeView === 'eos' ? (<EosView />` branch mirroring the `'planning'` branch exactly.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx @biomejs/biome check . && npm run test:run</automated>
  </verify>
  <acceptance_criteria>
    - `rtk grep -c "'eos'" src/components/ViewToggle.tsx` ≥2 (AppView union + VIEWS array) and `rtk grep -c 'role="group"' src/components/ViewToggle.tsx` ≥1 (CI grep gate preserved — D-05 idiom reuse)
    - `src/i18n/locales/en/eos.json` and `src/i18n/locales/fr/eos.json` exist with identical key sets: `diff <(node -p "JSON.stringify(Object.keys(require('./src/i18n/locales/en/eos.json')).sort())") <(node -p "JSON.stringify(Object.keys(require('./src/i18n/locales/fr/eos.json')).sort())")` produces no output
    - `nav.eos` present in both `en/inventory.json` and `fr/inventory.json` (`rtk grep -c '"eos"' src/i18n/locales/en/inventory.json src/i18n/locales/fr/inventory.json` ≥1 each)
    - `rtk grep -c "EosView" src/App.tsx` ≥2 (import + branch)
    - The verbatim raw OS string is rendered as an auto-escaped React text child only — no raw-HTML injection prop is used anywhere in `src/components/eos/EosView.tsx` (grep for the React raw-HTML prop name returns 0; XSS-safe — D-12 verbatim)
    - `rtk grep -c "useMemo" src/components/eos/EosView.tsx` returns 0 (presenter only — single-memo invariant, D-00)
    - `rtk grep -c "DataTable" src/components/eos/EosView.tsx` ≥1 (D-08 reuse — no new table)
    - `rtk grep -ci "recommend\|should\|poor\| good\|better" src/i18n/locales/en/eos.json src/i18n/locales/fr/eos.json` returns 0 (no editorial verbs — D-00)
    - `npx tsc --noEmit` exits 0; `npx @biomejs/biome check .` exits 0; `npm run test:run` exits 0
  </acceptance_criteria>
  <done>The 5th 'eos' segment, App branch, EosView presenter (bucket strip, chart, P3 DataTable drill, unknown-OS verbatim list, freshness lines), and EN/FR-parity i18n all ship; no new memo, no new table, no editorial verbs, no XSS sink.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile REQUIREMENTS.md EOS→Phase 7 + ROADMAP progress</name>
  <read_first>
    - .planning/REQUIREMENTS.md (the Traceability table — the six rows `EOS-01..EOS-06 | Phase 5 | Pending` that are STALE and must read `Phase 7`)
    - .planning/ROADMAP.md (### Phase 7 — the `**Plans**: TBD` line + the Progress table row `7. OS End-of-Support Forecast | 0/TBD`)
    - CLAUDE.md Gotchas ("gsd-sdk roadmap update-plan-progress does not match this ROADMAP — flip [ ]→[x] and the Progress-table row manually")
  </read_first>
  <action>
    In `.planning/REQUIREMENTS.md` Traceability table, change the six `EOS-01 | Phase 5 | Pending` … `EOS-06 | Phase 5 | Pending` rows to `EOS-0N | Phase 7 | Pending` (correct the stale Phase-5 mapping per 07-RESEARCH §Phase Requirements; leave status `Pending` until the phase is verified — do not pre-flip to Complete). In `.planning/ROADMAP.md` ### Phase 7, replace `**Plans**: TBD` with `**Plans**: 3 plans` and the three-line plan list (`- [ ] 07-01-PLAN.md — build-time catalogue spine`, `- [ ] 07-02-PLAN.md — pure EOS engines + fixtures`, `- [ ] 07-03-PLAN.md — EstateView wiring + EOS view + i18n`); update the Progress-table row `7. OS End-of-Support Forecast` from `0/TBD | Not started` to `0/3 | Not started` (the [ ]→[x] flip + Complete status is a verify-time action per the CLAUDE.md gotcha, not done here). Do NOT alter any other phase's rows.
  </action>
  <verify>
    <automated>rtk grep -c "EOS-0. | Phase 5" .planning/REQUIREMENTS.md; rtk grep -c "EOS-0. | Phase 7" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `rtk grep -c "EOS-0. | Phase 5" .planning/REQUIREMENTS.md` returns 0 (no stale Phase-5 EOS rows remain)
    - `rtk grep -c "EOS-0. | Phase 7" .planning/REQUIREMENTS.md` returns 6 (all six reconciled)
    - `.planning/ROADMAP.md` ### Phase 7 contains `**Plans**: 3 plans` and the three `07-0N-PLAN.md` list entries
    - The ROADMAP Progress table row for Phase 7 reads `0/3`
  </acceptance_criteria>
  <done>The stale REQUIREMENTS.md EOS→Phase-5 mapping is corrected to Phase 7; ROADMAP plan count + progress row reflect the 3-plan structure.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The EOS forecast view: a 5th 'eos' top-level ViewToggle segment opening a read-only forecast — always-visible "Forecast computed as of {today}" + "EOS catalogue verified: {date}" lines (plus a neutral "Catalogue may be outdated" caption when >90 days), a 6-tile bucket strip (Overdue / Within 3 / 6 / 9 / 12 months / Unknown OS), a neutral-ramp SVG forecast chart, a reconciliation line, one-click drill into the shipped P3 DataTable for the five entity buckets, and a verbatim raw-OS-string + occurrence-count list for the Unknown-OS bucket. EN/FR parity; no verdict color; no editorial verbs.</what-built>
  <how-to-verify>
    1. Run `npm run dev`, open http://localhost:5173/vatlas/, drop a real RVTools workbook from `tests/fixtures/` (e.g. `RVTools_export_all_2026-04-17…-MOM-vCenter.xlsx`).
    2. Click the new "OS end-of-support" (5th) segment in the top ViewToggle. Confirm the as-of line and the "EOS catalogue verified: …" line are visible at the top.
    3. Confirm the 6 bucket tiles render with mono tabular counts and that the reconciliation line's total equals the sum (and that VMs vs ESXi hosts are shown as distinct counts, never one merged total).
    4. Click each of the five entity buckets — confirm the shipped DataTable opens with the affected VMs/hosts (sortable/filterable). Click the "Unknown OS" bucket — confirm it shows the verbatim raw OS strings + "{n} occurrences" (not an aggregated count).
    5. Confirm no bucket is colored red/orange/green, there are no warning/alarm icons, and no copy uses "should/recommend/poor/good".
    6. Toggle FR/EN — confirm every label, line, and caption is translated (no raw keys, no English leaking into FR).
    7. Open DevTools Network — confirm ZERO non-same-origin requests while using the EOS view (privacy invariant). Open DevTools Elements on the chart — confirm it is inline `<svg>` (not `<canvas>`).
  </how-to-verify>
  <resume-signal>Type "approved" or describe the issues to fix</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Parsed RVTools OS strings → EosView rendering | Raw unrecognized OS strings are rendered verbatim in the unknown-OS list — untrusted text crosses into the DOM here |
| `loadEosCatalogue()` → `buildEstateView` | Already-validated typed catalogue (Plan 01 Zod boundary) enters the single aggregation pass |
| App runtime → network | The privacy invariant boundary — the P1 guard throws on any non-same-origin fetch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Tampering | XSS via a crafted RVTools OS string in the unknown-OS verbatim list | mitigate | React escapes text children by default; the list renders the string as a `{string}` child only (D-12 verbatim); no raw-HTML injection prop is used anywhere in `EosView.tsx` (grep-asserted == 0) |
| T-07-09 | Information disclosure | The EOS view triggering a non-same-origin fetch (e.g. a live catalogue lookup) | mitigate | Catalogue is `loadEosCatalogue()` (static import, validated at Plan 01 boundary); the view computes nothing and fetches nothing; P1 runtime guard throws on any non-same-origin request (checkpoint step 7 verifies zero network) |
| T-07-10 | Repudiation | Stale catalogue presented as authoritative without disclosure | mitigate | The as-of line + "EOS catalogue verified: {date}" line are always visible; a neutral ">90 days" caption surfaces staleness factually (D-03) — not hidden |
| T-07-11 | Tampering | Bucket selection persisted to localStorage (privacy breach) | accept→mitigate | Selected-bucket state is ephemeral component `useState`; no `localStorage` write (D-00 — no localStorage of dataset rows) — `rtk grep -c "localStorage" src/components/eos/EosView.tsx` == 0 enforced by the project privacy guard pattern |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx @biomejs/biome check .` exits 0
- `npm run test:run` exits 0
- `npm run test:coverage` — `src/engines/eos/` ≥75% (auto-gated by the existing `src/engines/**/*.ts` include — EOS-06)
- `rtk grep -c "useMemo" src/components/eos/EosView.tsx src/engines/aggregation/estateView.ts` returns 0 (single-memo invariant — D-00)
- No raw-HTML injection prop appears in `src/components/eos/EosView.tsx` (grep-asserted == 0; XSS-safe)
- EN/FR `eos.json` key sets are identical (parity gate)
- `rtk grep -c "EOS-0. | Phase 5" .planning/REQUIREMENTS.md` returns 0 (stale mapping reconciled)
</verification>

<success_criteria>

- The 5th 'eos' ViewToggle segment opens a read-only EOS view with bucket strip, chart, drill, and freshness lines (EOS-01/02/03, success criteria 1/3)
- ESXi hosts are surfaced classified by build→support state as a distinct kind, patch level = em-dash (EOS-04, success criterion 3)
- The unknown-OS bucket drills into a verbatim raw-string + occurrence-count list, nothing dropped (EOS-05, success criterion 2)
- The catalogue `lastVerified` line is always visible with a >90-day neutral caption (EOS-06, success criterion 4)
- EN/FR parity holds; no editorial verbs; no verdict color; no new memo; no XSS sink; zero runtime network (D-00/G1, privacy invariant)
- REQUIREMENTS.md EOS rows reconciled from the stale Phase 5 to Phase 7
</success_criteria>

<output>
After completion, create `.planning/phases/07-os-end-of-support-forecast/07-03-SUMMARY.md`
</output>
</content>
