---
phase: 07-os-end-of-support-forecast
plan: 02
type: tdd
wave: 2
depends_on: [01]
files_modified:
  - src/engines/eos/fixtures/real-os-strings.ts
  - src/engines/eos/normalizeOs.ts
  - src/engines/eos/normalizeOs.test.ts
  - src/engines/eos/classifyEsxi.ts
  - src/engines/eos/classifyEsxi.test.ts
  - src/engines/eos/bucketEos.ts
  - src/engines/eos/bucketEos.test.ts
autonomous: true
requirements: [EOS-01, EOS-02, EOS-04, EOS-05]
must_haves:
  truths:
    - "Every RVTools OS string resolves to either a (slug, version) pair or first-class unknown — nothing is silently dropped; the raw string is preserved verbatim"
    - "A VMware ESXi 8.0.3 build host classifies to major 8.0 (eolFrom 2027-10-11); the patch level resolves to the em-dash sentinel, never a fabricated date"
    - "The bucket partition is disjoint and its counts sum exactly to the entity total (overdue + windows + beyond12 + unknown == total), with host vs VM cardinality split, never conflated"
    - "RHEL-8 four variants all normalize to (rhel,8); Oracle-Linux three variants all normalize to (oracle-linux,8); unknown-OS rate <5% on the 50+ real-string fixture"
  artifacts:
    - path: "src/engines/eos/normalizeOs.ts"
      provides: "Pure regex-bank rawOs → { slug, version } | null (composes classifyOsFamily, Zod-free)"
      contains: "export function normalizeOs"
    - path: "src/engines/eos/classifyEsxi.ts"
      provides: "Pure esxVersion → { majorEol, patchEol: null } against the catalogue"
      contains: "export function classifyEsxi"
    - path: "src/engines/eos/bucketEos.ts"
      provides: "Pure (vinfo, vhost, catalogue, today) → EosProjection disjoint partition"
      contains: "export function buildEosProjection"
    - path: "src/engines/eos/fixtures/real-os-strings.ts"
      provides: "50+ harvested REAL_OS_STRINGS + REAL_ESX_VERSIONS fixture"
      contains: "REAL_OS_STRINGS"
  key_links:
    - from: "src/engines/eos/normalizeOs.ts"
      to: "src/engines/aggregation/osFamily.ts"
      via: "compose classifyOsFamily as the coarse gate (DRY — never duplicate the family regexes)"
      pattern: "classifyOsFamily"
    - from: "src/engines/eos/bucketEos.ts"
      to: "src/engines/eos/catalogueSchema.ts"
      via: "import type { EosCatalogue } (catalogue passed in as a parameter — never imports catalogue.json)"
      pattern: "import type.*EosCatalogue"
---

<objective>
Build the three pure, Zod-free `src/engines/eos/` functions — OS-string normalizer, ESXi build classifier, lifecycle bucketer — and the harvested real-string fixture, all test-first.

Purpose: Satisfies EOS-01 (+3/6/9/12 buckets), EOS-02 (overdue bucket), EOS-04 (ESXi build→support state with patch em-dash), EOS-05 (first-class unknown-OS, nothing dropped) and ROADMAP success criteria 5 (<5% unknown) and 6 (RHEL-8×4 + Oracle-Linux×3, ≥75% coverage — auto-gated by the existing `src/engines/**/*.ts` include).
Output: `normalizeOs.ts`, `classifyEsxi.ts`, `bucketEos.ts`, `fixtures/real-os-strings.ts`, and their `.test.ts` siblings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-os-end-of-support-forecast/07-CONTEXT.md
@.planning/phases/07-os-end-of-support-forecast/07-RESEARCH.md
@.planning/phases/07-os-end-of-support-forecast/07-PATTERNS.md
@.planning/phases/07-os-end-of-support-forecast/07-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (src/engines/eos/catalogueSchema.ts): -->
type EosCatalogue = { lastVerified: string; products: Record<string, {
  name: string; releases: { name: string; label: string; releaseDate: string|null;
  isEol: boolean; eolFrom: string|null; isMaintained: boolean }[] }> }

<!-- From src/engines/aggregation/osFamily.ts (DRY — compose, do not duplicate): -->
export function classifyOsFamily(osConfig: string, osTools: string): OsFamily  // 'windows'|'linux'|'other'

<!-- From src/types/vinfo.ts: VInfoRow has vmName, osConfig (string), osTools (string) -->
<!-- From src/types/vhost.ts: VHostRow has esxVersion (string), model (string), vendor (string) -->

<!-- EosProjection shape — verbatim from 07-RESEARCH.md §Pattern 3 (lines 232-242): -->
interface EosProjection {
  reference: { today: string; lastVerified: string }
  partition: { overdue: Row[]; w3: Row[]; w3to6: Row[]; w6to9: Row[]; w9to12: Row[]; beyond12: Row[]; unknown: Row[] }
  cumulative: { overdue: number; le3: number; le6: number; le9: number; le12: number; unknown: number }
  rawUnknown: { osString: string; count: number }[]
  esxi: { /* split by kind — host counts NEVER summed with VM counts (D-09b) */ }
}
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Real OS-string fixture + normalizeOs regex bank (RED→GREEN)</name>
  <read_first>
    - src/engines/aggregation/osFamily.ts (the purity/header idiom — pure, no deps, import type only, lowercase-normalize then regex cascade with a guaranteed terminal fallback; lines 1-20)
    - src/engines/aggregation/osFamily.test.ts (the exact test-shape analog)
    - .planning/phases/07-os-end-of-support-forecast/07-RESEARCH.md §Pattern 2 (lines 205-225, the RULES array verbatim) + §Code Examples (lines 346-397, REAL_OS_STRINGS + REAL_ESX_VERSIONS verbatim) + Pitfalls 2 & 3
    - .planning/phases/07-os-end-of-support-forecast/07-PATTERNS.md §normalizeOs.ts section
  </read_first>
  <behavior>
    - RHEL 8 four variants all → `{ slug: 'rhel', version: '8' }`: `'Red Hat Enterprise Linux 8 (64-bit)'`, `'Red Hat Enterprise Linux 8.10'`, `'RHEL 8 (64-bit)'`, `'redhat enterprise linux 8'` (case-insensitive, D-12)
    - Oracle-Linux three variants all → `{ slug: 'oracle-linux', version: '8' }`: `'Oracle Linux 8'`, `'Oracle Enterprise Linux 8'`, `'Oracle Linux Server 8.10'`
    - `'Microsoft Windows Server 2012 (64-bit)'` → `{ slug: 'windows-server', version: '2012' }`; `'Debian GNU/Linux 12 (64-bit)'` → `{ slug: 'debian', version: '12' }`
    - Versionless / multi-version forms → `null` (first-class unknown, D-10): `'Other (64-bit)'`, `'Other 3.x or later Linux (64-bit)'`, `'CentOS 4/5/6/7 (64-bit)'` (multi-version → unknown per Pitfall 3, A3), `'Ubuntu Linux (64-bit)'` (versionless → unknown)
    - normalizeOs whitespace-normalizes for MATCHING ONLY; it never mutates/returns the raw input (caller preserves verbatim — D-12)
    - On REAL_OS_STRINGS (≥50 distinct harvested strings): unknown rate <5% measured per-distinct-string; the test prints BOTH the per-distinct-string and (if occurrence weights are available) the per-string count for transparency (A4)
  </behavior>
  <action>
    Create `src/engines/eos/fixtures/real-os-strings.ts` exporting `REAL_OS_STRINGS` and `REAL_ESX_VERSIONS` copied VERBATIM from 07-RESEARCH.md §Code Examples (lines 352-397) — `as const` arrays. Write `src/engines/eos/normalizeOs.test.ts` FIRST (RED): the RHEL-8×4, Oracle-Linux×3, Windows-Server/Debian positive cases, the versionless/multi-version→null cases, the D-12 verbatim-preservation case, and a `<5%` assertion over `REAL_OS_STRINGS` that lists every string the bank failed to resolve (so the unknown residue is the honest long tail per Pitfall 2). Then create `src/engines/eos/normalizeOs.ts` (GREEN): a pure, Zod-free, `import type`-only module exporting `export function normalizeOs(raw: string): { slug: string; version: string | null } | null`, using the `RULES` slug-bank verbatim from §Pattern 2 (most-specific slug first; case-insensitive `/i`; `norm = raw.replace(/\s+/g,' ').trim()` for matching only). Call `classifyOsFamily` as the coarse gate / fallback signal (DRY — do NOT re-implement the windows/linux family regexes; 07-PATTERNS §normalizeOs.ts). Multi-version `4/5/6/7` and versionless forms fall through to `return null`. Document the multi-version-CentOS→unknown choice (Pitfall 3, A3) in the file header.
  </action>
  <verify>
    <automated>npx vitest run src/engines/eos/normalizeOs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/engines/eos/normalizeOs.ts` exports `normalizeOs`
    - `src/engines/eos/fixtures/real-os-strings.ts` exports `REAL_OS_STRINGS` and `REAL_ESX_VERSIONS`; `node -p "require('ts')" ` not needed — `rtk grep -c "as const" src/engines/eos/fixtures/real-os-strings.ts` ≥1 and the array has ≥50 entries
    - `rtk grep -c "from 'zod'" src/engines/eos/normalizeOs.ts` returns 0 (pure, Zod-free)
    - `rtk grep -c "classifyOsFamily" src/engines/eos/normalizeOs.ts` ≥1 (DRY compose)
    - `npx vitest run src/engines/eos/normalizeOs.test.ts` exits 0 (incl. the <5% unknown assertion and the RHEL-8×4 + Oracle-Linux×3 tests)
  </acceptance_criteria>
  <done>RHEL-8×4 and Oracle-Linux×3 normalize correctly; unknown rate <5% on the 50+ fixture; raw strings never mangled; module is pure and composes osFamily.ts.</done>
</task>

<task type="tdd">
  <name>Task 2: classifyEsxi — build→support state with patch em-dash (RED→GREEN)</name>
  <read_first>
    - src/engines/aggregation/osFamily.ts (same purity/return-contract analog)
    - src/utils/format.ts (the em-dash "not determinable" sentinel idiom — lines 14, 20-21)
    - .planning/phases/07-os-end-of-support-forecast/07-RESEARCH.md Pitfall 1 (lines 280-284 — ESXi v1 is MAJOR-only; patch level → em-dash, never fabricated) + §Code Examples ESXi records (lines 320-327) + Pitfall 6 (guest-OS "VMware ESXi" rows are VMs, never host counts)
    - .planning/phases/07-os-end-of-support-forecast/07-PATTERNS.md §classifyEsxi.ts section
  </read_first>
  <behavior>
    - `'VMware ESXi 8.0.3 build-24674464'` → major `8.0`, `majorEol` resolved from catalogue `esxi` release `8.0` (`2027-10-11`), `patchEol` = `null` (surfaced as the em-dash sentinel by the presenter; never a fabricated date — D-09c)
    - `'VMware ESXi 7.0.x build-N'` → major `7.0`, `majorEol` `2025-10-02`, `isEol` true (overdue relative to a 2026 reference)
    - An ESXi version string with no catalogue match → `majorEol: null` (em-dash), never invented
    - The function consumes ONLY the `vhost.esxVersion` string + the passed `EosCatalogue`; it does not look at VM `osConfig` (Pitfall 6 — nested-ESXi guest rows are VMs, never host counts)
  </behavior>
  <action>
    Write `src/engines/eos/classifyEsxi.test.ts` FIRST (RED) covering the four behaviors above, using `REAL_ESX_VERSIONS` and an inline minimal `EosCatalogue` with the `esxi` `8.0`/`7.0` releases. Then create `src/engines/eos/classifyEsxi.ts` (GREEN): pure, Zod-free, `import type { EosCatalogue }` only, exporting `export function classifyEsxi(esxVersion: string, catalogue: EosCatalogue): { major: string | null; majorEol: string | null; patchEol: null }`. Extract the major (`/esxi\s*(\d+\.\d+)/i`), look it up in `catalogue.products.esxi.releases` by `name`, return its `eolFrom` as `majorEol` (or `null` if absent). `patchEol` is ALWAYS `null` — the v1 catalogue has no patch-level EOL (confirmed fact, Pitfall 1); never fabricate. Document this confirmed-catalogue-fact in the header.
  </action>
  <verify>
    <automated>npx vitest run src/engines/eos/classifyEsxi.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/engines/eos/classifyEsxi.ts` exports `classifyEsxi`
    - `rtk grep -c "from 'zod'" src/engines/eos/classifyEsxi.ts` returns 0 (pure, Zod-free)
    - `rtk grep -c "patchEol: null\|patchEol = null\|patchEol:null" src/engines/eos/classifyEsxi.ts` ≥1 (patch level is the em-dash sentinel — never fabricated, D-09c / Pitfall 1)
    - `rtk grep -c "osConfig\|osTools" src/engines/eos/classifyEsxi.ts` returns 0 (consumes vhost.esxVersion only — Pitfall 6)
    - `npx vitest run src/engines/eos/classifyEsxi.test.ts` exits 0
  </acceptance_criteria>
  <done>ESXi build strings classify to major-version EOL from the catalogue; patch level is always the null/em-dash sentinel; host classification never touches VM OS strings.</done>
</task>

<task type="tdd">
  <name>Task 3: bucketEos — disjoint partition + reconciliation + raw-unknown (RED→GREEN)</name>
  <read_first>
    - src/engines/aggregation/estateView.ts (the consumer / single-pass idiom — lines 108-126 the vinfo loop, 250-267 return shape)
    - src/engines/aggregation/estateView.test.ts (the test-shape analog for an EstateView projection)
    - .planning/phases/07-os-end-of-support-forecast/07-RESEARCH.md §Pattern 3 (lines 228-242 — the EosProjection shape verbatim) + Pitfall 4 (today is an INJECTED parameter — never `new Date()` inside the bucketer) + §Phase Requirements → Test Map (lines 494-503)
    - .planning/phases/07-os-end-of-support-forecast/07-UI-SPEC.md §Copywriting Contract (reconciliation line; host/VM split D-09b) + Open Item 2 (cumulative overlay derived from the disjoint partition)
    - .planning/phases/07-os-end-of-support-forecast/07-PATTERNS.md §bucketEos.ts section
  </read_first>
  <behavior>
    - `buildEosProjection({ vinfo, vhost, catalogue, today })` returns an EosProjection whose `partition` is DISJOINT and whose member counts sum EXACTLY to the VM entity total (overdue + w3 + w3to6 + w6to9 + w9to12 + beyond12 + unknown == vinfo.length) — reconciliation invariant, nothing dropped (D-06/D-10)
    - "Overdue" = catalogue `eolFrom` strictly before the injected `today` (D-02 semantics; D-07 reference = today, NOT capture date)
    - A VM whose OS normalizes but whose (slug,version) has no catalogue EOL, OR whose OS does not normalize, lands in `unknown`; its raw verbatim string + occurrence count appears in `rawUnknown` (D-11 — never aggregated-count-only; D-12 verbatim)
    - `cumulative` (le3/le6/le9/le12) is derived FROM the disjoint partition (monotonic ⊇); reconciliation is computed from the partition, never the cumulative sum (no double-count — Open Item 2)
    - ESXi host classification is reported in `esxi` split by entity kind; host counts are NEVER summed into the VM partition total (D-09b)
    - `today` is a function parameter; the bucketer never calls `new Date()` (deterministic tests — Pitfall 4)
  </behavior>
  <action>
    Write `src/engines/eos/bucketEos.test.ts` FIRST (RED): a reconciliation test (sum of disjoint partition == vinfo length on a synthetic mixed set), an overdue test with an injected fixed `today`, an unknown-with-rawUnknown test (verbatim string + count), a cumulative-derived-from-partition test, and a host/VM-split test (D-09b — assert host count is NOT added to the VM total). Then create `src/engines/eos/bucketEos.ts` (GREEN): pure, Zod-free, `import type { EosCatalogue }` + `import { normalizeOs }` + `import { classifyEsxi }`, exporting `export function buildEosProjection(args: { vinfo: VInfoRow[]; vhost: VHostRow[]; catalogue: EosCatalogue; today: Date }): EosProjection`. Implement the EosProjection shape verbatim from §Pattern 3: a single pass over `vinfo` (rides the same operation-class as the existing `classifyOsFamily` pass), preserving each row's raw OS string verbatim for the unknown list (D-12), occurrence-counting raw unknowns (D-11), partitioning into the disjoint windows by `eolFrom` vs the injected `today`, then deriving the `cumulative` overlay from that partition. Classify `vhost` via `classifyEsxi` into the separate `esxi` sub-structure (split by kind — never summed with VMs, D-09b). `reference = { today: <today ISO>, lastVerified: catalogue.lastVerified }`. NEVER call `new Date()` inside this function (Pitfall 4).
  </action>
  <verify>
    <automated>npx vitest run src/engines/eos/bucketEos.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/engines/eos/bucketEos.ts` exports `buildEosProjection`
    - `rtk grep -c "from 'zod'" src/engines/eos/bucketEos.ts` returns 0 (pure, Zod-free)
    - `rtk grep -c "new Date(" src/engines/eos/bucketEos.ts` returns 0 (today injected — Pitfall 4)
    - `rtk grep -c "import type" src/engines/eos/bucketEos.ts` ≥1 and it imports `EosCatalogue` as a type (never imports `catalogue.json`)
    - `npx vitest run src/engines/eos/bucketEos.test.ts` exits 0 (incl. the reconciliation == total assertion and the host/VM split assertion)
    - `npx vitest run src/engines/eos` exits 0 (all three engine suites green together)
  </acceptance_criteria>
  <done>The disjoint partition reconciles to the entity total; overdue uses the injected today; raw unknown strings are verbatim + occurrence-counted; cumulative is a derived overlay; host/VM cardinality is split, never conflated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Parsed RVTools rows → `src/engines/eos/` | Already Zod-trimmed at the parser boundary; the EOS engines treat OS strings as inert data only |
| Typed `EosCatalogue` (parameter) → engines | Validated upstream at the `catalogue.ts` boundary (Plan 01); engines never re-validate, never import the JSON |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Tampering | Raw RVTools OS string treated as code in the normalizer | mitigate | Strings are only matched against static `RegExp` literals (data, not `eval`/`Function`); no dynamic regex from input; raw string never executed |
| T-07-06 | Information disclosure | Engine reaching for the network (e.g., live EOL lookup) | mitigate | Engines are pure, Zod-free, dependency-free; catalogue is a passed-in parameter; `rtk grep -c "fetch\|import.*catalogue.json" src/engines/eos/*.ts` == 0 verified |
| T-07-07 | Repudiation | Stale lifecycle data presented as current | accept | `reference.lastVerified` is carried into the projection and surfaced in the view (Plan 03 / D-03); not hidden — handled in the presenter, low risk here |
</threat_model>

<verification>
- `npx vitest run src/engines/eos` exits 0 (all three suites + the <5%-unknown + RHEL-8×4 + Oracle-Linux×3 assertions)
- `npx @biomejs/biome check .` exits 0
- `rtk grep -rc "from 'zod'" src/engines/eos/normalizeOs.ts src/engines/eos/classifyEsxi.ts src/engines/eos/bucketEos.ts` all return 0 (engines stay pure)
- `rtk grep -c "new Date(" src/engines/eos/bucketEos.ts` returns 0
</verification>

<success_criteria>
- `normalizeOs` resolves RHEL-8×4 + Oracle-Linux×3 and keeps unknown <5% on the 50+ real fixture (criteria 5/6)
- `classifyEsxi` maps build strings to major-version EOL; patch level is the em-dash sentinel, never fabricated (EOS-04, Pitfall 1)
- `buildEosProjection` produces a disjoint partition that reconciles exactly to the entity total with first-class unknown + raw-string capture (EOS-01/02/05, D-10/D-11/D-12)
- All three engines are pure, Zod-free, and receive the catalogue as a parameter; `today` is injected (Pitfall 4)
- `src/engines/eos/` auto-gated ≥75% by the existing coverage include (EOS-06 structurally satisfied)
</success_criteria>

<output>
After completion, create `.planning/phases/07-os-end-of-support-forecast/07-02-SUMMARY.md`
</output>
