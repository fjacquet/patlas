---
phase: 06-allocation-dr-re-derived
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/components/ViewToggle.tsx
  - src/App.tsx
  - src/components/dashboard/GlobalDashboard.tsx
  - src/components/planning/PlanningView.tsx
  - src/components/planning/PlannedRatiosControl.tsx
  - src/i18n/locales/en/inventory.json
  - src/i18n/locales/fr/inventory.json
  - src/i18n/locales/en/alloc.json
  - src/i18n/locales/fr/alloc.json
autonomous: true
requirements: [PLN-01, PLN-02, PLN-03, PLN-04]
must_haves:
  truths:
    - "A user sees a 4th 'Planning' segment in the top-level ViewToggle and can open it"
    - "A user sets planned CPU/RAM ratios via preset buttons that fill an editable numeric field; default is CPU 4:1 / RAM 1:1"
    - "Planned ratios are not the measured value — the planned surface carries a 'planned' qualifier and a read-only 'measured' caption pointing at Operational Insights"
    - "Planned ratios persist nowhere — refresh = data gone (no URL-hash, no localStorage)"
    - "The killed AllocationSliders + URL-hash mechanism no longer appears on the Dashboard"
    - "D-00: every displayed planned/measured figure echoes parsed RVTools-derived values only (no fabricated numbers), strings carry no editorial verbs, EN/FR key sets stay identical, and presenters add no useMemo (single-useMemo invariant)"
    - "D-01: no new realized-ratio component is added — the measured value is read from the shipped P5 Operational Insights via a read-only caption, never rebuilt here"
    - "D-02: the planned surface references the realized figure read-only with a 'measured' label and never overwrites or hides it with planning inputs"
    - "D-03: the planned lens and the measured reference are structurally separate labelled surfaces — they never share a tile or number"
    - "D-04: the planning lens is a new 4th top-level ViewToggle segment built by extending the shipped fieldset/aria-pressed idiom, not a new nav component"
    - "D-05: the Personal-Ratios control is named preset buttons (1:1, 4:1, 8:1, VDI 10:1) filling an editable numeric field — no slider widget — defaulting CPU 4:1 / RAM 1:1"
    - "D-06: planned what-if state is in-memory Zustand only — no URL-hash codec, no localStorage; the killed useAllocationHash mechanism is excluded"
  artifacts:
    - path: "src/components/ViewToggle.tsx"
      provides: "4th 'planning' segment"
      contains: "'planning'"
    - path: "src/components/planning/PlanningView.tsx"
      provides: "Planning screen shell hosting the planned-ratios control (+ DR panel slot for Plan 03)"
    - path: "src/components/planning/PlannedRatiosControl.tsx"
      provides: "Preset buttons + editable numeric field, Zustand-backed, no URL-hash"
  key_links:
    - from: "src/App.tsx"
      to: "PlanningView"
      via: "activeView === 'planning' branch in the view switch"
      pattern: "planning.*PlanningView"
    - from: "src/components/planning/PlannedRatiosControl.tsx"
      to: "useSnapshotStore plannedRatios slice"
      via: "selectPlannedRatios / selectSetPlannedRatios"
      pattern: "selectPlannedRatios"
---

<objective>
Ship the new top-level "Planning" surface: extend the shipped ViewToggle to a 4th
segment, build the Planning view shell, replace the killed slider+URL-hash
allocation mechanism with an in-memory preset+numeric planned-ratios control, wire
App, and remove `AllocationSliders` + `useAllocationHash` from the Dashboard.

Purpose: D-04 (4th ViewToggle segment), D-05 (preset→editable numeric, no slider),
D-06 (in-memory only — kill URL-hash), D-03 (structurally separate "planned" vs
"measured" surfaces). This makes the planned lens a distinct, explicitly-labelled
surface that never shares a tile with the realized value.

Output: Reworked ViewToggle/App, new PlanningView + PlannedRatiosControl, Dashboard
cleaned of the killed mechanism, EN/FR i18n parity for the new keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-allocation-dr-re-derived/06-CONTEXT.md
@.planning/phases/06-allocation-dr-re-derived/06-PATTERNS.md
@.planning/phases/06-allocation-dr-re-derived/06-UI-SPEC.md

<interfaces>
<!-- ViewToggle.tsx:3-5 — EXTEND (P5 commit 123ecb2 is the 4-line precedent): -->
export type AppView = 'dashboard' | 'inventory' | 'hosts'   // + | 'planning'
const VIEWS = ['dashboard', 'inventory', 'hosts'] as const   // + 'planning'

<!-- App.tsx:37-43 view switch to extend; :21 useState<AppView>('dashboard') -->

<!-- Plan 06-01 store slice (consume, do not redefine): -->
selectPlannedRatios = (s) => s.plannedRatios          // { cpu:number; ram:number }
selectSetPlannedRatios = (s) => s.setPlannedRatios    // (r) => void  (REPLACE, no persist)

<!-- AllocationSliders.tsx:14-22 — PRESET source + matchesPreset (reuse VERBATIM): -->
PRESETS = [ {key:'1to1',{cpu:1,ram:1}}, {key:'4to1',{cpu:4,ram:1}},
            {key:'8to1',{cpu:8,ram:1}}, {key:'vdi10to1',{cpu:10,ram:1}} ]

<!-- ANTI-PATTERN — DO NOT REUSE: src/hooks/useAllocationHash.ts (the killed
     URL-hash codec). D-06 forbids URL-hash/localStorage. AllocRatios/DEFAULT_RATIOS
     *types* may be re-homed but the hash hook itself must not be imported. -->

<!-- fmtRatio (src/utils/format.ts) for the factual numeric echo caption -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend ViewToggle to a 4th 'planning' segment + App view switch + i18n nav key</name>
  <files>src/components/ViewToggle.tsx, src/App.tsx, src/i18n/locales/en/inventory.json, src/i18n/locales/fr/inventory.json</files>
  <read_first>
    - src/components/ViewToggle.tsx (the whole file — the generic fieldset/aria-pressed/Arrow-key idiom; lines 3-5 union+array, 47-74 render, 49-50 the CI-gated biome-ignore role="group")
    - src/App.tsx (lines 21, 37-44 — useState<AppView> + the view switch to extend)
    - 06-PATTERNS.md §`ViewToggle.tsx` (EXTEND — lines 164-181, the P5 commit 123ecb2 precedent + the App-shell wiring precedent)
    - 06-UI-SPEC.md Component Inventory `ViewToggle` row + Copywriting "DR mode selector"/nav strings + the "always-show, accent-gold active segment" rule
    - src/i18n/locales/en/inventory.json + fr/inventory.json (the `nav` block — `nav.hosts` is the exact precedent for `nav.planning`)
  </read_first>
  <action>
    In `ViewToggle.tsx`: add `'planning'` to the `AppView` union and to the `VIEWS` const array — nothing else changes (the fieldset/legend/aria-pressed/Arrow-key/wraparound idiom is generic over `VIEWS`; keep the accent-gold active style and the `biome-ignore` literal `role="group"` verbatim — CI grep gate asserts it).
    In `App.tsx`: add `: activeView === 'planning' ? (<PlanningView />)` to the view switch (PlanningView is created in Task 2; import it). Keep the existing dashboard fallback.
    In `inventory.json` (EN + FR): add `nav.planning` under the existing `nav` block — EN `"Planning"`, FR parity (factual noun, no editorial verb). Keep EN/FR key sets identical.
  </action>
  <verify>
    <automated>grep -nE "'dashboard' \| 'inventory' \| 'hosts' \| 'planning'" src/components/ViewToggle.tsx && grep -n "planning" src/App.tsx && node -e "const e=require('./src/i18n/locales/en/inventory.json'),f=require('./src/i18n/locales/fr/inventory.json');const k=o=>JSON.stringify(Object.keys(o).sort());if(k(e.nav)!==k(f.nav))process.exit(1);if(!e.nav.planning||!f.nav.planning)process.exit(1)" && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - `AppView` union contains `'planning'` and `VIEWS` array contains `'planning'`
    - `App.tsx` has an `activeView === 'planning'` branch rendering `PlanningView`
    - `en/inventory.json` and `fr/inventory.json` both have `nav.planning` and identical `nav` key sets
    - `grep -c 'role="group"' src/components/ViewToggle.tsx` ≥ 1 (CI-gated idiom kept)
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0
  </acceptance_criteria>
  <done>ViewToggle has 4 segments; App routes 'planning' to PlanningView; nav.planning present EN+FR with parity.</done>
</task>

<task type="auto">
  <name>Task 2: Build PlannedRatiosControl (preset + editable numeric, Zustand-backed, no URL-hash) and the PlanningView shell</name>
  <files>src/components/planning/PlannedRatiosControl.tsx, src/components/planning/PlanningView.tsx, src/i18n/locales/en/alloc.json, src/i18n/locales/fr/alloc.json</files>
  <read_first>
    - src/components/allocation/AllocationSliders.tsx (the preset-group idiom lines 14-22 + 80-105 to REUSE verbatim; lines 47-78 the sliders to DROP; line 108-110 the echo caption to keep)
    - src/hooks/useAllocationHash.ts (ANTI-PATTERN — read only to confirm what NOT to import: no `useAllocationHash`, no `history.replaceState`, no `parseAllocHash`)
    - src/store/snapshotStore.ts (the new `plannedRatios` slice + `selectPlannedRatios`/`selectSetPlannedRatios` from Plan 06-01)
    - 06-PATTERNS.md §"Planned-ratios control (NEW component)" (lines 214-231 — preset reuse + DROP sliders + DROP URL-hash) and §"Planning view shell (NEW)" (lines 23-24 analog) and the "`<fieldset role=\"group\">` + `aria-pressed`" Shared Pattern
    - 06-UI-SPEC.md Copywriting Contract (ratio presets, editable field labels + helper caption, section labelling D-03) + Component Inventory `Planned-ratios control` + `Planning view shell` rows + the "Planning-screen focal hierarchy" + Spacing (`2xl` break) + Color (preset active = `bg-primary-600 text-white`, NOT accent) rules
    - src/i18n/locales/en/alloc.json + fr/alloc.json (reuse `preset.*` keys verbatim; `echo` interpolation is the caption template)
  </read_first>
  <action>
    Create `PlannedRatiosControl.tsx`: reuse the `PRESETS` const + `matchesPreset` + the `<fieldset role="group">`+`aria-pressed` segmented preset group from `AllocationSliders` VERBATIM (keep the `biome-ignore` literal `role="group"`, preset button `h-8`, active style `bg-primary-600 text-white`). Replace the two `<input type="range">` with one `<input type="number">` for CPU and one for RAM (`aria-label` required, EN/FR); clicking a preset fills the numeric field via `setPlannedRatios`; typing an off-preset value clears the active preset (reuse the existing `matchesPreset`→no-active behaviour — factual, no error state). State source is the Zustand `plannedRatios` slice via `selectPlannedRatios`/`selectSetPlannedRatios` — NO `useAllocationHash`, NO URL-hash, NO localStorage (D-06). Keep the factual echo caption using `fmtRatio` + i18n interpolation; add a `planned.*` i18n group (separate from the realized surface, D-03) with the field labels + helper caption ("Planned lens — applies {cpu} CPU / {ram} RAM. Not the measured value.") + the "measured" read-only caption ("Measured ratio — calculated from this snapshot (see Dashboard › Operational Insights).").
    Create `PlanningView.tsx`: an in-app view-state branch (like P5 Hosts/cluster-detail, no router). Layout per UI-SPEC: heading "Capacity planning — what-if"; the `PlannedRatiosControl` block on top; a `2xl` (48px) vertical break; a slot below for the reworked `DrSimPanel` (Plan 03 fills this — for this plan render a placeholder section or leave a clearly-named region the executor can wire; do NOT add a second `useMemo` — consume `useEstateView` once here). Show the read-only "measured" caption that points at Operational Insights (do NOT restyle/duplicate the P5 component). Lift the `applyPlannedToDr` flag as component state here (default false) for Plan 03's affordance to consume.
    Add the new `planned.*` and field/label keys to `alloc.json` EN + FR with identical key sets.
  </action>
  <verify>
    <automated>[ "$(grep -v '^#' src/components/planning/PlannedRatiosControl.tsx | grep -c 'useAllocationHash\|location.hash\|history.replaceState\|localStorage')" = "0" ] && grep -nE "type=\"number\"" src/components/planning/PlannedRatiosControl.tsx && grep -nE "selectPlannedRatios" src/components/planning/PlannedRatiosControl.tsx && [ "$(grep -rc 'useMemo' src/components/planning/)" = "0" ] && node -e "const e=require('./src/i18n/locales/en/alloc.json'),f=require('./src/i18n/locales/fr/alloc.json');const flat=(o,p='')=>Object.entries(o).flatMap(([k,v])=>typeof v==='object'?flat(v,p+k+'.'):[p+k]);if(JSON.stringify(flat(e).sort())!==JSON.stringify(flat(f).sort()))process.exit(1)" && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - `PlannedRatiosControl` uses `<input type="number">` (no `type="range"`) and the reused preset fieldset with `role="group"`
    - `grep -v '^#' src/components/planning/PlannedRatiosControl.tsx | grep -c "useAllocationHash\|location.hash\|history.replaceState\|localStorage"` == 0
    - control reads/writes via `selectPlannedRatios`/`selectSetPlannedRatios` (in-memory slice)
    - `grep -rc 'useMemo' src/components/planning/` == 0 (single-memo invariant — presenters only)
    - `en/alloc.json` and `fr/alloc.json` have identical flattened key sets including the new `planned.*` group
    - PlanningView shows a "planned" heading + a "measured" read-only caption pointing at Operational Insights (D-03 separation), with a `2xl` break before the DR slot
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0
  </acceptance_criteria>
  <done>PlannedRatiosControl is preset+numeric, Zustand-backed, zero URL-hash/localStorage; PlanningView shell hosts it with the structural measured-vs-planned separation + a DR slot for Plan 03.</done>
</task>

<task type="auto">
  <name>Task 3: Remove the killed AllocationSliders + useAllocationHash from the Dashboard</name>
  <files>src/components/dashboard/GlobalDashboard.tsx</files>
  <read_first>
    - src/components/dashboard/GlobalDashboard.tsx (lines 4-5,15,17 imports; 64-65 useAllocationHash+useEstateView; 123 AllocationSliders render; 134-143 DrSimPanel render)
    - 06-CONTEXT.md D-03/D-04 (planned + DR move to the Planning surface — they leave the Dashboard) and the Out-of-P6 boundary
    - 06-PATTERNS.md §"Planned-ratios control" CRITICAL note (lines 230-232 — useAllocationHash is the killed mechanism) and §`DrSimPanel.tsx` (the panel moves to the Planning view — Plan 03 owns the rework)
    - 06-UI-SPEC.md Component Inventory (DrSimPanel + Planned-ratios control now live on the Planning surface, not the Dashboard)
  </read_first>
  <action>
    In `GlobalDashboard.tsx`: remove the `AllocationSliders` import + its render (line ~123); remove the `useAllocationHash` import + the `const [ratios, setRatios] = useAllocationHash()` line; call `useEstateView(mode)` without the `ratios` arg (the measured path uses the engine default — Plan 06-01 keeps `useEstateView`'s `ratios` default). Remove the `DrSimPanel` import + its render block (lines ~134-143) and the now-unused `drMode`/`setDrMode`/`scenario`/`setScenario`/DR-related local state ONLY if no other Dashboard code uses them (keep `stretchedClusters` wiring — it stays on the Dashboard for the per-cluster pill). Do NOT delete `DrSimPanel.tsx` or `AllocationSliders.tsx` files (Plan 03 reworks `DrSimPanel`; `AllocationSliders` is superseded by `PlannedRatiosControl` and may be deleted in Plan 03's cleanup — out of scope here). Verify the Dashboard still renders the GlobalSummaryCard/OperationalInsights/OsBreakdown/PerClusterColumns/CpuReadyPanel unchanged.
  </action>
  <verify>
    <automated>[ "$(grep -c 'AllocationSliders\|useAllocationHash\|DrSimPanel' src/components/dashboard/GlobalDashboard.tsx)" = "0" ] && [ "$(grep -c 'useMemo' src/components/dashboard/GlobalDashboard.tsx)" = "0" ] && npx tsc --noEmit -p tsconfig.app.json && npx vitest run src/components/dashboard 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AllocationSliders\|useAllocationHash\|DrSimPanel" src/components/dashboard/GlobalDashboard.tsx` == 0
    - `grep -c 'useMemo' src/components/dashboard/GlobalDashboard.tsx` == 0 (no memo introduced)
    - GlobalDashboard still renders summary/insights/donut/columns/cpu-ready (existing dashboard tests pass)
    - `useEstateView` is still called exactly once in the Dashboard, now without the `ratios` arg
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0
  </acceptance_criteria>
  <done>Dashboard no longer references the killed allocation slider/URL-hash mechanism or the DR panel; existing dashboard rendering intact.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User UI input (preset click / numeric typing) → in-memory Zustand `plannedRatios` slice | The only untrusted input crossing here. Numeric component state; never persisted, never networked. |
| In-memory `EstateView` → presenter components | Read-only render boundary; components are pure presenters (no recompute, no second memo). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Information disclosure | `PlannedRatiosControl` state path | mitigate | Control binds only to the in-memory Zustand slice; Task 2 gate asserts `grep -c "useAllocationHash\|location.hash\|history.replaceState\|localStorage"` == 0 — the killed URL-hash codec is structurally excluded (D-06). |
| T-06-07 | Tampering / misleading output | measured vs planned conflation | mitigate | D-03 structural separation: planned surface carries a "planned" qualifier on its own ViewToggle screen; the realized value stays read-only on the Dashboard with a "measured" qualifier; acceptance gate asserts the separation copy. The realized value is never overwritten by planning input. |
| T-06-08 | Input validation | numeric ratio field | mitigate | Native `<input type="number">`; off-preset/empty values resolve to the in-memory slice value with the existing `matchesPreset`→no-active behaviour (factual, no error, no crash). No downstream eval/injection sink exists (client-only render). |
| T-06-09 | Spoofing / Elevation / DoS | n/a (no auth, no server, no network) | accept | Architecturally impossible — 100% client-side; Phase-1 privacy guard throws on any non-same-origin request. No new network or storage surface introduced. |
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.app.json` exits 0
- `npx vitest run src/components` exits 0
- `npx @biomejs/biome check .` clean on modified files
- ViewToggle `AppView` union contains `'planning'`; App routes it
- `grep -c "useAllocationHash\|location.hash\|localStorage" src/components/planning/PlannedRatiosControl.tsx` == 0
- EN/FR parity holds for `inventory.json` `nav` + new `alloc.json` keys
- No `useMemo` anywhere under `src/components/planning/` or in `GlobalDashboard.tsx`
</verification>

<success_criteria>

- 4th 'planning' ViewToggle segment present, App-routed, accent-gold active, keyboard idiom intact
- PlannedRatiosControl: preset buttons + editable numeric, default 4:1/1:1, in-memory only
- PlanningView structurally separates "planned" from the read-only "measured" reference (D-03)
- Killed slider/URL-hash mechanism removed from the Dashboard
- EN/FR i18n parity on every new key
</success_criteria>

<output>
After completion, create `.planning/phases/06-allocation-dr-re-derived/06-02-SUMMARY.md`
</output>
