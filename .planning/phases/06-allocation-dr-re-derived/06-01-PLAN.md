---
phase: 06-allocation-dr-re-derived
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/estate.ts
  - src/engines/drSim/runScenario.ts
  - src/engines/drSim/allocate.ts
  - src/engines/aggregation/estateView.ts
  - src/store/snapshotStore.ts
  - src/hooks/useEstateView.ts
  - src/engines/drSim/runScenario.test.ts
autonomous: true
requirements: [DRX-02, DRX-03, DRX-04, DRX-05, PLN-03, PLN-04]
must_haves:
  truths:
    - "The DR engine accepts exactly two modes — server and site — and no longer accepts cluster or vCenter loss"
    - "Every DR impact figure is physical CPU + physical RAM removed, never vCPU"
    - "No DR result carries a confidence field; caveats and assumptions survive"
    - "Planned ratios are an in-memory Zustand inputs slice with no localStorage and no URL-hash"
    - "The planned what-if projection is produced inside the single buildEstateView pass — no second useMemo"
  artifacts:
    - path: "src/types/estate.ts"
      provides: "Reworked DrMode/DrScenario/DrSimResult + planned-projection field on EstateView"
      contains: "type DrMode = 'server' | 'site'"
    - path: "src/engines/drSim/runScenario.ts"
      provides: "Two-mode survivor sim with physical-impact metric, no confidence"
    - path: "src/store/snapshotStore.ts"
      provides: "plannedRatios in-memory inputs slice + selectors"
      contains: "plannedRatios"
    - path: "src/engines/aggregation/estateView.ts"
      provides: "Planned projection composed in the single pass"
  key_links:
    - from: "src/hooks/useEstateView.ts"
      to: "buildEstateView"
      via: "plannedRatios passed into opts inside the one useMemo"
      pattern: "plannedRatios"
    - from: "src/engines/aggregation/estateView.ts"
      to: "runScenario / aggregateClusters"
      via: "second pass with planned ratios in the same composition"
      pattern: "runScenario|aggregateClusters"
---

<objective>
Rework the DR engine + DR contract + planned-ratios store slice + the single-memo
projection so Phase 6 UI (Plans 02/03) can consume a two-mode physical-impact DR
result and a separate in-memory planned what-if — with zero greenfield rewrites.

Purpose: This is the pure (non-React) spine the entire phase depends on. G3 (DR =
Server+Site, physical impact), D-10 (no confidence, keep caveats), D-06 (planned
ratios in-memory only), D-02/D-11 (planned projection through the one memo) are all
contract changes that must land before any UI is wired.

Output: Reworked `runScenario.ts`/`allocate.ts`/`estate.ts`, a new `plannedRatios`
Zustand slice, the planned projection inside `buildEstateView`, the `useEstateView`
dep wiring, and a fully-updated `runScenario.test.ts` keeping `engines/drSim` ≥75%.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-allocation-dr-re-derived/06-CONTEXT.md
@.planning/phases/06-allocation-dr-re-derived/06-PATTERNS.md

<interfaces>
<!-- Current DR contract (src/types/estate.ts:406-440) — REWORK targets: -->
type DrMode = 'host' | 'cluster' | 'vcenter'            // → 'server' | 'site'
interface DrScenario {
  failedHosts: Set<string>
  failedClusters: Set<string>   // remove
  failedVCenters: Set<string>   // remove
}                               // add: failedSites: Set<string>
interface DrSimResult {
  mode: DrMode
  before: GlobalSummary; after: GlobalSummary
  evacueeVcpu: Cores            // → physical CPU removed (GHz + cores)
  evacueeVramMib: MiB           // → physical RAM removed (keep MiB brand)
  perSurvivor: { cluster: string; verdict: Verdict }[]   // KEEP
  confidence: 'high'|'medium'|'low'                      // DELETE (D-10)
  caveats: string[]                                      // KEEP
}

<!-- GlobalSummary already carries physical fields (globals.ts:22-38): -->
physicalCores, usablePhysicalCores: Cores
physicalGhz, consumedGhz, availableGhz, drReservedGhz: GHz
physicalRamMib, consumedRamMib, drReservedRamMib, availableRamMib: MiB

<!-- aggregateClusters site identity for the Site-loss filter: -->
VHostRow.faultDomain: string   // canonical "site" value (aggregateClusters.ts:57,71-74)
ClusterAggregate.siteData: 'detected' | 'assumed'

<!-- Store slice templates (snapshotStore.ts): stretchedClusters (44-47,107,164),
     scenario (48-52,109-116,167-168), clearAll (136-143), EMPTY_SCENARIO (5-9) -->

<!-- units (src/engines/units/types.ts:7-23): ghz(), cores(), mib() branded ctors -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rework the DR contract + drSim engine to two modes + physical impact, drop confidence</name>
  <files>src/types/estate.ts, src/engines/drSim/runScenario.ts, src/engines/drSim/allocate.ts, src/engines/drSim/index.ts</files>
  <read_first>
    - src/types/estate.ts (lines 406-440 — the DrMode/DrScenario/DrSimResult contract being reworked)
    - src/engines/drSim/runScenario.ts (the engine being reworked in place)
    - src/engines/drSim/allocate.ts (Verdict/band/worse/survivorVerdict — REUSE, change feed only)
    - 06-PATTERNS.md §`runScenario.ts` (REWORK — lines 33-79: exact mode/strip/impact/confidence change-points) and §`allocate.ts` (REUSE, change feed only — lines 83-97) and §`estate.ts` (DR contract change — lines 134-160) and the "Branded units (physical DR impact)" Shared Pattern
    - 06-CONTEXT.md D-08, D-09, D-10, D-11 (the binding intent)
    - src/engines/aggregation/globals.ts (lines 22-38, 64-94 — confirm physical GHz/cores/RAM field names on GlobalSummary)
    - src/engines/aggregation/aggregateClusters.ts (lines 57, 70-105, 248-249 — faultDomain site identity + the ratio-bake point)
  </read_first>
  <behavior>
    - DrMode is exactly the union 'server' | 'site' (no 'host'/'cluster'/'vcenter')
    - DrScenario is { failedHosts: Set<string>; failedSites: Set<string> } (failedClusters/failedVCenters removed)
    - runScenario with empty scenario (both sets empty) → null
    - Server loss: failedHosts removes those host rows + their VMs; survivor aggregate re-run via the KEPT aggregate()/aggregateGlobals composition (unchanged)
    - Site loss: failedSites contains faultDomain values; the engine removes vhost rows whose faultDomain ∈ failedSites and the vinfo rows on those hosts; non-stretched workload physically at a lost site is reflected as removed (factually lost) — the result exposes enough for the UI "lost — no DR target" line
    - No-fault-domain metadata case keeps the symmetric-50% behaviour already in aggregateClusters (unchanged — do not re-derive here)
    - DrSimResult exposes physical impact: physical CPU removed as branded GHz AND Cores, physical RAM removed as branded MiB — derived from before.physicalGhz/physicalCores/physicalRamMib − after.* via ghz()/cores()/mib(); never vCPU
    - DrSimResult has NO confidence field; caveats[] (i18n key suffixes) and the perSurvivor Verdict array are kept; survivor verdict is computed against PHYSICAL headroom
    - dominantMode returns 'site' when failedSites non-empty else 'server'
  </behavior>
  <action>
    In `estate.ts`: change `DrMode` to `'server' | 'site'`; replace `DrScenario` fields with `failedHosts: Set<string>` + `failedSites: Set<string>`; on `DrSimResult` remove `confidence`, rename the impact fields to physical-removed semantics — add `physicalCpuRemovedGhz: GHz`, `physicalCpuRemovedCores: Cores`, `physicalRamRemovedMib: MiB` (replacing `evacueeVcpu`/`evacueeVramMib`); keep `perSurvivor`/`caveats`; update the JSDoc to drop the DRS-05 confidence rationale and cite DRX-03/DRX-05. Add the new planned-projection field to `EstateView` per Task 3 coordination (declare the field name `plannedDrSim: DrSimResult | null` and `plannedView` shape there — see Task 3 for the exact shape; this task only adds the `DrSimResult`/`DrScenario`/`DrMode` reworks).
    In `runScenario.ts`: replace `isEmpty` to test `failedHosts.size === 0 && failedSites.size === 0`; replace `dominantMode` to `failedSites.size > 0 ? 'site' : 'server'`; DELETE the `vcLostClusters`/`clusterFailed` block and the `failedVCenters` survivor filters (PATTERNS lines 42-61); add a site filter that removes `merged.vhost` rows where `failedSites.has(h.faultDomain)` and `merged.vinfo` rows whose host is in the removed-host set (derive the removed host names from the site-filtered vhost diff). KEEP the `aggregate()`/`aggregateGlobals`/`perSurvivor` spine verbatim (PATTERNS lines 48-58). Replace the `evacueeVcpu`/`evacueeVramMib` derivation with physical: `ghz(max(0, before.physicalGhz − after.physicalGhz))`, `cores(max(0, before.physicalCores − after.physicalCores))`, `mib(max(0, before.physicalRamMib − after.physicalRamMib))`. KEEP the `caveats` block verbatim; DELETE the `verdicts`/`confidence` lines and `confidence` from the return object.
    In `allocate.ts`: keep `Verdict`/`band`/`worse` unchanged; add a physical-basis survivor verdict feed — feed physical capacity-vs-load (use the cluster's physical headroom fields) without re-deriving any ratio (honor the DRY comment lines 33-37); do NOT change the existing `survivorVerdict` signature consumers rely on unless adding a sibling is cleaner — decide minimal-blast-radius and document the choice in the SUMMARY.
    In `index.ts`: ensure the barrel re-exports stay consistent with the renamed result fields.
  </action>
  <verify>
    <automated>grep -nE "type DrMode = 'server' \| 'site'" src/types/estate.ts && [ "$(grep -v '^#' src/engines/drSim/runScenario.ts | grep -c 'failedClusters\|failedVCenters\|confidence')" = "0" ] && [ "$(grep -v '^#' src/types/estate.ts | grep -c 'evacueeVcpu')" = "0" ] && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "type DrMode = 'server' | 'site'" src/types/estate.ts` ≥ 1
    - `grep -v '^#' src/engines/drSim/runScenario.ts | grep -c "failedClusters\|failedVCenters\|confidence"` == 0
    - `grep -v '^#' src/types/estate.ts | grep -c "evacueeVcpu\|: 'high' | 'medium' | 'low'"` == 0
    - DrScenario type members are exactly failedHosts + failedSites (verified by tsc + grep)
    - Physical impact fields exist on DrSimResult typed as GHz/Cores/MiB branded units
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0 after all consumers compile
  </acceptance_criteria>
  <done>DrMode/DrScenario/DrSimResult reworked; runScenario is two-mode + physical-impact + no confidence; allocate feed is physical; typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: Add the in-memory plannedRatios Zustand slice + selectors</name>
  <files>src/store/snapshotStore.ts</files>
  <read_first>
    - src/store/snapshotStore.ts (the whole file — slice templates: stretchedClusters lines 44-47/107/164-166, scenario lines 48-52/109-116/167-168, clearAll 136-143, EMPTY_SCENARIO 5-9, store header 11-30)
    - 06-PATTERNS.md §"Zustand planned-ratios slice" (lines 235-252 — the exact inputs-only/REPLACE-never-mutate/no-persist template) and the "Inputs-only Zustand slice (no persist — privacy)" Shared Pattern
    - 06-CONTEXT.md D-05 (default CPU 4:1 / RAM 1:1), D-06 (in-memory only, no URL-hash, no localStorage)
  </read_first>
  <action>
    Add `plannedRatios: { cpu: number; ram: number }` to `SnapshotState` with JSDoc mirroring the `stretchedClusters`/`scenario` "Inputs-only, REPLACED never mutated; no persist, no localStorage (PROJECT.md line 53)" wording, default `{ cpu: 4, ram: 1 }` (D-05). Add `setPlannedRatios: (r: { cpu: number; ram: number }) => void` to the interface; implement in the store as `set({ plannedRatios: { ...r } })` (REPLACE never mutate). Add it to the `clearAll` reset object (reset to `{ cpu: 4, ram: 1 }`). Export `selectPlannedRatios = (s) => s.plannedRatios` and `selectSetPlannedRatios = (s) => s.setPlannedRatios` mirroring the existing stable-ref selector idiom (lines 164-168 — never construct a new object in the selector). Add NO persist middleware, NO localStorage, NO URL-hash (D-06 / PAR-05).
  </action>
  <verify>
    <automated>[ "$(grep -v '^#' src/store/snapshotStore.ts | grep -c 'localStorage\|sessionStorage\|location.hash\|persist(')" = "0" ] && grep -nE "plannedRatios|selectPlannedRatios|selectSetPlannedRatios" src/store/snapshotStore.ts && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - `plannedRatios` state field + `setPlannedRatios` setter + `selectPlannedRatios`/`selectSetPlannedRatios` exports all present
    - Default is `{ cpu: 4, ram: 1 }`; reset in `clearAll`
    - `grep -v '^#' src/store/snapshotStore.ts | grep -c "localStorage\|sessionStorage\|location.hash\|persist("` == 0 (no persistence introduced)
    - Setter REPLACES (`set({ plannedRatios: { ...r } })`), never mutates
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0
  </acceptance_criteria>
  <done>plannedRatios in-memory inputs slice present with selectors, default 4:1/1:1, reset in clearAll, zero persistence.</done>
</task>

<task type="auto">
  <name>Task 3: Compose the planned projection in the single buildEstateView pass + wire useEstateView + update drSim tests</name>
  <files>src/engines/aggregation/estateView.ts, src/hooks/useEstateView.ts, src/types/estate.ts, src/engines/drSim/runScenario.test.ts</files>
  <read_first>
    - src/engines/aggregation/estateView.ts (lines 52-62 opts shape, 201-223 the drSim/return composition, 245-260 EMPTY_VIEW — the precedent for adding a projection in the single pass)
    - src/hooks/useEstateView.ts (the single useMemo site; lines 37-54 the P4 ratios/scenario precedent)
    - src/engines/drSim/runScenario.test.ts (the existing test matrix — host/cluster/vCenter/confidence cases that must be reworked to server/site/physical/no-confidence)
    - 06-PATTERNS.md §`estateView.ts` (add planned projection — lines 117-129) and §`useEstateView.ts` (lines 255-274 the exact dep-array precedent) and the "Single-`useMemo` invariant" Shared Pattern
    - 06-CONTEXT.md D-02 (planned reads through the one memo, read-only realized), D-11 (Custom Failover = same sim with planned ratios)
  </read_first>
  <action>
    In `estate.ts`: add to `EstateView` a `plannedView: { globals: GlobalSummary; clusters: ClusterAggregate[] } | null` (the planned-ratio what-if aggregate) and `plannedDrSim: DrSimResult | null` (the Custom-Failover DR run under planned ratios), mirroring the `drSim: DrSimResult | null` / `trends: T | null` idiom with JSDoc citing PLN-04/D-11; add both to `EMPTY_VIEW`.
    In `estateView.ts`: extend the `opts` shape (lines 55-59) with `plannedRatios?: { cpuRatio: number; ramRatio: number }` and `applyPlannedToDr?: boolean`. Inside the SAME `buildEstateView` pass (no new function, no second memo): when `opts.plannedRatios` is present, compute `plannedView` by calling `aggregateClusters`/`aggregateGlobals` with the planned ratios (reuse the existing aggregate composition — do NOT add a second memo or a new file); compute `plannedDrSim` by calling `runScenario(merged, opts.scenario, { mode, stretchedClusters, allocRatios: plannedRatios })` only when a scenario is present AND `applyPlannedToDr` is true, else `null`. The measured `drSim` path stays exactly as-is (never conflated — D-11). Add `plannedView`/`plannedDrSim` to the returned object and to `EMPTY_VIEW`.
    In `useEstateView.ts`: read `plannedRatios` via `selectPlannedRatios`; pass `plannedRatios: { cpuRatio: planned.cpu, ramRatio: planned.ram }` into `buildEstateView`'s opts; add `planned.cpu, planned.ram` to the `useMemo` dependency array exactly like `ratios.cpu, ratios.ram`. Do NOT add a second `useMemo`. (The `applyPlannedToDr` flag is in-app component state lifted in Plan 02/03's PlanningView; for this plan default it via opts when absent.)
    In `runScenario.test.ts`: rework the suite to the new contract — replace the `host-loss`/`cluster-loss`/`vCenter-loss`/`confidence reflects worst verdict` cases with `server-loss removes host + VMs`, `site-loss removes the fault-domain hosts + VMs`, `site-loss with no fault-domain metadata is symmetric` (assert the kept aggregate behaviour), `physical impact = before−after physical GHz/cores/RAM (branded, never vCPU)`, `no confidence field on the result`, keep the `does NOT mutate the merged estate` and `reservation-vs-capacity caveat key` and `stretched survivor reservation flows through` cases adapted to the new scenario shape. Keep coverage of `engines/drSim` ≥75%.
  </action>
  <verify>
    <automated>[ "$(grep -c 'useMemo' src/hooks/useEstateView.ts)" = "1" ] && grep -nE "plannedView|plannedDrSim|selectPlannedRatios" src/hooks/useEstateView.ts src/engines/aggregation/estateView.ts && npx vitest run src/engines/drSim && npx vitest run --coverage src/engines/drSim 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'useMemo' src/hooks/useEstateView.ts` == 1 (single-memo invariant held)
    - `plannedView` + `plannedDrSim` present on `EstateView`, in `EMPTY_VIEW`, and produced inside `buildEstateView` (no new file, no second useMemo)
    - measured `drSim` composition unchanged (still computed from `opts.scenario` with measured `allocRatios`)
    - `npx vitest run src/engines/drSim` exits 0; suite has no `failedClusters`/`failedVCenters`/`confidence` assertions
    - `engines/drSim` coverage ≥ 75%
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0
  </acceptance_criteria>
  <done>Planned projection + Custom-Failover DR run compose inside the one buildEstateView pass; useEstateView passes plannedRatios through the single memo; drSim tests rewritten to the two-mode/physical/no-confidence contract at ≥75% coverage.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RVTools workbook → parsed in-memory rows | Already validated upstream (parser-in-Worker + Zod, Phase 1). Phase 6 only re-aggregates already-trusted in-memory rows; no new external input crosses here. |
| User what-if input (planned ratios / DR selection) → in-memory Zustand slice | The only new untrusted input in this plan. It is numeric/string-set component state, never persisted, never networked. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information disclosure | `plannedRatios` Zustand slice | mitigate | Slice is in-memory only; Task 2 acceptance gate asserts `grep -c "localStorage\|sessionStorage\|location.hash\|persist("` == 0 — accidental persistence of planned inputs is structurally prevented (D-06). |
| T-06-02 | Tampering (data integrity) | `runScenario` physical-impact math | mitigate | Physical impact derived only from already-trusted `GlobalSummary` physical fields via branded `ghz()/cores()/mib()`; test suite asserts before−after physical derivation and no vCPU leakage. |
| T-06-03 | Repudiation / misleading output | DR result confidence verdict | mitigate | `confidence` field deleted entirely (D-10); the tool emits no judgement of the user's scenario — only factual caveats. Grep gate asserts zero `confidence` in the engine. |
| T-06-04 | Denial of service | aggregation re-run on survivor subset | accept | Bounded by estate size already aggregated once on the main `useEstateView` path; a second in-memory pass on the same data is O(n) and within the shipped P2–P5 performance envelope. No external amplification possible (client-only, no network). |
| T-06-05 | Spoofing / Elevation | n/a (no auth, no server, no network) | accept | Architecturally impossible — 100% client-side, privacy guard throws on any non-same-origin request (Phase 1). No server/auth surface exists in this phase. |
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.app.json` exits 0
- `npx vitest run src/engines/drSim src/engines/aggregation` exits 0
- `engines/` coverage ≥ 75% (`npm run test:coverage`)
- `npx @biomejs/biome check .` clean on the modified files
- `grep -c 'useMemo' src/hooks/useEstateView.ts` == 1
- No `confidence` / `failedClusters` / `failedVCenters` / `evacueeVcpu` tokens remain in engine/type code (grep gates above)
- No `localStorage` / URL-hash write in the planned-ratios slice
</verification>

<success_criteria>

- DR engine is exactly two modes (server/site), physical impact, no confidence, caveats+assumptions kept
- plannedRatios is an in-memory inputs slice with zero persistence
- planned projection + Custom-Failover DR ride the single buildEstateView pass; single-memo invariant intact
- engines/drSim tests rewritten to the new contract, ≥75% coverage, all green
</success_criteria>

<output>
After completion, create `.planning/phases/06-allocation-dr-re-derived/06-01-SUMMARY.md`
</output>
