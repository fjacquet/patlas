---
phase: 06-allocation-dr-re-derived
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - src/components/dr/DrSimPanel.tsx
  - src/components/planning/PlanningView.tsx
  - src/i18n/locales/en/dr.json
  - src/i18n/locales/fr/dr.json
  - src/components/dr/DrSimPanel.test.tsx
autonomous: true
requirements: [DRX-01, DRX-02, DRX-03, DRX-04, DRX-05, DRX-06]
must_haves:
  truths:
    - "A user picks exactly two DR modes — Server loss and Site loss — no third"
    - "Server loss supports both individual host multi-select and a per-cluster 'N of M' host-count stepper"
    - "Site loss picks a declared-stretched fault-domain value; non-stretched workload at the lost site shows an explicit factual 'lost — no DR target' line"
    - "DR impact shows physical CPU removed (GHz/cores) + physical RAM removed (MiB), never vCPU; survivor verdict is a neutral factual word + numbers"
    - "No confidence indicator anywhere; assumptions + caveats kept; failed-selection stays reversible/neutral"
    - "A single in-panel 'Apply planned ratios to this scenario' affordance re-runs the same sim with planned ratios — not a 3rd mode"
    - "D-00: every DR figure is rendered factually from engine-computed branded values with an em-dash sentinel for not-derivable, no judgement/verdict colour or editorial verb, EN/FR dr.json key sets identical, and the presenter adds no useMemo (single-useMemo invariant)"
    - "D-07: Server loss exposes both an individual-named-host multi-select and a per-cluster host-count stepper feeding the kept reversible/neutral failed-selection UI"
    - "D-08: Site loss picker is over declared-stretched fault-domain values; non-stretched workload physically at the lost site renders an explicit factual 'lost — no DR target' line; Site segment is always shown with the picker disabled+factual-note when zero stretched are declared"
    - "D-09: the single gold impact figure is physical CPU (GHz/cores) + physical RAM (MiB), never vCPU; the survivor verdict reuses the Verdict word fed physical headroom as a neutral word + numbers, no traffic-light"
    - "D-10: no confidence indicator is rendered and no confidence.* i18n key remains; assumptions + caveats are kept and failed-selection stays reversible/neutral"
    - "D-11: Custom Failover is a single in-panel toggle switching the presenter between view.drSim and view.plannedDrSim — the same two-mode sim with planned ratios, never a third DR mode and never conflated"
  artifacts:
    - path: "src/components/dr/DrSimPanel.tsx"
      provides: "Two-mode physical-impact DR presenter, no confidence, Custom-Failover affordance"
      contains: "server"
    - path: "src/components/planning/PlanningView.tsx"
      provides: "DR slot wired to the reworked DrSimPanel + applyPlannedToDr state"
  key_links:
    - from: "src/components/planning/PlanningView.tsx"
      to: "DrSimPanel"
      via: "PlanningView renders DrSimPanel below the 2xl break with applyPlannedToDr"
      pattern: "DrSimPanel"
    - from: "src/components/dr/DrSimPanel.tsx"
      to: "view.drSim / view.plannedDrSim"
      via: "presenter reads the engine result chosen by the Apply-planned toggle"
      pattern: "plannedDrSim|drSim"
---

<objective>
Rework the shipped `DrSimPanel` in place to the G3 two-mode physical-impact model:
Server + Site loss only, physical CPU/RAM impact, no confidence, the per-cluster
host-count stepper, the site picker with the explicit "lost — no DR target" line,
the Custom-Failover affordance, and wire it into the PlanningView DR slot. Update
`dr.json` EN/FR.

Purpose: D-07 (Server loss = host multi-select + per-cluster stepper), D-08 (Site
loss = declared-stretched fault domain + factual lost line), D-09 (physical impact
+ neutral verdict word per UI-SPEC Open-Item-2), D-10 (drop confidence, keep
assumptions+caveats, reversible/neutral selection), D-11 (Custom Failover = in-panel
toggle, not a 3rd mode).

Output: Reworked `DrSimPanel.tsx`, PlanningView DR slot wired, `dr.json` EN/FR
reworked, a presenter test covering the two-mode/physical/no-confidence contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-allocation-dr-re-derived/06-CONTEXT.md
@.planning/phases/06-allocation-dr-re-derived/06-PATTERNS.md
@.planning/phases/06-allocation-dr-re-derived/06-UI-SPEC.md

<interfaces>
<!-- Plan 06-01 reworked contract (consume, do not redefine): -->
type DrMode = 'server' | 'site'
interface DrScenario { failedHosts: Set<string>; failedSites: Set<string> }
interface DrSimResult {
  mode: DrMode; before: GlobalSummary; after: GlobalSummary
  physicalCpuRemovedGhz: GHz; physicalCpuRemovedCores: Cores; physicalRamRemovedMib: MiB
  perSurvivor: { cluster: string; verdict: Verdict }[]
  caveats: string[]              // no confidence
}
// EstateView gains: plannedDrSim: DrSimResult | null  (Custom-Failover run)

<!-- DrSimPanel.tsx current state (REWORK in place): MODES line 14;
     mode selector 102-128 (keep fieldset/aria/Arrow + bg-primary-600 active +
     CI-gated biome-ignore role="group"); reversible checkbox+strike+chip
     16-22,131-161 (keep verbatim); single gold figure 184-192 (re-point to
     physical); confidence block 209-212 (DELETE); assumptions 227-231 (keep);
     caveats 214-223 (keep). fmtGhzValue/fmtInt imported line 4. -->

<!-- Site identity for the Site picker: ClusterAggregate.siteData ('detected'
     |'assumed'), VHostRow.faultDomain; declared-stretched = the user's
     stretchedClusters set (snapshotStore selectStretchedClusters). -->

<!-- dr.json: mode keys en/dr.json:3-8 (→ server/site, drop host/cluster/vcenter);
     confidence block 28-33 (DELETE EN+FR); keep failed.chip 13, verdict.* 18-22,
     assumptions.* 23-27, caveats.* 34-37. EN/FR parity is a hard gate. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rework DrSimPanel to two modes + physical impact + Server stepper + Site picker + drop confidence</name>
  <files>src/components/dr/DrSimPanel.tsx</files>
  <read_first>
    - src/components/dr/DrSimPanel.tsx (the whole file — every change-point: MODES line 14, selector 102-128, toggleIn 16-22, options/activeSet 66-88, summary 90-96, gold figure 184-192, verdict render 203-204, confidence 209-212, caveats 214-223, assumptions 227-231)
    - 06-PATTERNS.md §`DrSimPanel.tsx` (REWORK in place — lines 185-211: exact keep/delete/add classification per change-point) and the "Factual presenter + sentinel + no-verdict" Shared Pattern
    - 06-CONTEXT.md D-07, D-08, D-09, D-10, D-11 + the two Claude's-Discretion items
    - 06-UI-SPEC.md Copywriting Contract (DR mode selector, Server-loss selection, Site-loss selection + lost line, failed chip, Custom Failover affordance, impact metric labels, survivor verdict, assumptions, caveats, confidence REMOVED, empty states) + Color (single gold figure = physical removed total; mode active stays `bg-primary-600 text-white`; no traffic-light) + the two Claude's-Discretion Recommendations (Open Item 1: always-show Site segment, disable picker with factual note when zero declared-stretched; Open Item 2: render Verdict word + numbers, neutral)
    - src/components/dashboard/GlobalDashboard.tsx (the prior DrSimPanel prop wiring being superseded — read for the old prop contract only)
  </read_first>
  <action>
    Rework `MODES` to `['server', 'site']`. Keep the mode `<fieldset role="group">`+`aria-pressed`+Arrow-key selector verbatim incl. the `biome-ignore` literal `role="group"` and `bg-primary-600 text-white` active style. Always render BOTH segments (Open Item 1: when the declared-stretched set is empty, render the Site segment but disable the site picker with the factual inline note key `empty.noStretched` — do NOT hide the segment).
    Server loss: keep the individual-host multi-select (reuse the reversible checkbox + grey strike-through + `failed.chip` block verbatim — no red/icon/dialog) AND add a per-cluster host-count stepper (native `<input type="number">` + `<label className="flex items-center gap-2 text-sm">` idiom already in-file): "Hosts failed in {cluster}" with "{n} of {m}"; stepping selects the first n host names of that cluster into `failedHosts` (replace-never-mutate via the existing `toggleIn`-style helper, generalised to a set-replace).
    Site loss: render a site picker whose options are the distinct `faultDomain` values of the user's declared-stretched clusters; selecting one puts it in `failedSites`. Render the explicit factual "lost — no DR target" line for non-stretched workload physically at the lost site using the `site.lost` key (counts/figures via `fmt*`, never fabricated — em-dash sentinel when not derivable).
    Impact figure: re-point the single gold `text-accent-500` figure from vCPU to "Physical CPU removed" ({GHz}/{cores}) + "Physical RAM removed" ({MiB}) using `fmtGhzValue`/`fmtInt`; exactly one gold figure. Keep the before/after `Stat` columns + the neutral `perSurvivor` verdict word render (`t(\`verdict.${p.verdict}\`)` + the physical headroom numbers, neutral text — Open Item 2). DELETE the entire `confidence` block. KEEP the caveats + assumptions blocks verbatim (content keys updated to the two-mode/physical model).
    Custom Failover: add a single labelled checkbox INSIDE the panel (not a mode tab) — "Apply planned ratios to this scenario" — bound to the `applyPlannedToDr` prop/state (lifted in PlanningView, Task 2). When on, the presenter reads `view.plannedDrSim`; when off, `view.drSim`. Caption text per UI-SPEC. Update the `DrSimPanelProps` to the new `DrMode`/`DrScenario` shape + the `applyPlannedToDr` boolean + `onApplyPlannedToDr`.
  </action>
  <verify>
    <automated>[ "$(grep -v '^#' src/components/dr/DrSimPanel.tsx | grep -c 'confidence\|failedClusters\|failedVCenters\|evacueeVcpu')" = "0" ] && grep -nE "'server', 'site'|'server'\] as|server.*site" src/components/dr/DrSimPanel.tsx && grep -nE "physical|Physical" src/components/dr/DrSimPanel.tsx && grep -c 'role="group"' src/components/dr/DrSimPanel.tsx && [ "$(grep -c 'useMemo' src/components/dr/DrSimPanel.tsx)" = "0" ] && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - `MODES` is exactly `['server','site']`; both segments always rendered
    - `grep -v '^#' src/components/dr/DrSimPanel.tsx | grep -c "confidence\|failedClusters\|failedVCenters\|evacueeVcpu"` == 0
    - panel renders physical CPU (GHz/cores) + physical RAM (MiB) as the single gold figure; no "vCPU" label in the impact figure
    - Server loss has both host multi-select AND a per-cluster host-count `<input type="number">` stepper
    - Site loss has a picker over declared-stretched fault domains + the factual `site.lost` line; Site segment present-but-picker-disabled-with-note when zero declared-stretched (Open Item 1)
    - reversible/neutral checkbox + grey strike + `failed.chip` kept (no red, no icon, no dialog)
    - a single in-panel "Apply planned ratios" checkbox (not a mode tab) toggles `view.drSim` vs `view.plannedDrSim`
    - `grep -c 'role="group"' src/components/dr/DrSimPanel.tsx` ≥ 1; `grep -c 'useMemo' ...` == 0
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0
  </acceptance_criteria>
  <done>DrSimPanel is two-mode + physical-impact + Server stepper + Site picker/lost-line + Custom-Failover toggle; confidence gone; assumptions/caveats/reversible-selection kept; single-memo invariant intact.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the reworked DrSimPanel into the PlanningView DR slot + applyPlannedToDr state</name>
  <files>src/components/planning/PlanningView.tsx</files>
  <read_first>
    - src/components/planning/PlanningView.tsx (the shell + DR slot + the `applyPlannedToDr` component state created in Plan 06-02)
    - src/components/dr/DrSimPanel.tsx (the new `DrSimPanelProps` shape from Task 1)
    - src/store/snapshotStore.ts (`selectScenario`/`selectSetScenario`, `selectStretchedClusters` — the inputs the DR panel needs; the new `DrScenario` shape from Plan 06-01)
    - src/hooks/useEstateView.ts (already called once in PlanningView per Plan 06-02 — DO NOT call it again; pass `view` down as a prop)
    - 06-PATTERNS.md §"Planning view shell" + the "Single-`useMemo` invariant" Shared Pattern
    - 06-UI-SPEC.md "Planning-screen focal hierarchy" + Spacing `2xl` break rule + Interaction Contract (Custom Failover is one in-panel affordance; single-memo invariant)
  </read_first>
  <action>
    In `PlanningView.tsx`: render the reworked `<DrSimPanel>` in the DR slot below the `2xl` break (the slot left by Plan 06-02). Source `scenario`/`setScenario` from the store (`selectScenario`/`selectSetScenario`), `declaredStretched` from `selectStretchedClusters`, and pass the single `view` from the one `useEstateView` call (consumed once in PlanningView — do NOT introduce a second `useMemo` or a second `useEstateView`). Lift `drMode` as local component state (default `'server'`). Wire `applyPlannedToDr` (the boolean lifted in Plan 06-02) + its setter into the panel's Custom-Failover affordance. The measured planned-ratios reference caption (the read-only "measured" link to Operational Insights) stays as placed by Plan 06-02 — do not duplicate it.
  </action>
  <verify>
    <automated>grep -nE "DrSimPanel" src/components/planning/PlanningView.tsx && [ "$(grep -c 'useMemo' src/components/planning/PlanningView.tsx)" = "0" ] && [ "$(grep -c 'useEstateView' src/components/planning/PlanningView.tsx)" = "1" ] && npx tsc --noEmit -p tsconfig.app.json && npx vitest run src/components/planning src/components/dr 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - PlanningView renders `<DrSimPanel>` below the `2xl` break with `scenario`/`setScenario`/`drMode`/`applyPlannedToDr` wired
    - `grep -c 'useEstateView' src/components/planning/PlanningView.tsx` == 1 and `grep -c 'useMemo' ...` == 0 (single-memo invariant)
    - the planned-ratios control + the DR panel both live on the Planning screen, structurally separated by the `2xl` break (D-03)
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0; planning + dr component tests pass
  </acceptance_criteria>
  <done>Reworked DrSimPanel is wired into the Planning screen DR slot with Custom-Failover toggle; one useEstateView, no second memo.</done>
</task>

<task type="auto">
  <name>Task 3: Rework dr.json EN/FR (server/site, physical, drop confidence, new keys) + presenter test</name>
  <files>src/i18n/locales/en/dr.json, src/i18n/locales/fr/dr.json, src/components/dr/DrSimPanel.test.tsx</files>
  <read_first>
    - src/i18n/locales/en/dr.json + src/i18n/locales/fr/dr.json (the whole files — mode keys, confidence block to delete, verdict/assumptions/caveats to keep)
    - 06-PATTERNS.md §i18n (lines 282-294 — exact dr.json rework: server/site mode keys, DELETE confidence block, KEEP failed.chip/verdict/assumptions/caveats, ADD site-lost + stepper + apply-planned + no-stretched keys) and the "Sentinel/denylist invariants" note
    - 06-UI-SPEC.md Copywriting Contract (every DR string — exact EN copy; FR parity required; no editorial verbs; em-dash sentinel)
    - src/components/dr/DrSimPanel.tsx (the reworked component — the keys it now references)
    - an existing component test for the presenter idiom (e.g. a sibling `*.test.tsx` under src/components) for the testing-library pattern
  </read_first>
  <action>
    In `dr.json` (EN + FR): rework `mode` to `{ label, server, site }` (drop `host`/`cluster`/`vcenter`); DELETE the entire `confidence` block; KEEP `failed.chip`, `verdict.*`, `assumptions.*` (content updated to two-mode/physical wording), `caveats.*` (extend content for the two-mode/physical model); ADD keys for the Site-loss "lost — no DR target" line, the per-cluster host-count stepper label ("Hosts failed in {cluster}" / "{n} of {m}"), the "Apply planned ratios to this scenario" affordance + its on/off captions, the empty-state-no-stretched note, and the physical impact labels ("Physical CPU removed" / "Physical RAM removed"). No pre-formatted numbers in strings (interpolation only); no editorial verb (denylist); em-dash sentinel for not-derivable. Keep EN/FR flattened key sets identical.
    Create `DrSimPanel.test.tsx`: render the panel with a stub `EstateView` and assert — exactly two mode segments (server/site) and no third; the impact figure shows physical labels and not "vCPU"; no "confidence" text rendered; the reversible failed chip appears/disappears on toggle; the "Apply planned ratios" checkbox switches the presented result between `drSim` and `plannedDrSim`; the Site segment shows the no-stretched note when the declared-stretched set is empty.
  </action>
  <verify>
    <automated>node -e "const e=require('./src/i18n/locales/en/dr.json'),f=require('./src/i18n/locales/fr/dr.json');const flat=(o,p='')=>Object.entries(o).flatMap(([k,v])=>typeof v==='object'?flat(v,p+k+'.'):[p+k]);const ke=flat(e).sort(),kf=flat(f).sort();if(JSON.stringify(ke)!==JSON.stringify(kf))process.exit(1);if(ke.some(k=>k.startsWith('confidence.')||k.startsWith('mode.host')||k.startsWith('mode.cluster')||k.startsWith('mode.vcenter')))process.exit(1)" && npx vitest run src/components/dr/DrSimPanel.test.tsx 2>&1 | tail -3 && npx @biomejs/biome check src/components/dr src/i18n/locales/en/dr.json src/i18n/locales/fr/dr.json</automated>
  </verify>
  <acceptance_criteria>
    - `en/dr.json` and `fr/dr.json` have identical flattened key sets
    - no `confidence.*` and no `mode.host`/`mode.cluster`/`mode.vcenter` keys remain; `mode.server`/`mode.site` present
    - new keys present: site-lost line, host-count stepper, apply-planned affordance + captions, no-stretched note, physical impact labels — EN + FR
    - no editorial-verb tokens in the new strings (project denylist); no pre-formatted numbers
    - `DrSimPanel.test.tsx` asserts two-modes / physical-not-vCPU / no-confidence / reversible chip / apply-planned switch and exits 0
    - `npx @biomejs/biome check` clean on the modified files
  </acceptance_criteria>
  <done>dr.json reworked EN/FR with parity (server/site, physical labels, no confidence, new keys); presenter test green covering the new contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User DR selection (host checkbox / cluster stepper / site pick / apply-planned toggle) → in-memory `DrScenario` Zustand slice | The untrusted input. String-set / numeric component state; never persisted, never networked. |
| In-memory `EstateView.drSim`/`plannedDrSim` → DrSimPanel presenter | Read-only render boundary; presenter only, no recompute, no second memo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Repudiation / misleading output | DR confidence verdict | mitigate | `confidence` block + i18n keys deleted entirely (D-10); Task 1 + Task 3 gates assert zero `confidence` tokens in the panel and zero `confidence.*` keys. The tool renders no judgement of the user's scenario — only factual caveats + assumptions. |
| T-06-11 | Tampering / misleading output | physical impact figures | mitigate | Panel is a pure presenter of `view.drSim`/`view.plannedDrSim` computed by the Plan-01 engine; gate asserts physical labels and no `vCPU` / `evacueeVcpu` leakage; em-dash sentinel for not-derivable (never a fabricated 0). |
| T-06-12 | Tampering | measured vs Custom-Failover conflation | mitigate | The Apply-planned toggle switches between two distinct engine results (`drSim` vs `plannedDrSim`); they are never merged into one number (D-11); the presenter test asserts the switch is exclusive. |
| T-06-13 | Information disclosure | DR scenario inputs | mitigate | Scenario lives in the in-memory store slice (Plan 01/02 gates assert no persistence); this plan adds no persistence or network surface. |
| T-06-14 | Spoofing / Elevation / DoS | n/a (no auth, no server, no network) | accept | Architecturally impossible — 100% client-side; Phase-1 privacy guard throws on any non-same-origin request. No new surface introduced. |
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.app.json` exits 0
- `npx vitest run src/components/dr src/components/planning` exits 0
- `npx @biomejs/biome check .` clean on modified files
- `dr.json` EN/FR flattened key sets identical; no `confidence.*`/`mode.host|cluster|vcenter` keys
- DrSimPanel: exactly two modes, physical impact (no vCPU), no confidence, reversible selection kept, Custom-Failover is one in-panel toggle
- No `useMemo` in `src/components/dr/` or `src/components/planning/`
</verification>

<success_criteria>
- DR is exactly Server + Site loss; Server has host multi-select + per-cluster stepper; Site has the declared-stretched picker + factual lost line
- Physical CPU+RAM impact (single gold figure); neutral verdict word + numbers; no confidence
- Assumptions + caveats kept; failed-selection reversible/neutral; Custom Failover = one in-panel toggle
- dr.json EN/FR parity; presenter test green
</success_criteria>

<output>
After completion, create `.planning/phases/06-allocation-dr-re-derived/06-03-SUMMARY.md`
</output>
