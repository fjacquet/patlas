---
phase: 05-rich-cluster-host-esx-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engines/parser/adapters/rvtools.ts
  - src/engines/parser/schemas.ts
  - src/types/vinfo.ts
  - src/types/vhost.ts
  - src/engines/parser/adapters/rvtools.test.ts
  - src/engines/aggregation/perEsx.ts
  - src/engines/aggregation/aggregateClusters.ts
  - src/engines/aggregation/globals.ts
  - src/engines/aggregation/guestData.ts
  - src/engines/aggregation/estateView.ts
  - src/types/estate.ts
  - src/engines/aggregation/aggregateClusters.test.ts
  - src/engines/aggregation/perEsx.test.ts
  - src/engines/aggregation/guestData.test.ts
  - src/engines/aggregation/globals.test.ts
autonomous: true
requirements: [RCI-01, RCI-02, RCI-03, RCI-04]
user_setup: []

must_haves:
  truths:
    - "RVTools Powerstate parses to a real enum (poweredOn|poweredOff|suspended); a derived `poweredOn` boolean still exists so no existing consumer breaks"
    - "RVTools vInfo `Template` flag parses to `VInfoRow.template`; absent column → false (factual, no crash)"
    - "vHost host Model / Vendor / ESX Version parse to factual VHostRow text fields (empty when absent — NO lifecycle verdict)"
    - "The MiB canary + every existing parser fixture stay green (validated parser regression intact)"
    - "EstateView carries the full operational-insights metric set at BOTH estate scope and per-cluster, all computed from parsed columns; vPartition-absent → guest data factual em-dash, never invented"
  artifacts:
    - path: "src/engines/aggregation/guestData.ts"
      provides: "Pure vPartition → per-cluster + estate guest-used aggregation; empty/absent → null sentinel"
      contains: "export const aggregateGuestData"
    - path: "src/types/estate.ts"
      provides: "EstateView operational-insights fields (estate + per-cluster) + ClusterDetail projection"
      contains: "operationalInsights"
    - path: "src/engines/parser/adapters/rvtools.ts"
      provides: "powerState enum + template + model/vendor/esxVersion aliases; poweredOn derived"
      contains: "powerState"
  key_links:
    - from: "src/engines/aggregation/estateView.ts"
      to: "src/engines/aggregation/guestData.ts"
      via: "called inside the single buildEstateView pass"
      pattern: "aggregateGuestData"
    - from: "src/engines/parser/adapters/rvtools.ts"
      to: "src/types/vinfo.ts VInfoRow.powerState"
      via: "Powerstate cell → enum, poweredOn derived from it"
      pattern: "powerState"
---

<objective>
Phase-5 DATA LAYER. Extend the (validated) parser additively for the exact
powered-state enum + Template + host model/vendor/ESX-version, add the pure
operational-insights aggregations (realized overcommit, core-weighted avg
CPU%, host-mem-weighted avg mem%, powered-state breakdown,
provisioned-vs-in-use, datastore footprint incl .vswp+snap, guest data,
total cores/host-mem rollups), and surface them on `EstateView` at BOTH
estate scope and per-cluster — all calc-from-real-data (RESEARCH F-1..F-6),
nothing invented, vPartition-absent degrades to a factual sentinel. NO new
`useMemo`. UI is Plan 05-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-rich-cluster-host-esx-intelligence/05-CONTEXT.md
@.planning/phases/05-rich-cluster-host-esx-intelligence/05-RESEARCH.md
@src/engines/parser/adapters/rvtools.ts
@src/engines/parser/schemas.ts
@src/types/vinfo.ts
@src/types/vhost.ts
@src/engines/aggregation/perEsx.ts
@src/engines/aggregation/aggregateClusters.ts
@src/engines/aggregation/globals.ts
@src/engines/aggregation/estateView.ts
@src/types/estate.ts

<interfaces>
<!-- RESEARCH F-1: rvtools.ts:56 alias poweredOn:['powerstate','power state',
     'état','status'], read :184 `=== 'poweredon'`. Change: parse raw cell →
     powerState: 'poweredOn'|'poweredOff'|'suspended' (RVTools emits these;
     unknown → 'poweredOff'); DERIVE poweredOn = powerState==='poweredOn'
     (keep the field so perEsx/aggregateClusters/exports unbroken).
     schemas.ts:52 add powerState enum; vinfo.ts add powerState + template.
   F-2: alias template:['template'] + VInfoRow.template boolean.
   F-4: VHostRow + aliases model/vendor/esxVersion (plain text, '' default).
   F-3: guestData.ts new pure module over snapshot.vpartition (capacity/
     used per VM→cluster); estate+per-cluster; absent vpartition ⇒ null.
   EstateView: add operationalInsights (estate) + per-cluster mirror on
     ClusterAggregate (or a parallel map) + a ClusterDetail projection
     (everything one cluster needs for the 05-02 detail screen). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parser extension — powerState enum + template + model/vendor/esxVersion (regression-gated)</name>
  <files>src/engines/parser/adapters/rvtools.ts, src/engines/parser/schemas.ts, src/types/vinfo.ts, src/types/vhost.ts, src/engines/parser/adapters/rvtools.test.ts</files>
  <behavior>
    - RVTools Powerstate cell `poweredOn`/`poweredOff`/`suspended` → `VInfoRow.powerState` enum; unknown/blank → `'poweredOff'`. `VInfoRow.poweredOn` STILL exists, DERIVED `= powerState === 'poweredOn'` (every existing consumer unchanged).
    - vInfo `Template` TRUE/FALSE → `VInfoRow.template: boolean`; absent column → `false`.
    - vHost `Model`/`Vendor`/`ESX Version` → `VHostRow.model`/`vendor`/`esxVersion: string` (`''` when absent — factual, NO verdict).
    - The MiB canary fixture + ALL existing `rvtools.test.ts` cases pass unchanged (validated-parser regression).
  </behavior>
  <action>Extend the alias dictionary (rvtools.ts ~:56/104-106 longest-spelling-first convention): add `template:['template']`, `model:['model','host model']`, `vendor:['vendor']`, `esxVersion:['esx version','esxi version','version']` (host scope). Keep the existing `poweredOn` alias list (it locates Powerstate). At the read site (:184) compute `powerState` from the normalized cell (`poweredon→'poweredOn'`, `poweredoff→'poweredOff'`, `suspended→'suspended'`, else `'poweredOff'`) and set `poweredOn = powerState === 'poweredOn'`. Add `powerState`+`template` to `VInfoRow` (vinfo.ts) and `VPowerState` union; add `model`/`vendor`/`esxVersion` to `VHostRow` (vhost.ts). Update `schemas.ts`: `powerState: z.enum([...])`, `template: z.boolean()`, vhost string fields. Empty-cell→default via the shipped Minor-3 preprocessor idiom. Extend `rvtools.test.ts`: a fixture row per powerState incl suspended; template true/false + absent→false; model/vendor/esxVersion present + absent→''; assert the MiB canary + existing assertions untouched.</action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/engines/parser --coverage --reporter=dot</automated>
  </verify>
  <done>powerState/template/model/vendor/esxVersion parsed; poweredOn derived (no consumer breakage); absent columns factual-default; parser suite + MiB canary green; engines/parser ≥75%.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Operational-insights aggregations (pure) — overcommit/avg/power-breakdown/footprint/guest-data/totals</name>
  <files>src/engines/aggregation/perEsx.ts, src/engines/aggregation/aggregateClusters.ts, src/engines/aggregation/globals.ts, src/engines/aggregation/guestData.ts, src/engines/aggregation/aggregateClusters.test.ts, src/engines/aggregation/perEsx.test.ts, src/engines/aggregation/guestData.test.ts, src/engines/aggregation/globals.test.ts</files>
  <behavior>
    - Realized CPU overcommit = `vcpuAllocated / usablePhysicalCores` (already `vcpuPerPcpu`); surface estate + per-cluster.
    - Avg CPU usage = Σ(host cpuRatio×cores)/Σcores (core-weighted); avg mem usage = Σ(host ramRatio×memMib)/ΣmemMib (host-mem-weighted) — estate + per-cluster.
    - Powered-state breakdown: counts of poweredOn/poweredOff/suspended/template — estate + per-cluster (uses Task-1 enum/flag).
    - Provisioned-vs-in-use: Σ provisionedMib vs Σ inUseMib — estate + per-cluster.
    - Datastore footprint incl .vswp+snap = Σ inUseMib (RVTools In Use already includes them, RESEARCH F-5).
    - Guest data: `aggregateGuestData(vpartition)` → per-cluster + estate guest-used; vpartition empty/absent ⇒ `null` (factual sentinel, never invented).
    - Total physical cores / total host memory rollups (estate + per-cluster).
  </behavior>
  <action>Create pure `guestData.ts` (`aggregateGuestData(vpartition, vinfo)` mapping partition rows → owning VM → cluster; returns `{estate: GuestData|null, byCluster: Map<string,GuestData|null>}`; `null` when no vPartition — calc-from-real-data). Extend `perEsx.ts`/`aggregateClusters.ts` with the weighted averages + powered-state counts (consume Task-1 `powerState`/`template`) + provisioned/in-use sums + totals — pure, branded round-trip, no new dep. Extend `globals.ts` for the estate rollups. Add an `operationalInsights` block to `EstateView` (estate) AND per-cluster (extend `ClusterAggregate` or a parallel `Map<cluster, ClusterInsights>`), plus a `ClusterDetail` projection type (all fields the 05-02 detail screen needs for one cluster). Wire everything INSIDE `buildEstateView` (the single pass) — NO new `useMemo`. Tests: weighted-average math (heterogeneous hosts), powered-state/template counts incl suspended, guest-data present + absent(null), estate==Σ per-cluster identities, em-dash sentinel where not derivable.</action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/engines/aggregation --coverage --reporter=dot && grep -rn 'useMemo(' src --include='*.ts' --include='*.tsx' | grep -v '\.test\.' | grep -vE ':\s*\*|// ' | wc -l</automated>
  </verify>
  <done>All metrics computed from real columns, estate AND per-cluster; guest-data null when vPartition absent; estate=Σcluster identities tested; engines/aggregation ≥75%; exactly 2 useMemo call sites (unchanged); typecheck green.</done>
</task>

</tasks>

<threat_model>

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-05D-01 | Tampering | parser regression while extending aliases | mitigate | poweredOn kept derived; MiB canary + full parser fixtures gate Task 1 |
| T-05D-02 | Info Disclosure | inventing a metric when source data absent | mitigate | vPartition-absent ⇒ null sentinel; model/vendor plain text; no lifecycle verdict (G1) |
| T-05D-03 | Info Disclosure | new dep / network | mitigate | zero new deps; pure engines; supply-chain + fetch guard in <verification> |
</threat_model>

<verification>
From `/Users/fjacquet/Projects/vatlas`:
1. `npx tsc --noEmit` — clean (app + test config)
2. `npx @biomejs/biome check .` — clean
3. `npx vitest run --coverage --reporter=dot` — full suite green; engines/parser + engines/aggregation ≥75%; MiB canary intact
4. Single-useMemo grep gate: exactly 2 real `useMemo(` call sites (useEstateView + pre-existing SnapshotListSidebar) — no new memo
5. `npm run check:supply-chain` + `node scripts/check-bundle-size.mjs` — green (no new dep/chunk)
6. REAL-FILE UAT (mandatory per feedback_real_file_uat): throwaway harness on the real allvCenters.xlsx → powerState distinct set includes the real states; template count is the real count; per-cluster avg CPU%/mem% and overcommit are finite & estate==Σ; guest-data is a real number if vPartition present else null (NOT 0 invented)
7. ROADMAP/STATE reconcile per project_roadmap_reconcile
</verification>

<success_criteria>

- Parser additively captures powerState/template/model/vendor/esxVersion; `poweredOn` still derived; validated-parser regression green.
- Full operational-insights metric set computed from parsed columns, surfaced estate AND per-cluster, inside the single memo.
- Guest data factual (real or null) — never invented when vPartition absent.
- Coverage gates green; single-useMemo invariant intact; real-file-validated.
</success_criteria>

<output>
After completion create `.planning/phases/05-rich-cluster-host-esx-intelligence/05-01-SUMMARY.md`.
</output>
