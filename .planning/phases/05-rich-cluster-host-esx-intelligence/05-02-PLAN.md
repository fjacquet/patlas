---
phase: 05-rich-cluster-host-esx-intelligence
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/components/inventory/ViewToggle.tsx
  - src/App.tsx
  - src/components/hosts/HostsView.tsx
  - src/components/dashboard/OperationalInsights.tsx
  - src/components/dashboard/GlobalDashboard.tsx
  - src/components/dashboard/ClusterColumn.tsx
  - src/components/cluster/ClusterDetail.tsx
  - src/i18n/index.ts
  - src/i18n/locales/en/rci.json
  - src/i18n/locales/fr/rci.json
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [RCI-01, RCI-02, RCI-03, RCI-04, RCI-05]
user_setup: []

must_haves:
  truths:
    - "The shipped ViewToggle gains a third 'Hosts' segment (reusing the fieldset+aria-pressed idiom verbatim); Dashboard | Inventory | Hosts"
    - "Hosts view = estate host-rollup on top + expandable per-cluster host lists; columns incl model/vendor/ESXi version as plain factual text (no lifecycle verdict)"
    - "Dashboard shows GlobalSummaryCard (unchanged) PLUS a new Operational-Insights tile row"
    - "Clicking a dashboard cluster card opens a dedicated single-cluster detail screen with the FULL metric set, laid out to fit one screen (so Phase 10 = 1 PPTX slide/cluster); a back affordance returns to the dashboard"
    - "All new UI consumes EstateView (05-01) via the single useEstateView; NO second useMemo; drill nav is in-app view state (lifted, like `mode`), no router"
  artifacts:
    - path: "src/components/hosts/HostsView.tsx"
      provides: "Estate host-rollup + per-cluster drill, reusing P3 DataTable/ColumnPicker"
      contains: "HostsView"
    - path: "src/components/cluster/ClusterDetail.tsx"
      provides: "One-screen-fit single-cluster full-metric detail (export-ready for P10)"
      contains: "ClusterDetail"
    - path: "src/components/dashboard/OperationalInsights.tsx"
      provides: "Estate operational-insights tile row"
      contains: "OperationalInsights"
  key_links:
    - from: "src/components/dashboard/ClusterColumn.tsx"
      to: "src/components/cluster/ClusterDetail.tsx"
      via: "card click → onSelectCluster lifted state → ClusterDetail screen"
      pattern: "onSelectCluster"
    - from: "src/components/inventory/ViewToggle.tsx"
      to: "src/components/hosts/HostsView.tsx"
      via: "third 'hosts' segment selects the Hosts view in App"
      pattern: "hosts"
---

<objective>
Phase-5 UI LAYER (depends on 05-01's EstateView). Extend the shipped
ViewToggle with a "Hosts" segment, build the estate-rollup+drill Hosts
view, add the estate Operational-Insights tile row alongside the kept
GlobalSummaryCard, make the dashboard cluster cards the drill entry point
into a dedicated one-screen-fit per-cluster detail screen (export-ready so
Phase 10 = 1 slide/cluster), and the `rci` i18n namespace. Pure presenters
off `useEstateView`; NO new memo; drill = in-app lifted view state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-rich-cluster-host-esx-intelligence/05-CONTEXT.md
@.planning/phases/05-rich-cluster-host-esx-intelligence/05-RESEARCH.md
@.planning/phases/05-rich-cluster-host-esx-intelligence/05-01-PLAN.md
@src/components/inventory/ViewToggle.tsx
@src/components/inventory/DataTable.tsx
@src/components/inventory/ColumnPicker.tsx
@src/App.tsx
@src/components/dashboard/GlobalDashboard.tsx
@src/components/dashboard/GlobalSummaryCard.tsx
@src/components/dashboard/ClusterColumn.tsx
@src/components/dashboard/AccountingModeToggle.tsx
@src/i18n/index.ts
@src/utils/format.ts

<interfaces>
<!-- ViewToggle is the shipped fieldset role=group + aria-pressed segmented
     control (the CI grep gate asserts literal role="group"). Add a 'hosts'
     option to its union + segment list — DRY, no new component.
     App.tsx currently switches Dashboard|Inventory on the toggle value;
     add the 'hosts' branch → <HostsView/>. Per-cluster drill is NOT a
     toggle value: it is lifted state (selectedCluster: string|null) in
     the Dashboard; non-null → render <ClusterDetail/> instead of the grid
     with a back control (mirrors the `mode` useState pattern; NO router,
     NO 2nd useMemo). HostsView/OperationalInsights/ClusterDetail are pure
     presenters taking EstateView props (05-01 shapes). Reuse P3 DataTable/
     ColumnPicker for the host rollup. ClusterDetail one-screen-fit:
     fixed grid, no internal scroll, sized for a 16:9 slide (Phase-10
     snapshot). Region errors via shipped DashboardError idiom. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ViewToggle "Hosts" segment + App wiring + HostsView (estate rollup + per-cluster drill)</name>
  <files>src/components/inventory/ViewToggle.tsx, src/App.tsx, src/components/hosts/HostsView.tsx, src/i18n/index.ts, src/i18n/locales/en/rci.json, src/i18n/locales/fr/rci.json</files>
  <action>Add `'hosts'` to the ViewToggle value union + a third segment (reuse the EXACT fieldset+legend+aria-pressed idiom; keep the `biome-ignore` + literal `role="group"` — CI grep gate). Wire `App.tsx`: toggle `'hosts'` → `<HostsView view={view}/>` (view from the single `useEstateView`). Create `HostsView.tsx` — pure presenter: top estate host-rollup (total hosts, cores, host-mem, avg CPU%/mem%, powered-on VM count) then expandable per-cluster sections each a P3 `DataTable` of that cluster's hosts (columns: hostName, cores, mem, mean CPU%, mean RAM%, powered-on VMs, ESXi version, fault domain, model, vendor — model/vendor/ESXi as plain factual text, NO verdict, em-dash when absent). Reuse `ColumnPicker`/CSV from P3. Create `rci` i18n namespace (en+fr, key parity, placeholders only, no editorial verbs); register in `i18n/index.ts` NAMESPACES+resources.</action>
  <verify>
    <automated>npx tsc --noEmit && npx @biomejs/biome check src/ && npx vitest run --reporter=dot && node -e "const e=require('./src/i18n/locales/en/rci.json'),f=require('./src/i18n/locales/fr/rci.json');const fl=o=>Object.entries(o).flatMap(([k,v])=>typeof v==='object'?fl(v).map(s=>k+'.'+s):[k]);if(JSON.stringify(fl(e).sort())!==JSON.stringify(fl(f).sort()))throw new Error('rci parity')"</automated>
  </verify>
  <done>3-segment ViewToggle (idiom reused, grep gate intact); Hosts view renders estate rollup + per-cluster host tables with factual model/vendor/ESXi; rci EN/FR parity; typecheck/biome/suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Operational-Insights estate row + ClusterColumn drill entry + one-screen-fit ClusterDetail</name>
  <files>src/components/dashboard/OperationalInsights.tsx, src/components/dashboard/GlobalDashboard.tsx, src/components/dashboard/ClusterColumn.tsx, src/components/cluster/ClusterDetail.tsx, src/i18n/locales/en/rci.json, src/i18n/locales/fr/rci.json, .planning/REQUIREMENTS.md</files>
  <action>Create `OperationalInsights.tsx` — estate tile row (realized overcommit, avg CPU%, avg mem%, powered-state breakdown, provisioned-vs-in-use, footprint, guest data [em-dash when null], total cores/host-mem) from `view.operationalInsights`; mount in `GlobalDashboard` AFTER the shipped `GlobalSummaryCard` (card kept). `ClusterColumn`: make the card the drill entry point — add `onSelectCluster(cluster)` (clickable card / explicit "Details" affordance, keyboard-accessible); do NOT remove the stretched block. `GlobalDashboard`: lifted `const [selectedCluster,setSelectedCluster]=useState<string|null>(null)` (like `mode`); when non-null render `<ClusterDetail detail={view.clusterDetail.get(selectedCluster)} onBack={()=>setSelectedCluster(null)}/>` in place of the cluster grid (NO router, NO 2nd useMemo). Create `ClusterDetail.tsx` — pure presenter: one-screen-fit fixed grid (NO internal scroll) showing that cluster's FULL metric set (capacity, overcommit, avg CPU%/mem%, powered-state, provisioned/in-use, footprint, guest data, per-site/stretched block, host count) sized for a 16:9 area so Phase-10 can snapshot it as one slide; a back control returns to the dashboard. Region errors via the shipped `DashboardError` idiom. Add `rci` keys (en+fr parity). Update `.planning/REQUIREMENTS.md`: add RCI-01..05 (rich per-cluster intelligence; Hosts view; estate operational-insights; cluster-detail drill screen; calc-from-real-data both scopes) + tracking-table rows, mapped to Phase 5.</action>
  <verify>
    <automated>npx tsc --noEmit && npx @biomejs/biome check src/ && npx vitest run --reporter=dot && grep -rn 'useMemo(' src --include='*.ts' --include='*.tsx' | grep -v '\.test\.' | grep -vE ':\s*\*|// ' | wc -l</automated>
  </verify>
  <done>Estate Operational-Insights row alongside kept GlobalSummaryCard; cluster card drills to a one-screen-fit ClusterDetail with full metrics + back; guest-data em-dash when null; in-app view state only (exactly 2 useMemo sites); REQUIREMENTS RCI-01..05 added; typecheck/biome/suite green.</done>
</task>

</tasks>

<threat_model>

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-05U-01 | Tampering | a 2nd useMemo / router creeping in | mitigate | drill = lifted useState like `mode`; grep gate asserts 2 useMemo sites |
| T-05U-02 | Info Disclosure | model/vendor framed as a lifecycle judgement | mitigate | plain text only, em-dash when absent, no verdict (G1); denylist |
| T-05U-03 | DoS | host rollup at 100s of hosts | accept | reuses P3 virtualised DataTable (10k-validated) |
</threat_model>

<verification>
From `/Users/fjacquet/Projects/vatlas`:
1. `npx tsc --noEmit` clean
2. `npx @biomejs/biome check .` clean
3. `npx vitest run --coverage --reporter=dot` full suite green
4. Single-useMemo grep: exactly 2 real call sites (no new memo; drill is useState)
5. ViewToggle CI grep gate: literal `role="group"` present (idiom reused, not reinvented)
6. i18n EN/FR parity for `rci` (+ unchanged str/mvc/dr/alloc)
7. `npm run check:supply-chain` + `node scripts/check-bundle-size.mjs` green
8. REAL-FILE/browser UAT (mandatory): on the real allvCenters estate — ViewToggle shows Dashboard|Inventory|Hosts; Hosts view shows estate rollup + per-cluster host tables with real model/vendor/ESXi (or em-dash); dashboard shows GlobalSummaryCard + Operational-Insights row; clicking a cluster card opens its detail screen, full metrics, fits one screen with no scroll, back returns; guest-data shows a real number or em-dash (never invented 0)
9. ROADMAP/STATE reconcile (project_roadmap_reconcile) — Phase 5 plans + Progress
</verification>

<success_criteria>

- 3-segment ViewToggle (idiom reused); Hosts estate-rollup+drill view with factual model/vendor/ESXi.
- Estate Operational-Insights row + kept GlobalSummaryCard; cluster card → one-screen-fit ClusterDetail (export-ready for P10) + back.
- All new UI off the single `useEstateView`; no 2nd memo; no router; drill is lifted state.
- rci EN/FR parity; REQUIREMENTS RCI-01..05 recorded; real-file-validated; gates green.
</success_criteria>

<output>
After completion create `.planning/phases/05-rich-cluster-host-esx-intelligence/05-02-SUMMARY.md`, then reconcile ROADMAP (Phase-5 plans + Progress) + STATE.
</output>
