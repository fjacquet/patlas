---
phase: 08-in-session-trends
plan: 03
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified:
  - src/components/trends/TrendsView.tsx
  - src/components/trends/TrendChart.tsx
  - src/components/trends/TrendSparkline.tsx
  - src/components/trends/DeltaPanel.tsx
  - src/components/trends/trendsChartOptions.ts
  - src/components/ViewToggle.tsx
  - src/App.tsx
  - src/components/SnapshotCard.tsx
  - src/components/SnapshotListSidebar.tsx
  - src/components/dashboard/ClusterColumn.tsx
  - src/i18n/locales/en/trends.json
  - src/i18n/locales/fr/trends.json
  - src/i18n/locales/en/inventory.json
  - src/i18n/locales/fr/inventory.json
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [TRD-01, TRD-02, TRD-03, TRD-04, TRD-05]
must_haves:
  truths:
    - "A user reaches the trends view via a new 'trends' ViewToggle segment; it shows a factual empty-state line when <2 snapshots are loaded"
    - "The trend chart uses xAxis:{type:'time'} with [Date,value] pairs so irregular capture dates show visibly non-uniform spacing (criterion 2)"
    - "Per-cluster sparklines appear on dashboard cluster cards only when trends has >=2 points for that cluster"
    - "The delta panel shows factual signed branded consecutive-pair deltas (no color/verb/arrow)"
    - "The snapshot list shows inline-editable capturedAt (reusing setCapturedAt), the factual warm-up N/M indicator, per-snapshot vCenter+RVTools-version metadata, and the inferred-order caption when orderInferred"
    - "REQUIREMENTS.md TRD-01..05 traceability rows are reconciled from Phase 6 to Phase 8"
  artifacts:
    - path: "src/components/trends/TrendsView.tsx"
      provides: "EosView-shell trends view (empty-state + chart + delta sections)"
    - path: "src/components/trends/trendsChartOptions.ts"
      provides: "Pure ECharts time-axis line + sparkline option builders off the memoized view"
      contains: "type: 'time'"
    - path: "src/components/trends/DeltaPanel.tsx"
      provides: "Factual consecutive-pair delta rows (TRD-04)"
    - path: "src/i18n/locales/en/trends.json"
      provides: "trends namespace EN"
    - path: "src/i18n/locales/fr/trends.json"
      provides: "trends namespace FR (parity)"
  key_links:
    - from: "src/components/trends/TrendChart.tsx"
      to: "trendsChartOptions -> Chart"
      via: "xAxis:{type:'time'}; category fallback only when orderInferred"
      pattern: "type: 'time'"
    - from: "src/components/dashboard/ClusterColumn.tsx"
      to: "view.trends per-cluster points"
      via: "minimal sparkline Chart rendered only when >=2 points"
      pattern: "TrendSparkline"
    - from: "src/components/SnapshotCard.tsx"
      to: "setCapturedAt"
      via: "inline date input on commit (no local state bypass)"
      pattern: "setCapturedAt\\("
---

<objective>
Ship the trends UI exactly as the APPROVED 08-UI-SPEC binds it: a new 'trends' ViewToggle segment + App branch; a TrendsView reusing the verbatim EosView shell; a TrendChart rendering through the single shipped Chart with xAxis:{type:'time'} (category fallback only when orderInferred); per-cluster sparklines on dashboard cluster cards; a factual DeltaPanel; inline-editable capturedAt (reusing setCapturedAt) + warm-up N/M indicator + per-snapshot vCenter/version metadata + inferred-order caption in the snapshot list; the new trends i18n namespace (EN+FR parity) and inventory:nav.trends. Reconcile REQUIREMENTS.md TRD-01..05 traceability from the stale "Phase 6" to "Phase 8" (mirrors the P7 precedent). End with a single human-verify checkpoint covering all six success criteria.

Purpose: This is the user-facing delivery of TRD-01..05. It consumes plan 01's EstateView.trends (plain props off the existing single memo — no new memo) and plan 02's {done,total} + registered LineChart + releaseRawRows.
Output: a navigable, factual trends view + dashboard sparklines + snapshot-list edits, EN/FR parity, and a clean requirements traceability table.
</objective>

<resolved_decisions>
- **D-05 presentation (LOCKED by UI-SPEC, Open Question 1 recommendation (a)):** when `view.trends.orderInferred === true` the whole chart renders with `xAxis:{type:'category'}` over stable-ordered labels PLUS the factual inferred-order caption; when all points carry real distinct dates, `xAxis:{type:'time'}`. Whole-series, never mixed — honest, no fabricated positions. The data layer always keeps real dates where known (plan 01).
- **DD-B visible delta set (LOCKED):** vmCount, poweredOnVms, hostCount, clusterCount, vcpuAllocated, vramAllocated->GiB, totalStorage->GiB/TiB — factual signed branded rows, em-dash when null on either side, no color/verb/arrow (UI-SPEC Copywriting + Color contract).
- **D-03 (LOCKED):** the inline capturedAt input calls the shipped setCapturedAt store action on commit — no new mutation, no local date state that bypasses the store (Pitfall 5). The recompute is automatic through the single memo.
</resolved_decisions>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-in-session-trends/08-CONTEXT.md
@.planning/phases/08-in-session-trends/08-RESEARCH.md
@.planning/phases/08-in-session-trends/08-PATTERNS.md
@.planning/phases/08-in-session-trends/08-UI-SPEC.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: trends i18n namespace (EN+FR), ViewToggle 'trends' segment, App branch, REQUIREMENTS reconciliation</name>
  <files>src/i18n/locales/en/trends.json, src/i18n/locales/fr/trends.json, src/i18n/locales/en/inventory.json, src/i18n/locales/fr/inventory.json, src/components/ViewToggle.tsx, src/App.tsx, .planning/REQUIREMENTS.md</files>
  <read_first>
    - src/i18n/locales/en/eos.json AND src/i18n/locales/fr/eos.json (PATTERNS analog — the namespace-key shape EosView consumes; mirror for trends.json with double-brace interpolation only, no pre-formatted numbers)
    - src/components/ViewToggle.tsx (PATTERNS analog — AppView union line 3, VIEWS const line 5, the fieldset/legend/map(aria-pressed) idiom 47-75, gold-active-segment class 65, Arrow wraparound 31-45, t('nav.${view}') line 70)
    - src/App.tsx (PATTERNS analog — the view-branch ternary 39-49, the EosView import near line 5 — mirror for TrendsView)
    - src/i18n/locales/en/inventory.json (the existing nav.* keys — add nav.trends in BOTH en and fr)
    - .planning/REQUIREMENTS.md (the TRD-01..05 traceability rows currently mapped to Phase 6 / Pending to reconcile to Phase 8; the surrounding rows show the exact column format / P7 precedent)
  </read_first>
  <action>
    Create src/i18n/locales/en/trends.json and src/i18n/locales/fr/trends.json — a new trends namespace with parity keys (planner-set strings within the UI-SPEC Copywriting contract): heading (empty-state heading = section title), an empty-state body key (factual "Load 2 or more snapshots..." / FR parity — state the condition, not advice), warmup ("Trends preparing — {{done}}/{{total}}" / "Tendances en preparation — {{done}}/{{total}}"), orderInferred (factual ordering-inferred line / FR), released (factual raw-data-released line / FR), delta.<metric> label keys for vmCount/poweredOnVms/hostCount/clusterCount/vcpuAllocated/vramAllocated/totalStorage, and per-point tooltip label keys. NO pre-formatted numbers — interpolation placeholders only; numbers come from @/utils/format. Add a nav.trends key to BOTH src/i18n/locales/{en,fr}/inventory.json (factual "Trends" / "Tendances").

    In src/components/ViewToggle.tsx append 'trends' LAST: extend the AppView union (line 3) and the VIEWS const (line 5) to end with 'eos', 'trends'. Add NO new code — the fieldset/legend/aria-pressed/gold-active/Arrow-wraparound/focus-ring idiom is inherited verbatim; the label resolves via the existing t('nav.${view}') lookup to nav.trends.

    In src/App.tsx add an import of TrendsView from ./components/trends/TrendsView near the EosView import and append a branch to the view ternary mirroring the EOS branch exactly (activeView === 'trends' renders TrendsView, else GlobalDashboard).

    In .planning/REQUIREMENTS.md change the traceability rows for TRD-01, TRD-02, TRD-03, TRD-04, TRD-05 from the stale Phase-6 form to the Phase-8 form, matching the exact column format used by the P7-precedent rows in the same table. Do not alter unrelated rows.
  </action>
  <verify>
    <automated>node -e "const e=require('./src/i18n/locales/en/trends.json'),f=require('./src/i18n/locales/fr/trends.json');const a=JSON.stringify(Object.keys(e).sort()),b=JSON.stringify(Object.keys(f).sort());if(a!==b){console.error('EN/FR trends.json key mismatch');process.exit(1)}console.log('parity ok')"</automated>
  </verify>
  <acceptance_criteria>
    - src/i18n/locales/en/trends.json and src/i18n/locales/fr/trends.json both exist with identical top-level key sets (the node parity check exits 0)
    - grep for the new nav key in en/inventory.json and fr/inventory.json each return a match
    - grep -c "'trends'" src/components/ViewToggle.tsx is >= 2 (AppView union + VIEWS const), appended last after 'eos'
    - grep -c "TrendsView" src/App.tsx is >= 2 (import + ternary branch)
    - The TRD-01..05 rows in .planning/REQUIREMENTS.md show Phase 8; grep for "TRD-0[1-5]" together with "Phase 6" returns 0
    - No pre-formatted number literal in either trends.json (only interpolation placeholders for dynamic values)
    - npx tsc --noEmit -p tsconfig.app.json exits 0
  </acceptance_criteria>
  <done>trends namespace exists with EN/FR parity; nav.trends added both locales; 'trends' segment appended to ViewToggle; App routes to TrendsView; REQUIREMENTS TRD-01..05 reconciled Phase 6 -> Phase 8; typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: TrendsView shell + pure time-axis chart options + TrendChart + DeltaPanel + per-cluster sparklines</name>
  <files>src/components/trends/TrendsView.tsx, src/components/trends/trendsChartOptions.ts, src/components/trends/TrendChart.tsx, src/components/trends/TrendSparkline.tsx, src/components/trends/DeltaPanel.tsx, src/components/dashboard/ClusterColumn.tsx</files>
  <read_first>
    - src/components/eos/EosView.tsx (PATTERNS analog — verbatim shell at 157-159 + 280-282; the empty-state early return 55-65; the region-scoped EosError 25-36 reading ONLY error.message; the chartOption builder 127-155; numeric tile typography 196 / heading 162 / caption 165-171; @/utils/format import line 11; const loc = i18n.language line 49; reconcile/split factual caption tone 203-210)
    - src/components/Chart.tsx (the single option prop API; the ariaLabel hook; SVG injected — plan 02 registered LineChart; sparkline style height 36)
    - src/hooks/useEstateView.ts (the single memo — TrendsView/ClusterColumn consume view.trends as plain props off this; do NOT add a new memo or selector)
    - src/types/estate.ts (plan 01 output — TrendSeries/TrendPoint/TrendDelta/TrendHeadline; EstateView.trends typed TrendSeries|null)
    - src/utils/format.ts (fmtDate, fmtInt, fmtMemMb — locale-aware; no pre-formatted numbers in i18n)
    - src/engines/units/converters.ts (mibToGib/gibToTib/mibToTib — branded delta presentation; never the raw base-2 multiplier)
    - src/components/dashboard/ClusterColumn.tsx (the dashboard cluster card — the sparkline slot host; render only when view.trends has >=2 points for that cluster)
    - .planning/phases/08-in-session-trends/08-UI-SPEC.md (Component & Interaction Contract — the binding visual rules for chart axis, sparkline, delta panel, color/verb prohibitions)
  </read_first>
  <action>
    Create src/components/trends/trendsChartOptions.ts — PURE ECharts option builders taking the memoized view.trends (no hooks, no memo). The trend line builder MUST use xAxis:{ type:'time' } with series.data as [Date, value] pairs (TRD-02 / criterion 2 — type:'category' is the forbidden default); yAxis:{type:'value'}; tooltip:{trigger:'axis'} whose formatter lists the point date (via fmtDate/locale) and the per-point metadata array of {vCenterLabel, rvtoolsVersion} (criterion 6). D-05 fallback: when view.trends.orderInferred === true, build with xAxis:{type:'category'} over the stable-ordered labels (whole-series, never mixed). axisLabel.formatter defers to fmtDate/locale — never a baked formatted string in i18n. A separate minimal sparkline option builder: grid {top:2,bottom:2,left:2,right:2}, xAxis/yAxis show:false, series [{type:'line',showSymbol:false,lineStyle:{width:1.5}}], single neutral stroke token (no per-cluster color, no util-low/mid/high status ramp). No editorial-verb / verdict-color anywhere.

    Create src/components/trends/TrendChart.tsx (renders the trend line option via the shipped Chart with an ariaLabel), src/components/trends/TrendSparkline.tsx (renders the minimal sparkline option via Chart with style height 36), src/components/trends/DeltaPanel.tsx (a panel section of factual consecutive-pair rows: label key + signed branded number font-mono tabular-nums, em-dash when null on either side; the DD-B visible set; mirror EosView reconcile/split caption tone; no color/verb/arrow), and src/components/trends/TrendsView.tsx (verbatim EosView shell: main flex-1 overflow-y-auto p-8 -> ErrorBoundary with a TrendsError copied from EosError reading ONLY error.message -> div flex flex-col gap-6 of panel sections: heading -> chart -> delta; empty state when view.trends === null = the heading + the factual "Load 2 or more snapshots" line, no chart, no spinner).

    Modify src/components/dashboard/ClusterColumn.tsx to render TrendSparkline for that cluster ONLY when view.trends has >=2 points for the cluster (with <2 points the slot is absent — no placeholder, no skeleton). Sparklines fill progressively as warm-up lands via the store->memo reactive path (no per-sparkline spinner). Numbers via @/utils/format; locale via i18n.language; every className carries its dark: twin (CLAUDE.md hard gate). No new doc-comment contains the literal React-memo-hook token (grep-gate matches comments).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.app.json && npx @biomejs/biome check src/components/trends/ src/components/dashboard/ClusterColumn.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "type: 'time'" src/components/trends/trendsChartOptions.ts is >= 1 (TRD-02 mandate)
    - grep -c "type: 'category'" src/components/trends/trendsChartOptions.ts is >= 1 AND appears only in the orderInferred branch (D-05 fallback — confirm by reading)
    - grep -c for the raw base-2 multiplier literal across src/components/trends/ is 0 (branded units only)
    - grep -ci "util-low\|util-mid\|util-high" across the four trends component files is 0 (status-ramp forbidden on trend/delta/sparkline — UI-SPEC Color contract; the util-high hover is allowed only on the SnapshotCard remove glyph, handled in Task 3)
    - TrendsError reads only error.message (grep for error.cause or error.stack in src/components/trends/TrendsView.tsx returns nothing); the view renders parsed strings only via React text interpolation (no raw-HTML injection prop)
    - grep -c "TrendSparkline" src/components/dashboard/ClusterColumn.tsx is >= 1, guarded by a >=2-points condition
    - The grep-gated single-memo hook token count across non-test src/ is unchanged (no new memo introduced by these components)
    - npx tsc --noEmit -p tsconfig.app.json exits 0; biome check exits 0
  </acceptance_criteria>
  <done>Pure time-axis + sparkline option builders (category only as the orderInferred fallback); TrendChart/TrendSparkline/DeltaPanel/TrendsView render through the single Chart with the EosView shell + message-only error boundary; per-cluster sparklines on dashboard cluster cards gated at >=2 points; no color/verb/status-ramp/second-memo; typecheck + lint clean.</done>
</task>

<task type="auto">
  <name>Task 3: SnapshotCard inline capturedAt edit (setCapturedAt) + vCenter/version metadata; SnapshotListSidebar warm-up N/M + inferred-order caption</name>
  <files>src/components/SnapshotCard.tsx, src/components/SnapshotListSidebar.tsx</files>
  <read_first>
    - src/components/SnapshotCard.tsx (PATTERNS analog — read-only capturedAt at line 61 to REPLACE with the inline editor; remove glyph button 88-95 with the hover:text-util-high idiom; the text-xs card-meta tier; the active-card ring-2 ring-accent-500 idiom)
    - src/store/snapshotStore.ts (the shipped setCapturedAt action at lines 139-146 — D-03 reuses it verbatim; no new mutation, no local date state that bypasses the store — Pitfall 5)
    - src/components/SnapshotListSidebar.tsx (PATTERNS analog — sorted-list useMemo 25-28 (the LIST memo — distinct from the grep-gated single estate memo, not a violation), useSnapshotUpload use line 23 — read the new {done,total} here)
    - src/hooks/useSnapshotUpload.ts (plan 02 output — the {done,total} exposed for the warm-up indicator; rawReleased on released snapshots)
    - src/types/snapshot.ts (Snapshot.capturedAt / vCenterLabel / rvtoolsVersion / rawReleased — the metadata to render per criterion 6, and the released-snapshot factual note)
    - .planning/phases/08-in-session-trends/08-UI-SPEC.md (Snapshot list section — the exact interaction + typography + focus-ring + no-accent-on-input contract)
  </read_first>
  <action>
    In src/components/SnapshotCard.tsx replace the read-only capturedAt text (line 61) with an inline-editable date control that on commit calls the shipped setCapturedAt(snapshot.id, date) store action — NO new mutation, NO local date state that bypasses the store (Pitfall 5: a bypass means the chart will not reorder). The edited value is the highest-priority capture-date source (D-03); editing recomputes the series through the single memo automatically. Stay in the existing text-xs card-meta type tier; the input inherits panel/dark-twin tokens with focus-visible:ring-2 ring-primary-500; NO accent gold on the input (accent is reserved per UI-SPEC Color — the active-card ring stays accent, the input does not). Render the per-snapshot metadata factually (criterion 6): vCenterLabel + rvtoolsVersion in the text-xs meta tier. When snapshot.rawReleased === true, show the factual trends:released note (no editorial verb, no alarm styling) and prevent/explain selecting it as the active estate. Keep the existing remove glyph + its hover:text-util-high idiom unchanged (the only sanctioned util-high use in scope).

    In src/components/SnapshotListSidebar.tsx render the factual warm-up "trends preparing — N/M" line in its own region (scoped, never a global overlay), text-sm text-slate-500 dark:text-slate-400, present only while done < total (state source = useSnapshotUpload {done,total}), using the trends:warmup interpolated key (numbers via fmtInt, never baked into the string). Render the inferred-order caption (trends:orderInferred, neutral text-sm slate) near the snapshot list when view.trends?.orderInferred === true. Do not add a new memo — the existing sorted-list useMemo (lines 25-28) is the list memo and stays as-is; consume view.trends as plain props.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.app.json && npx @biomejs/biome check src/components/SnapshotCard.tsx src/components/SnapshotListSidebar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "setCapturedAt(" src/components/SnapshotCard.tsx is >= 1 (D-03 reuses the shipped action; no new store mutation added anywhere in this plan)
    - No local component state holds the edited date independently of the store (any date-related useState is a transient input buffer that commits via setCapturedAt; confirm by reading there is no store-bypassing source of truth)
    - grep -c "vCenterLabel\|rvtoolsVersion" src/components/SnapshotCard.tsx is >= 1 (criterion 6 metadata rendered)
    - grep -c "rawReleased" src/components/SnapshotCard.tsx is >= 1 (factual released note + active-selection guard)
    - grep for done/total in src/components/SnapshotListSidebar.tsx returns matches for the warm-up indicator; grep -c "warmup\|orderInferred" src/components/SnapshotListSidebar.tsx is >= 2
    - Every new className in both files carries a dark: twin (spot-check by reading; CLAUDE.md hard gate)
    - No accent-gold class on the date input (grep for accent- in src/components/SnapshotCard.tsx shows accent only on the existing active-card ring, not the input)
    - The grep-gated single-memo hook token count across non-test src/ is unchanged (the list memo is pre-existing, not new)
    - npx tsc --noEmit -p tsconfig.app.json exits 0; biome check exits 0
  </acceptance_criteria>
  <done>Inline capturedAt editor commits via the shipped setCapturedAt (no bypass, no new mutation); per-snapshot vCenter/version metadata + released note rendered factually; warm-up N/M indicator + inferred-order caption in the sidebar's own region; no new memo, no accent on input, dark-twin on every class; typecheck + lint clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification of the six Phase-8 success criteria</name>
  <what-built>
    The full in-session trends feature: pure trends engine composed into the single memo (plan 01), non-blocking latest-first warm-up + DD-C release + LineChart registration (plan 02), and the trends view + dashboard sparklines + snapshot-list edits + EN/FR i18n (this plan). REQUIREMENTS TRD-01..05 reconciled to Phase 8.
  </what-built>
  <how-to-verify>
    Run `npm run dev` and open http://localhost:5173/vatlas/.
    1. (Criterion 1 — TRD-01) Drop multiple monthly RVTools workbooks at once. Confirm the dashboard becomes interactive promptly off the latest snapshot while a factual "trends preparing — N/M" line shows in the snapshot-list area (no global blocking overlay) and disappears when done equals total.
    2. (Criterion 2 — TRD-02) Select the trends ViewToggle segment. Confirm the trend chart X-axis uses the actual capture dates with visibly NON-uniform spacing for irregular dates (e.g. month-end vs mid-month) — not evenly-spaced categorical labels. If any date had to be inferred, confirm the whole chart falls back to ordered category labels WITH the factual inferred-order caption.
    3. (Criterion 3 — TRD-03) Return to the dashboard with 2+ snapshots loaded; confirm per-cluster sparklines appear on the cluster cards (and are absent with fewer than 2 points — no placeholder box).
    4. (Criterion 4 — TRD-04) Confirm the delta panel shows factual signed branded consecutive-pair deltas (e.g. +12, -3, +480 GiB) with no color, no verb, no up/down arrow; em-dash where a value is null.
    5. (Criterion 5 — TRD-05) Refresh the page; confirm all trends/snapshots are gone (no cross-session persistence).
    6. (Criterion 6) In the snapshot list, confirm each snapshot shows its vCenter label + RVTools version, and the capture date is inline-editable; edit one date and confirm the timeline re-orders (proves the edit flows through setCapturedAt to the single memo). If more than 4 snapshots were loaded, confirm older snapshots show the factual released note while their trend points remain on the chart with unchanged numbers.
    Also confirm the language toggle shows FR + EN parity for every new trends string (no missing keys, no baked numbers).
    Describe behavior only — do not assert which real clusters grew/shrank (user owns ground truth).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues per criterion</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Parsed snapshot strings -> React render | vCenter labels / RVTools versions / dates rendered in the view + chart tooltip. React default-escapes all interpolated text; no raw-HTML injection prop in scope. |
| Error -> region ErrorBoundary | A bad workbook must not white-screen the app; the fallback must not leak raw row content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | Information disclosure | TrendsError boundary | mitigate | Copy the shipped EosError exactly — read ONLY error.message, never error.cause/error.stack/the error object (would leak VM/host names). Verified by acceptance criterion. |
| T-08-09 | Tampering / XSS | rendering parsed vCenter/version/date strings in view + tooltip | mitigate | All values rendered via React text interpolation (default-escaped) and @/utils/format; the unsafe raw-HTML React prop is not used and no innerHTML assignment is introduced. Workbook content already Zod-validated upstream (Phase 1). |
| T-08-10 | Information disclosure | i18n strings / persistence | mitigate | No pre-formatted dataset numbers baked into i18n; trends derived only, no localStorage/sessionStorage of rows (TRD-05 structurally intact from plans 01/02). |

No HIGH-severity threat: client-side only, no auth, no network, no new external input, no storage; the realistic surface (DOM rendering of already-validated parsed strings, error-message leakage) is mitigated by React's default escaping and the message-only error boundary. ASVS L1 — pass.
</threat_model>

<verification>
- npx tsc --noEmit -p tsconfig.app.json exits 0
- npx @biomejs/biome check src/components/ src/i18n/locales/ exits 0
- EN/FR trends.json top-level key sets identical (node parity check exits 0); nav.trends present both inventory locales
- npm run test:run exits 0 (no regression in shipped component/engine tests)
- REQUIREMENTS TRD-01..05 reconciled to Phase 8 (no Phase-6 TRD row remains)
- Single-memo grep-gate count unchanged (no new memo site)
- Human-verify checkpoint approved for all six success criteria
</verification>

<success_criteria>
- All six ROADMAP Phase-8 success criteria demonstrably true at the human-verify checkpoint
- Trend chart uses xAxis:{type:'time'} (category only as the whole-series orderInferred fallback with caption)
- Per-cluster sparklines on dashboard cards gated at >=2 points; delta panel factual/branded/no-verdict
- Inline capturedAt edit flows through the shipped setCapturedAt (no bypass, no new mutation); vCenter/version metadata + warm-up N/M + inferred-order + released notes all factual EN/FR parity
- REQUIREMENTS TRD-01..05 traceability reconciled Phase 6 -> Phase 8
</success_criteria>

<output>
After completion, create `.planning/phases/08-in-session-trends/08-03-SUMMARY.md`
</output>
