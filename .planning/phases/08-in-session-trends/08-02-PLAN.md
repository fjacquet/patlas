---
phase: 08-in-session-trends
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store/snapshotStore.ts
  - src/types/snapshot.ts
  - src/hooks/useSnapshotUpload.ts
  - src/components/Chart.tsx
autonomous: true
requirements: [TRD-01, TRD-02, TRD-05]
must_haves:
  truths:
    - "Dropping 12 workbooks renders the dashboard off the first (latest-capturedAt) snapshot without waiting for the rest to parse"
    - "Remaining snapshots warm in a background drain (singleton Worker, one parse at a time) and addSnapshot as each lands"
    - "useSnapshotUpload exposes a factual {done,total} count for the warm-up indicator"
    - "When loaded count > 4, the oldest-by-capturedAt non-active snapshot's raw rows are released (arrays emptied, aggregate carried) without touching the active/latest"
    - "ECharts LineChart is registered so line/sparkline options render; the echarts chunk stays ≤300 KB gz"
  artifacts:
    - path: "src/store/snapshotStore.ts"
      provides: "releaseRawRows(id) inputs-only mutation (REPLACE-never-mutate)"
      contains: "releaseRawRows"
    - path: "src/hooks/useSnapshotUpload.ts"
      provides: "Latest-first non-blocking background drain + {done,total}"
    - path: "src/components/Chart.tsx"
      provides: "LineChart registered in echarts.use([...])"
      contains: "LineChart"
  key_links:
    - from: "src/hooks/useSnapshotUpload.ts"
      to: "store.addSnapshot / releaseRawRows"
      via: "per-result fire as each parse lands; release when loaded>4"
      pattern: "releaseRawRows\\("
    - from: "src/components/Chart.tsx"
      to: "echarts/charts LineChart"
      via: "echarts.use([...]) registration"
      pattern: "LineChart"
---

<objective>
Make the warm-up honest and the chart renderable: convert `useSnapshotUpload` from a blocking sequential parse to a latest-`capturedAt`-first non-blocking background drain (D-01/D-02) exposing `{done,total}`; add the inputs-only `releaseRawRows(id)` store mutation (DD-C); register `LineChart` in the single shipped `<Chart>` ECharts registry and re-gate the bundle size. No file overlap with plan 01 → runs in the same wave.

Purpose: TRD-01 (load 2–12, dashboard interactive in 5 s while trends warm), TRD-02 (LineChart must be registered or every trend/sparkline option renders blank — Pitfall 2), TRD-05 (release keeps everything in-memory only). Plan 03's UI consumes `{done,total}` and the registered LineChart.
Output: progressive non-blocking warm-up, the DD-C release mutation, and a LineChart-capable `<Chart>` with the bundle gate re-verified.
</objective>

<resolved_decisions>

- **DD-C policy (LOCKED):** threshold fixed at 4 (ROADMAP Critical-5 / CONTEXT both say "N > 4"); not a user control (deferred polish, not a TRD). The active/latest snapshot (D-01) and the newest 4 stay hydrated; when loaded count > 4, release **oldest-by-`capturedAt` first**, never the active. `releaseRawRows(id)` REPLACES the Map (no mutation), empties `vinfo/vhost/vdatastore/vpartition` to `[]`, sets `rawReleased: true`, and carries `releasedAggregate` (the point's already-computed aggregate) as frozen input metadata.
- **A3 interpretation (LOCKED, not unresolved):** carrying `releasedAggregate` on the released `Snapshot` is CONSISTENT with inputs-only — once the rows are gone the surviving aggregate is the only remaining *input fact* for that timeline point, not a cached derivation of live state. Adopted explicitly per RESEARCH Open Question 2 / A3; not guessed. (The engine consumer side is plan 01 Task 2; this plan owns the store mutation that produces it.)
- **A1 (execution-time gate, not a design unknown):** LineChart is small and the budget should hold; this plan MUST re-run `npm run check:bundle-size` after registration and add a remediation note if the ≤300 KB gz gate trips.
</resolved_decisions>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-in-session-trends/08-CONTEXT.md
@.planning/phases/08-in-session-trends/08-RESEARCH.md
@.planning/phases/08-in-session-trends/08-PATTERNS.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add releaseRawRows(id) inputs-only mutation + rawReleased/releasedAggregate on Snapshot</name>
  <files>src/store/snapshotStore.ts, src/types/snapshot.ts</files>
  <read_first>
    - src/store/snapshotStore.ts (PATTERNS analog — `setCapturedAt` at lines 139-146 is the EXACT REPLACE-never-mutate template; `removeSnapshot` 96-110 secondary analog; store header 13-28 inputs-only / REPLACE-never-mutate / no-persist invariant; SnapshotState interface around line 68)
    - src/types/snapshot.ts (the Snapshot interface — add the new optional fields here; vinfo/vhost/vdatastore/vpartition array fields, capturedAt, vCenterLabel, rvtoolsVersion, parsedAt, id)
    - .planning/phases/08-in-session-trends/08-RESEARCH.md (DD-C section — exact `next.set(id, { ...snap, vinfo: [], vhost: [], vdatastore: [], vpartition: [], rawReleased: true, releasedAggregate })` shape and the "release oldest-by-capturedAt first, never active/newest-4" policy)
  </read_first>
  <action>
    In src/types/snapshot.ts add two optional fields to the `Snapshot` interface: `rawReleased?: boolean` and `releasedAggregate?: <the carried TrendPoint-aggregate shape>` (use the `TrendHeadline`/per-cluster aggregate type defined in plan 01 Task 1; import as a type from `@/types/estate` if no cycle, otherwise define a minimal carried-aggregate type local to the snapshot type and have plan 01 reference it — confirm direction by reading the existing types→engines import direction). Document the field factually: it is frozen input metadata that survives row release (the A3 resolved reading).

    In src/store/snapshotStore.ts add `releaseRawRows(id: string, releasedAggregate: <carried type>)` to the `SnapshotState` interface (after `setCapturedAt`, ~line 68) and implement it copying the `setCapturedAt` idiom verbatim: `set((state) => { const snap = state.snapshots.get(id); if (!snap) return {}; const next = new Map(state.snapshots); next.set(id, { ...snap, vinfo: [], vhost: [], vdatastore: [], vpartition: [], rawReleased: true, releasedAggregate }); return { snapshots: next } })`. The Map is REPLACED, never mutated (store header lines 26-28; Zustand `Object.is` requires a fresh reference). No persist, no localStorage of rows (PAR-05). Do NOT add a cached-aggregate selector or any derived state to the store — the only new state is the released `Snapshot`'s own fields. Add a selector only if a component needs to read `rawReleased`.

    Do NOT add any new mutation for the capture-date edit — D-03 reuses the shipped `setCapturedAt` (line 139) verbatim; that wiring is plan 03.
  </action>
  <verify>
    <automated>npx vitest run src/store/ && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "releaseRawRows" src/store/snapshotStore.ts` is ≥ 2 (interface declaration + implementation)
    - `grep -n "rawReleased" src/types/snapshot.ts` and `grep -n "releasedAggregate" src/types/snapshot.ts` each return a match
    - The implementation contains `const next = new Map(state.snapshots)` (REPLACE-never-mutate; no `state.snapshots.set(`)
    - `grep -c "state\.snapshots\.set(" src/store/snapshotStore.ts` is 0 (no in-place Map mutation introduced)
    - `grep -c "persist\|localStorage\|sessionStorage\|indexedDB" src/store/snapshotStore.ts` shows no NEW persistence introduced by this task (count unchanged vs baseline)
    - `npx vitest run src/store/` exits 0
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0
  </acceptance_criteria>
  <done>releaseRawRows(id) added as a REPLACE-never-mutate inputs-only mutation copying the setCapturedAt idiom; Snapshot carries rawReleased/releasedAggregate as frozen input metadata; no store-cached aggregate, no persistence; typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: Latest-first non-blocking background drain + {done,total}; trigger releaseRawRows when loaded>4</name>
  <files>src/hooks/useSnapshotUpload.ts</files>
  <read_first>
    - src/hooks/useSnapshotUpload.ts (PATTERNS analog — current blocking sequential loop at lines 27-47; the reused per-file body at 33-42 `parseInWorker` + `addSnapshot({ id: crypto.randomUUID(), parsedAt: new Date(), ...snapshot })`; the privacy-safe `err.message`-only toast at 40-41 + doc 21-22; `UseSnapshotUploadResult` interface 7-10; the singleton-Worker doc 13-19)
    - src/engines/parser/captureDate.ts (the shared `ISO_DATE_RE` / `filenameIsoDate` export added in plan 01 Task 1 — reuse it for pre-parse latest-first ordering; do NOT duplicate the regex — DRY)
    - src/store/snapshotStore.ts (Task 1 output — `releaseRawRows` and `addSnapshot`; how to read the current loaded set to pick the oldest-by-capturedAt non-active id)
    - .planning/phases/08-in-session-trends/08-RESEARCH.md (Pattern 4 + Pitfall 6 — non-blocking background drain mechanics; the singleton-Worker constraint must be preserved)
  </read_first>
  <action>
    Convert the blocking `for (file of files) { await parse }` (lines 27-47) into: (1) order `files` latest-`capturedAt`-first using the shared filename-ISO probe from captureDate.ts (files without a filename date fall back to drop order — the user can re-order via the D-03 inline edit in plan 03, which recomputes through the memo); (2) parse + `addSnapshot` the FIRST (latest) file and let the caller's render proceed off that first `addSnapshot` (5 s criterion); (3) continue draining the remaining files latest→oldest in a background promise chain WITHOUT the caller awaiting the rest. Preserve the singleton-Worker constraint — still exactly one `parseInWorker` in flight at a time (sequential drain); the change is non-blocking to the React render, not parallel parsing (RESEARCH Pattern 4 / Pitfall 6). Reuse the per-file body verbatim (parseInWorker, addSnapshot with `crypto.randomUUID()`/`parsedAt: new Date()`, the `err.message`-only toast — never pass the error object, privacy doc lines 21-22).

    Extend `UseSnapshotUploadResult` (lines 7-10) with a factual `{ done: number; total: number }` (or equivalent) so plan 03's "trends preparing — N/M" indicator has a state source; `done` increments per successful `addSnapshot`, `total` = requested file count; reset appropriately on a new drop. No global blocking overlay (would fail success criterion 1).

    DD-C trigger: after each `addSnapshot`, if the loaded snapshot count > 4, compute the oldest-by-`capturedAt` non-active id (active = the latest-`capturedAt` per D-01; never release it or the newest 4) and call `releaseRawRows(id, <that snapshot's already-computed aggregate>)`. The carried aggregate must be the point's aggregate as currently derived (read it from the current `EstateView.trends` for that point, or recompute via the shipped aggregation for that single snapshot before emptying its rows) — the ordering constraint (RESEARCH Pitfall 4): the aggregate MUST be obtained BEFORE the rows are emptied. Keep the privacy/in-memory invariant — release just drops arrays for GC, no storage spill.
  </action>
  <verify>
    <automated>npx vitest run src/hooks/ && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ISO_DATE_RE\|filenameIsoDate" src/hooks/useSnapshotUpload.ts` is ≥ 1 (reuses the shared probe — no duplicated date regex; `grep -c "[0-9]{4}-[0-9]{2}-[0-9]{2}" src/hooks/useSnapshotUpload.ts` shows no new inline ISO regex literal)
    - `UseSnapshotUploadResult` exposes a done/total pair (`grep -n "done\|total" src/hooks/useSnapshotUpload.ts` returns matches in the result interface)
    - `grep -c "releaseRawRows(" src/hooks/useSnapshotUpload.ts` is ≥ 1, gated on loaded count > 4
    - The drain does not `await` the full batch before returning (the first `addSnapshot` is reachable without awaiting the rest — verify by reading: the background continuation is a non-awaited promise chain)
    - Exactly one `parseInWorker` call site, invoked sequentially (singleton Worker preserved — `grep -c "parseInWorker(" src/hooks/useSnapshotUpload.ts` unchanged from baseline count)
    - The error toast still passes only `err.message` (`grep -n "\.message" src/hooks/useSnapshotUpload.ts` present; no `err.cause`/`err.stack`/bare `err` passed to the toast)
    - `npx vitest run src/hooks/` exits 0; `npx tsc --noEmit -p tsconfig.app.json` exits 0
  </acceptance_criteria>
  <done>Latest-first non-blocking background drain; dashboard renders off the first addSnapshot; {done,total} exposed; releaseRawRows fires for oldest-non-active when loaded>4 with the aggregate captured before rows are emptied; singleton Worker + privacy-safe toast preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Register LineChart in the shipped Chart.tsx ECharts registry + re-gate bundle size</name>
  <files>src/components/Chart.tsx</files>
  <read_first>
    - src/components/Chart.tsx (PATTERNS analog — the per-chart `import { BarChart, GaugeChart, HeatmapChart, PieChart } from 'echarts/charts'` at line 1; the tree-shaken `echarts.use([...])` registry at lines 29-41 with the `HeatmapChart, // P7 EOS` comment style; the single `option` prop API 48-65 unchanged; the bundle-size gate doc 24-27)
    - .planning/phases/08-in-session-trends/08-RESEARCH.md (Pitfall 2 / Assumption A1 — LineChart not registered ⇒ blank chart; ≤300 KB gz gate must be re-verified, not assumed)
    - ./CLAUDE.md (Commands — `npm run check:bundle-size` fails if echarts chunk > 300 KB gz)
  </read_first>
  <action>
    One-line registration addition (RESEARCH Pitfall 2 / A1): add `LineChart` to the `echarts/charts` import on line 1 (`import { BarChart, GaugeChart, HeatmapChart, LineChart, PieChart } from 'echarts/charts'`) and include `LineChart,` inside `echarts.use([...])` (lines 29-41), mirroring the existing `HeatmapChart, // P7 EOS` comment style with a factual `// P8 trend line + sparkline (SVG — VIZ-01)` note. Do NOT change the single `option` prop API (lines 48-65) — the sparkline is just a different `option`, no new prop, no renderer change (SVG stays mandated). Then run `npm run build` (so the echarts chunk is produced) and `npm run check:bundle-size`. If the ≤300 KB gz gate passes, done. If it trips, record the exact over-budget number in the SUMMARY and apply the minimal tree-shake remediation (e.g. confirm no `import * as echarts from 'echarts'` crept in; ensure only the per-feature imports are used) — do not loosen the gate.
  </action>
  <verify>
    <automated>npm run build && npm run check:bundle-size</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "LineChart" src/components/Chart.tsx` is ≥ 2 (import + `echarts.use` registration)
    - `grep -c "import \* as echarts from 'echarts'" src/components/Chart.tsx` is 0 (tree-shaken path only — `echarts/core` + per-feature imports)
    - The single `option` prop API is unchanged (`grep -n "option" src/components/Chart.tsx` shows the same prop signature; no new chart prop added)
    - `npm run build` exits 0
    - `npm run check:bundle-size` exits 0 (echarts chunk ≤ 300 KB gz — RESEARCH A1 verified, not assumed)
    - `npx @biomejs/biome check src/components/Chart.tsx` exits 0
  </acceptance_criteria>
  <done>LineChart registered in the tree-shaken echarts.use registry; single option-prop API unchanged; build green; the ≤300 KB gz bundle gate re-run and passing (or the trip number recorded with a tree-shake-only remediation).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User file drop → upload hook → singleton Worker | Untrusted workbook bytes; parsed/validated at the shipped Phase-1 Zod boundary inside the Worker. This plan changes the *ordering/non-blocking* of the existing drain, not the parse or validation. |
| Upload hook → store | In-memory only; `addSnapshot`/`releaseRawRows` REPLACE the Map; no persistence (privacy invariant; the P1 guard throws on any network). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Information disclosure | error toast in the drain | mitigate | Reuse the shipped privacy-safe path: pass ONLY `err.message` to the toast, never `err.cause`/`err.stack`/the error object (would leak VM/host names). Verified by acceptance criterion. |
| T-08-05 | Information disclosure | releaseRawRows / no persistence | mitigate | Release empties row arrays in-memory for GC; no spill to localStorage/sessionStorage/IndexedDB (TRD-05; store header invariant). releasedAggregate carries only derived counts, no raw rows. |
| T-08-06 | Denial of service | non-blocking background drain | accept | Singleton Worker preserved — exactly one parse in flight; bounded by the 2–12 cap and the shipped per-workbook size budget. Non-blocking change reduces (not increases) main-thread contention. |
| T-08-07 | Tampering | LineChart registration | accept | Pure additive tree-shaken import of a first-party ECharts module; no external resource, no network. Bundle gate enforces no full-bundle regression. |

No HIGH-severity threat: client-side only, no auth, no network, no new external input, no storage. ASVS L1 — pass.
</threat_model>

<verification>
- `npx vitest run src/store/ src/hooks/` exits 0
- `npx tsc --noEmit -p tsconfig.app.json` exits 0
- `npm run build && npm run check:bundle-size` exits 0
- `npx @biomejs/biome check src/store/snapshotStore.ts src/types/snapshot.ts src/hooks/useSnapshotUpload.ts src/components/Chart.tsx` exits 0
- No new persistence introduced (privacy/TRD-05 invariant intact)
</verification>

<success_criteria>

- releaseRawRows(id) is an inputs-only REPLACE-never-mutate mutation; Snapshot carries rawReleased/releasedAggregate; no store-cached aggregate; no persistence
- useSnapshotUpload drains latest-first, non-blocking, exposes {done,total}, and releases the oldest non-active snapshot when loaded>4 with the aggregate captured before rows are emptied; singleton Worker + privacy-safe toast preserved
- LineChart registered; single option-prop API unchanged; bundle ≤300 KB gz re-verified
</success_criteria>

<output>
After completion, create `.planning/phases/08-in-session-trends/08-02-SUMMARY.md`
</output>
