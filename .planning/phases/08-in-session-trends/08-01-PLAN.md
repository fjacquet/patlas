---
phase: 08-in-session-trends
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/estate.ts
  - src/engines/trends/buildTrendSeries.ts
  - src/engines/trends/captureDateOrdinal.ts
  - src/engines/trends/index.ts
  - src/engines/trends/buildTrendSeries.test.ts
  - src/engines/trends/captureDateOrdinal.test.ts
  - src/engines/aggregation/estateView.ts
  - src/hooks/useEstateView.ts
  - src/engines/parser/captureDate.ts
autonomous: true
requirements: [TRD-01, TRD-02, TRD-04, TRD-05]
must_haves:
  truths:
    - "D-00: the pure trends engine composes inside the single buildEstateView pass — no new useMemo / no second memo site (grep-gated), inputs-only store untouched, calc-from-real-data only, branded units, em-dash sentinel; all P4/P7 invariants hold"
    - "D-04: capture-date resolution reuses the shipped inferCaptureDate chain (explicit -> filename ISO -> vMetaData -> mtime) plus the new deterministic ordinal fallback; no new sheet probe added"
    - "D-05: tied/undeterminable capture dates get a deterministic ordinal position and orderInferred=true; nothing dropped, no fake Date fabricated"
    - "Loading 2+ snapshots produces a temporal series of per-snapshot headline aggregates carried on EstateView.trends"
    - "Same-capture-date multi-vCenter files are spatially merged into one timeline point so counts reconcile with the dashboard"
    - "Consecutive-snapshot count-deltas (vmCount, poweredOnVms, hostCount, clusterCount, vcpuAllocated, vramAllocatedMib, totalStorageMib) are computed factually"
    - "Tied/undeterminable capture dates get a deterministic ordinal position and orderInferred=true; nothing is dropped, no fake Date fabricated"
    - "A released snapshot's trend point survives with identical numbers via carried frozen aggregate metadata"
  artifacts:
    - path: "src/engines/trends/buildTrendSeries.ts"
      provides: "Pure per-snapshot temporal aggregation + consecutive deltas"
      exports: ["buildTrendSeries"]
    - path: "src/engines/trends/captureDateOrdinal.ts"
      provides: "Deterministic D-05 ordinal fallback (no clock/IO/crypto)"
      exports: ["captureDateOrdinal"]
    - path: "src/engines/trends/index.ts"
      provides: "Trends engine barrel"
    - path: "src/types/estate.ts"
      provides: "TrendSeries / TrendPoint / TrendDelta types; EstateView.trends widened"
      contains: "interface TrendSeries"
  key_links:
    - from: "src/engines/aggregation/estateView.ts"
      to: "buildTrendSeries"
      via: "single-pass pure sub-call before return"
      pattern: "buildTrendSeries\\("
    - from: "src/hooks/useEstateView.ts"
      to: "buildEstateView"
      via: "selected: Snapshot[] threaded as 2nd arg, inside the existing single memo"
      pattern: "buildEstateView\\("
---

<objective>
Build the pure trends engine and compose it into the single sanctioned aggregation pass: per-snapshot temporal aggregation (DD-A A2 — point = one capture date, same-date multi-vCenter files merged spatially first), consecutive-pair count-deltas (DD-B B1), the D-05 deterministic ordinal fallback, and the DD-C released-aggregate carry. Replace the deprecated `TimelinePoint` placeholder with `TrendSeries`/`TrendPoint`/`TrendDelta`. Thread the pre-merge `selected: Snapshot[]` through `buildEstateView` without adding a second memo or a store cache.

Purpose: This is the data foundation for TRD-01/02/04/05. Every UI element in plan 03 consumes `EstateView.trends`; the store/warm-up in plan 02 fills it progressively. Resolving DD-A/DD-B/DD-C correctly here makes every downstream task mechanical.
Output: A Vitest-gated (≥75%) pure `src/engines/trends/` module, widened estate types, and `EstateView.trends` populated through the existing single memo.
</objective>

<resolved_decisions>

- **DD-A = A2 (per-`capturedAt`-date, same-date files spatially merged first).** Rationale: only option where a timeline point's VM count equals the dashboard count for that month (counts-must-reconcile hard constraint), and it reuses the shipped `mergeSnapshotsToEstate` so Phase-4 semantics compose with the temporal axis. Each point carries `metadata: { vCenterLabel, rvtoolsVersion }[]` (an array — a merged point legitimately spans multiple vCenters; criterion 6).
- **DD-B = B1 (consecutive-pair count-deltas on existing aggregate fields).** Rationale: every field already exists on the shipped aggregates; pure subtraction; KISS/DRY; matches the ROADMAP "between consecutive snapshots" wording verbatim. Identity-resolved churn (B2) is REJECTED — it conflicts with DD-C (a released snapshot has no rows to diff) and is the explicitly-deferred v2 DIF-01.
- **DD-C = release oldest-by-`capturedAt` first when loaded count > 4; active/latest never released; carried frozen aggregate.** This plan owns the engine side: `buildTrendSeries` MUST use a released snapshot's carried `releasedAggregate` instead of re-aggregating absent rows (Pitfall 4). The store mutation itself is plan 02.
- **A3 interpretation (LOCKED, not unresolved):** carrying a released point's surviving aggregate as a frozen `releasedAggregate` field on the `Snapshot` is CONSISTENT with the inputs-only invariant. Rationale: once the raw rows are released, the surviving aggregate is no longer a *cached derivation of live state* — it is the only remaining *input fact* for that timeline point (the rows that would re-derive it no longer exist). This is the resolved reading per RESEARCH Open Question 2 / Assumption A3; it is adopted explicitly here, not guessed.
</resolved_decisions>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-in-session-trends/08-CONTEXT.md
@.planning/phases/08-in-session-trends/08-RESEARCH.md
@.planning/phases/08-in-session-trends/08-PATTERNS.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace TimelinePoint stub with TrendSeries/TrendPoint/TrendDelta + D-05 ordinal helper</name>
  <files>src/types/estate.ts, src/engines/trends/captureDateOrdinal.ts, src/engines/trends/captureDateOrdinal.test.ts, src/engines/parser/captureDate.ts, src/engines/trends/index.ts</files>
  <read_first>
    - src/types/estate.ts (lines 325-440 — the deprecated `TimelinePoint` placeholder at 330-338 to REPLACE, `EstateView.trends` typed `TimelinePoint[] | null` at line 407, and the `EosProjection`/`EosRow` typed-projection template at 349-385; the safe doc-comment phrasing at lines 427/437)
    - src/engines/parser/captureDate.ts (PATTERNS analog — the pure no-clock/no-IO/no-crypto header at lines 4-8, `ISO_DATE_RE` at line 11, `inferCaptureDate` at 78-113)
    - src/engines/aggregation/index.ts (barrel shape template for src/engines/trends/index.ts)
    - src/engines/aggregation/estateView.test.ts (lines 1-17 imports + fixed TEST_TODAY, 19-109 host/vm/snapshot partial-override factories — the Vitest scaffold to mirror)
    - src/types/snapshot.ts (Snapshot.parsedAt, id, capturedAt, vCenterLabel, rvtoolsVersion fields the ordinal/metadata logic reads)
  </read_first>
  <behavior>
    - Test: captureDateOrdinal returns a stable load order sorted by ascending parsedAt, tiebroken by id (string compare), for snapshots given out of order
    - Test: when all snapshots have distinct real capture dates, no point is assigned an ordinal and orderInferred is false
    - Test: when two snapshots resolve to the same calendar day AND are treated as distinct points (not a same-date spatial merge), each gets a deterministic ordinal index from the stable load order
    - Test: when a snapshot's capture date cannot be inferred (mtime fallback absent/epoch), it gets an ordinal and orderInferred is true
    - Test: function is pure — calling twice with the same input yields deeply-equal output; no Date.now / no mutation of inputs
  </behavior>
  <action>
    In src/types/estate.ts, delete the `TimelinePoint` interface (the `{ capturedAt: Date; vmCount: number }` stub) and add three exported interfaces: `TrendPoint` (`date: Date`, `ordinal: number | null`, `metadata: { vCenterLabel: string; rvtoolsVersion: string }[]`, `headline: TrendHeadline`, `byCluster: Map<string, TrendHeadline>`), `TrendDelta` (`from: Date`, `to: Date`, plus one numeric field per headline metric in branded units), and `TrendSeries` (`points: TrendPoint[]`, `deltas: TrendDelta[]`, `orderInferred: boolean`). Add a `TrendHeadline` type that is a `Pick` of the shipped `GlobalSummary`/`OperationalInsights` fields used for trends (vmCount, poweredOnVms, hostCount, clusterCount, vcpuAllocated, vramAllocatedMib, totalStorageMib — confirm exact field names by reading the shipped aggregate types). Change `EstateView.trends` from `TimelinePoint[] | null` to `TrendSeries | null`. Doc-comment the new fields in the EOS field-comment style ("Produced inside the single buildEstateView pass") — do NOT write the literal React-memo-hook token in any comment (grep-gate/security-hook matches comments — CLAUDE.md gotcha; phrase as estate.ts:437 does).

    In src/engines/parser/captureDate.ts, export `ISO_DATE_RE` (and/or a small pure `filenameIsoDate(filename: string): Date | null` helper that uses it) as a named export so the warm-up ordering in plan 02 reuses it instead of duplicating the regex (DRY is binding). Do not change the existing `inferCaptureDate` behavior.

    Create src/engines/trends/captureDateOrdinal.ts: a pure deterministic `captureDateOrdinal(selected: Snapshot[])` helper with the no-clock/no-IO/no-crypto header style copied from captureDate.ts. It establishes the stable load order (ascending `parsedAt`, tiebreak by `id`), and returns per-snapshot ordinal assignment + an `orderInferred` flag following RESEARCH Pattern 5 (real dates kept where known; ordinal only for genuine ties/undeterminable; never fabricate a Date). Create src/engines/trends/index.ts as the barrel exporting `buildTrendSeries` and `captureDateOrdinal` (mirror src/engines/aggregation/index.ts shape; doc header without the memo-hook token).

    Create src/engines/trends/captureDateOrdinal.test.ts mirroring the estateView.test.ts scaffold (vitest imports, fixed clock constant, a `snapshots(): Snapshot[]` factory varying capturedAt/parsedAt built from the host/vm/snapshot partial-override factory idiom). Cover the five behaviors above.
  </action>
  <verify>
    <automated>npx vitest run src/engines/trends/captureDateOrdinal.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "interface TrendSeries" src/types/estate.ts` returns a match; `grep -c "interface TimelinePoint" src/types/estate.ts` is 0
    - `grep -n "trends: TrendSeries | null" src/types/estate.ts` (or equivalent typed field) returns a match
    - `src/engines/trends/index.ts` exports `buildTrendSeries` and `captureDateOrdinal`
    - `src/engines/parser/captureDate.ts` has a named export reused for filename-ISO date (e.g. `export const ISO_DATE_RE` or `export function filenameIsoDate`)
    - `npx vitest run src/engines/trends/captureDateOrdinal.test.ts` exits 0
    - No new doc-comment in any modified/created file contains the literal React-memo-hook token (`grep -rn "useMemo" src/engines/trends/ src/types/estate.ts` shows no NEW occurrence introduced by this task)
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0
  </acceptance_criteria>
  <done>TimelinePoint stub replaced by TrendSeries/TrendPoint/TrendDelta; EstateView.trends typed TrendSeries|null; captureDateOrdinal pure + tested; ISO date probe shared from captureDate.ts; barrel exports both engine functions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: buildTrendSeries — DD-A A2 per-date aggregation, DD-B B1 deltas, DD-C released-aggregate carry</name>
  <files>src/engines/trends/buildTrendSeries.ts, src/engines/trends/buildTrendSeries.test.ts</files>
  <read_first>
    - src/engines/aggregation/estateView.ts (PATTERNS analog — lines 94-107 the exact `aggregateClusters({...})` then `aggregateGlobals(...)` calls buildEstateView makes for the active estate; lines 1-22 engine purity header + `@/engines/...` import style; the P7 EOS sub-projection composition at 258-289 — the verbatim shape to mirror)
    - src/engines/snapshotMerge/index.ts (the `mergeSnapshotsToEstate` export reused for same-date groups)
    - src/engines/aggregation/index.ts (barrel — import aggregateClusters/aggregateGlobals via this, not private files)
    - src/engines/units/converters.ts (mibToGib line 19, gibToTib 20, mibToTib 21 — branded delta math; never `* 1.048576`)
    - src/engines/trends/captureDateOrdinal.ts (Task 1 output — the ordinal/orderInferred helper this composes with)
    - src/engines/aggregation/estateView.test.ts (Vitest scaffold + factories to mirror; the merge-routing helper at lines 16-17)
    - src/types/estate.ts (Task 1 output — TrendSeries/TrendPoint/TrendDelta/TrendHeadline shapes; AccountingMode)
    - src/types/snapshot.ts (Snapshot shape incl. the `rawReleased`/`releasedAggregate` fields plan 02 adds — import as type; this plan defines how buildTrendSeries consumes them)
  </read_first>
  <behavior>
    - Test: 2 snapshots on distinct dates produce 2 TrendPoints sorted ascending by date; each headline equals the shipped aggregateGlobals/aggregateClusters result for that snapshot's merged estate
    - Test: 2 same-`capturedAt`-day multi-vCenter snapshots produce ONE TrendPoint whose headline equals mergeSnapshotsToEstate([both]) aggregated — count reconciles with the dashboard active-estate path for that date
    - Test: a point's metadata is an array containing one {vCenterLabel, rvtoolsVersion} per contributing snapshot
    - Test: deltas has length points.length-1; each TrendDelta is the signed difference of consecutive headlines (vmCount, poweredOnVms, hostCount, clusterCount, vcpuAllocated, vramAllocatedMib→GiB, totalStorageMib→GiB/TiB) in branded units
    - Test (MANDATORY — Pitfall 4 / DD-C): given an oldest snapshot with rawReleased=true and a populated releasedAggregate, its TrendPoint headline equals the carried releasedAggregate (NOT recomputed from empty rows) and equals the pre-release value; the rest of the series and the dashboard count are unchanged
    - Test: selected.length < 2 → buildTrendSeries returns null (degenerate, like Phase 2 trends:null)
    - Test: function is pure — no `new Date()` inside (any "now" is the injected today/opts); deeply-equal output on repeat calls
  </behavior>
  <action>
    Create src/engines/trends/buildTrendSeries.ts: a pure `buildTrendSeries(selected: Snapshot[], mode: AccountingMode, opts: { stretchedClusters; allocRatios })` returning `TrendSeries | null`. Engine purity header copied from captureDate.ts/estateView.ts style (no React/DOM/Zustand/Zod; no clock — any "now" comes from opts/caller). Algorithm (RESEARCH DD-A "Data flow (exact)" + Pattern 1):
    1. resolve each snapshot's `capturedAt` (already on Snapshot, explicit edits applied via the store before this runs);
    2. group by calendar day (`groupByDay`);
    3. for each group: if any snapshot in the group has `rawReleased === true`, use its carried `releasedAggregate` for that point's headline/byCluster (DD-C / Pitfall 4 — do NOT re-aggregate absent rows); otherwise `mergeSnapshotsToEstate(group)` then call `aggregateClusters({ vinfo, vhost, mode, stretchedClusters, datastoreCountByCluster, allocRatios })` and `aggregateGlobals(...)` exactly as estateView.ts lines 94-107 does (DRY — same engine, counts reconcile by construction);
    4. emit a `TrendPoint` per group with `metadata` = array of {vCenterLabel, rvtoolsVersion} for every contributing snapshot;
    5. apply `captureDateOrdinal` (Task 1) for D-05 ties/undeterminable → set `ordinal` per point + series-level `orderInferred`;
    6. sort points (real dates ascending; ordinal-keyed points by ordinal per D-05);
    7. compute `deltas` as consecutive-pair signed differences of headline fields, memory/storage via `mibToGib`/`gibToTib`/`mibToTib` from converters.ts (NEVER `* 1.048576`).
    Use the em-dash/null-safe path for any headline field that is null on either side of a pair (the delta engine emits the raw numeric/null; presentation of `—` is plan 03). Doc-comment without the memo-hook token.

    Create src/engines/trends/buildTrendSeries.test.ts mirroring estateView.test.ts (vitest, fixed clock, multi-snapshot `snapshots()` factory). Cover all seven behaviors; the DD-C release-correctness case is mandatory and explicit.
  </action>
  <verify>
    <automated>npx vitest run src/engines/trends/ --coverage</automated>
  </verify>
  <acceptance_criteria>
    - `src/engines/trends/buildTrendSeries.ts` exports `buildTrendSeries`
    - `npx vitest run src/engines/trends/` exits 0
    - Coverage report shows `src/engines/trends/` ≥ 75% (CLAUDE.md engines gate)
    - The test file contains an `it(...)` whose title references release/DD-C and asserts the released point's headline equals its pre-release value
    - `grep -c "1\.048576" src/engines/trends/buildTrendSeries.ts` is 0 (branded units only — use `grep -v '^\s*//' ` is not needed; the literal must not appear at all)
    - `grep -rn "new Date(" src/engines/trends/buildTrendSeries.ts` returns no occurrence (pure — clock injected)
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0
  </acceptance_criteria>
  <done>buildTrendSeries reuses shipped aggregateClusters/aggregateGlobals/mergeSnapshotsToEstate per DD-A A2; consecutive-pair branded deltas per DD-B B1; DD-C released-aggregate carry covered by a dedicated passing test; engines/trends/ ≥75% coverage; pure.</done>
</task>

<task type="auto">
  <name>Task 3: Compose buildTrendSeries into the single buildEstateView pass; thread selected through the one memo</name>
  <files>src/engines/aggregation/estateView.ts, src/hooks/useEstateView.ts</files>
  <read_first>
    - src/engines/aggregation/estateView.ts (PATTERNS analog — current signature at lines 55-77 `(merged: MergedEstate, mode, today, opts)`; the P5/P6/P7 single-pass composition at 136/224/258-289; `trends: null` literal at line 279; frozen EMPTY_VIEW at 338-356 incl. `trends: null` at line 347)
    - src/hooks/useEstateView.ts (PATTERNS analog — the single sanctioned memo at line 48, `const selected = [...snapshots.values()].filter(...)` at line 49, the `buildEstateView(mergeSnapshotsToEstate(selected), mode, today, {...})` call at 55-59, dep array at 60, the single clock site `const today = new Date()` at 54, the "ONLY orchestrates + memoizes" doc at 33-34)
    - src/engines/trends/index.ts (Task 1/2 output — `buildTrendSeries` import source)
  </read_first>
  <action>
    Widen `buildEstateView`'s signature to also accept the pre-merge `selected: Snapshot[]` (recommended RESEARCH Pattern 1: `buildEstateView(merged, selected, mode, today, opts)`) so the entire composition stays in one pure function and the hook stays a thin orchestrator. Immediately before the existing `return` (after the P7 `eos` block ~line 268), add the trends sub-call mirroring the EOS composition exactly: `const trends = selected.length >= 2 ? buildTrendSeries(selected, mode, { stretchedClusters, allocRatios }) : null` (import `buildTrendSeries` from `@/engines/trends`). Change the `trends: null,` literal (~line 279) to `trends,`. Keep `trends: null` in the frozen EMPTY_VIEW (line 347). Add the doc-comment in the shipped EOS field-comment style — no React-memo-hook token in any comment.

    In src/hooks/useEstateView.ts, pass the already-computed `selected` as the new 2nd arg to `buildEstateView` inside the EXISTING single `useMemo` (line 48 memo, call at 55-59). Do NOT add a new memo, a new selector, or a new memoized hook — the trends projection composes inside the one pure pass, consumed as a plain field by components. The dependency array (line 60) is unchanged — `snapshots`/`selectedIds` already drive recompute when warm-up `addSnapshot`s land (so progressive fill is free). Confirm the single-memo grep gate count is unchanged.
  </action>
  <verify>
    <automated>npx vitest run src/engines/aggregation/ src/engines/trends/ && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "buildTrendSeries(" src/engines/aggregation/estateView.ts` is ≥ 1
    - `grep -c "trends: null," src/engines/aggregation/estateView.ts` is 1 (only the EMPTY_VIEW frozen literal remains; the live return uses `trends,`)
    - The grep-gated single-memo hook token count across non-test `src/` is UNCHANGED from before this plan (no new memo site introduced — run the same grep the CLAUDE.md invariant uses; it must not increase)
    - `npx vitest run src/engines/aggregation/` exits 0 (existing estateView tests still pass with the widened signature)
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0
    - `npx @biomejs/biome check src/engines/aggregation/estateView.ts src/hooks/useEstateView.ts` exits 0
  </acceptance_criteria>
  <done>EstateView.trends populated by buildTrendSeries inside the single buildEstateView pass; `selected` threaded through the one memo with no new memo/selector/store cache; existing aggregation tests green; typecheck + lint clean.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RVTools workbook → parser (Zod) | Untrusted file content; already validated at the shipped Phase-1 Zod parser boundary BEFORE it reaches any engine. This plan adds no new input ingestion. |
| Parsed rows → pure trends engine | In-memory only; pure functions, no network (the P1 guard throws on any non-same-origin call), no storage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | buildTrendSeries consuming parsed rows | accept | Rows already schema-validated at the shipped Zod parser boundary (Phase 1); trends engine performs only pure arithmetic/grouping on validated data. No new parsing surface introduced. |
| T-08-02 | Information disclosure | trends engine + delta math | mitigate | Pure in-memory computation; no logging of dataset values, no network (privacy guard throws by design), no storage write. Engine returns derived numbers only. |
| T-08-03 | Denial of service | N-snapshot per-date aggregation loop | accept | Bounded by the 2–12 snapshot product cap (TRD-01) and the shipped per-workbook size budget; DD-C release caps retained raw rows. No unbounded recursion/allocation. |

No HIGH-severity threat: this plan is 100% client-side pure computation, no auth, no network, no storage, no new external input. ASVS L1 — pass.
</threat_model>

<verification>
- `npx vitest run src/engines/trends/ src/engines/aggregation/` exits 0
- Coverage on `src/engines/trends/` ≥ 75%
- `npx tsc --noEmit -p tsconfig.app.json` exits 0
- `npx @biomejs/biome check src/engines/trends/ src/types/estate.ts src/engines/aggregation/estateView.ts src/hooks/useEstateView.ts` exits 0
- Single-memo grep-gate count unchanged (no second memo site)
</verification>

<success_criteria>

- TrendSeries/TrendPoint/TrendDelta replace the TimelinePoint stub; EstateView.trends typed TrendSeries|null and populated by buildTrendSeries
- DD-A A2, DD-B B1, DD-C carry, and the D-05 ordinal fallback are implemented and tested (incl. the mandatory release-correctness case)
- buildTrendSeries composes inside the single buildEstateView pass; no second memo, no store cache (grep-gated)
- engines/trends/ Vitest-gated ≥75%; pure (no clock/IO/network)
</success_criteria>

<output>
After completion, create `.planning/phases/08-in-session-trends/08-01-SUMMARY.md`
</output>
