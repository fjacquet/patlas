---
phase: 03-inventory-navigation
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/inventory/columns/vmColumns.ts
  - src/components/inventory/columns/esxColumns.ts
  - src/components/inventory/columns/datastoreColumns.ts
  - src/components/inventory/VmTable.tsx
  - src/components/inventory/EsxTable.tsx
  - src/components/inventory/DatastoreTable.tsx
  - src/components/inventory/columns/columns.test.ts
  - src/components/inventory/DataTable.csv.test.tsx
autonomous: true
requirements: [INV-02, INV-03, INV-04, INV-05, INV-06]
must_haves:
  truths:
    - "VM table is bound to projected EstateView.vmRows (never OsBreakdown fields)"
    - "ESX table is bound to EstateView.hosts verbatim (no projection, no re-aggregation)"
    - "Datastore table is bound to EstateView.datastores verbatim — NAA dedupe preserved, no cluster column, no re-derive"
    - "Each table has ~8 default-visible columns; identity columns set enableHiding:false"
    - "CSV export of a filtered + column-hidden table contains exactly the visible filtered rows with raw values and preserved newlines"
  artifacts:
    - path: "src/components/inventory/columns/vmColumns.ts"
      provides: "ColumnDef<VmDisplayRow>[] (INV-02)"
      contains: "ColumnDef"
    - path: "src/components/inventory/columns/esxColumns.ts"
      provides: "ColumnDef<EsxAggregate>[] (INV-03)"
      contains: "ColumnDef"
    - path: "src/components/inventory/columns/datastoreColumns.ts"
      provides: "ColumnDef<DatastoreAggregate>[] (INV-04), no cluster column"
      contains: "ColumnDef"
    - path: "src/components/inventory/VmTable.tsx"
      provides: "Thin wrapper: view.vmRows + vmColumns → DataTable"
      exports: ["VmTable"]
    - path: "src/components/inventory/EsxTable.tsx"
      provides: "Thin wrapper: view.hosts + esxColumns → DataTable"
      exports: ["EsxTable"]
    - path: "src/components/inventory/DatastoreTable.tsx"
      provides: "Thin wrapper: view.datastores + datastoreColumns → DataTable"
      exports: ["DatastoreTable"]
  key_links:
    - from: "src/components/inventory/VmTable.tsx"
      to: "EstateView.vmRows"
      via: "props from the single useEstateView caller (no per-table hook)"
      pattern: "vmRows"
    - from: "src/components/inventory/DatastoreTable.tsx"
      to: "EstateView.datastores"
      via: "verbatim consumption — NAA dedupe not re-derived"
      pattern: "datastores"
---

<objective>
Wire the three object tables to the existing `EstateView` outputs through the generic `DataTable` from 03-01: VM table from the new `vmRows` projection, ESX table from `EstateView.hosts` verbatim, datastore table from `EstateView.datastores` verbatim (NAA dedupe already done in Phase 2 — never re-derived). Three ColumnDef modules plus three thin prop-consuming wrappers.

Purpose: INV-02/03/04 are pure consumption of already-correct engine output. The hand-roll trap here is re-deriving the datastore NAA dedupe (Moderate-11) — this plan forbids it structurally.
Output: Three ColumnDef configs, three wrapper components, and the CSV-of-filtered×visible-view contract verified end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-inventory-navigation/03-RESEARCH.md
@.planning/phases/03-inventory-navigation/03-PATTERNS.md
@.planning/phases/03-inventory-navigation/03-UI-SPEC.md
@.planning/phases/03-inventory-navigation/03-01-SUMMARY.md
@src/types/estate.ts
@src/utils/format.ts
@src/components/dashboard/GlobalDashboard.tsx

<interfaces>
<!-- Extracted from the codebase. Bind columns to EXACTLY these fields. -->

VmDisplayRow (src/types/estate.ts — added by 03-01): `vmName: string`, `cluster: string`, `host: string`, `vcpu: Cores`, `vramMib: MiB`, `os: string`, `poweredOn: boolean`, `provisionedMib: MiB`. The VM ColumnDef MUST reference ONLY these fields — never `OsBreakdown` (`{windows,linux,other}`); referencing `OsBreakdown` fields is the 03-RESEARCH Pitfall-1 warning sign.

EsxAggregate (src/types/estate.ts:237-254) — consume verbatim: `hostName: string`, `cluster: string`, `sockets: Sockets`, `cores: Cores`, `speedMhz: MHz`, `physicalGhz: GHz`, `memoryMib: MiB`, `vmCount: number`, `vcpuAllocated: Cores`, `vramAllocatedMib: MiB`, `cpuRatio: number`, `ramRatio: number`, `meanCpuReadinessPercent: number | null`, `maxCpuReadinessPercent: number | null`, `vmsAboveReadinessWarning: number`, `readinessAvailable: boolean`.

DatastoreAggregate (src/types/estate.ts:211-229) — consume verbatim, NO cluster column (03-RESEARCH Open Q3; UI-SPEC §Three object tables "states no cluster column"): `key: string` (`naa ?? name` — the dedupe key), `name: string`, `type: string`, `capacityMib: MiB`, `freeMib: MiB`, `usedMib: MiB`, `usedRatio: number`, `provisionedMib: MiB`, `overProvisionRatio: number`, `sharedDuplicateCount: number`.

format.ts (src/utils/format.ts) — locale-aware DISPLAY formatters (GiB/TiB suffixes, em-dash sentinel on non-finite). Use ONLY for cell display; CSV uses raw `row.getValue` (03-RESEARCH Pitfall 4 — two paths).

GlobalDashboard.tsx (src/components/dashboard/GlobalDashboard.tsx:48-87) — the "single useEstateView caller, children get derived EstateView as plain props" template. The Vm/Esx/Datastore wrappers receive the relevant slice as a prop; they do NOT call useEstateView and contain NO useMemo (the InventoryView shell in 03-03 is the single caller).

DataTable (src/components/inventory/DataTable.tsx — from 03-01): generic `DataTable<T>({ data, columns, headerFor, objectKind })`. The wrappers compose it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Three ColumnDef modules (vm/esx/datastore)</name>
  <files>src/components/inventory/columns/vmColumns.ts, src/components/inventory/columns/esxColumns.ts, src/components/inventory/columns/datastoreColumns.ts</files>
  <action>
Create `src/components/inventory/columns/vmColumns.ts` exporting `vmColumns: ColumnDef<VmDisplayRow>[]` (import `ColumnDef` from `@tanstack/react-table`, `VmDisplayRow` from `@/types/estate`). One column per `VmDisplayRow` field; `accessorKey` references ONLY VmDisplayRow fields (03-RESEARCH Pitfall 1 warning sign — never `OsBreakdown`). Header text via i18n key `inventory.col.<field>` (the `headerFor` callback resolves it at render — column `header` carries the key/id, not literal text). Numeric cells (`vcpu`, `vramMib`, `provisionedMib`) format for DISPLAY via the inherited `src/utils/format.ts` formatters with the em-dash `—` sentinel on absent/non-finite (UI-SPEC §Copywriting; matches format.ts). The annotation/OS string cell renders via `oneLine()` with original text in `title` (Minor-2 — applied at the DataTable cell boundary established in 03-01; the ColumnDef just declares the accessor, the cell renderer in DataTable applies oneLine). `vmName` is the identity column → `enableHiding: false` (03-RESEARCH §Column visibility line 299). ~8 columns; all default-visible since VmDisplayRow is already the lean 8-field subset.

Create `src/components/inventory/columns/esxColumns.ts` exporting `esxColumns: ColumnDef<EsxAggregate>[]` — consume `EstateView.hosts` verbatim (INV-03, no projection). Default-visible set ~8 columns (hostName, cluster, sockets, cores, physicalGhz, memoryMib, vmCount, vcpuAllocated); the rest (speedMhz, vramAllocatedMib, cpuRatio, ramRatio, readiness fields) opt-in (`column` present but not in the default visibility map — 03-03 passes the default-visibility map; here just mark non-default columns so the wrapper can build it, e.g. via a `meta.defaultHidden` flag or an exported `esxDefaultVisible` id list — choose the exported-id-list approach, KISS). `hostName` identity → `enableHiding: false`. Readiness number columns branch on `readinessAvailable` for display (never render `0%` when unreported — same discipline as Phase-2 CpuReadyPanel) but the column itself stays plain; CSV uses the raw value.

Create `src/components/inventory/columns/datastoreColumns.ts` exporting `datastoreColumns: ColumnDef<DatastoreAggregate>[]` — consume `EstateView.datastores` verbatim. NO cluster column (03-RESEARCH Open Q3 / Anti-Pattern line 232; UI-SPEC). Do NOT re-derive or re-key — `key`/`name`/`type`/capacity fields/`sharedDuplicateCount` come straight off `DatastoreAggregate`. `key` (the NAA dedupe key) is the identity column → `enableHiding: false`. Capacity/free/used/provisioned format via `format.ts` GiB/TiB for display; CSV raw. Export a `datastoreDefaultVisible` id list (~8 default columns). All three modules: zero React, zero useMemo, pure config.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && ! grep -rn 'windows\|linux\|other\|OsBreakdown' src/components/inventory/columns/vmColumns.ts && ! grep -rni 'cluster' src/components/inventory/columns/datastoreColumns.ts</automated>
  </verify>
  <done>Three ColumnDef modules typecheck/lint clean; `vmColumns` references zero `OsBreakdown` fields; `datastoreColumns` has zero `cluster` reference; identity columns (`vmName`/`hostName`/`key`) carry `enableHiding: false`; default-visible id lists exported for esx/datastore.</done>
</task>

<task type="auto">
  <name>Task 2: Three thin table wrappers (Vm/Esx/Datastore)</name>
  <files>src/components/inventory/VmTable.tsx, src/components/inventory/EsxTable.tsx, src/components/inventory/DatastoreTable.tsx</files>
  <action>
Create `src/components/inventory/VmTable.tsx`, `EsxTable.tsx`, `DatastoreTable.tsx` following the `GlobalDashboard.tsx:48-87` "props-from-view, no per-component hook" pattern (03-PATTERNS Vm/Esx/DatastoreTable analog). Each is a thin wrapper that receives its data slice as a prop (NOT calling `useEstateView` — the 03-03 `InventoryView` shell is the single caller; same lift discipline as `GlobalDashboard`):
- `VmTable({ rows }: { rows: VmDisplayRow[] })` → `<DataTable data={rows} columns={vmColumns} headerFor={...} objectKind="vm" />`
- `EsxTable({ hosts }: { hosts: EsxAggregate[] })` → `<DataTable data={hosts} columns={esxColumns} headerFor={...} objectKind="esx" />`
- `DatastoreTable({ datastores }: { datastores: DatastoreAggregate[] })` → `<DataTable data={datastores} columns={datastoreColumns} headerFor={...} objectKind="datastore" />`
`headerFor` = `(id) => t(\`inventory.col.${id}\`)` via `useTranslation('inventory')` (namespace registered in 03-03; acceptable to reference here, exercised end-to-end in 03-03). Pass the default-visibility map built from the exported `esxDefaultVisible`/`datastoreDefaultVisible` id lists to DataTable (so opt-in columns start hidden; ColumnPicker's "Reset" restores this set). NO `useMemo`, NO `useEstateView` call, NO `@/engines` import in any wrapper (consume EstateView slices verbatim — 03-RESEARCH Anti-Pattern line 232; the datastore wrapper passes `EstateView.datastores` straight through, never re-deriving NAA dedupe).
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && ! grep -rn 'useMemo\|useEstateView\|@/engines\|from .*engines' src/components/inventory/VmTable.tsx src/components/inventory/EsxTable.tsx src/components/inventory/DatastoreTable.tsx</automated>
  </verify>
  <done>Three wrappers exist, typecheck/lint clean; none imports `@/engines`, calls `useEstateView`, or uses `useMemo`; each composes `DataTable` with its ColumnDef module and a slice prop; default-visibility map wired from the exported id lists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ColumnDef + CSV-of-filter×visible integration tests + single-useMemo & NAA-preserved gates</name>
  <files>src/components/inventory/columns/columns.test.ts, src/components/inventory/DataTable.csv.test.tsx</files>
  <behavior>
    columns.test.ts:
    - vmColumns: every accessorKey ∈ VmDisplayRow keys; `vmName` column has `enableHiding === false`; zero column references an OsBreakdown key
    - esxColumns / datastoreColumns: identity column `enableHiding === false`; datastoreColumns exposes no column whose id/accessor is `cluster`
    DataTable.csv.test.tsx (jsdom + @testing-library/react, mock anchor click + URL.createObjectURL):
    - Render DataTable with a small fixture incl. one row whose value contains a `\n`
    - Apply a global filter that excludes ≥1 row; hide ≥1 hideable column via ColumnPicker
    - Click "Export CSV"; assert the captured Blob text = ONLY the filtered rows × ONLY the visible columns, raw values (no thousands separator / no unit suffix), the embedded `\n` preserved & RFC-4180 quoted (INV-05 × INV-06)
    - Assert filename matches `vatlas-vm-\d{8}.csv`
    NAA-preserved assertion: drive `buildEstateView` on a fixture with a shared-LUN datastore (same `naa` across 2 clusters); assert `EstateView.datastores` has ONE entry for that key with `sharedDuplicateCount > 1`, and `datastoreColumns` surfaces it as a single row (no re-derivation widened it).
  </behavior>
  <action>
RED first. Write `src/components/inventory/columns/columns.test.ts` using the `estateView.test.ts` pure-fn vitest idiom + `src/test/arrays.ts` `first()` for `noUncheckedIndexedAccess` narrowing. Assert the behavior bullets above by introspecting the ColumnDef arrays (accessorKey/id/enableHiding) and by running `buildEstateView` on a shared-LUN fixture for the NAA-preserved assertion (consume `EstateView.datastores` verbatim — proves no re-derive widened the dedupe; 03-RESEARCH Anti-Pattern line 232 / ROADMAP Phase-3 #5).

Write `src/components/inventory/DataTable.csv.test.tsx` using the `AccountingModeToggle.test.tsx` component-test idiom (`render`/`screen`/`userEvent`, `beforeEach(() => i18n.changeLanguage('en'))`). Stub `URL.createObjectURL`/`revokeObjectURL` and capture the Blob; assert the CSV-of-current-view contract (filtered rows × visible columns, raw values, preserved+quoted newline, filename pattern) — INV-05 × INV-06 end-to-end. Add the single-useMemo grep assertion as a test or rely on the verify gate below (the gate is authoritative). GREEN: tests pass against the 03-01/03-02 components.
  </action>
  <verify>
    <automated>npm run test:run -- src/components/inventory/columns/columns.test.ts src/components/inventory/DataTable.csv.test.tsx && [ "$(grep -rl 'useMemo(' src/ --include='*.ts' --include='*.tsx' | grep -v test | grep -v -F 'SnapshotListSidebar' | grep -v -F 'useEstateView' | wc -l | tr -d ' ')" = "0" ]</automated>
  </verify>
  <done>Both test files green; CSV export proven to contain exactly filtered×visible rows with raw values + preserved newline + correct filename (INV-05/INV-06); shared-LUN NAA dedupe proven preserved (one row, `sharedDuplicateCount > 1`) with no re-derivation; single-useMemo gate still passes (zero real `useMemo(` outside `useEstateView` + pre-existing `SnapshotListSidebar`).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EstateView slices → table cells | Aggregated/projected rows rendered; VM names upstream-influenceable |
| Filtered view → CSV | Visible-column projection serialised to a client file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering | datastore re-derivation | mitigate | Datastore table consumes `EstateView.datastores` verbatim; test asserts shared-LUN stays one NAA-keyed row (no double-count reintroduced — Moderate-11) |
| T-03-07 | Info disclosure | CSV column projection | mitigate | Export uses `getVisibleLeafColumns` × `getFilteredRowModel` only; integration test asserts hidden columns + filtered-out rows are absent |
| T-03-08 | Tampering/XSS | VM-name cells | mitigate | Cells rendered as React text via the 03-01 DataTable boundary; ColumnDefs declare accessors only, no HTML sink |
| T-03-09 | Info disclosure (privacy) | per-table state | accept | Sort/filter/visibility ephemeral in DataTable `useState` (established + gated in 03-01); no new persistence surface here |
</threat_model>

<verification>
- `npm run typecheck` / `npm run lint` exit 0
- `npm run test:run -- src/components/inventory/columns/columns.test.ts src/components/inventory/DataTable.csv.test.tsx` green
- `vmColumns` references zero `OsBreakdown` fields; `datastoreColumns` has zero `cluster` reference (grep gate)
- No `@/engines` import, no `useEstateView`, no `useMemo` in any wrapper (grep gate)
- Single-useMemo grep gate still passes
- Shared-LUN NAA dedupe proven preserved verbatim (ROADMAP Phase-3 #5)
- CSV-of-filtered×visible-view proven end-to-end with preserved newline + filename pattern (ROADMAP Phase-3 #4)
</verification>

<success_criteria>

- A user sorting/filtering the VM table operates on the projected `vmRows` (ROADMAP Phase-3 #2/#3 data path)
- A user exporting the current filtered VM table gets exactly the visible filtered rows, raw values (ROADMAP Phase-3 #4)
- A user viewing the datastore table sees a shared LUN once — NAA dedupe consumed verbatim, never re-derived (ROADMAP Phase-3 #5)
- ESX/datastore tables consume `EstateView.hosts`/`datastores` with zero new engine/hook/memo
</success_criteria>

<output>
After completion, create `.planning/phases/03-inventory-navigation/03-02-SUMMARY.md`
</output>
