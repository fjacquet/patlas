---
phase: 03-inventory-navigation
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - src/components/inventory/InventoryTree.tsx
  - src/components/inventory/InventoryView.tsx
  - src/i18n/index.ts
  - src/i18n/locales/en/inventory.json
  - src/i18n/locales/fr/inventory.json
  - src/App.tsx
  - scripts/generate-inventory-10k.mjs
  - package.json
  - src/components/inventory/InventoryTree.test.tsx
  - src/__tests__/inventory-stress.test.tsx
  - src/__tests__/e2e-smoke.test.tsx
autonomous: true
requirements: [INV-01, INV-02, INV-03, INV-04, INV-05, INV-06]
must_haves:
  truths:
    - "A user toggles Dashboard↔Inventory in the App header and the main pane switches (Phase-1 sidebar + Phase-2 dashboard untouched)"
    - "The inventory tree shows synthetic vCenter root → Cluster → ESX → VM, virtualised, lazy children"
    - "Collapsed parents do not materialise their children in the flat array (memory budget, Critical-5)"
    - "Tree selection scopes the active table; root = unscoped"
    - "inventory i18n namespace ships EN+FR with strict key parity, no editorial verbs"
    - "A 10k-VM synthetic fixture exists (script, not bundled) and sort completes < 200 ms on it"
    - "The @tanstack bundle gate goes LIVE (first production-graph import) and passes ≤ 60 KiB gz"
  artifacts:
    - path: "src/components/inventory/InventoryTree.tsx"
      provides: "Flatten-visible + react-virtual + lazy-children tree (INV-01)"
      exports: ["InventoryTree"]
    - path: "src/components/inventory/InventoryView.tsx"
      provides: "Shell: tree pane + tabbed tables, single useEstateView caller"
      exports: ["InventoryView"]
    - path: "src/i18n/locales/en/inventory.json"
      provides: "inventory namespace EN"
    - path: "src/i18n/locales/fr/inventory.json"
      provides: "inventory namespace FR (strict parity)"
    - path: "scripts/generate-inventory-10k.mjs"
      provides: "Deterministic 10k-VM synthetic fixture generator (not bundled)"
    - path: "src/App.tsx"
      provides: "ViewToggle wiring + Dashboard/Inventory pane switch"
      contains: "ViewToggle"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/inventory/InventoryView.tsx"
      via: "activeView useState gates GlobalDashboard vs InventoryView"
      pattern: "InventoryView"
    - from: "src/components/inventory/InventoryView.tsx"
      to: "useEstateView"
      via: "single caller, props down to tree + tables"
      pattern: "useEstateView"
    - from: "src/components/inventory/InventoryTree.tsx"
      to: "EstateView.vmRows / hosts / clusters"
      via: "pre-indexed flatten-visible recipe"
      pattern: "buildVisibleRows"
---

<objective>
Land the user-visible Phase-3 surface: the virtualised lazy-children tree, the inventory shell (tree pane + tabbed Vm/Esx/Datastore tables), the new `inventory` i18n namespace EN+FR, the `App.tsx` Dashboard↔Inventory switch, the 10k synthetic fixture generator, and the integration/stress proofs. The TanStack bundle gate goes LIVE here (first production-graph import of `DataTable`).

Purpose: This is the phase that proves vatlas survives a real production-sized workbook in the DOM. The tree must use the flatten-visible + lazy-children recipe — `getExpandedRowModel` is forbidden (defeats the Critical-5 memory budget at 36k leaves).
Output: A working inventory view at 10k+ VMs, both themes, strict-parity i18n, and the ROADMAP Phase-3 success gates green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-inventory-navigation/03-RESEARCH.md
@.planning/phases/03-inventory-navigation/03-PATTERNS.md
@.planning/phases/03-inventory-navigation/03-UI-SPEC.md
@.planning/phases/03-inventory-navigation/03-01-SUMMARY.md
@.planning/phases/03-inventory-navigation/03-02-SUMMARY.md
@src/App.tsx
@src/i18n/index.ts
@src/components/dashboard/GlobalDashboard.tsx

<interfaces>
<!-- Extracted from the codebase. -->

App.tsx (src/App.tsx:13-42) — the `hasSnapshots ? <SnapshotListSidebar/> + <GlobalDashboard/> : <UploadZone hero/>` branch and the header `<div className="flex items-center gap-2"><LanguageToggle/><ThemeToggle/></div>`. Wiring deviation: add `const [activeView, setActiveView] = useState<'dashboard'|'inventory'>('dashboard')` (component useState, mirrors GlobalDashboard's lifted mode — NOT a memo), place `<ViewToggle value={activeView} onChange={setActiveView}/>` at the LEADING edge of the header toolbar (left of LanguageToggle/ThemeToggle — UI-SPEC §toggle placement), and inside the `hasSnapshots` branch render `<SnapshotListSidebar/>` + `(activeView === 'inventory' ? <InventoryView/> : <GlobalDashboard/>)`. No router (03-RESEARCH A5). Keep the top-level `<ErrorBoundary FallbackComponent={FallbackError}>` + `<Toaster>` untouched. The no-snapshot hero branch is untouched.

i18n/index.ts (src/i18n/index.ts:5-38) — 3 exact edits copying the `dashboard` lines: (1) `import enInventory from './locales/en/inventory.json'` + fr twin; (2) `NAMESPACES = ['common', 'upload', 'dashboard', 'inventory'] as const`; (3) add `inventory: enInventory` to `resources.en`, `inventory: frInventory` to `resources.fr`. `DEFAULT_NS` stays `'common'`.

GlobalDashboard.tsx (src/components/dashboard/GlobalDashboard.tsx:48-87) — InventoryView mirrors this: single `useEstateView(mode)` caller, region-scoped inline `ErrorBoundary` reading ONLY `error.message`/`error.name` (never stack/cause — privacy, 03-PATTERNS Scoped ErrorBoundary), no-snapshot empty `<section className="panel">`, `<main className="flex-1 overflow-y-auto p-8">` wrapper.

EstateView (src/types/estate.ts) — `clusters: ClusterAggregate[]`, `hosts: EsxAggregate[]` (group by `.cluster`), `vmRows: VmDisplayRow[]` (from 03-01; group by `.host`). `Snapshot.vCenterLabel` is the synthetic tree root label.

DataTable / Vm|Esx|DatastoreTable / ViewToggle — from 03-01 + 03-02. AccountingMode segmented idiom: `src/components/dashboard/AccountingModeToggle.tsx` (reuse for the inner VM|ESX|Datastore tab strip).

scripts/generate-mib-canary.mjs — exact structural template for the 10k generator (shebang, determinism header-comment, `XLSX.set_fs(fs)`, `dirname(fileURLToPath)` → `src/__fixtures__`, AoA `vInfo`/`vHost`/`vMetaData` sheets, `XLSX.writeFile(..., { bookType:'xlsx', compression:false })`, final `console.warn`). package.json scripts already include `generate-mib-canary`; add a sibling `generate-inventory-10k`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: inventory i18n namespace (EN+FR strict parity) + 10k fixture generator</name>
  <files>src/i18n/index.ts, src/i18n/locales/en/inventory.json, src/i18n/locales/fr/inventory.json, scripts/generate-inventory-10k.mjs, package.json</files>
  <action>
Create `src/i18n/locales/en/inventory.json` and `fr/inventory.json` mirroring the `dashboard.json` nesting. Keys per UI-SPEC §i18n (lines 154-165): `nav.{dashboard,inventory}`, `tab.{vm,esx,datastore}`, `tree.{root,empty,loading,error}`, `filter.{placeholder,clear}`, `columns.{button,reset}`, `export.csv`, `table.{empty,emptyFiltered}`, and `col.<field>` per-column header labels for every VM/ESX/Datastore column id introduced in 03-02. Copy text from UI-SPEC §Copywriting Contract verbatim. NO pre-formatted numbers in strings; NO editorial verbs (`recommend|should|healthy|critical|good|bad` — lint/grep gate, 03-RESEARCH line 27). Author EN+FR TOGETHER with strict key parity (03-RESEARCH Pitfall 5).

Edit `src/i18n/index.ts` with the 3 exact edits from the interfaces block (copy the `dashboard` import/NAMESPACES/resources lines, add `inventory`).

Create `scripts/generate-inventory-10k.mjs` from the `scripts/generate-mib-canary.mjs` structural template (03-PATTERNS analog): shebang, header-comment stating purpose + determinism + git-commit policy + "NOT bundled in the app — test/stress only" (03-RESEARCH line 400), `XLSX.set_fs(fs)`, write to `src/__fixtures__`. Generate ~10 000 deterministic `vInfo` rows via a seeded counter (vary vmName/cluster/host/vcpu/memory/provisioned for realistic sort/filter; include AT LEAST ONE row whose annotation/OS-ish field carries an embedded `\n` to exercise oneLine/CSV Pitfall 4) + proportional `vHost` rows + a minimal `vMetaData`. `XLSX.writeFile(..., { bookType:'xlsx', compression:false })` for byte-stable output; final `console.warn`. Add `"generate-inventory-10k": "node scripts/generate-inventory-10k.mjs"` to `package.json` scripts (mirror `generate-mib-canary`). Do NOT commit the generated fixture into the app bundle path; it lands under `src/__fixtures__` for tests only.
  </action>
  <verify>
    <automated>npm run generate-inventory-10k && node -e "const en=require('./src/i18n/locales/en/inventory.json'),fr=require('./src/i18n/locales/fr/inventory.json');const fl=(o,p='')=>Object.entries(o).flatMap(([k,v])=>typeof v==='object'?fl(v,p+k+'.'):[p+k]);const a=fl(en).sort(),b=fl(fr).sort();if(JSON.stringify(a)!==JSON.stringify(b)){console.error('EN/FR key drift');process.exit(1)}console.log('parity OK '+a.length+' keys')" && ! grep -riE 'recommend|should|healthy|critical|"good"|"bad"' src/i18n/locales/en/inventory.json src/i18n/locales/fr/inventory.json && npm run typecheck</automated>
  </verify>
  <done>`inventory` namespace registered in `i18n/index.ts` (NAMESPACES + both resource maps); EN↔FR strict key parity (flatten-diff passes, equal key count); zero editorial verbs in either locale; `npm run generate-inventory-10k` writes a ~10k-row fixture under `src/__fixtures__` (not bundled) and the `package.json` script exists.</done>
</task>

<task type="auto">
  <name>Task 2: InventoryTree (flatten-visible + lazy children) + InventoryView shell + App wiring</name>
  <files>src/components/inventory/InventoryTree.tsx, src/components/inventory/InventoryView.tsx, src/App.tsx</files>
  <action>
Create `src/components/inventory/InventoryTree.tsx` implementing the 03-RESEARCH §Pattern 2 flatten-visible recipe (lines 197-207) — NO in-repo analog; React/Tailwind conventions from `GlobalDashboard.tsx`. Props receive pre-indexed data from `InventoryView` (NOT a new memo/hook): `clustersOrdered: string[]` (from `EstateView.clusters`), `hostsByCluster: Map<string, EsxAggregate[]>` (group `EstateView.hosts` by `.cluster`), `vmsByHost: Map<string, VmDisplayRow[]>` (group `EstateView.vmRows` by `.host`), `rootLabel` (`Snapshot.vCenterLabel`). Component state: `expanded: Set<string>` of node ids (`cl:<name>`, `esx:<name>`) and `selected: string | null`. `buildVisibleRows(expanded)`: synthetic vCenter root → for each cluster push a cluster row; if `cl:<name> ∈ expanded` push its hosts; if `esx:<name> ∈ expanded` push that host's VMs → returns `FlatNode[]` whose length ≈ only-opened (LAZY children — children included ONLY when the parent id ∈ expanded; collapsed = absent). `useVirtualizer({ count: flat.length, getScrollElement, estimateSize: () => 36, overscan: 12 })`; render the window only; indent by `node.depth` via `paddingLeft` 16px/level (UI-SPEC §Spacing). `buildVisibleRows` is render-derived presentation state — a local `useMemo` keyed on `expanded` is acceptable render-memoization OR a plain call (03-RESEARCH line 199 — that rule is about *aggregation*, not render memoization); prefer a plain call unless the window cost demands memoizing. DO NOT use TanStack `getExpandedRowModel()` (03-RESEARCH Anti-Pattern line 234 — materializes the full subtree). NO Datacenter level (03-RESEARCH A1/Open Q2; root label key `inventory.tree.root`). Chevron rotates 90° with `aria-expanded`; count badge Caption size on secondary surface (count always known — `0` shows `0`, never em-dash; UI-SPEC §Virtualised tree). Selecting a node sets `selected` and surfaces an accent indicator; root selected = unscoped. Never hold a parallel tree-of-objects — the flat array is derived on demand (Critical-5, 03-RESEARCH line 207). Cell text rendered as React text children only.

Create `src/components/inventory/InventoryView.tsx` mirroring `GlobalDashboard.tsx:48-87` (03-PATTERNS shell analog): it is the SINGLE `useEstateView(mode)` caller for the inventory region (mode from a lifted `useState`, default `'active'` matching GlobalDashboard), pre-indexes `hostsByCluster`/`vmsByHost` once from the view (plain functions, NOT new memos — the projection already happened in `useEstateView`), and lays out a two-pane UI: left `.panel` virtualised `<InventoryTree>`, right tabbed object-table area, gap 32px/xl (UI-SPEC §Inventory layout). Inner sub-tab strip (VM | ESX | Datastore) reuses the `AccountingModeToggle` `aria-pressed role="group"` idiom (03-PATTERNS Shared Pattern — keep the literal `role="group"` + `noRedundantRoles` biome-ignore). Tree `selected` scopes the active table (filter the slice passed to the active `<VmTable>`/`<EsxTable>`/`<DatastoreTable>` to the selected node's subtree; root/null = unscoped; the Datastore table is cluster-agnostic so tree scoping above Cluster has no effect on it — UI-SPEC §Three object tables). Region-scoped inline `ErrorBoundary` reading ONLY `error.message`/`error.name` (privacy, 03-PATTERNS), no-snapshot empty `<section className="panel">` + loading/error states per UI-SPEC §Copywriting. `useTranslation('inventory')`. Owns: tree-selection scope state, active-tab state, the single `view` call — props down to children. NO per-child `useMemo`/`useEstateView` (children are plain prop consumers).

Edit `src/App.tsx` per the interfaces wiring deviation: add `activeView` `useState`, place `<ViewToggle>` at the header toolbar leading edge, gate `activeView === 'inventory' ? <InventoryView/> : <GlobalDashboard/>` inside the `hasSnapshots` branch beside `<SnapshotListSidebar/>`. Phase-1 sidebar, Phase-2 dashboard, header (LanguageToggle/ThemeToggle), top-level ErrorBoundary/Toaster, and the no-snapshot hero branch stay byte-equivalent except the additions.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && ! grep -rn 'getExpandedRowModel' src/components/inventory/ && ! grep -rn 'localStorage' src/components/inventory/InventoryView.tsx src/components/inventory/InventoryTree.tsx && grep -q 'ViewToggle' src/App.tsx && [ "$(grep -rl 'useEstateView(' src/components/inventory/ | wc -l | tr -d ' ')" = "1" ]</automated>
  </verify>
  <done>Tree uses the flatten-visible lazy-children recipe (no `getExpandedRowModel`); `InventoryView` is the single `useEstateView` caller in `src/components/inventory/`; App switches Dashboard↔Inventory via `ViewToggle` with sidebar/dashboard/header untouched; no `localStorage` in tree/shell; typecheck/lint clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tree-virtualisation + 10k stress + e2e integration + LIVE bundle gate</name>
  <files>src/components/inventory/InventoryTree.test.tsx, src/__tests__/inventory-stress.test.tsx, src/__tests__/e2e-smoke.test.tsx</files>
  <behavior>
    InventoryTree.test.tsx (jsdom + @testing-library/react):
    - Lazy children: a collapsed cluster's hosts are NOT in the rendered/flat output; expand → its hosts appear; collapse → gone again
    - A collapsed host's VMs are absent until the host is expanded
    - Chevron toggles `aria-expanded`; count badge renders `0` (never em-dash) for an empty node
    - Selecting a node scopes the active table; root selected = unscoped
    inventory-stress.test.tsx (load the generated 10k fixture via parseSnapshot → buildEstateView):
    - Sort the VM table by `provisionedMib` descending; assert wall-clock < 200 ms (ROADMAP Phase-3 #2)
    - Tree expand/collapse keeps the rendered row-window BOUNDED (assert virtualised window node count ≪ total — fps not measurable in jsdom, so assert window size per 03-RESEARCH line 462 / ROADMAP Phase-3 #1 proxy)
    - CSV export of a filtered VM view end-to-end = filtered×visible rows, raw values, newline preserved (ROADMAP Phase-3 #4) — both `light` and `dark` theme classes mounted
    e2e-smoke.test.tsx (extend existing): toggling ViewToggle to inventory renders the tree+tables; toggling back renders the dashboard; sidebar present throughout
  </behavior>
  <action>
RED first. Write `src/components/inventory/InventoryTree.test.tsx` using the `AccountingModeToggle.test.tsx` component-test idiom (`render`/`screen`/`userEvent`, `beforeEach(() => i18n.changeLanguage('en'))`, `src/test/arrays.ts` `first()` for narrowing). Assert the lazy-children, chevron-aria, count-badge-`0`, and selection-scoping behavior bullets.

Write `src/__tests__/inventory-stress.test.tsx`: load the `scripts/generate-inventory-10k.mjs` fixture (generate it in a `beforeAll` if absent, or assume the npm script ran in CI), drive `parseSnapshot → buildEstateView`, mount the inventory surface. Assert sort < 200 ms (ROADMAP #2), bounded virtualised window on tree expand/collapse (ROADMAP #1 proxy — 03-RESEARCH line 462), and the filtered-CSV end-to-end under both `light` and `dark` (toggle the `.dark` class). If sort > 200 ms or filter > 100 ms on the synthetic fixture, that is the ROADMAP gate breach — do NOT silently widen the budget; surface it (03-RESEARCH Pitfall 3).

Extend `src/__tests__/e2e-smoke.test.tsx` (the existing Phase-2 file — minimal-diff, preserve existing assertions; Phase-2 relaxed `/2026/` to `getAllByText`, keep that): add a case toggling `ViewToggle` → inventory renders tree+tables, → back renders dashboard, sidebar present throughout. GREEN: all pass.

Then run the LIVE bundle gate: `npm run build && node scripts/check-bundle-size.mjs`. This is the FIRST production-graph import of `DataTable` (via `InventoryView` ← `App.tsx`), so the `@tanstack` chunk now exists and the 03-01 sibling gate (≤ 60 KiB gz) goes live. Record the measured gz size in the SUMMARY. The echarts gate must still pass unchanged.
  </action>
  <verify>
    <automated>npm run test:run -- src/components/inventory/InventoryTree.test.tsx src/__tests__/inventory-stress.test.tsx src/__tests__/e2e-smoke.test.tsx && npm run build && node scripts/check-bundle-size.mjs && npm run check:supply-chain</automated>
  </verify>
  <done>Tree lazy-children/selection tests green; 10k stress proves sort < 200 ms + bounded tree window (ROADMAP Phase-3 #1/#2) + filtered-CSV end-to-end in both themes (#4); e2e-smoke proves the Dashboard↔Inventory switch with sidebar intact; `npm run build` succeeds; `check-bundle-size.mjs` exits 0 with the LIVE `@tanstack` chunk ≤ 60 KiB gz AND the echarts chunk still ≤ 300 KiB gz; supply-chain clean.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RVTools data → tree/table DOM | VM/host names rendered into tree nodes and table cells |
| Tree/table state → persistence | Expansion/selection/filter/visibility could leak row-derived data |
| 10k raw rows → memory | A 36k-leaf tree could materialise unbounded row objects |
| Inventory error → UI | A parse/render failure could leak stack/VM names |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | DoS / memory (Critical-5) | InventoryTree | mitigate | Flatten-visible + lazy children + react-virtual; verify gate forbids `getExpandedRowModel`; stress test asserts a bounded rendered window at 10k |
| T-03-11 | Info disclosure (privacy) | tree/shell state | mitigate | Expansion/selection/scope all component `useState`; verify gate asserts zero `localStorage` in tree + shell |
| T-03-12 | Info disclosure | InventoryView error path | mitigate | Region-scoped `ErrorBoundary` reads `error.message`/`error.name` only (never stack/cause — privacy); Phase-1 top-level boundary is the outer net |
| T-03-13 | Tampering/XSS | tree node + cell text | mitigate | All node/cell labels rendered as React text children (auto-escaped); no raw-HTML sink |
| T-03-14 | Tampering | LIVE @tanstack chunk | mitigate | Bundle gate ≤ 60 KiB gz now enforced in the production graph; `check:supply-chain` green; deps pinned in 03-01 |
| T-03-15 | Info disclosure | i18n strings | mitigate | No pre-formatted numbers / no editorial verbs in `inventory.json` (grep gate); EN↔FR parity gate prevents raw-key leakage |
</threat_model>

<verification>
- `npm run typecheck` / `npm run lint` exit 0
- `npm run test:run -- src/components/inventory/InventoryTree.test.tsx src/__tests__/inventory-stress.test.tsx src/__tests__/e2e-smoke.test.tsx` green
- EN↔FR `inventory.json` strict key parity (flatten-diff); zero editorial verbs
- No `getExpandedRowModel` anywhere in `src/components/inventory/`
- No `localStorage` in `InventoryTree.tsx`/`InventoryView.tsx`
- Exactly one `useEstateView(` caller in `src/components/inventory/` (InventoryView)
- `npm run build` succeeds; `node scripts/check-bundle-size.mjs` exits 0 with LIVE `@tanstack` chunk ≤ 60 KiB gz AND echarts chunk ≤ 300 KiB gz
- 10k stress: sort < 200 ms (ROADMAP #2), bounded tree window (ROADMAP #1 proxy), filtered-CSV end-to-end both themes (#4)
- `npm run check:supply-chain` exits 0
</verification>

<success_criteria>

- A user toggles Dashboard↔Inventory and browses a virtualised tree + sortable/filterable tables (INV-01..04)
- A user expands/collapses the tree on a 10k-VM fixture with a bounded DOM window (ROADMAP Phase-3 #1)
- A user sorts the VM table by Provisioned MiB descending in < 200 ms on 10k (ROADMAP Phase-3 #2)
- A user filters the table and results update without re-rendering the dashboard (ROADMAP Phase-3 #3 — inventory state is component-local, dashboard owns its own useEstateView)
- A user exports the current filtered VM table and the CSV contains exactly the visible filtered rows (ROADMAP Phase-3 #4)
- A user sees a shared LUN once in the datastore table (ROADMAP Phase-3 #5 — verified in 03-02, surfaced here)
- Every UI string available EN+FR; light + dark theme working
</success_criteria>

<output>
After completion, create `.planning/phases/03-inventory-navigation/03-03-SUMMARY.md`
</output>
