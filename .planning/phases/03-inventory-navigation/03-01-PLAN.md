---
phase: 03-inventory-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - scripts/check-bundle-size.mjs
  - src/types/estate.ts
  - src/engines/aggregation/estateView.ts
  - src/utils/csv.ts
  - src/utils/csv.test.ts
  - src/utils/oneLine.ts
  - src/utils/oneLine.test.ts
  - src/components/inventory/DataTable.tsx
  - src/components/inventory/ColumnPicker.tsx
  - src/components/ViewToggle.tsx
autonomous: true
requirements: [INV-05, INV-06]
must_haves:
  truths:
    - "@tanstack/react-table@8.21.3 + @tanstack/react-virtual@3.13.24 are installed and pinned"
    - "toCsv(headers, rows) produces RFC-4180 output with raw values, preserved newlines, and a CSV-injection guard"
    - "oneLine(s) collapses whitespace to a single line for display only"
    - "EstateView carries vmRows: VmDisplayRow[] derived inside the existing single buildEstateView pass (no second useMemo, no new hook, no new engine call site)"
    - "A generic DataTable<T> renders virtualised, sortable, filterable, column-hideable rows and exports the current filtered×visible view to CSV"
    - "ViewToggle is a 2-segment aria-pressed control reusing the AccountingModeToggle idiom"
  artifacts:
    - path: "src/utils/csv.ts"
      provides: "Pure RFC-4180 toCsv + csvCell with injection guard"
      contains: "export function toCsv"
    - path: "src/utils/oneLine.ts"
      provides: "Pure display-only whitespace collapse"
      contains: "export const oneLine"
    - path: "src/engines/aggregation/estateView.ts"
      provides: "vmRows projection inside the existing snapshot.vinfo loop"
      contains: "vmRows"
    - path: "src/types/estate.ts"
      provides: "VmDisplayRow interface + vmRows on EstateView"
      contains: "interface VmDisplayRow"
    - path: "src/components/inventory/DataTable.tsx"
      provides: "Generic headless virtualised table + CSV-of-current-view primitive"
      exports: ["DataTable"]
    - path: "src/components/inventory/ColumnPicker.tsx"
      provides: "INV-06 show/hide popover"
      exports: ["ColumnPicker"]
    - path: "src/components/ViewToggle.tsx"
      provides: "Dashboard↔Inventory segmented control"
      exports: ["ViewToggle"]
    - path: "scripts/check-bundle-size.mjs"
      provides: "Sibling @tanstack chunk gate (≤60 KiB gz)"
      contains: "tanstack"
  key_links:
    - from: "src/engines/aggregation/estateView.ts"
      to: "EstateView.vmRows"
      via: "1:1 projection in the existing vinfo loop"
      pattern: "vmRows"
    - from: "src/components/inventory/DataTable.tsx"
      to: "src/utils/csv.ts"
      via: "toCsv over getFilteredRowModel × getVisibleLeafColumns"
      pattern: "toCsv"
---

<objective>
Build the shared, app-agnostic table infrastructure Phase 3 stands on: install and gate the two TanStack deps, write the two net-new pure utils (CSV serializer, oneLine), add the `VmDisplayRow` projection inside the existing `buildEstateView` single pass, and build the generic `DataTable` + `ColumnPicker` + `ViewToggle` components. No app wiring yet — that lands in 03-03.

Purpose: Every Phase-3 surface (3 tables, tree, CSV export, view switch) reuses exactly these primitives. Building them once, DRY-justified by 3 real table consumers, is the load-bearing decision.
Output: Pinned deps, a live-able bundle gate, two unit-tested pure utils, the `vmRows` projection on `EstateView`, and three reusable components with no consumers yet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-inventory-navigation/03-RESEARCH.md
@.planning/phases/03-inventory-navigation/03-PATTERNS.md
@.planning/phases/03-inventory-navigation/03-UI-SPEC.md
@src/engines/aggregation/estateView.ts
@src/types/vinfo.ts
@src/components/dashboard/AccountingModeToggle.tsx
@scripts/check-bundle-size.mjs

<interfaces>
<!-- Extracted from the codebase — executor uses these directly, no exploration needed. -->

EXISTING buildEstateView vinfo loop (src/engines/aggregation/estateView.ts:48-67) — the VmDisplayRow projection rides INSIDE this exact loop; do NOT add a second iteration:
```
const osBreakdown = emptyBreakdown()
const vmsByCluster = new Map<string, OsBreakdown>()
for (const vm of snapshot.vinfo) {
  const family = classifyOsFamily(vm.osConfig, vm.osTools)
  osBreakdown[family] += 1
  const perCluster = vmsByCluster.get(vm.cluster) ?? emptyBreakdown()
  perCluster[family] += 1
  vmsByCluster.set(vm.cluster, perCluster)
}
return { globals, clusters, hosts, datastores, vmsByCluster, osBreakdown, accountingMode: mode, trends: null }
```
EMPTY_VIEW (same file, after the function) freezes every array field as `Object.freeze([]) as never[]` — `vmRows` must be added there too.

VInfoRow (src/types/vinfo.ts) — projection source. Fields used: `vmName: string`, `cluster: string`, `host: string`, `vcpu: Cores`, `vramMib: MiB`, `osConfig: string`, `osTools: string`, `poweredOn: boolean`, `provisionedMib: MiB`. `Cores`/`MiB` are branded — import from `@/engines/units`.

EstateView (src/types/estate.ts:283-300) — interface to extend with `vmRows: VmDisplayRow[]`, placed adjacent to `hosts`/`datastores`. EsxAggregate (estate.ts:237-254) is the lean-interface doc-comment + flat-field template to mirror for VmDisplayRow.

AccountingModeToggle (src/components/dashboard/AccountingModeToggle.tsx) — copy the `<fieldset role="group">` + `<legend className="sr-only">` + `move(delta)`/`onKeyDown` Arrow-key handler + `aria-pressed` button map + the literal `biome-ignore lint/a11y/noRedundantRoles` comment (a CI grep gate asserts `role="group"`). It is the exact ViewToggle template.

check-bundle-size.mjs (scripts/check-bundle-size.mjs:36-90) — the echarts gate: `MAX_GZIP_BYTES`, `ECHARTS_MARKER='echarts'`, `jsChunks.filter(...includes(marker))`, gzipSync ≤ MAX, "no chunk → exit 0 nothing to gate" graceful path. The echarts gate stays UNTOUCHED; add a sibling tanstack scan beside it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install + pin TanStack deps, extend bundle gate, add VmDisplayRow projection</name>
  <files>package.json, package-lock.json, scripts/check-bundle-size.mjs, src/types/estate.ts, src/engines/aggregation/estateView.ts</files>
  <action>
Run `npm install @tanstack/react-table@8.21.3 @tanstack/react-virtual@3.13.24 --save-exact` (per 03-RESEARCH Standard Stack; both npm-verified current 2026-05-16, React-19 peers OK). Verify both versions resolved exactly via `npm ls @tanstack/react-table @tanstack/react-virtual`.

In `scripts/check-bundle-size.mjs`, add a sibling chunk gate beside (NOT replacing) the echarts block per 03-PATTERNS scripts/check-bundle-size.mjs analog: a second marker scan with marker substring `tanstack`, constant `MAX_TANSTACK_GZIP_BYTES = 61440` (60 KiB — 03-RESEARCH line 460 / A6: generous headroom, tighten after first production build), same gzipSync-and-assert structure and the same "no chunk found → log OK, do not fail" graceful behaviour as the echarts path (the tanstack chunk only enters the production graph in 03-03; this gate must pass as a no-op until then). Keep the existing echarts gate byte-identical.

In `src/types/estate.ts`, add `export interface VmDisplayRow` mirroring the `EsxAggregate` doc-comment + flat-field idiom (estate.ts:237-254): fields `vmName: string`, `cluster: string`, `host: string`, `vcpu: Cores`, `vramMib: MiB`, `os: string`, `poweredOn: boolean`, `provisionedMib: MiB`. Doc-comment MUST state "pure projection of `Snapshot.vinfo`, NOT an aggregation" (03-PATTERNS estate.ts deviation). Import `Cores`/`MiB` from the same units source the file already uses. Add `vmRows: VmDisplayRow[]` to the `EstateView` interface adjacent to `hosts`/`datastores`.

In `src/engines/aggregation/estateView.ts`, inside the EXISTING `for (const vm of snapshot.vinfo)` loop (estateView.ts:50-56 — do NOT add a second iteration, do NOT add a new useMemo/hook/engine call site), push a `VmDisplayRow` into a `vmRows` accumulator declared next to `vmsByCluster`: `{ vmName: vm.vmName, cluster: vm.cluster, host: vm.host, vcpu: vm.vcpu, vramMib: vm.vramMib, os: vm.osTools || vm.osConfig, poweredOn: vm.poweredOn, provisionedMib: vm.provisionedMib }`. This is a 1:1 filter/map projection (NEVER group/sum) — the same operation-class as the `classifyOsFamily` call already in this loop (03-RESEARCH A2, line 344). Add `vmRows` to the returned object literal AND to `EMPTY_VIEW` as `Object.freeze([]) as never[]` mirroring the `hosts` field. KISS placement note (03-RESEARCH A2): a tiny `projectVmRows` helper is acceptable but inlining in the existing loop is the minimal-diff choice — prefer inline.
  </action>
  <verify>
    <automated>npm ls @tanstack/react-table @tanstack/react-virtual && npm run typecheck && node scripts/check-bundle-size.mjs && [ "$(grep -rl 'useMemo(' src/ --include='*.ts' --include='*.tsx' | grep -v test | grep -v -F 'SnapshotListSidebar' | grep -v -F 'useEstateView' | wc -l | tr -d ' ')" = "0" ]</automated>
  </verify>
  <done>Both TanStack deps pinned exact in package.json/lock; `node scripts/check-bundle-size.mjs` exits 0 (echarts gate unchanged, tanstack gate is graceful no-op pre-build); `VmDisplayRow` + `vmRows` exist on `EstateView`/`EMPTY_VIEW`; projection lives inside the single existing vinfo loop; zero real `useMemo(` call sites outside `useEstateView` and the pre-existing Phase-1 `SnapshotListSidebar`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Net-new pure utils — csv.ts + oneLine.ts (TDD, ≥75% coverage)</name>
  <files>src/utils/csv.ts, src/utils/csv.test.ts, src/utils/oneLine.ts, src/utils/oneLine.test.ts</files>
  <behavior>
    csv.ts:
    - toCsv([], []) → '' (empty headers + no rows)
    - toCsv(['a','b'], [['1','2']]) → 'a,b\r\n1,2' (CRLF row sep, comma field sep)
    - csvCell with value containing comma/quote/newline → wrapped in double-quotes, internal `"` doubled
    - csvCell with embedded `\n` → newline PRESERVED inside the quoted field (not collapsed)
    - csvCell with leading `=`,`+`,`-`,`@` → prefixed with a single quote `'` (CSV-injection guard, 03-PATTERNS csv.ts line 105 / 03-RESEARCH Security)
    - csvCell(null) and csvCell(undefined) → '' ; csvCell(0) → '0' ; numbers/booleans stringified raw (NO toLocaleString — opposite of format.ts)
    oneLine.ts:
    - oneLine('a\n  b\tc') → 'a b c' ; oneLine('  trim me  ') → 'trim me' ; oneLine('') → ''
  </behavior>
  <action>
Create `src/utils/oneLine.ts` following the `src/utils/format.ts` pure-module idiom (top-of-file doc-comment with provenance/lineage, named export, zero React/i18n imports): `export const oneLine = (s: string): string => s.replace(/\s+/g, ' ').trim()`. Doc-comment MUST state it is the DISPLAY-ONLY boundary transform (Minor-2 / 03-PATTERNS oneLine.ts) — the CSV path passes the original value, never the one-lined one. Two deliberate code paths.

Create `src/utils/csv.ts` with the exact logic from 03-PATTERNS csv.ts (lines 103-110) + the injection guard:
- `csvCell(v: unknown): string` — `let s = v == null ? '' : String(v)`; if `/^[=+\-@]/.test(s)` then `s = \`'${s}\``; return `/[",\n\r]/.test(s) ? \`"${s.replace(/"/g, '""')}"\` : s`.
- `export function toCsv(headers: string[], rows: string[][]): string` — `[headers, ...rows].map((r) => r.map(csvCell).join(',')).join('\r\n')`.
Header doc-comment MUST explicitly state the CONTRAST with `format.ts`: raw values, NO `toLocaleString`, newlines PRESERVED (03-PATTERNS csv.ts deviation; 03-RESEARCH Anti-Pattern "Formatting numbers in the CSV"). Zero React/i18n imports.

Write `csv.test.ts` and `oneLine.test.ts` using the `src/engines/aggregation/estateView.test.ts` pure-fn vitest idiom (describe/it/expect). RED first (import not-yet-written modules → fail), then GREEN. Cover every behavior bullet above; ≥75% coverage (ADR-0005, 03-RESEARCH line 460).
  </action>
  <verify>
    <automated>npm run test:run -- src/utils/csv.test.ts src/utils/oneLine.test.ts && npx vitest run --coverage src/utils/csv.test.ts src/utils/oneLine.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>Both util test files pass; csv.ts produces RFC-4180 CRLF output with raw values, preserved newlines, doubled internal quotes, and the leading-`=+-@` injection guard; oneLine collapses whitespace; ≥75% statement+branch coverage on both util files; no React/i18n import in either.</done>
</task>

<task type="auto">
  <name>Task 3: Generic DataTable + ColumnPicker + ViewToggle (no app wiring)</name>
  <files>src/components/inventory/DataTable.tsx, src/components/inventory/ColumnPicker.tsx, src/components/ViewToggle.tsx</files>
  <action>
Create `src/components/inventory/DataTable.tsx` anchored on 03-RESEARCH §Pattern 1 (lines 172-195) + §Virtualised rows (lines 323-331); no in-repo analog (03-PATTERNS No Analog Found). Generic `function DataTable<T>(props: { data: T[]; columns: ColumnDef<T>[]; headerFor: (id: string) => string; objectKind: 'vm' | 'esx' | 'datastore' })`. Owns `useReactTable({ data, columns, state: { sorting, columnFilters, globalFilter, columnVisibility }, onSortingChange, onColumnVisibilityChange, onGlobalFilterChange, getCoreRowModel, getSortedRowModel, getFilteredRowModel })`. Sort/filter/visibility state = component `useState` (NEVER `useMemo` — useEstateView is the only memo site; 03-PATTERNS Shared Pattern). Body windowed by `useVirtualizer({ count: rows.length, getScrollElement, estimateSize: () => 36, overscan: 12 })` — fixed 36px row height, no measured/variable rows (03-RESEARCH line 195). Global filter input debounced 150 ms (03-RESEARCH Pattern 4). Sticky header + sticky first column on the secondary `.panel` surface; active sort header uses accent gold (UI-SPEC §Color reserved list). Render cell values as React text children ONLY — never a raw-HTML sink (03-RESEARCH Anti-Pattern / Security line 423); apply `oneLine()` at the cell boundary with the original text in the cell `title` attribute (03-PATTERNS oneLine.ts; UI-SPEC §Three object tables). Expose an "Export CSV" toolbar button calling a CSV-of-current-view handler: `cols = table.getVisibleLeafColumns()`, `headers = cols.map(c => headerFor(c.id))`, `rows = table.getFilteredRowModel().rows.map(r => cols.map(c => { const v = r.getValue(c.id); return v == null ? '' : String(v) }))` (RAW value, no locale, newline kept), then `toCsv(headers, rows)` → `new Blob([csv], { type: 'text/csv;charset=utf-8' })` → `URL.createObjectURL` → anchor `download="vatlas-${objectKind}-${YYYYMMDD}.csv"` → click → `URL.revokeObjectURL` (03-RESEARCH §Pattern 3 lines 305-319; UI-SPEC §CSV export filename). Render the `ColumnPicker` in the toolbar right of the filter. Tailwind: every color utility carries its `dark:` twin and `focus-visible:ring-2 focus-visible:ring-primary-500`, copying `GlobalDashboard.tsx`/`AccountingModeToggle.tsx` conventions. Imports: `flexRender, useReactTable, getCoreRowModel, getSortedRowModel, getFilteredRowModel` from `@tanstack/react-table`; `useVirtualizer` from `@tanstack/react-virtual`. Use `useTranslation('inventory')` for filter placeholder / export label / columns button — keys may reference the namespace that 03-03 registers (component is not mounted until 03-03, so missing-namespace at this stage is acceptable and is exercised end-to-end there).

Create `src/components/inventory/ColumnPicker.tsx` (INV-06) anchored on 03-RESEARCH §Column visibility (lines 293-300); a11y/Tailwind idiom from `ThemeToggle.tsx`/`AccountingModeToggle.tsx`. Props `{ table: Table<T> }` (generic). Render a "Columns" toolbar button (icon + Label) opening a popover listing `table.getAllLeafColumns().filter(c => c.getCanHide())` as checkboxes calling `column.toggleVisibility()`; identity columns are excluded automatically because their ColumnDef sets `enableHiding: false` (set by 03-02). "Reset" action restores the default visible set (accept a `defaultVisibility` prop or a reset callback). Keyboard-operable, closes on Escape / outside click, applies immediately (UI-SPEC §Column-picker). Visibility state is owned by `DataTable`'s `useState` — ephemeral in-memory, NEVER `localStorage` (03-RESEARCH Anti-Pattern line 235; privacy invariant). Every color utility carries its `dark:` twin.

Create `src/components/ViewToggle.tsx` — the strongest analog (03-PATTERNS line 233): copy `AccountingModeToggle.tsx` almost verbatim. 2 segments instead of 3: `const VIEWS = ['dashboard', 'inventory'] as const`; controlled `{ value: 'dashboard' | 'inventory'; onChange: (v) => void }`. Keep the `<fieldset role="group">` + `<legend className="sr-only">` + `move(delta)`/`onKeyDown` Arrow-Left/Right/Up/Down wrap handler + `aria-pressed` button map AND the literal `biome-ignore lint/a11y/noRedundantRoles` comment on `role="group"` (a CI grep gate asserts the literal `role="group"`; 03-PATTERNS line 254). i18n keys `inventory.nav.dashboard` / `inventory.nav.inventory` via `useTranslation('inventory')`. Active segment uses accent gold per UI-SPEC §Color reserved list (the active *view* segment follows the UI-SPEC accent, not necessarily `bg-primary-600` — 03-PATTERNS line 256: follow UI-SPEC accent gold token). Default segment `dashboard`. State is lifted by the consumer (03-03 wires it in App.tsx) — this component is controlled and unmounted-consumer here.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && grep -q 'role="group"' src/components/ViewToggle.tsx && grep -q 'noRedundantRoles' src/components/ViewToggle.tsx && ! grep -rn 'localStorage' src/components/inventory/ src/components/ViewToggle.tsx && ! grep -rn 'useMemo' src/components/inventory/DataTable.tsx src/components/inventory/ColumnPicker.tsx src/components/ViewToggle.tsx</automated>
  </verify>
  <done>`DataTable.tsx`, `ColumnPicker.tsx`, `ViewToggle.tsx` exist and typecheck/lint clean; `ViewToggle` keeps the literal `role="group"` + `noRedundantRoles` biome-ignore; no `localStorage` and no `useMemo` anywhere in the three files; DataTable exposes the CSV-of-filtered×visible-view primitive via `toCsv`; no consumers wired (App/InventoryView untouched).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RVTools data → DOM cells | Parsed VM names/annotations are upstream-influenceable, rendered into table cells |
| Table state → persistence | Sort/filter/visibility could leak row-derived data if written to storage |
| Current view → CSV file | Filtered rows serialised to a client Blob; opened later in Excel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering/XSS | DataTable cell render | mitigate | All cell values rendered as React text children (auto-escaped); `oneLine` is a pure text transform; never a raw-HTML sink (03-RESEARCH Anti-Pattern, verify gate forbids it) |
| T-03-02 | Tampering | csv.ts → Excel | mitigate | `csvCell` prefixes a leading `=+-@` with `'` (CSV-injection guard, unit-tested) |
| T-03-03 | Info disclosure (privacy) | ColumnPicker / DataTable state | mitigate | Sort/filter/visibility kept in component `useState`; verify gate asserts zero `localStorage` in inventory + ViewToggle |
| T-03-04 | Info disclosure | CSV Blob | accept | Client-side Blob → anchor download; no fetch, no server (privacy-clean by construction; the Phase-1 fetch/XHR guard + CSP `connect-src 'self'` still apply) |
| T-03-05 | Tampering | new @tanstack deps | mitigate | Versions pinned exact (`--save-exact`); `check:supply-chain` stays green; no CSV dependency added (in-repo serializer) |
</threat_model>

<verification>
- `npm ls @tanstack/react-table @tanstack/react-virtual` → both exactly 8.21.3 / 3.13.24
- `npm run typecheck` exits 0
- `npm run lint` exits 0
- `npm run test:run -- src/utils/csv.test.ts src/utils/oneLine.test.ts` green; ≥75% coverage on both util files
- `node scripts/check-bundle-size.mjs` exits 0 (echarts gate unchanged; tanstack gate graceful no-op pre-3-03-build)
- Single-useMemo grep gate: zero real `useMemo(` call sites outside `useEstateView` + pre-existing Phase-1 `SnapshotListSidebar`
- No `localStorage` in `src/components/inventory/` or `ViewToggle.tsx`
- `ViewToggle.tsx` contains literal `role="group"` + `noRedundantRoles` biome-ignore
- `npm run check:supply-chain` exits 0
</verification>

<success_criteria>
- TanStack Table 8.21.3 + TanStack Virtual 3.13.24 installed, pinned, supply-chain-clean
- `toCsv` round-trips a filtered×visible view as RFC-4180 with raw values + preserved newlines + injection guard (ROADMAP Phase-3 #4 foundation)
- `EstateView.vmRows` is a pure projection produced in the existing single `buildEstateView` pass — still exactly one `useMemo` site project-wide
- A generic, virtualised, sortable, filterable, column-hideable `DataTable` exists with a CSV-of-current-view primitive; `ColumnPicker` (INV-06) and `ViewToggle` exist — all unwired
</success_criteria>

<output>
After completion, create `.planning/phases/03-inventory-navigation/03-01-SUMMARY.md`
</output>
</invoke>
