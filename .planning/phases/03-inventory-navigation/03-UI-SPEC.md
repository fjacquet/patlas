---
phase: 3
slug: inventory-navigation
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-16
---

# Phase 3 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

**Visual continuity:** Phase 3 inherits the APPROVED Phase-2 UI-SPEC verbatim — same Midnight Executive tokens, same spacing scale, same `.panel`/`.label` utilities, same light/dark dark-pair classes, same Phase-2 number formatter. Zero new design primitives are introduced; the Dashboard↔Inventory toggle reuses the existing ThemeToggle `aria-pressed` segmented-control idiom. No charts — tables and a tree only.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none (manual design system, Tailwind v4 `@theme`) |
| Icon library | inherited from Phase 1/2 (lucide, chevron + table affordances only) |
| Font | inherited from Phase 2 (system UI stack) |

Manual design system: Tailwind v4 `@theme` tokens. TanStack Table and TanStack Virtual are npm libraries — they are NOT a component registry and require no registry safety gate.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, chevron-to-label gap, count-badge padding |
| sm | 8px | Tree row vertical padding, per-level indentation step, cell padding |
| md | 16px | Default element spacing, toolbar gaps, pane padding |
| lg | 24px | Section padding, header padding |
| xl | 32px | Layout gaps (tree pane ↔ table pane) |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

Tree indentation = 16px (md) per depth level. Exceptions: none.

---

## Typography

EXACTLY 4 sizes, EXACTLY 2 weights. (Supersedes the rejected 02-UI-SPEC block of 5 sizes / 3 weights — that block must NOT recur.)

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Caption | 12px | 400 | 1.5 |
| Body | 14px | 400 | 1.5 |
| Label | 16px | 600 | 1.5 |
| Heading | 20px | 600 | 1.2 |

Label role stays at weight 600 (`.label` utility). Table cells use Body (14/400); table headers and tree node labels use Label (16/600 via `.label`); the toolbar object-type/section title uses Heading.

---

## Color

60/30/10 split, stated explicitly:

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | Midnight Executive base surface (inherited Phase-2 token) | App background, table body, tree pane background |
| Secondary (30%) | Midnight Executive panel (inherited `.panel`) | Toolbar, tree pane container, sticky table header, sticky first column, column-picker popover |
| Accent (10%) | Midnight Executive gold (inherited) | See reserved list below |
| Destructive | Midnight Executive destructive (inherited) | Listed for token completeness — **unused in Phase 3** |

Accent reserved for (closed list, nothing else):
- Active segment of the Dashboard↔Inventory toggle
- Selected tree node row (selection scope indicator)
- Active sort column header indicator
- Focus ring on keyboard-focused interactive controls

Destructive note: Phase 3 tree and tables are strictly read-only. There are no destructive actions in this phase — snapshot removal remains in the Phase-1 sidebar and is out of scope here. Destructive color is declared only so the token table is complete.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | "Export CSV" (toolbar button; i18n key `inventory.export.csv`) |
| Empty state heading | No snapshot loaded |
| Empty state body | Load a vCenter snapshot from the sidebar to browse the inventory. |
| Error state | Snapshot could not be parsed. Reload the snapshot or pick another one from the sidebar. |
| Destructive confirmation | not applicable — no destructive actions in Phase 3 |

State copy detail (all FR+EN, no editorial verbs, no pre-formatted numbers):
- Tree empty (no snapshot): "No snapshot loaded" + body above.
- Tree loading: "Loading inventory…"
- Tree error (parse fail): error-state copy above.
- Table empty (snapshot loaded, zero objects of that type): "No {object} in this snapshot."
- Table zero rows after filter: "No {object} match the current filter." + secondary action: clear filter.
- Cell with absent data: em-dash sentinel `—` (not empty string, not "N/A").

---

## Interaction Contract

**Dashboard↔Inventory toggle**
- Two-segment control reusing the ThemeToggle idiom: `role` segmented control, each segment a button with `aria-pressed`, active segment uses accent gold.
- Default segment: Dashboard. Keyboard-operable (Tab to focus, Enter/Space to switch, arrow keys move between segments).
- Placement: App header/toolbar, leading edge of the primary toolbar, left of the global filter and Export button.

**Inventory layout**
- Left pane: virtualised tree. Right pane: object-table area. Tree↔table gap = 32px (xl). Tree pane uses `.panel`.
- Tree hierarchy (simplest coherent model): synthetic vCenter root → Cluster → ESX host → VM. NO Datacenter level.
- Tree selection scopes the active table: selecting a node filters the right-pane table to objects within that node's subtree. Root selected = unscoped (all objects). Selected row uses accent indicator.

**Virtualised tree**
- Chevron expand/collapse per expandable node (rotates 90°, `aria-expanded`).
- Indentation: 16px per depth level.
- Count badge per node: child/descendant count, Caption size, secondary surface, em-dash never used here (count is always known; 0 shows `0`).
- Lazy children: children materialise on first expand.
- 10k-scroll: row virtualisation keeps DOM windowed; fixed row height; smooth scroll with no layout shift.
- States: empty (no snapshot) / loading / error per copywriting table.

**Three object tables (VM / ESX / Datastore)**
- Tab strip switching VM / ESX / Datastore tables; Datastore table is cluster-agnostic (per Phase-2 A1) — tree scoping above Cluster has no effect on it, and it states no cluster column.
- Columns: each table has a default visible set and a wider available set; show/hide via column-picker (INV-06).
- Global text filter (single input, toolbar) + per-column sort affordance in header (click to cycle asc/desc/none; active column uses accent indicator).
- Sticky header row + sticky first column (both secondary surface).
- Density: compact single-line rows; multi-line source values are one-lined for display (Minor-2) with full text in title attribute; CSV preserves newlines.
- Absent data: em-dash `—` sentinel.
- Numbers/locale: counts, GiB, GHz, % rendered via the inherited Phase-2 formatter (locale-aware display only).

**Column-picker (INV-06)**
- Popover/menu anchored to a "Columns" toolbar button (icon + Label), placed in the toolbar right of the filter.
- Checkbox list of available columns; default columns pre-checked; "Reset" action restores the default set.
- Keyboard-operable; closes on Escape / outside click; selection applies immediately.

**CSV export (INV-05)**
- Toolbar button "Export CSV" (key `inventory.export.csv`), right of the column-picker.
- Exports filtered rows × currently visible columns only.
- Raw values — no locale formatting (this is data, not display): no thousands separators, no unit suffixes injected by the formatter; the underlying numeric/string value is written.
- Multi-line cell values: newlines preserved in the CSV field (quoted per RFC 4180); the same value is one-lined in the table view (Minor-2).
- Filename convention: `vatlas-{object}-{YYYYMMDD}.csv` (object ∈ vm | esx | datastore).

---

## i18n

New namespace `inventory` (FR + EN). No pre-formatted numbers in strings; no editorial verbs. Approximate keys:

```
inventory.nav.dashboard          inventory.nav.inventory
inventory.tab.vm                 inventory.tab.esx
inventory.tab.datastore
inventory.tree.root              inventory.tree.empty
inventory.tree.loading           inventory.tree.error
inventory.filter.placeholder     inventory.filter.clear
inventory.columns.button         inventory.columns.reset
inventory.export.csv
inventory.table.empty            inventory.table.emptyFiltered
inventory.col.<field>            (per-column header labels, VM/ESX/Datastore)
```

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| none (manual Tailwind v4 `@theme` design system) | none | not applicable |
| shadcn official | none — shadcn not initialized | not applicable |
| third-party | none | not applicable |

TanStack Table / TanStack Virtual are npm dependencies, not component-registry blocks; no `shadcn view` vetting applies.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
