---
phase: 09-storage-network-detailed-views-threshold-alerting
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - src/store/snapshotStore.ts
  - src/store/snapshotStore.test.ts
  - src/engines/aggregation/thresholdFlags.ts
  - src/engines/aggregation/thresholdFlags.test.ts
  - src/engines/aggregation/estateView.ts
  - src/engines/aggregation/estateView.test.ts
  - src/types/estate.ts
  - src/hooks/useEstateView.ts
autonomous: true
requirements: [ALR-01, ALR-02, ALR-03, ALR-04]

must_haves:
  truths:
    - "A filesystem partition whose consumed/capacity ≥ the filesystem threshold is flagged; a datastore whose used% > the datastore threshold is flagged; an LU (NAA-keyed datastore) over the LU threshold is flagged"
    - "Threshold flags carry counts + a per-row boolean only — no verdict, no editorial verb, no traffic-light"
    - "The thresholds config is held in the in-memory Zustand inputs slice, REPLACE-never-mutate, with no localStorage key"
    - "clearAll restores the default thresholds (refresh = defaults restored)"
    - "All P9 projections (storage, vsan, network, flags) are produced inside the single buildEstateView pass — no second useMemo anywhere in non-test src/"
    - "D-01: threshold alerting is delivered in this milestone (not deferred) — the thresholdFlags projection + thresholds slice ship in this phase"
    - "D-02: the thresholds config is held in-memory only as a Zustand inputs slice (the plannedRatios class), REPLACE-never-mutate, with NO new localStorage key and no URL-hash — refresh restores defaults"
    - "D-03: default thresholds mirror the RVTools-Analyser defaults — guest filesystem at <10% free / ≥90% used and datastore at >85% used, with the LU/logical-unit default decided here (NAA-keyed datastore used%, 85%) and documented; all user-editable at runtime"
    - "D-04: threshold-flag engine output is factual only — per-row booleans + counts, with no verdict, no editorial verb, and no traffic-light/severity enum"
  artifacts:
    - path: "src/engines/aggregation/thresholdFlags.ts"
      provides: "pure threshold-flag projection driven by the thresholds slice"
      contains: "computeThresholdFlags"
      min_lines: 20
    - path: "src/store/snapshotStore.ts"
      provides: "thresholds inputs slice + setThresholds + selectors + clearAll reset"
      contains: "setThresholds"
    - path: "src/types/estate.ts"
      provides: "storage/vsan/network/flags fields on EstateView"
      contains: "flags"
    - path: "src/engines/aggregation/estateView.ts"
      provides: "single-pass composition of the four new pure calls + frozen EMPTY equivalents"
      contains: "computeThresholdFlags"
  key_links:
    - from: "src/hooks/useEstateView.ts"
      to: "buildEstateView opts.thresholds"
      via: "selectThresholds read + opts thread + memo dep"
      pattern: "thresholds"
    - from: "src/engines/aggregation/estateView.ts"
      to: "relinkBlankClusterDatastores / storageByX / networkRollup / computeThresholdFlags"
      via: "pure calls in the single buildEstateView pass"
      pattern: "computeThresholdFlags|storageByX|relinkBlankClusterDatastores|networkRollup"
    - from: "src/store/snapshotStore.ts"
      to: "clearAll"
      via: "resets thresholds to defaults"
      pattern: "thresholds:\\s*\\{"
---

<objective>
Add the in-memory `thresholds` inputs slice (the `plannedRatios` precedent — no localStorage), the pure `computeThresholdFlags` projection, and compose all four new pure engine calls (`vsanRelink`, `storageByX`, `networkRollup`, `computeThresholdFlags`) into the single `buildEstateView` pass — extending `EstateView` and threading `thresholds` through the one sanctioned `useEstateView` memo.

Purpose: D-01..D-04 (in-memory thresholds + factual flags), D-02 (no new localStorage key, refresh = defaults), and the grep-gated single-`useMemo` invariant (every P9 projection composes in `buildEstateView`; a 2nd `useMemo` fails CI). This plan makes the engines from plans 01-02 reachable by the UI.
Output: thresholds slice + selectors, `thresholdFlags.ts`, extended `estateView.ts`/`EstateView`/`useEstateView` with frozen `EMPTY` equivalents.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-storage-network-detailed-views-threshold-alerting/09-RESEARCH.md
@.planning/phases/09-storage-network-detailed-views-threshold-alerting/09-PATTERNS.md
@.planning/phases/09-storage-network-detailed-views-threshold-alerting/09-01-SUMMARY.md
@.planning/phases/09-storage-network-detailed-views-threshold-alerting/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: thresholds in-memory Zustand inputs slice (the plannedRatios precedent)</name>
  <files>src/store/snapshotStore.ts, src/store/snapshotStore.test.ts</files>
  <read_first>
    - src/store/snapshotStore.ts (modified file — the `plannedRatios` slice: state field line 59, init line 87, action `setPlannedRatios` line 137, `clearAll` reset line 181, selectors lines 209-213; the PAR-05 no-persistence comment lines 21-29)
    - src/store/snapshotStore.test.ts (modified file — the existing plannedRatios slice test structure to mirror)
    - 09-PATTERNS.md §`src/store/snapshotStore.ts` (the exact slice pattern; clearAll MUST reset thresholds; NO new localStorage key — only vatlas-theme/vatlas-lang allowed)
    - 09-RESEARCH.md §Architecture Patterns Pattern 4 + §Open Questions Q2/A3 (LU semantic = NAA-keyed datastore used%; LU default chosen here and documented) and §user_constraints D-03 (fs ≥90% used, ds >85% used; LU default Claude's discretion)
  </read_first>
  <behavior>
    - `thresholds` defaults to `{ fsUsedPct: 90, dsUsedPct: 85, luUsedPct: 85 }`
    - `setThresholds(t)` REPLACES with a fresh object (`set({ thresholds: { ...t } })`) so Zustand `Object.is` fires
    - `clearAll()` resets `thresholds` back to the defaults
    - No `localStorage`/`sessionStorage`/IndexedDB write occurs for thresholds (only the allowed `vatlas-theme`/`vatlas-lang` keys exist)
    - `selectThresholds`/`selectSetThresholds` return stable references (never construct in the selector)
  </behavior>
  <action>
    Add a `thresholds: { fsUsedPct: number; dsUsedPct: number; luUsedPct: number }` field + `setThresholds` action to the store interface, init `thresholds: { fsUsedPct: 90, dsUsedPct: 85, luUsedPct: 85 }` (LU default = 85% per D-03 Claude's discretion: LU = NAA-keyed datastore used%, a second datastore-used% line per RESEARCH A3 — document this rationale in a doc-comment that does NOT spell forbidden verbs literally), `setThresholds: (t) => set({ thresholds: { ...t } })` (REPLACE never mutate), reset to the same defaults inside `clearAll`, and `selectThresholds`/`selectSetThresholds` stable selectors — all mirroring the `plannedRatios` slice exactly. Add NO new localStorage key. Add thresholds tests to `snapshotStore.test.ts` mirroring the plannedRatios tests (default, REPLACE-never-mutate, clearAll resets, no storage write).
  </action>
  <verify>
    <automated>npx vitest run src/store/snapshotStore.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/store/snapshotStore.test.ts` exits 0 with default / REPLACE-never-mutate / clearAll-resets / no-storage-write threshold tests
    - `grep -c "setThresholds\|selectThresholds" src/store/snapshotStore.ts` ≥ 3
    - `grep -c "localStorage\|sessionStorage\|indexedDB" src/store/snapshotStore.ts` shows no NEW persistence call referencing thresholds (only the pre-existing theme/lang usage)
    - `grep -n "thresholds" src/store/snapshotStore.ts` shows a reset inside the `clearAll` body
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0
  </acceptance_criteria>
  <done>The thresholds in-memory inputs slice mirrors plannedRatios exactly: defaults restored on clearAll, REPLACE-never-mutate, no new localStorage key; stable selectors; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: thresholdFlags.ts — pure factual flag projection (no verdict, no traffic-light)</name>
  <files>src/engines/aggregation/thresholdFlags.ts, src/engines/aggregation/thresholdFlags.test.ts, src/engines/aggregation/index.ts</files>
  <read_first>
    - src/engines/aggregation/guestData.ts (the pure-aggregate-helper analog consumed by estateView.ts)
    - src/types/snapshot.ts (`VPartitionRow` `Capacity MiB`/`Consumed MiB` lines 93-99 — the filesystem-used signal; do NOT add a freePct column, compute the ratio)
    - 09-PATTERNS.md §`src/engines/aggregation/thresholdFlags.ts` (signature `(vpartition, datastores, thresholds) → { fsFlagged, dsFlagged, luFlagged, counts }`; ratio = consumedMib/capacityMib; LU = NAA-keyed datastore used%; D-04 return counts + boolean only; the grep-gate gotcha: doc-comments must NOT spell forbidden tokens literally)
    - 09-RESEARCH.md §user_constraints D-04 + §Security Domain (factual only) and §Open Questions A3 (LU semantic)
  </read_first>
  <behavior>
    - A partition with `consumedMib/capacityMib ≥ fsUsedPct/100` is flagged; below is not
    - A datastore with used% > `dsUsedPct` is flagged; an NAA-keyed datastore (LU) over `luUsedPct` is flagged on the LU line
    - The result is `{ fsFlagged: boolean[], dsFlagged: boolean[], luFlagged: boolean[], counts: { fs, ds, lu } }` — no string verdict, no severity enum, no color
    - A partition with `capacityMib === 0` does not divide-by-zero (treated as not-flagged / not-derivable, never `Infinity`/`NaN`)
  </behavior>
  <action>
    Create `thresholdFlags.ts` exporting a pure `computeThresholdFlags(vpartition, datastores, thresholds)` returning per-row boolean flags + counts only. Filesystem signal = `consumedMib / capacityMib` from `VPartitionRow` (compute the ratio — do NOT add a column, the smaller-blast-radius RESEARCH choice); guard `capacityMib === 0`. Datastore flag = datastore used% > `dsUsedPct`. LU flag = NAA-keyed (`naa ?? name`) datastore used% > `luUsedPct` (the RESEARCH A3 semantic). Return `{ fsFlagged, dsFlagged, luFlagged, counts }` — D-04: counts + booleans only, NO verdict string, NO severity enum, NO color. Doc-comments must NOT spell the denylisted verbs/severity words literally (the CLAUDE.md grep-gate/security-hook gotcha — phrase the "no judgment" intent without the literal tokens). Add the barrel export. Add `thresholdFlags.test.ts`: default thresholds, edited thresholds, the boundary (`≥`/`>` exactness), and the `capacityMib === 0` guard.
  </action>
  <verify>
    <automated>npx vitest run src/engines/aggregation/thresholdFlags.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/engines/aggregation/thresholdFlags.test.ts` exits 0 with default, edited-threshold, boundary, and zero-capacity-guard tests
    - `grep -ric "recommend\|should\|poor\|good\|critical\|warning\|traffic" src/engines/aggregation/thresholdFlags.ts` returns 0 (no denylisted token, including in doc-comments)
    - `grep -rn "import .*react\|zustand\|from 'zod'" src/engines/aggregation/thresholdFlags.ts` returns nothing
    - `npx vitest run --coverage src/engines/aggregation/thresholdFlags.ts` reports ≥75%
    - `npx @biomejs/biome check src/engines/aggregation/thresholdFlags.ts` exits 0
  </acceptance_criteria>
  <done>computeThresholdFlags returns factual per-row booleans + counts only (no verdict/severity/color), guards zero capacity, pure, ≥75% covered, no denylisted token anywhere; barrel-exported; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Compose the four pure calls into the single buildEstateView pass + thread thresholds through the one memo</name>
  <files>src/engines/aggregation/estateView.ts, src/engines/aggregation/estateView.test.ts, src/types/estate.ts, src/hooks/useEstateView.ts</files>
  <read_first>
    - src/engines/aggregation/estateView.ts (modified file — the single-pass extension shape: pure calls beside `perDatastore`/`buildEosProjection`/`buildTrendSeries` ~lines 112/272/284, the `opts` bag ~lines 72-88, the return literal lines 286-305, `EMPTY_INSIGHTS`/`EMPTY_EOS` `Object.freeze` lines 307-347, the frozen `EMPTY_VIEW` lines 354-372)
    - src/types/estate.ts (modified file — the `EstateView` interface lines 448-498; the `T | null` / always-present doc-comment idiom "Produced inside the single buildEstateView pass — no second useMemo")
    - src/hooks/useEstateView.ts (modified file — the `planned` slice threading: selector read line 47, `opts` thread line 58, memo dep array line 60 — the ONLY useMemo)
    - 09-PATTERNS.md §`estateView.ts` + §`useEstateView.ts` + §Shared Patterns "Single useMemo invariant (grep-gated)"
    - 09-RESEARCH.md §Architecture Patterns Pattern 2 (the `const x = pureCall(...)` + extend-return + EMPTY-frozen shape)
  </read_first>
  <behavior>
    - `buildEstateView` calls `relinkBlankClusterDatastores`, `storageByX`, `networkRollup`, `computeThresholdFlags` in its existing single pass and returns `storage`/`vsan`/`network`/`flags` on `EstateView`
    - `opts.thresholds` defaults (RVTools-Analyser defaults) when absent, mirroring `opts?.stretchedClusters ?? new Set()`
    - `EMPTY_VIEW` has frozen empty equivalents for all four new fields
    - `useEstateView` reads `selectThresholds`, passes it in `opts`, and adds it to the memo dep array — and remains the ONLY `useMemo` in non-test `src/`
    - `npx vitest run src/engines/aggregation/estateView.test.ts` confirms the four new fields are present on a built view and empty-but-typed on `EMPTY_VIEW`
  </behavior>
  <action>
    In `estateView.ts`, add `const vsan = relinkBlankClusterDatastores(merged.vinfo, merged.vdatastore)`, `const storage = storageByX(merged, mode, vsan)`, `const network = networkRollup(merged)`, `const flags = computeThresholdFlags(merged.vpartition, datastores, opts?.thresholds ?? DEFAULT_THRESHOLDS)` inside the existing single pass (beside the shipped pure calls); add a `thresholds?` field to the `opts` bag with the `?? DEFAULT` defaulting (mirror `stretchedClusters ?? new Set()`); extend the return literal with `storage, vsan, network, flags`. Add `EMPTY_STORAGE`/`EMPTY_VSAN`/`EMPTY_NETWORK`/`EMPTY_FLAGS` `Object.freeze({...})` constants (copy the `EMPTY_INSIGHTS`/`EMPTY_EOS` shape) and add them to the frozen `EMPTY_VIEW` literal. Add `storage`/`vsan`/`network`/`flags` fields to the `EstateView` interface in `estate.ts` with the always-present "produced inside the single buildEstateView pass — no second useMemo" doc-comment idiom. In `useEstateView.ts` add `const thresholds = useSnapshotStore(selectThresholds)`, pass `thresholds` into the `buildEstateView` `opts` object, and add it (or its scalar fields) to the single `useMemo` dep array. Introduce NO second `useMemo`. Extend `estateView.test.ts` to assert the four fields are present on a built view and empty-but-typed on `EMPTY_VIEW`.
  </action>
  <verify>
    <automated>npx vitest run src/engines/aggregation/estateView.test.ts src/hooks && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/engines/aggregation/estateView.test.ts` exits 0 asserting `storage`/`vsan`/`network`/`flags` present on a built view and frozen-empty on `EMPTY_VIEW`
    - `grep -c "computeThresholdFlags\|storageByX\|relinkBlankClusterDatastores\|networkRollup" src/engines/aggregation/estateView.ts` ≥ 4
    - `grep -rn --include='*.ts' --include='*.tsx' "useMemo" src/ | grep -v '\.test\.' | grep -v node_modules | wc -l` equals 1 (the single useEstateView memo — the grep-gated invariant holds)
    - `grep -c "thresholds" src/hooks/useEstateView.ts` > 0 (selector read + opts thread + dep)
    - `npx @biomejs/biome check .` exits 0
  </acceptance_criteria>
  <done>All four P9 pure projections compose inside the single buildEstateView pass and are returned on EstateView with frozen EMPTY equivalents; thresholds threads through the one sanctioned memo; no second useMemo exists; tests + typecheck + Biome green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Zustand thresholds slice → buildEstateView | User-entered threshold numbers cross from the in-memory store into the pure view builder |
| store/engines → in-memory only | No persistence boundary is crossed (the residual surface to keep closed) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | Information Disclosure (privacy) | thresholds slice | mitigate | In-memory-only slice (the plannedRatios precedent); NO new localStorage key; `clearAll` resets to defaults so refresh = data gone (PAR-05/D-02); slice holds numbers only — never a persistence/exfil path |
| T-09-08 | Tampering (data integrity / divide-by-zero DoS) | computeThresholdFlags ratio | mitigate | Guard `capacityMib === 0` so a malformed partition yields not-derivable rather than `Infinity`/`NaN`; flags are factual booleans only (D-04) |
| T-09-09 | Repudiation/Tampering (invariant erosion) | the single-useMemo composition | mitigate | All P9 projections compose in the one `buildEstateView` pass; the grep-count acceptance gate enforces exactly one `useMemo` in non-test `src/` |
</threat_model>

<verification>
- `npx vitest run src/engines/aggregation src/store/snapshotStore.test.ts src/hooks` exits 0
- `npx vitest run --coverage src/engines/aggregation/` reports ≥75% for thresholdFlags + the plan-02 engines
- exactly one `useMemo` in non-test `src/` (the grep-gated invariant)
- `npx tsc --noEmit -p tsconfig.app.json` and `npx @biomejs/biome check .` exit 0
</verification>

<success_criteria>

- thresholds in-memory slice mirrors plannedRatios (no localStorage, clearAll resets, REPLACE-never-mutate)
- computeThresholdFlags is factual-only (booleans + counts, no verdict/severity/color, no denylisted token)
- The four P9 projections compose in the single buildEstateView pass and are reachable via the one useEstateView memo
- No second useMemo introduced
</success_criteria>

<output>
After completion, create `.planning/phases/09-storage-network-detailed-views-threshold-alerting/09-03-SUMMARY.md`
</output>
