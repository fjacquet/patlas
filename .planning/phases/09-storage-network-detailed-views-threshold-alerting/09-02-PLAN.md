---
phase: 09-storage-network-detailed-views-threshold-alerting
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/engines/aggregation/vsanRelink.ts
  - src/engines/aggregation/vsanRelink.test.ts
  - src/engines/aggregation/storageByX.ts
  - src/engines/aggregation/storageByX.test.ts
  - src/engines/aggregation/network.ts
  - src/engines/aggregation/network.test.ts
  - src/engines/aggregation/index.ts
autonomous: true
requirements: [STG-01, STG-02, STG-03, NET-04, VSR-01, VSR-02, VSR-03]

must_haves:
  truths:
    - "A blank-`Cluster name` datastore that a VM's `vInfo.Path` references is attributed to that VM's cluster"
    - "A blank-cluster datastore no VM references stays estate-only with no fabricated cluster identity"
    - "A datastore whose referencing VMs span >1 cluster is surfaced as 'shared across N clusters' and excluded from single-cluster rollups (no proportional guess, no double-count)"
    - "Storage-by-X totals reconcile to the estate total under both lenses with no double-count of shared LUNs"
    - "Network rollups produce vSwitch/dvSwitch/portgroup/uplink aggregates from the parsed network arrays"
    - "D-09: blank-`Cluster name` datastores are attributed via the vInfo VM→datastore→cluster relink keyed off `vInfo.Path` (the P5-deferred relink, closing the STR-04 / Pitfall-6 vSAN under-count); zero-reference datastores stay estate-only with no fabricated cluster identity"
    - "D-10: a shared LUN (datastore whose referencing VMs span >1 cluster) uses the minimal-inference factual choice — surfaced as `shared across N clusters` and excluded from single-cluster rollups, with no proportional split and no double-count"
    - "D-07: storage-by-X ships both lenses — (a) provisioned vs in-use and (b) datastore capacity/used/free — each reconciling to the estate total with no double-count"
    - "D-08: the storage-by-X engine produces the datastore-footprint aggregates the treemap (consumption lens) and stacked-bar (capacity lens) consume"
  artifacts:
    - path: "src/engines/aggregation/vsanRelink.ts"
      provides: "relinkBlankClusterDatastores — vInfo.Path → datastore → cluster, shared-LUN handling"
      contains: "relinkBlankClusterDatastores"
      min_lines: 30
    - path: "src/engines/aggregation/storageByX.ts"
      provides: "storage-by-cluster/esx/vm/datastore, two lenses"
      contains: "storageByX"
      min_lines: 30
    - path: "src/engines/aggregation/network.ts"
      provides: "vswitch/dvswitch/portgroup/uplink rollups"
      contains: "networkRollup"
    - path: "src/engines/aggregation/vsanRelink.test.ts"
      provides: "synthetic relink + shared-LUN coverage (real-file gate is plan 05)"
      contains: "sharedAcross"
  key_links:
    - from: "src/engines/aggregation/vsanRelink.ts"
      to: "merged.vinfo[].path"
      via: "anchored bracket regex on vInfo.Path"
      pattern: "\\\\\\[\\(\\[\\^\\\\\\]\\]"
    - from: "src/engines/aggregation/storageByX.ts"
      to: "perDatastore first-row-capacity rule"
      via: "naa ?? name dedupe, never re-sum shared-LUN capacity"
      pattern: "naa"
    - from: "src/engines/aggregation/index.ts"
      to: "the three new engines"
      via: "barrel re-export"
      pattern: "vsanRelink|storageByX|network"
---

<objective>
Build the three new pure aggregation engines: the `vInfo.Path` vSAN relink (closes the open STR-04 vSAN under-count), the two-lens storage-by-X projection, and the network topology rollup. All are pure, Vitest-gated ≥75%, and compose into the single `buildEstateView` pass (no new `useMemo`) in plan 04.

Purpose: D-09/D-10 (vSAN relink + shared-LUN), D-07 (two storage lenses), D-11 (network rollups). The relink is the load-bearing correctness change — it MUST key off `vInfo.Path` (not `vDisk."Disk Path"`, which is empirically guest mount points). Synthetic tests here; the mandatory real-file validation gate is plan 05.
Output: `vsanRelink.ts`, `storageByX.ts`, `network.ts` + tests + barrel exports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-storage-network-detailed-views-threshold-alerting/09-RESEARCH.md
@.planning/phases/09-storage-network-detailed-views-threshold-alerting/09-PATTERNS.md
@.planning/phases/09-storage-network-detailed-views-threshold-alerting/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: vsanRelink.ts — vInfo.Path → datastore → cluster, shared-LUN, no double-attribution</name>
  <files>src/engines/aggregation/vsanRelink.ts, src/engines/aggregation/vsanRelink.test.ts, src/engines/aggregation/index.ts</files>
  <read_first>
    - src/engines/aggregation/perDatastore.ts (the analog — `datastoreCountByCluster` lines 51-79 Map-accumulate join; `dedupeKey = row.naa ?? row.name` line 21 — load-bearing; the first-row-capacity rule lines 91-109; the inert `splitHosts`/`hostClusterMap` Pitfall-6 path lines 28-32/67-74 that must NOT be deleted)
    - src/engines/aggregation/index.ts (modified file — the `export { perDatastore } from './perDatastore'` barrel convention)
    - 09-PATTERNS.md §`src/engines/aggregation/vsanRelink.ts` (the verbatim anchored-bracket regex, the `dsToClusters` Map-accumulate, the shared-LUN / unrelinkable rules, the no-double-attribution + never-re-sum-capacity anti-patterns)
    - 09-RESEARCH.md §Architecture Patterns Pattern 5, §Common Pitfalls Pitfall 2 (the STR-04 repeat), §Code Examples "Empirical relink result" (25/67 resolve, 1 shared-LUN, 42 unrelinkable — the expected real-file shape, validated in plan 05)
    - ~/.claude/projects/-Users-fjacquet-Projects-vatlas/memory/project_rvtools_vdatastore_hosts_is_count.md (vDatastore.Hosts is a COUNT — the relink is the only blank-cluster attribution path)
  </read_first>
  <behavior>
    - A synthetic vInfo row with `path: '[DS_A] vm/vm.vmx'` and `cluster: 'CL_1'` attributes blank-cluster datastore `DS_A` to `{CL_1}`
    - A blank-cluster datastore referenced by zero VMs is returned as unrelinkable (estate-only; the caller renders an em-dash) — never attributed to a cluster
    - A datastore whose referencing VMs span clusters `{CL_1, CL_2}` is flagged shared with `clusterCount: 2` and is NOT counted in any single-cluster rollup
    - The anchored regex `/^\[([^\]]+)\]/` is used (linear, no backtracking); a row with no parseable Path is skipped (factual), not errored
    - The inert `splitHosts`/`hostClusterMap` path in `perDatastore.ts` is untouched and produces no competing attribution (assert no double-attribution)
  </behavior>
  <action>
    Create `vsanRelink.ts` exporting a pure `relinkBlankClusterDatastores(vinfo, vdatastore)` that builds `dsToClusters: Map<string, Set<string>>` by running the verbatim anchored regex from PATTERNS over `vm.path ?? ''`, skipping unparseable rows and empty clusters. Dedupe datastores with the shipped `naa ?? name` key (copy `perDatastore.ts` line 21 verbatim — one blank ds has empty NAA so the name fallback is load-bearing). For each blank-`Cluster name` datastore: size-1 cluster set ⇒ attribute to that cluster; size-0 ⇒ mark unrelinkable (estate-only, caller renders em-dash, NEVER fabricated); size>1 ⇒ mark shared with `clusterCount = set.size` and EXCLUDE from single-cluster rollups (D-10 minimal-inference — no proportional split, no double-count; the empirically-rare 1/67 case). Attribute identity only — never re-sum shared-LUN capacity (perDatastore already takes capacity from the first key-group row; preserve that). Do NOT delete or modify `perDatastore.ts`'s `splitHosts`/`hostClusterMap`. Add `export { relinkBlankClusterDatastores } from './vsanRelink'` to `index.ts`. Add the synthetic + shared-LUN + unrelinkable + no-double-attribution unit tests to `vsanRelink.test.ts`.
  </action>
  <verify>
    <automated>npx vitest run src/engines/aggregation/vsanRelink.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/engines/aggregation/vsanRelink.test.ts` exits 0 with synthetic-relink, shared-LUN, unrelinkable, and no-double-attribution tests present
    - `grep -c "naa" src/engines/aggregation/vsanRelink.ts` > 0 (the `naa ?? name` dedupe key reused)
    - `git diff --stat src/engines/aggregation/perDatastore.ts` shows no changes (inert Pitfall-6 path untouched)
    - `grep -rn "import .*react\|zustand\|from 'zod'" src/engines/aggregation/vsanRelink.ts` returns nothing (pure engine — no React/Zustand/Zod)
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0
  </acceptance_criteria>
  <done>relinkBlankClusterDatastores attributes blank-cluster datastores via vInfo.Path, surfaces shared-LUN as "shared across N", leaves unrelinkable datastores estate-only, never double-attributes or re-sums capacity; pure engine; barrel-exported; synthetic tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: storageByX.ts — two-lens storage-by-Cluster/ESX/VM/Datastore, reconcile-to-estate</name>
  <files>src/engines/aggregation/storageByX.ts, src/engines/aggregation/storageByX.test.ts, src/engines/aggregation/index.ts</files>
  <read_first>
    - src/engines/aggregation/perDatastore.ts (the rollup analog — lines 81-112 Map-accumulate reduce + `mib()` branding + the Moderate-11 first-row-capacity rule lines 94-97)
    - src/engines/aggregation/perEsx.ts (per-ESX Map-accumulate analog)
    - src/engines/aggregation/vsanRelink.ts (this plan's task-1 output — storageByX consumes the relink attribution so blank-cluster storage lands on the right cluster)
    - 09-PATTERNS.md §`storageByX.ts / network.ts` (the rollup pattern, the two-lens definition: consumption = provisioned-vs-in-use incl `.vswp`+snapshots via `vInfo.inUseMib`; capacity = capacity-vs-used-vs-free from `DatastoreAggregate`; the no-double-count reconciliation rule)
    - 09-RESEARCH.md §Architecture Patterns Pattern 2 + State of the Art (the storage-by-X projections are new pure projections, same single-pass pattern)
  </read_first>
  <behavior>
    - Consumption lens produces provisioned + in-use totals per Cluster/ESX/VM/Datastore where in-use already includes `.vswp`+snapshots (it rides on the shipped `vInfo.inUseMib`)
    - Capacity lens produces capacity/used/free per Datastore from the deduped DatastoreAggregate
    - Summing any lens across all groups equals the estate total (no double-count); a shared-LUN datastore is counted once at estate scope and excluded from per-cluster sums
    - All MiB sums are branded via `mib()`; no raw `* 1.048576`
  </behavior>
  <action>
    Create `storageByX.ts` exporting a pure `storageByX(merged, mode, vsan)` that produces the two-lens projection grouped by Cluster, ESX, VM, and Datastore using the `perDatastore.ts` Map-accumulate-then-reduce shape, branding sums with `mib()`. Consumption lens = provisioned vs in-use (in-use rides on the shipped `vInfo.inUseMib`, which already includes `.vswp`+snapshots — do not re-derive). Capacity lens = capacity/used/free from the deduped `DatastoreAggregate` (`naa ?? name`, first-row capacity — never re-sum, the Moderate-11 rule). Use the `vsan` attribution from task 1 so blank-cluster storage lands on the relinked cluster and shared-LUN datastores are excluded from single-cluster rollups while remaining in the estate total. Add the barrel export. Add `storageByX.test.ts` asserting both lenses reconcile to the estate total with no double-count of a shared LUN.
  </action>
  <verify>
    <automated>npx vitest run src/engines/aggregation/storageByX.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/engines/aggregation/storageByX.test.ts` exits 0 with a reconcile-to-estate-total test and a shared-LUN-not-double-counted test
    - `grep -c "mib(" src/engines/aggregation/storageByX.ts` > 0 (branded sums)
    - `grep -rn "1.048576" src/engines/aggregation/storageByX.ts` returns nothing
    - `grep -rn "import .*react\|zustand\|from 'zod'" src/engines/aggregation/storageByX.ts` returns nothing
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0
  </acceptance_criteria>
  <done>storageByX produces both lenses by Cluster/ESX/VM/Datastore, reconciles to the estate total with no shared-LUN double-count, branded-unit-safe; pure; barrel-exported; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: network.ts — vSwitch/dvSwitch/portgroup/uplink rollups</name>
  <files>src/engines/aggregation/network.ts, src/engines/aggregation/network.test.ts, src/engines/aggregation/index.ts</files>
  <read_first>
    - src/engines/aggregation/perDatastore.ts (the Map-accumulate rollup analog)
    - src/types/snapshot.ts (the V{Network,Switch,DvSwitch,DvPort}Row interfaces from plan 01)
    - 09-PATTERNS.md §`storageByX.ts / network.ts` (network rolls up vswitch/dvswitch/portgroup/uplink the same Map-accumulate way)
    - 09-RESEARCH.md §Code Examples real 4.x headers (the key columns per sheet) + §Common Pitfalls Pitfall 1 (network arrays may all be empty — the rollup must return empty aggregates, never throw)
  </read_first>
  <behavior>
    - With populated vswitch/dvswitch/vnetwork arrays, networkRollup returns per-switch and per-portgroup aggregates (port counts, VLANs, uplinks, VM→portgroup mapping)
    - With all four network arrays empty (the trimmed-export case), networkRollup returns empty aggregates and does not throw
  </behavior>
  <action>
    Create `network.ts` exporting a pure `networkRollup(merged)` that Map-accumulates: vSwitch (standard) by host+switch with `# Ports`/`Free Ports`/`MTU`; dvSwitch (distributed) + dvPort by switch with `# Ports`/`# VMs`/VLAN/uplinks; vNetwork as the VM→portgroup/VLAN mapping. Empty input arrays ⇒ empty aggregates (never throw — the trimmed-export degrade is handled in the view shell, plan 05, but the engine must tolerate empty input). Add the barrel export. Add `network.test.ts` covering a populated rollup and the all-empty case.
  </action>
  <verify>
    <automated>npx vitest run src/engines/aggregation/network.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/engines/aggregation/network.test.ts` exits 0 with a populated-rollup and an all-empty-input test
    - `grep -c "networkRollup" src/engines/aggregation/index.ts` > 0
    - `grep -rn "import .*react\|zustand\|from 'zod'" src/engines/aggregation/network.ts` returns nothing
    - `npx vitest run --coverage src/engines/aggregation/` reports ≥75% for the three new engine files
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0
  </acceptance_criteria>
  <done>networkRollup produces switch/portgroup/uplink aggregates and tolerates all-empty input; pure; barrel-exported; the three new engines meet the ≥75% coverage gate.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parsed rows → aggregation engines | Parsed (already Zod-validated in plan 01) but operator-originated content (datastore/VM/portgroup names) flows into pure engines and onward to the UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | DoS (ReDoS) | the `vInfo.Path` bracket regex in vsanRelink.ts | mitigate | Use the verbatim anchored, negated-char-class, non-backtracking regex `/^\[([^\]]+)\]/` from PATTERNS; do NOT "improve" it into a backtracking pattern (documented in RESEARCH §Security Domain) |
| T-09-05 | Spoofing/Tampering (data integrity) | blank-cluster attribution | mitigate | Attribute only on a parseable `vInfo.Path` token; zero references ⇒ unrelinkable estate-only (no fabricated cluster identity — the no-domain-guesses invariant); shared-LUN excluded from single-cluster rollups (no allocation guess) |
| T-09-06 | Tampering (privacy) | new pure engines | accept | Pure functions, no persistence, no network, no DOM (engines purity invariant) — existing structural mitigation, not re-planned |
</threat_model>

<verification>
- `npx vitest run src/engines/aggregation/` exits 0
- `npx vitest run --coverage src/engines/aggregation/` reports ≥75% for vsanRelink/storageByX/network
- `npx tsc --noEmit -p tsconfig.app.json` exits 0
- `npx @biomejs/biome check .` exits 0
- `git diff --stat src/engines/aggregation/perDatastore.ts` shows no changes
</verification>

<success_criteria>

- The vSAN relink keys off `vInfo.Path`, surfaces shared-LUN as "shared across N", leaves unrelinkable datastores estate-only, never double-attributes or re-sums capacity
- storage-by-X delivers both lenses reconciling to the estate total with no shared-LUN double-count
- networkRollup produces the switch/portgroup/uplink aggregates and tolerates empty input
- All three engines pure and ≥75% covered
</success_criteria>

<output>
After completion, create `.planning/phases/09-storage-network-detailed-views-threshold-alerting/09-02-SUMMARY.md`
</output>
