---
phase: 09-storage-network-detailed-views-threshold-alerting
plan: 05
type: execute
wave: 5
depends_on: [04]
files_modified:
  - src/i18n/index.ts
  - src/i18n/locales/en/network.json
  - src/i18n/locales/fr/network.json
  - src/components/network/NetworkView.tsx
  - src/components/inventory/columns/networkColumns.ts
  - src/components/hosts/HostsView.tsx
  - src/components/hosts/EsxDetail.tsx
  - src/App.tsx
  - src/engines/aggregation/vsanRelink.realfile.test.ts
autonomous: true
requirements: [NET-05, DTL-03, VSR-05]

must_haves:
  truths:
    - "The Network view shows vSwitch (standard), dvSwitch+dvPort (distributed), and vNetwork (VM→portgroup/VLAN) tables via the P3 DataTable idiom"
    - "When the workbook has no network sheets the Network view shows the factual 'network inventory not available in this export' line with no error styling, no icon, no crash"
    - "Clicking a host in the Hosts view drills into a screen-fit ESX storage+network detail (datastores mounted + vSwitch/dvSwitch/uplinks) with a back affordance"
    - "On the real 75-blank-cluster workbook the vSAN relink resolves a non-zero count of blank-cluster datastores (the STR-04 regression guard)"
    - "D-06: the ESX storage+network detail augments the shipped P5 Hosts view via lifted in-Hosts drill state — it does NOT duplicate the P5 cluster-detail drill nor add a router or a second useMemo"
    - "D-11: the Network view consumes the OPTIONAL network inventory and, when the workbook has no network sheets, renders the single factual `network inventory not available` line (no error styling, no icon, no crash, no editorial verb)"
    - "D-09: the MANDATORY real-file relink validation gate asserts a non-zero blank-cluster relink count against the named 75-blank-cluster workbook (`20260430_1400_allvCenters.xlsx`) — unit tests alone are insufficient; a count of 0 is the STR-04 failure mode and MUST fail"
    - "D-10: the real-file gate also validates the shared-LUN behavior on the real multi-cluster workbook — the empirically-observed shared LUN is surfaced as `shared across N clusters` and excluded from single-cluster rollups"
  artifacts:
    - path: "src/components/network/NetworkView.tsx"
      provides: "network tables + optional-sheet factual degrade"
      contains: "useEstateView"
      min_lines: 30
    - path: "src/components/hosts/EsxDetail.tsx"
      provides: "screen-fit ESX storage+network drill augmenting HostsView"
      contains: "onBack"
    - path: "src/engines/aggregation/vsanRelink.realfile.test.ts"
      provides: "env-guarded real-file relink assertion (the STR-04 guard)"
      contains: "20260430_1400_allvCenters"
  key_links:
    - from: "src/components/network/NetworkView.tsx"
      to: "view.network"
      via: "single useEstateView consumer + plain props"
      pattern: "view\\.network"
    - from: "src/components/hosts/HostsView.tsx"
      to: "src/components/hosts/EsxDetail.tsx"
      via: "lifted useState drill + onBack body-replace"
      pattern: "EsxDetail"
    - from: "src/engines/aggregation/vsanRelink.realfile.test.ts"
      to: "relinkBlankClusterDatastores"
      via: "readFileSync real workbook + assert relink count > 0"
      pattern: "relinkBlankClusterDatastores"
---

<objective>
Ship the Network side of the UI (Network view + ESX storage+network detail augmenting the shipped Hosts view) and the MANDATORY real-file validation gate that proves the vSAN relink resolves a non-zero count of blank-cluster datastores on the real 75-blank-cluster workbook — the regression guard the binding `rvtools-vdatastore-hosts-is-count` memory and CONTEXT D-09 demand (unit tests alone are insufficient).

Purpose: D-11 (network inventory + the OPTIONAL-sheet factual degrade — the Downloads 8-sheet workbook has zero network sheets), D-06 (ESX detail augments Hosts, does NOT duplicate the P5 cluster-detail drill), D-09 (real-file relink validation — the STR-04 closure gate). Per binding LC-3/LC-4.
Output: `network` i18n namespace, `src/components/network/NetworkView.tsx` + network column defs, the ESX detail drill wired into HostsView, the App route, and the env-guarded real-file relink test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-storage-network-detailed-views-threshold-alerting/09-UI-SPEC.md
@.planning/phases/09-storage-network-detailed-views-threshold-alerting/09-PATTERNS.md
@.planning/phases/09-storage-network-detailed-views-threshold-alerting/09-RESEARCH.md
@.planning/phases/09-storage-network-detailed-views-threshold-alerting/09-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: NetworkView shell + network column defs + factual optional-sheet degrade + App route</name>
  <files>src/components/network/NetworkView.tsx, src/components/inventory/columns/networkColumns.ts, src/i18n/index.ts, src/i18n/locales/en/network.json, src/i18n/locales/fr/network.json, src/App.tsx</files>
  <read_first>
    - src/components/hosts/HostsView.tsx (the view-shell analog — single `useEstateView` consumer)
    - src/components/inventory/DatastoreTable.tsx + src/components/inventory/columns/datastoreColumns.ts (the thin-wrapper + column-def analog; the Pitfall-3 `inventory:col.<id>` requirement)
    - src/components/dashboard/GlobalDashboard.tsx (the inline per-region `<ErrorBoundary FallbackComponent={DashboardError}>` pattern lines 26-37/92-98 — message-only, never `error.cause`/`stack`)
    - src/i18n/index.ts + src/i18n/locales/en/inventory.json (modified file — register `network` namespace; `nav.network` already added in plan 04)
    - src/App.tsx (modified file — add the `activeView === 'network' ? <NetworkView />` branch)
    - 09-UI-SPEC.md §LC-3 (binding: vSwitch / dvSwitch+dvPort / vNetwork tables; the factual `network:empty.unavailable` line in caption style `text-[12px]`/`text-xs text-slate-500 dark:text-slate-400`, no error styling, no icon, no crash, no editorial verb) + §Copywriting Contract (the exact EN/FR strings)
    - 09-PATTERNS.md §`NetworkView.tsx` / §Storage-Network tables + column defs; 09-RESEARCH.md §Common Pitfalls Pitfall 1
  </read_first>
  <action>
    Create `networkColumns.ts` modeled on `datastoreColumns.ts` (accessor/id/cell, `defaultVisible`) for the three network tables; for EVERY new column id add the `inventory:col.<id>` key to BOTH `en/`+`fr/inventory.json` (Pitfall 3). Create `NetworkView.tsx` modeled on `HostsView` + the `DatastoreTable` wrapper: ONE `useEstateView` call, three P3 `DataTable` sections (vSwitch standard; dvSwitch+dvPort distributed; vNetwork VM→portgroup/VLAN) consuming `view.network` as plain props (no child `useMemo`), each wrapped in the inline `<ErrorBoundary FallbackComponent={DashboardError}>` pattern. When `view.network` is empty (all four parsed arrays absent — the trimmed-export case), render the single factual `network:empty.unavailable` line in the shipped caption style — no error styling, no icon, no crash, no editorial verb (mirror the shipped `vDatastore`/`vPartition` empty pattern). Create `network.json` in BOTH `en/`+`fr/` with the LC-3/Copywriting strings (EN/FR parity, no editorial verbs, no pre-formatted numbers); register `network` in `i18n/index.ts` `NAMESPACES`+`resources.en`+`resources.fr` (if plan 04 already registered it, this is a no-op — verify). Add the `activeView === 'network' ? <NetworkView />` branch to `App.tsx`.
  </action>
  <verify>
    <automated>npx vitest run src/components/network && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/network` exits 0 with a populated-tables test AND a network-sheets-absent test asserting the factual line renders and nothing throws
    - `grep -c "useEstateView" src/components/network/NetworkView.tsx` equals 1
    - `grep -rn --include='*.tsx' "useMemo" src/components/network/ | grep -v '\.test\.'` returns nothing
    - every column id in `networkColumns.ts` has a matching `col.<id>` key in BOTH en/ and fr/inventory.json
    - `grep -ric "recommend\|should\|poor\|good\|critical\|warning\|bg-red\|amber\|traffic" src/components/network/ src/i18n/locales/en/network.json` returns 0
    - `npx @biomejs/biome check src/components/network/ src/App.tsx` exits 0
  </acceptance_criteria>
  <done>NetworkView shows the three tables via the P3 DataTable idiom and factual-degrades to a single neutral line when network sheets are absent; new columns have inventory keys; App routes to it; EN/FR parity; no editorial verbs.</done>
</task>

<task type="auto">
  <name>Task 2: ESX storage+network detail drill augmenting HostsView</name>
  <files>src/components/hosts/EsxDetail.tsx, src/components/hosts/HostsView.tsx</files>
  <read_first>
    - src/components/cluster/ClusterDetail.tsx (the detail-drill analog — `{ detail, onBack }` props, screen-fit `overflow-hidden p-8` shell, the `← {t('detail.back')}` button, the `Row` sub-component, the `na = t('na')` em-dash sentinel)
    - src/components/hosts/HostsView.tsx (modified file — the shipped Hosts view; add a lifted `useState<string|null>` for the drilled host + the `if (esxDetail) return <EsxDetail .../>` body-replace, exactly the GlobalDashboard cluster-drill precedent — NOT a duplicate of the P5 cluster-detail drill, NOT App.tsx state)
    - 09-UI-SPEC.md §LC-4 (binding screen-fit / one-PPTX-slide contract; ESX detail = datastores mounted + vSwitch/dvSwitch/uplinks per host; AUGMENTS Hosts, does not duplicate ClusterDetail)
    - 09-PATTERNS.md §`DatastoreDetail.tsx / VmDetail.tsx + ESX detail` (ESX detail same `{detail,onBack}` shape, drill state lifted into HostsView, the GlobalDashboard wiring precedent)
  </read_first>
  <action>
    Create `EsxDetail.tsx` copying `ClusterDetail.tsx`'s `{ detail, onBack }` props, screen-fit shell (`overflow-hidden p-8`, `flex h-full flex-col gap-4`, the fixed grid), header (`<h2>` + the `← {t('detail.back')}` button verbatim classes), the `Row` sub-component, and the `na = t('na')` em-dash sentinel — verbatim. Rows: datastores mounted on the host + per-host vSwitch/dvSwitch/uplinks (sourced from `view.network`/`view.storage`). Wire it into `HostsView.tsx` by adding a lifted `useState<string|null>` for the drilled host and an `if (esxDetail) return <EsxDetail detail={...} onBack={() => setEsxDetail(null)} />` body-replace at the top of HostsView's render (the GlobalDashboard precedent — drill state in HostsView, NOT App.tsx, NOT a router, NOT a 2nd memo). Host rows become keyboard-activatable (native `<button>` or `role` + Enter/Space) with `cursor-pointer hover:bg-slate-50 dark:hover:bg-surface-800`. i18n namespace `network` (host-network labels) reusing the shipped `detail.back`/`na` keys. Do NOT duplicate the P5 cluster-detail drill.
  </action>
  <verify>
    <automated>npx vitest run src/components/hosts && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/hosts` exits 0 with an EsxDetail render + back-callback test and a HostsView host-click-drills-in test (existing HostsView tests stay green)
    - `grep -c "overflow-hidden p-8" src/components/hosts/EsxDetail.tsx` ≥ 1 (screen-fit shell)
    - `grep -c "EsxDetail" src/components/hosts/HostsView.tsx` > 0 (wired, drill state in HostsView)
    - `grep -rn --include='*.tsx' "useMemo" src/components/hosts/ | grep -v '\.test\.'` returns nothing (no 2nd memo)
    - `grep -ric "recommend\|should\|poor\|good\|critical\|warning\|bg-red\|amber\|traffic" src/components/hosts/EsxDetail.tsx` returns 0
    - `npx @biomejs/biome check src/components/hosts/` exits 0
  </acceptance_criteria>
  <done>EsxDetail is a screen-fit ClusterDetail-idiom drill (datastores + switches/uplinks per host) wired into HostsView via lifted state with a back affordance; it augments the Hosts view and does not duplicate the P5 cluster-detail drill.</done>
</task>

<task type="auto">
  <name>Task 3: MANDATORY real-file vSAN relink validation gate (the STR-04 regression guard)</name>
  <files>src/engines/aggregation/vsanRelink.realfile.test.ts</files>
  <read_first>
    - src/engines/parser/canary.test.ts (the `readFileSync` real-file pattern to copy — the env-guarded `it` that asserts when the file is present and skips gracefully in CI when absent)
    - src/engines/aggregation/vsanRelink.ts (plan 02 task 1 output — `relinkBlankClusterDatastores`, the function under real-file validation)
    - src/engines/parser/adapters/rvtools.ts (plan 01 — `adaptRvtools` to parse the real workbook end-to-end before relinking)
    - 09-RESEARCH.md §Validation Architecture (the exact gate spec: env-guarded `it` reading `~/Library/CloudStorage/OneDrive-Home/20260430_1400_allvCenters.xlsx`, assert relink count > 0 — empirically ≈25 distinct blank-cluster datastores / 116 VM rows; the trimmed Downloads workbook has no network sheets) + §Code Examples "Empirical relink result"
    - 09-CONTEXT.md §D-09 + §specifics + ~/.claude/projects/-Users-fjacquet-Projects-vatlas/memory/project_rvtools_vdatastore_hosts_is_count.md (unit tests insufficient — this real-file gate is the binding requirement before the phase can be declared done)
  </read_first>
  <action>
    Create `vsanRelink.realfile.test.ts` copying the `canary.test.ts` `readFileSync` pattern: an env-guarded `it` (skips gracefully — `it.skip`/`describe.skip` with a logged reason — when `~/Library/CloudStorage/OneDrive-Home/20260430_1400_allvCenters.xlsx` is absent, e.g. in CI; asserts when present). When present: parse the workbook through `adaptRvtools`, run `relinkBlankClusterDatastores`, and assert (a) the resolved blank-cluster datastore count is > 0 (empirically ≈25 distinct / 116 VM rows — assert a non-zero lower bound, NOT an exact brittle count), (b) at least the empirically-observed shared-LUN datastore is surfaced as "shared across N clusters" and excluded from single-cluster rollups, and (c) unrelinkable blank-cluster datastores remain estate-only with no fabricated cluster identity. This is the STR-04 regression guard the binding memory demands; a relink count of 0 on this file is the exact STR-04 failure mode and MUST fail the test.
  </action>
  <verify>
    <automated>npx vitest run src/engines/aggregation/vsanRelink.realfile.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/engines/aggregation/vsanRelink.realfile.test.ts` exits 0 — when the real file is present the relink-count-> 0 assertion runs and passes; when absent the suite skips with a logged reason (does NOT fail CI)
    - `grep -c "20260430_1400_allvCenters" src/engines/aggregation/vsanRelink.realfile.test.ts` ≥ 1 (the named real workbook)
    - `grep -c "relinkBlankClusterDatastores" src/engines/aggregation/vsanRelink.realfile.test.ts` ≥ 1
    - the test asserts a non-zero lower bound (a `> 0` / `toBeGreaterThan` assertion on the resolved count), NOT an exact brittle equality
    - running the suite with the real file present: the resolved blank-cluster datastore count assertion passes (manual confirmation logged in the SUMMARY)
  </acceptance_criteria>
  <done>An env-guarded real-file test asserts a non-zero vSAN relink count on the named 75-blank-cluster workbook (skipping gracefully when absent) — the STR-04 regression guard is in place and green on the real file.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parsed network strings -> NetworkView/EsxDetail | Untrusted operator-supplied portgroup/VLAN/switch/uplink names render in table cells and detail rows |
| real workbook on disk -> the real-file test | A real RVTools file is read in the test environment only; never shipped, never network-transmitted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-14 | Tampering/XSS | NetworkView/EsxDetail rendering of parsed strings | mitigate | React default escaping only; the raw-markup-injection React prop is never used in `src/components/network/` or `EsxDetail.tsx` (grep-gated); portgroup/VLAN names are untrusted text |
| T-09-15 | Information Disclosure (privacy) | the real-file relink test reading a real workbook | mitigate | The file is read only by the local/dev test runner via `readFileSync`; the privacy guard forbids any non-same-origin network call; the test never transmits or persists workbook content (the canary.test.ts precedent) |
| T-09-16 | DoS (degrade path) | network sheets absent | mitigate | The factual-degrade line renders instead of a crash (LC-3); the engine tolerates empty input (plan 02); never throws |
</threat_model>

<verification>
- `npx vitest run src/components/network src/components/hosts src/engines/aggregation/vsanRelink.realfile.test.ts` exits 0
- the real-file relink test passes with the named workbook present (resolved count > 0) and skips gracefully when absent
- exactly one `useMemo` in non-test `src/` still holds
- the raw-markup-injection React prop unused in `src/components/network/` and `EsxDetail.tsx`
- EN/FR key parity for network.json; no editorial verbs
- `npx @biomejs/biome check .` and `npx tsc --noEmit -p tsconfig.app.json` exit 0
- full suite: `npm run test:run` green; `npm run test:coverage` engines ≥75%
</verification>

<success_criteria>

- Network view: three P3 DataTable sections + the factual single-line degrade when sheets absent (no crash, no icon, no editorial verb)
- ESX detail augments HostsView as a screen-fit ClusterDetail-idiom drill (does not duplicate the P5 cluster-detail drill)
- The MANDATORY real-file vSAN relink gate asserts a non-zero relink count on the named 75-blank-cluster workbook (the STR-04 closure proof) and skips gracefully when absent
- No 2nd useMemo; EN/FR parity; no editorial verbs; full suite + coverage green
</success_criteria>

<output>
After completion, create `.planning/phases/09-storage-network-detailed-views-threshold-alerting/09-05-SUMMARY.md`
</output>
