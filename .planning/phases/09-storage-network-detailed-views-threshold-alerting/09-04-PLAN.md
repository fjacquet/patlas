---
phase: 09-storage-network-detailed-views-threshold-alerting
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - src/components/ViewToggle.tsx
  - src/components/Chart.tsx
  - src/i18n/index.ts
  - src/i18n/locales/en/inventory.json
  - src/i18n/locales/fr/inventory.json
  - src/i18n/locales/en/storage.json
  - src/i18n/locales/fr/storage.json
  - src/i18n/locales/en/alerts.json
  - src/i18n/locales/fr/alerts.json
  - src/components/storage/StorageView.tsx
  - src/components/storage/StorageLensToggle.tsx
  - src/components/storage/ThresholdConfig.tsx
  - src/components/storage/DatastoreDetail.tsx
  - src/components/storage/VmDetail.tsx
  - src/components/inventory/columns/storageColumns.ts
  - src/App.tsx
autonomous: true
requirements: [STG-04, STG-05, DTL-01, DTL-02, ALR-05]

must_haves:
  truths:
    - "ViewToggle shows 8 segments including Storage and Network with arrow-key wraparound and the gold active treatment"
    - "The Storage view shows a treemap (consumption lens) and a stacked-bar (capacity lens) via the single Chart SVG site, switched by the AccountingModeToggle-idiom lens toggle"
    - "A user edits filesystem/datastore/LU threshold percentages in an in-memory config panel and the flagged-row marker updates with no traffic-light and no editorial verb"
    - "Clicking a datastore row drills into a screen-fit Datastore detail; clicking a VM row drills into a screen-fit VM detail; both have a back affordance"
    - "Unrelinkable blank-cluster datastores render an em-dash, never a fabricated cluster"
    - "D-05: two new ViewToggle segments (Storage + Network) are added by extending the shipped fieldset + aria-pressed ViewToggle idiom in place (no new nav component) — 8 segments total"
    - "D-06: the Datastore detail and VM detail are click-drill in-app view-state branches lifted into the Storage view shell (the P5 pattern — not URL routing, not a second useMemo)"
    - "D-07: the storage-lens toggle UI reuses the shipped accounting-toggle (Configured/Active/Storage-realistic) fieldset idiom to switch consumption vs capacity"
    - "D-08: the datastore-footprint treemap (consumption lens) and the stacked-bar (capacity lens) render through the single <Chart> SVG site"
    - "D-04: threshold-flag presentation is factual only — tint / left-rule / badge marker (`bg-accent-500/15` + `border-l-2 border-accent-500`), with no traffic-light, no icon, and no verdict text"
  artifacts:
    - path: "src/components/storage/StorageView.tsx"
      provides: "single useEstateView consumer + lifted drill state + scope/lens controls"
      contains: "useEstateView"
      min_lines: 40
    - path: "src/components/storage/StorageLensToggle.tsx"
      provides: "consumption|capacity toggle (AccountingModeToggle idiom)"
      contains: "aria-pressed"
    - path: "src/components/storage/ThresholdConfig.tsx"
      provides: "in-memory threshold editor (PlannedRatiosControl idiom)"
      contains: "selectThresholds"
    - path: "src/components/storage/DatastoreDetail.tsx"
      provides: "screen-fit datastore drill (ClusterDetail idiom)"
      contains: "onBack"
    - path: "src/components/storage/VmDetail.tsx"
      provides: "screen-fit VM drill (ClusterDetail idiom)"
      contains: "onBack"
  key_links:
    - from: "src/components/ViewToggle.tsx"
      to: "AppView union + VIEWS array"
      via: "in-place storage/network append"
      pattern: "'storage'.*'network'|storage.*network"
    - from: "src/components/Chart.tsx"
      to: "echarts.use([...])"
      via: "TreemapChart registration"
      pattern: "TreemapChart"
    - from: "src/components/storage/StorageView.tsx"
      to: "view.storage / view.flags"
      via: "plain props off the single memo + lifted useState drill"
      pattern: "view\\.(storage|flags)"
    - from: "src/components/storage/ThresholdConfig.tsx"
      to: "snapshotStore thresholds slice"
      via: "selectThresholds / selectSetThresholds"
      pattern: "selectSetThresholds"
---

<objective>
Ship the Storage side of the UI: the ViewToggle extension, the one-line `TreemapChart` registration, the new `storage`/`alerts` i18n namespaces + ViewToggle `nav.*` keys, the Storage view shell (treemap/stacked-bar + lens toggle + threshold config + P3 DataTable), and the two click-drill detail screens (Datastore detail, VM detail).

Purpose: D-05 (ViewToggle +2 segments), D-07/D-08 (two lenses + treemap/stacked-bar), D-01..D-04 (in-memory threshold config + factual flag marker), D-06 (Datastore + VM detail drills) — all per the binding LC-1/2/4/5/6/7/8 UI-SPEC contracts. Every visual reuses a verified shipped idiom (zero new design mechanism).
Output: extended ViewToggle/Chart/i18n + `src/components/storage/*` + storage column defs + the App route.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-storage-network-detailed-views-threshold-alerting/09-UI-SPEC.md
@.planning/phases/09-storage-network-detailed-views-threshold-alerting/09-PATTERNS.md
@.planning/phases/09-storage-network-detailed-views-threshold-alerting/09-RESEARCH.md
@.planning/phases/09-storage-network-detailed-views-threshold-alerting/09-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ViewToggle +2 segments, TreemapChart registration, i18n namespaces + nav keys</name>
  <files>src/components/ViewToggle.tsx, src/components/Chart.tsx, src/i18n/index.ts, src/i18n/locales/en/inventory.json, src/i18n/locales/fr/inventory.json, src/i18n/locales/en/storage.json, src/i18n/locales/fr/storage.json, src/i18n/locales/en/alerts.json, src/i18n/locales/fr/alerts.json</files>
  <read_first>
    - src/components/ViewToggle.tsx (modified file — `AppView` union line 3, `VIEWS` array line 5, the `t('nav.<view>')` from the `inventory` namespace line 28/70, the literal `biome-ignore`/`role="group"` lines 48-55, the gold active treatment line 65)
    - src/components/Chart.tsx (modified file — the `echarts/charts` import line 1 + `echarts.use([...])` array lines 29-42; the universal `option` prop lines 49-66)
    - src/i18n/index.ts (modified file — the namespace import/`NAMESPACES`/`resources.en`+`resources.fr` registration lines 5-82)
    - src/i18n/locales/en/inventory.json + fr/inventory.json (the existing `nav.*` keys to extend with `nav.storage`/`nav.network`)
    - 09-UI-SPEC.md §LC-1, §LC-8, §Copywriting Contract (the exact EN strings: nav.storage=Storage/FR Stockage, nav.network=Network/FR Réseau, lens.consumption/lens.capacity, alerts.* keys, vsan.sharedAcross), §Color (gold reserved-list — ViewToggle stays gold)
    - 09-PATTERNS.md §`ViewToggle.tsx`, §`Chart.tsx`, §`App.tsx + i18n`
  </read_first>
  <action>
    Append `'storage'` then `'network'` (in that order) to BOTH the `AppView` union and the `VIEWS` array in `ViewToggle.tsx` IN PLACE — no new component, the fieldset/aria-pressed/arrow-wraparound/gold-active idiom and the literal `biome-ignore`/`role="group"` are reused unchanged. Import `TreemapChart` from `echarts/charts` in `Chart.tsx` and add it to the `echarts.use([...])` array (one-line addition beside `BarChart`/`HeatmapChart`); no `<Chart>` API change. Add `nav.storage`/`nav.network` to BOTH `locales/en/inventory.json` and `locales/fr/inventory.json` (Storage/Stockage, Network/Réseau). Create `storage.json` + `alerts.json` in BOTH `en/` and `fr/` with the LC strings (lens.consumption/lens.capacity, vsan.sharedAcross, detail.* labels, alerts:config.heading/echo, alerts:fs.count/ds.count) — no pre-formatted numbers (placeholders for `fmt*`), no editorial verbs, EN/FR parity key-for-key. Register `'storage'`/`'alerts'` (and `'network'` reserved for plan 05) in `i18n/index.ts` `NAMESPACES` + `resources.en` + `resources.fr`. Run `npm run check:bundle-size` after the ECharts change.
  </action>
  <verify>
    <automated>npx vitest run src/components/ViewToggle src/components/Chart && npm run check:bundle-size</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/ViewToggle.test.tsx` exits 0 asserting 8 segments incl storage+network with arrow-wrap (extend the shipped test)
    - `grep -c "TreemapChart" src/components/Chart.tsx` ≥ 2 (import + use array)
    - `npm run check:bundle-size` exits 0 (echarts chunk ≤300 KB gz after TreemapChart)
    - EN/FR key parity: the storage.json and alerts.json key sets are identical between `en/` and `fr/` (no key present in one and absent in the other)
    - `grep -ric "recommend\|should\|poor\|good\|critical\|warning" src/i18n/locales/en/storage.json src/i18n/locales/en/alerts.json` returns 0
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0
  </acceptance_criteria>
  <done>ViewToggle has 8 segments; TreemapChart is registered and the bundle gate stays green; storage/alerts namespaces + nav.storage/nav.network exist with EN/FR parity and no editorial verbs.</done>
</task>

<task type="auto">
  <name>Task 2: StorageView shell + StorageLensToggle + ThresholdConfig + storage column defs</name>
  <files>src/components/storage/StorageView.tsx, src/components/storage/StorageLensToggle.tsx, src/components/storage/ThresholdConfig.tsx, src/components/inventory/columns/storageColumns.ts, src/App.tsx</files>
  <read_first>
    - src/components/dashboard/GlobalDashboard.tsx (the single-useEstateView-consumer + lifted-drill analog — lines 55-129; `const view = useEstateView(mode)`, `const [selectedCluster, setSelectedCluster] = useState`, the `if (detail) return <Detail .../>` body-replace)
    - src/components/dashboard/AccountingModeToggle.tsx (the lens-toggle analog, entire file — copy verbatim, change union to `'consumption'|'capacity'`, namespace to `storage`, keep the literal `biome-ignore`/`role="group"`, active `bg-primary-600 text-white` NOT accent)
    - src/components/planning/PlannedRatiosControl.tsx (the threshold-config analog, entire file — `<section className="panel flex flex-col gap-4">`, the `safeNum` on-commit sanitizer, the shared-bounds pattern, the verbatim `<input type="number">` class, the factual echo line; state via `useSnapshotStore(selectThresholds)`/`selectSetThresholds`)
    - src/components/inventory/columns/datastoreColumns.ts + src/components/inventory/DatastoreTable.tsx (the column-def + thin-wrapper analog; the Pitfall-3 `inventory:col.<id>` header-key requirement)
    - src/App.tsx (modified file — the `activeView ===` chain lines 40-52; add storage/network branches before the GlobalDashboard fallback)
    - 09-UI-SPEC.md §LC-2, §LC-5, §LC-6, §LC-7, §LC-8 (binding visual contracts: treemap navy ramp / stacked-bar / lens toggle / flag marker `bg-accent-500/15`+`border-l-2 border-accent-500`, no traffic-light / threshold panel placement / single Chart site)
    - 09-PATTERNS.md §`StorageView.tsx` / §`StorageLensToggle.tsx + ThresholdConfig.tsx` / §Storage tables + column defs
    - CLAUDE.md §Gotchas (the `inventory:col.<id>` requirement + `<thead>`/`<tbody>` flex-layout match; Biome-not-ESLint)
  </read_first>
  <action>
    Create `StorageLensToggle.tsx` by copying `AccountingModeToggle.tsx` verbatim — union `'consumption'|'capacity'`, namespace `storage`, active `bg-primary-600 text-white`, keep the literal `biome-ignore lint/a11y/noRedundantRoles` + `role="group"` (CI grep gate asserts the literal), keep Arrow-wraparound. Create `ThresholdConfig.tsx` by copying `PlannedRatiosControl.tsx`'s shell/`safeNum`/shared-bounds/`<input type="number">` class/echo-line verbatim, three inputs (filesystem used %, datastore used %, LU used %), state from `useSnapshotStore(selectThresholds)`/`selectSetThresholds`, i18n namespace `alerts` — out-of-range/non-finite falls back to last valid (no error state, factual). Create `storageColumns.ts` modeled on `datastoreColumns.ts` (accessor/id/cell `fmt*`, `defaultVisible`); for EVERY new column id add the `inventory:col.<id>` key to BOTH `en/`+`fr/inventory.json` (Pitfall 3). Create `StorageView.tsx` modeled on `GlobalDashboard`: ONE `useEstateView` call, lifted `useState` for the storage-by-X scope (Cluster/ESX/VM/Datastore), the lens (`'consumption'|'capacity'`), and the drilled datastore/VM id; render `ThresholdConfig` (top), `StorageLensToggle`, the `<Chart>` (treemap for consumption — navy `--color-primary-*` ramp, `label.show`+`upperLabel.show`+`breadcrumb.show`, plain `name`/`value` data with no HTML formatter; stacked-bar for capacity reusing the registered `BarChart`), and the P3 `DataTable`+`ColumnPicker`+CSV below; flagged rows get `bg-accent-500/15`+`border-l-2 border-accent-500` (no red/amber/green, no icon, no verdict); unrelinkable blank-cluster datastores render the em-dash sentinel. Clickable datastore/VM rows are keyboard-activatable; `if (drilledDatastore) return <DatastoreDetail .../>` / `if (drilledVm) return <VmDetail .../>` replaces the shell body (no router, no 2nd memo). Add the `activeView === 'storage' ? <StorageView />` branch to `App.tsx` (drill state stays in the view shell, NOT App.tsx).
  </action>
  <verify>
    <automated>npx vitest run src/components/storage && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/storage` exits 0 with StorageView render + lens-switch + threshold-edit-updates-flag-marker tests
    - `grep -c "useEstateView" src/components/storage/StorageView.tsx` equals 1 (single consumer)
    - `grep -rn --include='*.ts' --include='*.tsx' "useMemo" src/components/storage/ | grep -v '\.test\.'` returns nothing (no 2nd memo)
    - `grep -c "biome-ignore lint/a11y/noRedundantRoles" src/components/storage/StorageLensToggle.tsx` ≥ 1 (literal preserved for the CI grep gate)
    - every column id in `storageColumns.ts` has a matching `col.<id>` key in BOTH en/ and fr/inventory.json
    - `grep -ric "util-low\|util-mid\|util-high\|bg-red\|text-red\|amber\|traffic" src/components/storage/` returns 0 (D-04 — no traffic-light)
    - `npx @biomejs/biome check src/components/storage/ src/App.tsx` exits 0
  </acceptance_criteria>
  <done>StorageView is a single-memo consumer with a lifted-drill shell, the lens toggle and threshold config reuse the shipped idioms verbatim, flagged rows use the factual gold marker only, new columns have inventory keys; App routes to it.</done>
</task>

<task type="auto">
  <name>Task 3: DatastoreDetail + VmDetail screen-fit click-drill screens (ClusterDetail idiom)</name>
  <files>src/components/storage/DatastoreDetail.tsx, src/components/storage/VmDetail.tsx</files>
  <read_first>
    - src/components/cluster/ClusterDetail.tsx (the detail-drill analog, entire file — `{ detail, onBack }` props, `<main className="flex-1 overflow-hidden p-8">`, `flex h-full flex-col gap-4`, the header `<h2 text-2xl font-semibold>` + `← {t('detail.back')}` button verbatim classes, the fixed `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-x-10`, the `Row` sub-component lines 27-34, the `na = t('na')` em-dash sentinel lines 24/71)
    - src/components/storage/StorageView.tsx (this plan's task-2 output — the drill state owner that renders these and supplies the detail prop)
    - 09-UI-SPEC.md §LC-4 (binding screen-fit / export-ready contract: no internal scroll, 16:9, one PPTX slide; Datastore detail = capacity/provisioned/in-use/free + VMs-on-it (from the vInfo.Path map built for the relink — single source) + host count + threshold flag marker; VM detail = vCPU/vRAM + disks + partitions with per-row flag marker + portgroups/switches + datastores)
    - 09-PATTERNS.md §`DatastoreDetail.tsx / VmDetail.tsx` (verbatim props + screen-fit shell + Row + em-dash; numbers via `fmt*` from `@/utils/format`; namespace `storage`)
  </read_first>
  <action>
    Create `DatastoreDetail.tsx` and `VmDetail.tsx` copying `ClusterDetail.tsx`'s props shape (`{ detail, onBack }`), screen-fit shell (`overflow-hidden p-8`, `flex h-full flex-col gap-4`, the fixed grid), header (`<h2>` + the `← {t('detail.back')}` button verbatim classes), the `Row` sub-component, and the `na = t('na')` em-dash sentinel — all verbatim. Datastore detail rows: capacity / provisioned / in-use / free, VMs-on-it list (sourced from the `vInfo.Path` map already built for the relink — single source, no new join), host count, and the threshold flag marker (`bg-accent-500/15` family) if over — no traffic-light, no verdict text. VM detail rows: vCPU/vRAM, disks, partitions (each partition row carries the flag marker when over `fsUsedPct`), portgroups/switches, datastores. Numbers via `fmt*` from `@/utils/format` with `i18n.language`; i18n namespace `storage`. Any not-derivable value renders the em-dash, never fabricated.
  </action>
  <verify>
    <automated>npx vitest run src/components/storage/DatastoreDetail.test.tsx src/components/storage/VmDetail.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/storage/DatastoreDetail.test.tsx src/components/storage/VmDetail.test.tsx` exits 0 (render + back-callback + factual-only + em-dash-when-not-derivable)
    - `grep -c "overflow-hidden p-8" src/components/storage/DatastoreDetail.tsx src/components/storage/VmDetail.tsx` shows the screen-fit shell in both
    - `grep -c "onBack" src/components/storage/DatastoreDetail.tsx src/components/storage/VmDetail.tsx` > 0 in both
    - `grep -ric "recommend\|should\|poor\|good\|critical\|warning\|util-high\|bg-red\|amber" src/components/storage/DatastoreDetail.tsx src/components/storage/VmDetail.tsx` returns 0
    - `npx @biomejs/biome check src/components/storage/DatastoreDetail.tsx src/components/storage/VmDetail.tsx` exits 0
  </acceptance_criteria>
  <done>DatastoreDetail and VmDetail are screen-fit, one-PPTX-slide, ClusterDetail-idiom drills with a back affordance, factual rows + flag marker only, em-dash for not-derivable values.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parsed strings -> ECharts/DataTable/detail screens | Untrusted operator-supplied datastore/VM/portgroup names render in chart labels, table cells, and detail rows |
| parsed strings -> CSV export | The shipped P3 CSV path handles formula-trigger lead characters for new columns |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-10 | Tampering/XSS | ECharts treemap/stacked-bar labels | mitigate | Pass datastore/portgroup names as plain `name`/`value` data; no raw-markup ECharts `formatter`; ECharts SVG renderer escapes text content (UI-SPEC LC-8) |
| T-09-11 | Tampering/XSS | DataTable + detail-screen rendering | mitigate | React default escaping only; the raw-markup-injection React prop is never used anywhere in `src/components/storage/` (grep-gated below); all parsed strings treated as untrusted text |
| T-09-12 | Tampering (CSV formula injection) | CSV-of-filtered export of new storage columns | mitigate | Reuse the shipped P3 CSV escaping verbatim (it neutralizes a leading `= + - @`); do NOT re-implement the CSV path — confirm the new columns route through the existing accessor escaping |
| T-09-13 | Information Disclosure (privacy) | ThresholdConfig state | mitigate | State source is the in-memory thresholds slice only (plan 03); no localStorage write; refresh restores defaults |
</threat_model>

<verification>
- `npx vitest run src/components/storage src/components/ViewToggle src/components/Chart` exits 0
- `npm run check:bundle-size` exits 0
- exactly one `useMemo` in non-test `src/` still holds (no 2nd memo added in `src/components/storage/`)
- `grep -rn "dangerouslySetInnerHTML" src/components/storage/` returns nothing; no raw-markup ECharts `formatter` in `src/components/storage/`
- EN/FR key parity for storage.json/alerts.json; no editorial verbs
- `npx @biomejs/biome check .` and `npx tsc --noEmit -p tsconfig.app.json` exit 0
</verification>

<success_criteria>

- ViewToggle 8 segments + TreemapChart registered + bundle gate green
- Storage view: lens toggle (treemap consumption / stacked-bar capacity via the single Chart site), in-memory threshold config, P3 DataTable, factual gold flag marker (no traffic-light), em-dash for unrelinkable datastores
- Datastore + VM detail are screen-fit ClusterDetail-idiom drills with a back affordance
- No 2nd useMemo; the raw-markup-injection React prop unused; EN/FR parity; no editorial verbs
</success_criteria>

<output>
After completion, create `.planning/phases/09-storage-network-detailed-views-threshold-alerting/09-04-SUMMARY.md`
</output>
