---
phase: 09-storage-network-detailed-views-threshold-alerting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/vinfo.ts
  - src/types/snapshot.ts
  - src/engines/parser/schemas.ts
  - src/engines/parser/adapters/rvtools.ts
  - src/engines/parser/adapters/rvtools.test.ts
  - src/engines/snapshotMerge/mergeSnapshotsToEstate.ts
  - src/store/snapshotStore.ts
autonomous: true
requirements: [NET-01, NET-02, NET-03, VSR-04]
user_setup: []

must_haves:
  truths:
    - "Parsing a workbook that contains vNetwork/vSwitch/dvSwitch/dvPort yields adapted rows on the Snapshot"
    - "Parsing a workbook with zero network sheets yields empty arrays plus a collected missing-sheet warning and never throws"
    - "Every parsed vInfo row carries a `path` string (the `[datastore] vm/vm.vmx` token, empty when absent)"
    - "The MiB canary fixture and all existing parser fixtures stay green unchanged"
    - "D-11: vNetwork/vSwitch/dvSwitch/dvPort ship as a regression-gated parser extension — the four new sheets are OPTIONAL (absent ⇒ collected warning + empty arrays + factual degrade, never a throw) and the MiB canary + all existing fixtures stay green unchanged"
    - "D-09: every parsed VInfoRow carries the `vInfo.Path` field (the `[datastore] vm/vm.vmx` token, empty when absent) — the parser half of the vSAN relink prerequisite"
  artifacts:
    - path: "src/engines/parser/adapters/rvtools.ts"
      provides: "adaptRvtoolsVNetwork/VSwitch/DvSwitch/DvPort + vInfo.path + OPTIONAL-sheet block for 4 network sheets"
      contains: "adaptRvtoolsVSwitch"
    - path: "src/engines/parser/schemas.ts"
      provides: "VNetworkRowSchema/VSwitchRowSchema/VDvSwitchRowSchema/VDvPortRowSchema + path on VInfoRowSchema"
      contains: "VSwitchRowSchema"
    - path: "src/types/snapshot.ts"
      provides: "V{Network,Switch,DvSwitch,DvPort}Row interfaces + Snapshot network arrays"
      contains: "VSwitchRow"
    - path: "src/types/vinfo.ts"
      provides: "path: string field on VInfoRow"
      contains: "path"
    - path: "src/engines/parser/adapters/rvtools.test.ts"
      provides: "network-present + network-absent + vInfo.path coverage"
      contains: "missing-sheet"
  key_links:
    - from: "src/engines/parser/adapters/rvtools.ts"
      to: "src/types/snapshot.ts"
      via: "adaptRvtools return type union extension"
      pattern: "vnetwork:\\s*VNetworkRow"
    - from: "src/engines/snapshotMerge/mergeSnapshotsToEstate.ts"
      to: "Snapshot network arrays"
      via: "flatten loop + MergedEstate fields"
      pattern: "vnetwork"
    - from: "src/store/snapshotStore.ts"
      to: "releaseRawRows"
      via: "clears the 4 new network arrays alongside vinfo"
      pattern: "vnetwork:\\s*\\[\\]"
---

<objective>
Extend the validated RVTools parser with the four OPTIONAL network sheets (vNetwork, vSwitch, dvSwitch, dvPort) and the `vInfo.Path` field — the regression-gated, P5-D-07-class change that every downstream P9 engine depends on. This plan is isolated in Wave 1 so the parser blast radius is contained and proven before any consumer is built.

Purpose: D-11: (network topology source) and D-09: (the `vInfo.Path` relink key) both require parser changes. The parser is validated; this change MUST NOT regress it (MiB canary + existing fixtures green) and the new sheets MUST factual-degrade (the Downloads 8-sheet workbook has zero network sheets).
Output: New row types + schemas + adapters + the `path` field, threaded through merge and the store's raw-row release, with additive tests for the present and absent cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-storage-network-detailed-views-threshold-alerting/09-RESEARCH.md
@.planning/phases/09-storage-network-detailed-views-threshold-alerting/09-PATTERNS.md
@.planning/phases/09-storage-network-detailed-views-threshold-alerting/09-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the four network row types + the vInfo.path field (types + Zod schemas)</name>
  <files>src/types/vinfo.ts, src/types/snapshot.ts, src/engines/parser/schemas.ts</files>
  <read_first>
    - src/types/vinfo.ts (modified file — `VInfoRow` interface, lines 16-53; the doc-comment-per-field convention)
    - src/types/snapshot.ts (modified file — `VDatastoreRow` lines 70-90 + `Snapshot` array fields lines 49-53; the analog for the new row interfaces)
    - src/engines/parser/schemas.ts (modified file — `VDatastoreRowSchema` lines 81-93, `VPartitionRowSchema` 95-101, `VInfoRowSchema` 45-63; `MibSchema` brand-transform convention)
    - 09-PATTERNS.md §`src/types/vinfo.ts + src/types/snapshot.ts` and §`src/engines/parser/schemas.ts` (the exact field shapes + the `z.ZodType<RowType>` annotation drift-stop rule)
    - 09-RESEARCH.md §Code Examples "Real RVTools column names" (the authoritative 4.x columns: vNetwork = VM/Network/Switch/Adapter/Connected/Cluster/Host; vSwitch = Host/Cluster/Switch/# Ports/Free Ports/MTU; dvSwitch = Switch/Name/Version/Host members/# Ports/# VMs/Max MTU; dvPort = Port/Switch/VLAN/Active Uplink/Standby Uplink)
  </read_first>
  <behavior>
    - VInfoRow gains `path: string` (default `''`); VInfoRowSchema accepts an absent/empty Path without rejecting the row
    - VNetworkRow/VSwitchRow/VDvSwitchRow/VDvPortRow interfaces exist with plain string fields + non-negative number counts (port/VLAN counts are NOT MiB-branded)
    - The four new schemas reject a row whose required string key is empty (mirror `name: z.string().trim().min(1)`) and accept empty optional text fields (`z.string().trim()` without `.min(1)`)
    - Snapshot gains `vnetwork`/`vswitch`/`dvswitch`/`dvport` array fields
  </behavior>
  <action>
    Add `path: string` to the `VInfoRow` interface in `src/types/vinfo.ts` with a doc-comment naming the RVTools source (`vInfo Path — [datastore] vm/vm.vmx; empty when absent`). Add `path: z.string().trim()` to `VInfoRowSchema` in `schemas.ts` (empty allowed — do NOT `.min(1)`; this is the P5 `Powerstate`/`Template` precedent, default `''`). Define four new interfaces in `src/types/snapshot.ts` modeled field-for-field on `VDatastoreRow` (doc-comment per field naming the RVTools column, "empty string when absent"): `VNetworkRow` (vm, network, switch, adapter, connected, cluster, host), `VSwitchRow` (host, cluster, switch, ports, freePorts, mtu), `VDvSwitchRow` (switch, name, version, hostMembers, ports, vms, maxMtu), `VDvPortRow` (port, switch, vlan, activeUplink, standbyUplink). Numeric counts are non-negative plain `number` (NOT `mib()`/`MibSchema`). Add `vnetwork: VNetworkRow[]`/`vswitch`/`dvswitch`/`dvport` to the `Snapshot` interface beside `vdatastore`/`vpartition`. Add the four `z.ZodType<RowType>`-annotated schemas to `schemas.ts` copying the `VDatastoreRowSchema` shape verbatim (annotation drift-stops compilation; non-negative `z.number()` for counts, NOT `MibSchema`).
  </action>
  <verify>
    <automated>npx vitest run src/engines/parser/schemas.test.ts && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "path" src/types/vinfo.ts` shows a `path: string` field on `VInfoRow`
    - `grep -c "VSwitchRow\|VNetworkRow\|VDvSwitchRow\|VDvPortRow" src/types/snapshot.ts` ≥ 4
    - `grep -c "VSwitchRowSchema\|VNetworkRowSchema\|VDvSwitchRowSchema\|VDvPortRowSchema" src/engines/parser/schemas.ts` ≥ 4
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0
    - `npx @biomejs/biome check src/types/ src/engines/parser/schemas.ts` exits 0
  </acceptance_criteria>
  <done>The four network row types + the vInfo.path field exist as TypeScript interfaces and Zod schemas; typecheck and Biome pass; existing schema tests stay green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the adapters + the OPTIONAL-sheet factual-degrade block; thread through merge + store release</name>
  <files>src/engines/parser/adapters/rvtools.ts, src/engines/snapshotMerge/mergeSnapshotsToEstate.ts, src/store/snapshotStore.ts</files>
  <read_first>
    - src/engines/parser/adapters/rvtools.ts (modified file — `adaptRvtoolsVDatastore` lines 240-257, `adaptRvtoolsVInfo` 178-216, the OPTIONAL-sheet block 353-389, the `VINFO_COLS`/`VDATASTORE_COLS` alias convention 49-137, `adaptRvtools` return type 323-332)
    - src/engines/snapshotMerge/mergeSnapshotsToEstate.ts (modified file — `MergedEstate` interface lines 9-17, `EMPTY_MERGED` 19-25, the flatten loop 120-125, return literal 127-133, the `{...v}` vInfo spread 99-107 that carries `path` automatically)
    - src/store/snapshotStore.ts (modified file — `releaseRawRows` lines 157-172 which clears raw arrays like `vinfo: []`)
    - 09-PATTERNS.md §`src/engines/parser/adapters/rvtools.ts`, §`mergeSnapshotsToEstate.ts`, §`snapshotStore.ts` (the verbatim OPTIONAL-sheet `findSheet → warnings.push({kind:'missing-sheet'}) → sheet ? adapt : []` block; the merge flatten + release blast-radius notes)
    - 09-RESEARCH.md §Common Pitfalls Pitfall 1 (network sheets frequently absent — the Downloads 8-sheet workbook) and §Code Examples real 4.x headers
  </read_first>
  <behavior>
    - adaptRvtoolsVNetwork/VSwitch/DvSwitch/DvPort each map sheet rows via `mapColumns(sheet.headers, V*_COLS)` + `readString`/`readNumber`, filtering internal rows like the shipped datastore adapter
    - adaptRvtoolsVInfo additionally returns `path: readString(readCol(row, cols.path))`
    - When a network sheet is absent: `warnings.push({sheet, kind:'missing-sheet', message})` and the adapter result is `[]` — adaptRvtools NEVER throws for a network sheet (only vInfo/vHost throw)
    - mergeSnapshotsToEstate flattens the four new arrays into MergedEstate; EMPTY_MERGED has `[]` for each
    - releaseRawRows clears the four new Snapshot arrays alongside `vinfo: []`
  </behavior>
  <action>
    Add `VNETWORK_COLS`/`VSWITCH_COLS`/`VDVSWITCH_COLS`/`VDVPORT_COLS` alias maps following the `VINFO_COLS` convention (longest/exact spelling first, then FR/DE variants defensively per Assumption A1; the real 4.x headers are the source of truth). Add `path: ['path']` to `VINFO_COLS` and `path: readString(readCol(row, cols.path))` to the `adaptRvtoolsVInfo` returned literal (default `''`). Write `adaptRvtoolsVNetwork`/`VSwitch`/`DvSwitch`/`DvPort` copying the `adaptRvtoolsVDatastore` shape verbatim; numeric port/VLAN counts use `Math.max(0, Math.trunc(readNumber(...)))`, never `mib()`. In `adaptRvtools`, add four `findSheet(workbook, ['vnetwork','rvtools_tabvnetwork'])`-style lookups (correct aliases per sheet), each followed by the verbatim `if (!sheet) warnings.push({sheet, kind:'missing-sheet', message: 'optional sheet … absent — network inventory will be empty'})` block, and extend the return object + its type union with `vnetwork: sheet ? adaptRvtoolsVNetwork(sheet) : []` (etc.). Do NOT use the REQUIRED-sheet `parseError(...)` path for network sheets. In `mergeSnapshotsToEstate.ts` add the four arrays to `MergedEstate`, `[]` to `EMPTY_MERGED`, a flatten loop entry per array, and extend the return literal (`vInfo.path` rides along via the existing `{...v}` spread). In `snapshotStore.ts` `releaseRawRows`, add `vnetwork: []`/`vswitch: []`/`dvswitch: []`/`dvport: []` beside the existing `vinfo: []` clears.
  </action>
  <verify>
    <automated>npx vitest run src/engines/parser src/engines/snapshotMerge src/store/snapshotStore.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "adaptRvtoolsVNetwork\|adaptRvtoolsVSwitch\|adaptRvtoolsDvSwitch\|adaptRvtoolsDvPort" src/engines/parser/adapters/rvtools.ts` ≥ 4
    - `grep -c "missing-sheet" src/engines/parser/adapters/rvtools.ts` shows ≥ 7 (the 3 existing + 4 new network sheets)
    - `grep -v '^ \*' src/engines/snapshotMerge/mergeSnapshotsToEstate.ts | grep -c "vnetwork\|vswitch\|dvswitch\|dvport"` > 0
    - `grep -n "vnetwork: \[\]" src/store/snapshotStore.ts` matches inside `releaseRawRows`
    - `npx vitest run src/engines/parser/canary.test.ts` exits 0 (MiB canary unchanged)
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0
  </acceptance_criteria>
  <done>The four adapters + the vInfo.path read exist; the OPTIONAL-sheet factual-degrade block covers all four network sheets without throwing; merge and store release thread the new arrays; the MiB canary is green and unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Additive parser tests — network sheets present, network sheets absent, vInfo.path</name>
  <files>src/engines/parser/adapters/rvtools.test.ts</files>
  <read_first>
    - src/engines/parser/adapters/rvtools.test.ts (modified file — the existing adaptRvtools test structure, the synthetic-workbook fixture builder used for vDatastore/vPartition present/absent cases)
    - src/engines/parser/canary.test.ts (the `readFileSync` real-file pattern + the MiB-canary assertion that must stay unchanged)
    - 09-RESEARCH.md §Validation Architecture "Phase Requirements → Test Map" (the exact parser-ext test rows) and §Code Examples real 4.x headers (synthetic fixture column names)
    - 09-PATTERNS.md §Parser regression gate (additive-tests-only rule; canary stays unchanged)
  </read_first>
  <behavior>
    - A synthetic workbook WITH vNetwork/vSwitch/dvSwitch/dvPort sheets parses to non-empty adapted arrays with the expected field values
    - A synthetic workbook WITHOUT any network sheets parses to four empty arrays AND pushes a `missing-sheet` warning per absent sheet AND does not throw
    - A synthetic vInfo row whose Path cell is `[DS_X] vm/vm.vmx` yields `path === '[DS_X] vm/vm.vmx'`; an absent Path yields `path === ''`
  </behavior>
  <action>
    Add `describe`/`it` blocks to `rvtools.test.ts` (additive only — do NOT modify `canary.test.ts` or existing fixtures). Build a synthetic workbook with the four network sheets using the real 4.x headers from RESEARCH; assert the adapted arrays are non-empty with expected values and counts are plain numbers (not MiB-branded). Add a second case with no network sheets asserting four `[]` results, a `warnings` entry of `kind:'missing-sheet'` per absent network sheet, and that `adaptRvtools` returns normally (no throw). Add a vInfo.path case asserting the bracket-token round-trips and an absent Path yields `''`.
  </action>
  <verify>
    <automated>npx vitest run src/engines/parser/adapters/rvtools.test.ts src/engines/parser/canary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/engines/parser/adapters/rvtools.test.ts` exits 0 and includes a network-present, a network-absent, and a vInfo.path test
    - `npx vitest run src/engines/parser/canary.test.ts` exits 0 (unchanged — `git diff --stat src/engines/parser/canary.test.ts` shows no changes)
    - `npx @biomejs/biome check src/engines/parser/adapters/rvtools.test.ts` exits 0
  </acceptance_criteria>
  <done>Additive parser tests cover network-present, network-absent (warning + no throw), and vInfo.path; the MiB canary test is byte-unchanged and green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.xlsx` file → parser worker | Untrusted operator-supplied workbook bytes cross into the app; the only untrusted-input boundary in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering (input validation) | new V*RowSchema at the parser boundary | mitigate | Every new row type validated by a `z.ZodType<RowType>`-annotated Zod schema at the parser boundary; invalid rows dropped + reported, never thrown unchecked (ASVS V5; mirrors shipped VDatastoreRowSchema) |
| T-09-02 | DoS (ReDoS / zip-bomb) | xlsx parse of new sheets in the Web Worker | accept | `xlsx@0.20.3` (CVE-fixed tarball, not npm 0.18.5); parser already runs in a Web Worker (UI not blocked); `readNumber` regex is bounded — existing shipped mitigation, not re-planned |
| T-09-03 | Tampering (privacy) | new Snapshot network arrays | mitigate | No new persistence: network arrays live only in the in-memory Snapshot; `releaseRawRows` clears them alongside `vinfo` (refresh = data gone, PAR-05); no localStorage key added |
</threat_model>

<verification>
- `npx vitest run src/engines/parser src/engines/snapshotMerge src/store/snapshotStore.test.ts` exits 0
- `npx vitest run src/engines/parser/canary.test.ts` exits 0 and `canary.test.ts` is byte-unchanged
- `npx tsc --noEmit -p tsconfig.app.json` exits 0
- `npx @biomejs/biome check .` exits 0
</verification>

<success_criteria>

- The four network row types + the `vInfo.path` field exist as interfaces + Zod schemas
- adaptRvtools parses network sheets when present and factual-degrades (warning + `[]`, no throw) when absent
- Merge + store-release thread the new arrays; refresh-equals-data-gone holds
- MiB canary + all existing parser fixtures green and unchanged (no regression)
</success_criteria>

<output>
After completion, create `.planning/phases/09-storage-network-detailed-views-threshold-alerting/09-01-SUMMARY.md`
</output>
</content>
