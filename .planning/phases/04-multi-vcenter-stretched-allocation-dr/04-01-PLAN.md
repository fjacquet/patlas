---
phase: 04-multi-vcenter-stretched-allocation-dr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engines/parser/adapters/rvtools.ts
  - src/engines/parser/captureDate.ts
  - src/engines/parser/parser.worker.ts
  - src/types/snapshot.ts
  - src/types/vhost.ts
  - src/engines/snapshotMerge/mergeSnapshotsToEstate.ts
  - src/engines/snapshotMerge/vCenterIndex.ts
  - src/engines/snapshotMerge/index.ts
  - src/engines/snapshotMerge/mergeSnapshotsToEstate.test.ts
  - src/engines/snapshotMerge/vCenterIndex.test.ts
  - src/engines/aggregation/estateView.ts
  - src/hooks/useEstateView.ts
  - src/store/snapshotStore.ts
  - src/components/SnapshotCard.tsx
  - src/i18n/index.ts
  - src/i18n/locales/en/mvc.json
  - src/i18n/locales/fr/mvc.json
autonomous: true
requirements: [MVC-01, MVC-02, MVC-03, MVC-04]
user_setup: []

must_haves:
  truths:
    - "User drops 2+ separate RVTools .xlsx files from different vCenters and sees ONE merged estate whose VM total equals the sum of per-vCenter totals (no double-count, no silent merge)"
    - "User drops the single allvCenters.xlsx (1 workbook, 3 vCenters) and sees ONE estate with 3 vCenters, same merge code path as multi-file"
    - "Two clusters sharing a name across distinct viSdkUuid render with a vCenter-label suffix, never silently combined"
    - "A VM vMotioned across vCenters appears exactly once in the merged estate"
    - "The snapshot card shows the correct RVTools version (4.7.x for a 4.7 file, not '3.11+') and the per-vCenter Server labels"
  artifacts:
    - path: "src/engines/snapshotMerge/mergeSnapshotsToEstate.ts"
      provides: "Pure Snapshot[] -> merged {vinfo,vhost,vdatastore,vcenters} with collision suffix + vMotion dedupe"
      contains: "export const mergeSnapshotsToEstate"
    - path: "src/engines/snapshotMerge/vCenterIndex.ts"
      provides: "Map<viSdkUuid, VCenterEntry> built from row viSdkUuid (not snapshot id)"
      contains: "export const buildVCenterIndex"
    - path: "src/engines/snapshotMerge/mergeSnapshotsToEstate.test.ts"
      provides: "Multi-file merge + collision + vMotion dedupe fixtures"
      contains: "describe"
  key_links:
    - from: "src/hooks/useEstateView.ts"
      to: "src/engines/snapshotMerge/mergeSnapshotsToEstate.ts"
      via: "called inside the single useMemo before buildEstateView"
      pattern: "mergeSnapshotsToEstate"
    - from: "src/engines/parser/captureDate.ts"
      to: "columnar vMetaData RVTools version column"
      via: "columnar 4.x detection branch"
      pattern: "rvtools version"
    - from: "src/components/SnapshotCard.tsx"
      to: "per-vCenter vMetaData list"
      via: "MVC-04 meta line"
      pattern: "vcenters"
---

<objective>
Deliver THE headline Phase-4 capability: merging MULTIPLE separate RVTools .xlsx
files into ONE logical estate (the primary path and primary test), with the
single-workbook-N-vCenters real file (allvCenters.xlsx, 3 vCenters) riding the
exact same row-keyed code. Includes the two parser prerequisites that block
correct MVC-04 and clean vCenter labelling (per 04-RESEARCH Pitfalls 2 & 3).

Purpose: Every later Phase-4/5/6/7 capability reads the merged estate, not raw
snapshots. The merge identity keys must be correct before stretched/DR build on
them.
Output: `src/engines/snapshotMerge/` module, the columnar-vMetaData parser fix,
the fault-domain alias prerequisite, the single-useMemo multi-snapshot contract
change, the `selectedSnapshotIds` store slice, MVC-04 snapshot-card labels, and
the `mvc` i18n namespace.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# RESEARCH / PATTERNS / UI-SPEC — read the named sections
@.planning/phases/04-multi-vcenter-stretched-allocation-dr/04-RESEARCH.md
# ^ §Pitfall 1 (allvCenters is 1 file/3 vCenters — group by ROW viSdkUuid),
#   §Pitfall 2 (vmInstanceUuid is ABSENT in 4.7; key on vmBiosUuid = VM UUID,
#   never SMBIOS), §Pitfall 3 (columnar vMetaData fix → correct MVC-04),
#   §Pattern 1 (generalized collision), §Pattern 2 (dedupe key),
#   §Code Examples "Building the vCenter index from rows", Assumptions A2/A6.
@.planning/phases/04-multi-vcenter-stretched-allocation-dr/04-PATTERNS.md
# ^ snapshotMerge/mergeSnapshotsToEstate section (vsizer resolveClusterCollisions
#   :41-74 port + required deviation; perDatastore.ts:21-44 dedupe-by-key shape),
#   vCenterIndex section (perDatastore.ts:33-44 Map-accumulate),
#   Parser fixes section (rvtools.ts:235-247 / :118-121, captureDate.ts:17-91 /
#   :80-90), useEstateView SPECIAL ATTENTION section, snapshotStore section,
#   SnapshotCard section (:39-51), i18n section (NAMESPACES :26).
@.planning/phases/04-multi-vcenter-stretched-allocation-dr/04-UI-SPEC.md
# ^ §Layout MVC-02/MVC-04 (suffix is a data transform — no new component;
#   one card lists "3 vCenters"), §Copywriting (mvc.* keys), §i18n.

# Skeleton source to port + generalize (read in full — small)
@/Users/fjacquet/Projects/vsizer/src/engines/parser/resolveClusterCollisions.ts

# Self-extend targets (read before modifying)
@src/engines/parser/adapters/rvtools.ts
@src/engines/parser/captureDate.ts
@src/engines/parser/parser.worker.ts
@src/types/snapshot.ts
@src/types/vhost.ts
@src/engines/aggregation/estateView.ts
@src/hooks/useEstateView.ts
@src/store/snapshotStore.ts
@src/components/SnapshotCard.tsx
@src/i18n/index.ts

<interfaces>
<!-- Contracts the executor must honor; extracted from shipped code + research. -->

useEstateView today (src/hooks/useEstateView.ts):
  export function useEstateView(mode: AccountingMode): EstateView
  — the ONLY useMemo in non-test src. selectActiveSnapshot → buildEstateView.

buildEstateView today (src/engines/aggregation/estateView.ts:33):
  export function buildEstateView(snapshot: Snapshot, mode: AccountingMode): EstateView
  — currently takes ONE Snapshot; must take the merged row bundle in this plan.
  EMPTY_VIEW frozen constant (:99-109) stays the no-snapshot path.

vmBiosUuid alias (rvtools.ts:60) — ALREADY CORRECT, DO NOT CHANGE:
  vmBiosUuid: ['vm uuid','bios uuid','uuid']  // resolves to RVTools VM UUID in 4.7

VCenterEntry shape (define in vCenterIndex.ts; from RESEARCH Code Examples):
  interface VCenterEntry { viSdkUuid: string; server: string; label: string;
                           clusters: Set<string>; vmCount: number }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parser prerequisites — columnar vMetaData + fault-domain/Hosts aliases + types</name>
  <files>src/engines/parser/adapters/rvtools.ts, src/engines/parser/captureDate.ts, src/engines/parser/parser.worker.ts, src/types/snapshot.ts, src/types/vhost.ts</files>
  <behavior>
    - Given a RVTools 4.7 columnar vMetaData sheet (headers include an "RVTools version" COLUMN, one row per vCenter), `adaptRvtoolsVMetaData` returns a list of per-vCenter `{ server, rvtoolsVersion, exportedTimestamp }` and `inferRvtoolsVersion` returns "4.7.1.4" (NOT "3.11+").
    - Given a legacy Property/Value vMetaData sheet, the existing path still returns the correct version (regression — no behavior change for old exports).
    - `mapColumns` resolves `vSAN Fault Domain Name` / `Fault Domain Name` → `VHostRow.faultDomain` (empty string when absent) and `Hosts` / `# Hosts` → `VDatastoreRow.hosts` (empty string when absent).
    - `vmBiosUuid` alias list is unchanged (assert the alias array still equals the shipped value — guard against accidental edit; RESEARCH Pitfall 2 / A2).
  </behavior>
  <action>Extend `adaptRvtoolsVMetaData` (rvtools.ts:235-247) and `VMETADATA_COLS` (:118-121) to detect the columnar 4.x shape by presence of an `RVTools version` COLUMN (headers `["RVTools major version","RVTools version","xlsx creation datetime","Server"]`, one row per vCenter); read `RVTools version`, `xlsx creation datetime`, `Server` PER ROW. Keep the legacy Property/Value branch (alias-dictionary `mapColumns` style, rvtools.ts:147-148 convention) for older exports. Change `VMetaDataRow` (snapshot.ts:60-64) to a list of per-vCenter `{ server, rvtoolsVersion, exportedTimestamp }`; mirror in `captureDate.ts` `metaValue`/`inferRvtoolsVersion`/`inferVCenterLabel` (:17-91, :80-90 — drop the `'3.11+'` marker fallback as PRIMARY when columnar present) and `parser.worker.ts:23-25` so the worker surfaces the per-vCenter list on `Snapshot`. Add `VHOST_COLS.faultDomain: ['vsan fault domain name','fault domain name']` + `VHostRow.faultDomain: string` and `VDATASTORE_COLS.hosts: ['hosts','# hosts']` + `VDatastoreRow.hosts: string` (exact-normalized alias, longest spelling first per rvtools.ts:104-106 convention) — these are prerequisites consumed by Plan 04-02. Do NOT touch the `vmBiosUuid` alias (rvtools.ts:60) — it is already correct (RESEARCH A2). Branded/empty-cell discipline: empty cell → `''`, never `0` (shipped Minor-3 preprocessor idiom).</action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/rvtui && npx vitest run src/engines/parser --reporter=dot</automated>
  </verify>
  <done>Columnar 4.x vMetaData parses to a per-vCenter list with correct RVTools version; legacy Property/Value still passes; faultDomain + hosts columns parse (empty-string default); vmBiosUuid alias unchanged; parser tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: snapshotMerge engine — vCenterIndex + mergeSnapshotsToEstate + barrel + tests</name>
  <files>src/engines/snapshotMerge/vCenterIndex.ts, src/engines/snapshotMerge/mergeSnapshotsToEstate.ts, src/engines/snapshotMerge/index.ts, src/engines/snapshotMerge/vCenterIndex.test.ts, src/engines/snapshotMerge/mergeSnapshotsToEstate.test.ts</files>
  <behavior>
    - `buildVCenterIndex(selected)` groups selected snapshots' vinfo rows by ROW `viSdkUuid` (NOT snapshot id) → `Map<viSdkUuid, VCenterEntry>`; vCenter count = distinct viSdkUuid (NEVER file count). label = vMetaData Server for that uuid ?? row.viSdkServer ?? snapshot.vCenterLabel.
    - `mergeSnapshotsToEstate(selected)` over N separately-dropped snapshots returns `{vinfo,vhost,vdatastore,vcenters}`; estate VM total === sum of per-vCenter distinct-VM totals (no double-count).
    - Same function over ONE 3-vCenter snapshot (allvCenters shape) yields 3 vcenters and the same merged result as if the 3 vCenters arrived as 3 files (Pitfall 1).
    - Cluster name colliding across distinct viSdkUuid → BOTH vinfo+vhost rows rewritten `"<cluster> (<vCenterLabel>)"`; non-colliding → pass-through references (input never mutated).
    - VM appearing under two viSdkUuid with the same `vmBiosUuid` (cross-vCenter vMotion/clone) → kept once (first occurrence). Blank vmBiosUuid → fallback key `(viSdkUuid, vmName, cluster)`. `vmInstanceUuid` is NOT a required path.
  </behavior>
  <action>Create `vCenterIndex.ts` copying the `perDatastore.ts:33-44` Map-accumulate idiom; required deviation: group by `row.viSdkUuid`, produce `Map<viSdkUuid,{viSdkUuid;server;label;clusters:Set<string>;vmCount}>`, label resolved per RESEARCH Code-Example "Building the vCenter index from rows". Create `mergeSnapshotsToEstate.ts` porting vsizer `resolveClusterCollisions.ts:41-74` (build `Map<clusterName,Set<viSdkUuid>>`; `size>1` ⇒ colliding; immutable `{...row,cluster:suffixed}` rewrite of BOTH vinfo+vhost, pass-through refs otherwise); required deviations: key on `viSdkUuid` not filename, suffix `"${cluster} (${vCenterLabelFor(viSdkUuid)})"`, ADD VM dedupe (copy the `perDatastore.ts:21-44` first-occurrence-wins `naa ?? name` shape as `vmBiosUuid ?? (viSdkUuid,vmName,cluster)`), output `{vinfo,vhost,vdatastore,vcenters:VCenterEntry[]}`. Pure: no React/Zustand/Zod (PROJECT.md line 52); never mutate input. Add `index.ts` barrel mirroring `engines/aggregation/index.ts`. Tests: build synthetic fixtures for (a) PRIMARY: 2 separate snapshots, distinct viSdkUuid, a colliding `CL_WRK_PFLEX` name → suffixed both sides + VM total = sum; (b) ONE snapshot with 3 viSdkUuid (allvCenters shape) → 3 vcenters, identical merge result as (a)-style; (c) a vMotion'd VM (same vmBiosUuid under 2 viSdkUuid) → deduped to 1; (d) blank-vmBiosUuid fallback key.</action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/rvtui && npx vitest run src/engines/snapshotMerge --coverage --reporter=dot</automated>
  </verify>
  <done>Multi-FILE merge (primary) and single-workbook-3-vCenter (additional) produce identical correct estates; collision suffix applied to both row sets; vMotion VM deduped; input never mutated; ≥75% coverage on `engines/snapshotMerge/`.</done>
</task>

<task type="auto">
  <name>Task 3: Single-useMemo multi-snapshot contract + store slice + MVC-04 card + i18n</name>
  <files>src/engines/aggregation/estateView.ts, src/hooks/useEstateView.ts, src/store/snapshotStore.ts, src/components/SnapshotCard.tsx, src/i18n/index.ts, src/i18n/locales/en/mvc.json, src/i18n/locales/fr/mvc.json</files>
  <action>Add inputs-only `selectedSnapshotIds: Set<string>` slice + replace-never-mutate setter + `selectSelectedSnapshots` selector to `snapshotStore.ts` (copy the `:39-44` `new Map(state.snapshots)` Set-replace idiom; default selection = ALL snapshot ids — multi-FILE merge is the primary path, single snapshot is the degenerate case; NO persist middleware, NO localStorage — PROJECT.md line 53). Change `useEstateView` (the ONLY useMemo): select `selectedSnapshots` instead of `selectActiveSnapshot`; INSIDE the SAME `useMemo` call `mergeSnapshotsToEstate(selected)` then `buildEstateView(merged, mode)`; memo dep array `[selected-snapshot-identities, mode]` (stable Set, store replaces never mutates). DO NOT add a second `useMemo` anywhere (grep-gated). Change `buildEstateView` signature from `(snapshot: Snapshot, mode)` to `(merged: MergedEstate, mode)` (the merged row bundle from Task 2); keep `EMPTY_VIEW` frozen-constant for empty selection. MVC-04: in `SnapshotCard.tsx` add a Caption (12/400) meta line BELOW the existing capture-date line (analog :39-51) listing `{{count}} vCenters` (or `1 vCenter`), the per-`viSdkUuid` `Server` labels stacked, and the parsed `RVTools {{version}}` (now correct from Task 1) — one 3-vCenter workbook = ONE card reading "3 vCenters", never implying 1 file = 1 vCenter (UI-SPEC §Layout MVC-04). i18n: add `'mvc'` to `NAMESPACES` (i18n/index.ts:26) + register in `resources` for both `en` and `fr` (:29-42); create `locales/{en,fr}/mvc.json` with keys per UI-SPEC §i18n (`mvc.snapshot.vcenters`, `mvc.snapshot.vcenter.one`, `mvc.snapshot.rvtoolsVersion`, `mvc.merge.loading`, `mvc.merge.singleVCenter`) — `{{placeholder}}` only, NO pre-formatted numbers, NO editorial verbs; EN/FR key parity (CI key-diff gate applies).</action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/rvtui && npx tsc --noEmit && npx vitest run --reporter=dot && grep -rn 'useMemo' src --include='*.ts' --include='*.tsx' | grep -v '\.test\.' | grep -v node_modules</automated>
  </verify>
  <done>useEstateView remains the ONLY non-test useMemo (grep shows exactly one occurrence, in useEstateView.ts); merge runs inside it; default selection = all snapshots; SnapshotCard renders N-vCenters + correct RVTools version; mvc namespace registered with EN/FR parity; typecheck + full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RVTools .xlsx → parser worker | Untrusted workbook bytes; parsed in the shipped Web Worker (V12) |
| merged row identity | Cluster-name / VM-UUID collisions are a data-integrity concern, not a security one |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | merge identity keying (silent cluster merge / SMBIOS double-count) | mitigate | Data-integrity: key on `(viSdkUuid, clusterName)` + suffix never silent-merge; dedupe on `vmBiosUuid` (= VM UUID, never SMBIOS); covered by Task 2 fixtures |
| T-04-02 | Information Disclosure | merged cluster/vCenter label rendered in SnapshotCard | accept | React text children auto-escape (shipped P3 idiom); no raw-HTML sink; low risk |
| T-04-03 | DoS | merging many large snapshots in the single memo | accept | Pure JS merge is tens of ms for ~10k VMs (02-02 benchmark); memo keyed on stable Set identities |
| T-04-04 | Information Disclosure | new dependency phoning home | mitigate | Zero new deps (locked); `check:supply-chain` + fetch guard re-verified in <verification> |
</threat_model>

<verification>
Run, in order, from `/Users/fjacquet/Projects/rvtui`:
1. `npx tsc --noEmit` — typecheck clean
2. `npx @biomejs/biome check .` — lint/format clean (NOT `npm run lint`; RTK intercept)
3. `npx vitest run --coverage --reporter=dot` — full suite green, `engines/snapshotMerge/` ≥75%
4. `node scripts/check-bundle-size.mjs` — echarts ≤300 KB gz gate still green (no new chunk)
5. `npm run check:supply-chain` — no telemetry/new deps
6. Single-useMemo grep gate: `grep -rn 'useMemo' src --include='*.ts' --include='*.tsx' | grep -v '\.test\.'` MUST return exactly ONE line (useEstateView.ts)
7. REAL-FILE MERGE HARNESS (mandatory per feedback_real_file_uat — tests passing ≠ verified):
   - Throwaway node+xlsx inspection / browser UAT with `~/Library/CloudStorage/OneDrive-Home/*allvCenters*.xlsx` (1 file, 3 vCenters) → estate shows 3 vCenters, VM total = 1446 (1 vMotion/clone dup collapsed from 1447), RVTools version reads 4.7.x (NOT 3.11+)
   - AND ≥2 separately-dropped real RVTools files → one merged estate, per-vCenter VM totals sum correctly, any colliding cluster name suffixed
8. ROADMAP reconcile note (per project_roadmap_reconcile memory): the SDK roadmap update verb does not match this ROADMAP format — at end of execute-phase, manually flip the Phase-4 checkbox + Progress row and commit `chore: reconcile ROADMAP — phase 4 plan 01`.
</verification>

<success_criteria>
Mirrors ROADMAP Phase-4 Success Criterion 1 + the multi-FILE-merge headline:
- A user drops TWO separate RVTools workbooks from different vCenters with a colliding cluster name and sees both clusters with vCenter-suffix disambiguation, estate VM total = sum of per-vCenter totals (no double-count, no silent merge) — PRIMARY path.
- The single allvCenters.xlsx (1 file, 3 vCenters) yields the same correct 3-vCenter estate via the identical row-keyed code — ADDITIONAL case.
- Snapshot card shows the correct RVTools version (4.7.x) and per-vCenter Server labels for a 4.7 file.
- `useEstateView` is still the project's ONLY `useMemo`.
- `engines/snapshotMerge/` ≥75% Vitest coverage including colliding-name + vMotion'd-VM fixtures.
</success_criteria>

<output>
After completion, create `.planning/phases/04-multi-vcenter-stretched-allocation-dr/04-01-SUMMARY.md`
</output>
</invoke>
