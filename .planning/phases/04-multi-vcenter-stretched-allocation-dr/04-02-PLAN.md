---
phase: 04-multi-vcenter-stretched-allocation-dr
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/engines/aggregation/aggregateClusters.ts
  - src/engines/aggregation/perCluster.ts
  - src/engines/aggregation/perDatastore.ts
  - src/engines/aggregation/estateView.ts
  - src/engines/aggregation/globals.ts
  - src/types/estate.ts
  - src/engines/aggregation/aggregateClusters.test.ts
  - src/engines/aggregation/perDatastore.test.ts
  - src/store/snapshotStore.ts
  - src/hooks/useEstateView.ts
  - src/components/stretched/StretchedPill.tsx
  - src/components/dashboard/ClusterColumn.tsx
  - src/i18n/index.ts
  - src/i18n/locales/en/str.json
  - src/i18n/locales/fr/str.json
autonomous: true
requirements: [STR-01, STR-02, STR-03, STR-04]
user_setup: []

must_haves:
  truths:
    - "User toggles the Étendu/Stretched pill on a cluster and per-site reservation rows + confidence appear on that card"
    - "A symmetric 4+4 stretched cluster reserves 50%; an asymmetric 6+4 reserves 60% (NOT flat 50%)"
    - "A stretched cluster with fault-domain tags on all hosts shows confidence 'high'; one without site data shows 'medium'/'low'"
    - "A low-confidence stretched cluster shows a neutral grey warning chip (never red, never editorial)"
    - "vSAN/host-local datastores with blank Cluster name are attributed to their hosts' cluster(s) (no under-count)"
  artifacts:
    - path: "src/engines/aggregation/aggregateClusters.ts"
      provides: "Per-site reservedFraction replacing flat 0.5 + stretchedConfidence"
      contains: "reservedFraction"
    - path: "src/components/stretched/StretchedPill.tsx"
      provides: "aria-pressed segmented pill reusing AccountingModeToggle idiom + low-confidence chip"
      contains: "aria-pressed"
    - path: "src/types/estate.ts"
      provides: "ClusterAggregate.stretchedConfidence + per-site capacity + reservedFraction"
      contains: "stretchedConfidence"
  key_links:
    - from: "src/engines/aggregation/aggregateClusters.ts"
      to: "VHostRow.faultDomain"
      via: "group hosts by fault domain for maxSiteCap/totalCap"
      pattern: "faultDomain"
    - from: "src/components/dashboard/ClusterColumn.tsx"
      to: "src/components/stretched/StretchedPill.tsx"
      via: "rendered in the reserved footer band"
      pattern: "StretchedPill"
    - from: "src/engines/aggregation/perDatastore.ts"
      to: "vHost.hostName -> vHost.cluster map"
      via: "blank-clusterName Hosts->cluster attribution join"
      pattern: "hostClusterMap"
---

<objective>
Activate the DORMANT ported stretched-cluster math: replace the flat `0.5`
reservation with per-site `max(siteCap)/totalCap`, derive a factual
`stretchedConfidence` from fault-domain presence, surface the Étendu/Stretched
pill + per-site rows + confidence + low-confidence chip on the cluster card.
PLUS the named datastore-attribution fix: blank-`Cluster name` datastores
(vSAN/host-local) attributed to their hosts' cluster(s) via a pure join.

Purpose: Asymmetric stretched clusters must reserve correctly (wrong DR numbers
are the project's #1 risk); metadata absence must be surfaced (low confidence
chip), never silently collapsed to "confident 50%". DR (04-04) subtracts this
reservation.
Output: extended `aggregateClusters.ts`, `perDatastore.ts` Hosts→cluster join,
`StretchedPill` + `stretchedClusters` store slice, `str` i18n namespace.

NOTE (RESEARCH A3 — flagged): the `vDatastore.Hosts → vHost.cluster`
attribution fix is included per the brief and memory `project_datastore_cluster_attribution`.
Descope risk if removed: stretched/DR vSAN storage stays under-counted. It is a
named Phase-4 input — keep it; surface the inclusion in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/04-multi-vcenter-stretched-allocation-dr/04-RESEARCH.md

# ^ §Pattern 3 (per-site reservation + confidence), §Pitfall 4 (50% is

# conditional — 4+4=0.5 high, 6+4=0.6, absence ⇒ medium/low)

# §Pitfall 6 (vDatastore.Cluster name 44% blank — Hosts→cluster join)

# §Code Examples "Per-site reservation", Assumptions A1/A3/A4

# Open Questions 1 (synthetic 6+4 fixtures) & 2 (per-resource fraction; pin ADR)

@.planning/phases/04-multi-vcenter-stretched-allocation-dr/04-PATTERNS.md

# ^ aggregateClusters section (SELF dormant math :59-76 — extend, don't rewrite

# estate.ts:158-161 dormant block; perCluster.ts:40-79 discards fault domain)

# perDatastore section (datastoreCountByCluster :33-44 — Hosts→cluster join

# keep NAA dedupe Moderate-11), StretchedPill section (AccountingModeToggle

# :52-82 / :65-78 aria-pressed idiom verbatim; ThemeToggle :6-68 SVG glyph

# ClusterColumn Row :17-24), ClusterColumn section (:122 reserved footer band)

# Shared Patterns (segmented idiom, branded-unit retrofit, format.ts em-dash)

@.planning/phases/04-multi-vcenter-stretched-allocation-dr/04-UI-SPEC.md

# ^ §Color (confidence/chip/verdict NEUTRAL — no traffic-light, chip grey not

# red), §Interaction "Étendu/Stretched pill" + "Per-site reservation display"

# + "Confidence indicator + low-confidence chip", §Copywriting (str.* keys

# em-dash sentinel when a site cannot be determined), §Typography/§Spacing

# (200px column floor unchanged, Caption confidence, Body mono values)

@src/engines/aggregation/aggregateClusters.ts
@src/engines/aggregation/perCluster.ts
@src/engines/aggregation/perDatastore.ts
@src/engines/aggregation/estateView.ts
@src/engines/aggregation/globals.ts
@src/types/estate.ts
@src/store/snapshotStore.ts
@src/hooks/useEstateView.ts
@src/components/dashboard/ClusterColumn.tsx
@src/components/dashboard/AccountingModeToggle.tsx
@src/components/ThemeToggle.tsx
@src/utils/format.ts

<interfaces>
<!-- The dormant math to extend (aggregateClusters.ts:59-76) -->
  drReservedGhz   = isStretched ? 0.5 * physicalGhz : 0
  drReservedRamMib= isStretched ? 0.5 * physicalRamMib : 0
  usablePhysicalCores = isStretched ? 0.5 * physicalCores : physicalCores
  cpuDrFactor     = isStretched && physicalGhz>0 ? physicalGhz/(physicalGhz-drReservedGhz) : 1
  ramDrFactor     = isStretched && physicalRamMib>0 ? physicalRamMib/(physicalRamMib-drReservedRamMib) : 1
<!-- Generalize: replace literal 0.5 with f = reservedFraction(clusterHosts)
     (per-resource: GHz fraction for GHz, RAM-MiB for RAM, cores for cores —
     RESEARCH Open-Q 2 default; pin in an ADR comment). factor = 1/(1-f). -->

<!-- estate.ts dormant block :158-161 -->
  stretched: boolean
  drReservedGhz: GHz
<!-- ADD: stretchedConfidence: 'high'|'medium'|'low'; siteACapacityGhz/RamMib;
     siteBCapacityGhz/RamMib; reservedFraction: number. Em-dash sentinel when a
     site cannot be determined (UI-SPEC §STR-02). -->

<!-- AccountingModeToggle.tsx:65-78 — copy this button skeleton verbatim for the pill -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-site reservation + stretchedConfidence in aggregateClusters (the math)</name>
  <files>src/engines/aggregation/aggregateClusters.ts, src/engines/aggregation/perCluster.ts, src/engines/aggregation/globals.ts, src/types/estate.ts, src/engines/aggregation/aggregateClusters.test.ts</files>
  <behavior>
    - reservedFraction over a symmetric 4+4 fault-domain split → 0.5; over an asymmetric 6+4 → 0.6 (per-resource: computed from the chosen basis per resource).
    - stretchedConfidence: ≥2 distinct fault domains covering ALL cluster hosts → 'high'; stretched but fault domains absent/partial → 'medium' (assume symmetric 0.5); odd/asymmetric host count with NO site data → 'low' (cannot prove split). NEVER collapse absence to 'high'.
    - Non-stretched cluster (8+0, not in stretchedClusters Set) → reservedFraction effectively 0, no DR reservation (failback to shipped behavior — regression intact).
    - 2+2 minimum stretched → 0.5, 'high' when tagged.
    - Real `CL_VXB1K_CORE` (4+4 symmetric, fault domains Secondary×4 + UNI×4) → 0.5, 'high' (validated against real file in <verification>).
  </behavior>
  <action>Extend `aggregateClusters.ts:59-76` (the dormant ported math — EXTEND, do not rewrite; 02-02-SUMMARY confirms a regression test already exercises this branch). Replace the literal `0.5` with `f = reservedFraction(clusterHosts)` where hosts are grouped by `VHostRow.faultDomain` (the new parsed column from Plan 04-01); `f = maxSiteCapacity / totalCapacity` computed PER RESOURCE (GHz fraction for GHz, RAM-MiB fraction for RAM, cores for cores — RESEARCH Open-Question 2 default; PIN this choice in an ADR-style comment block at the top of the helper). `aggregateHostsPerCluster` (perCluster.ts:40-79) currently discards per-host fault domain — either thread fault-domain grouping into perCluster.ts OR pass raw `vhost` rows alongside into the reservation helper; keep the `groupByCluster` Map-accumulate shape (perCluster.ts:21-30). Derive `stretchedConfidence: 'high'|'medium'|'low'` per the behavior block (NEVER collapse fault-domain absence to 'high' — Anti-Pattern). Extend the `estate.ts:158-161` dormant block with `stretchedConfidence`, `siteACapacityGhz`/`siteBCapacityGhz` + RAM analogues, `reservedFraction: number`; em-dash sentinel where a site cannot be determined. Roll `stretchedConfidence` into `globals.ts` only if it surfaces an estate-level summary (otherwise leave globals untouched — KISS, no premature rollup). Branded units: `(x as number)` → arithmetic → re-wrap `ghzOf`/`mib`/`coresOf` (the established retrofit used throughout aggregateClusters.ts). Tests: extend `aggregateClusters.test.ts` with the FULL matrix — 4+4 (→0.5, high), 6+4 tagged (→0.6, high), 6+4 untagged (→assumed-symmetric, low + chip), 8+0 (non-stretched failback), 2+2 (→0.5, high). The asymmetric 6+4 cases are SYNTHETIC fixtures (RESEARCH A1 — no asymmetric stretched cluster exists in the real file).</action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/rvtui && npx vitest run src/engines/aggregation/aggregateClusters --coverage --reporter=dot</automated>
  </verify>
  <done>4+4→0.5/high, 6+4→0.6 (high tagged / low+chip untagged), 8+0 failback, 2+2→0.5/high all pass; per-resource fraction pinned in an ADR comment; estate.ts type extended; aggregateClusters ≥75% coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hosts→cluster datastore attribution (Pitfall 6 named fix)</name>
  <files>src/engines/aggregation/perDatastore.ts, src/engines/aggregation/estateView.ts, src/engines/aggregation/perDatastore.test.ts</files>
  <behavior>
    - A datastore with non-blank `Cluster name` → attributed exactly as today (regression intact).
    - A datastore with blank `Cluster name` but a populated `Hosts` list → attributed to the cluster(s) of those hosts via the `vHost.hostName → vHost.cluster` map.
    - A shared LUN (same NAA) appearing across hosts of two clusters → counted ONCE per attributed cluster (NAA dedupe preserved, Moderate-11).
    - A blank-clusterName datastore whose hosts span no known cluster → not silently dropped into a wrong cluster (attributed to each owning cluster, or none if hosts unmatched).
  </behavior>
  <action>Modify `datastoreCountByCluster` (perDatastore.ts:33-44): when `row.clusterName === ''`, instead of `continue`, attribute the datastore to the cluster(s) of its `Hosts` (the new `VDatastoreRow.hosts` host-name list from Plan 04-01) via a `vHost.hostName → vHost.cluster` map (pure join). `datastoreCountByCluster` signature gains a `hostClusterMap` param; `estateView.ts:38-39` builds and passes it (a single pass over `merged.vhost`). Keep NAA `dedupeKey` dedupe WITHIN each attributed cluster (Moderate-11 — a shared LUN counts once per cluster it appears in). Pure; no new dep. Tests: extend `perDatastore.test.ts` — non-blank clusterName unchanged; blank clusterName + Hosts → attributed; shared NAA across 2 clusters' hosts → once per cluster.</action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/rvtui && npx vitest run src/engines/aggregation/perDatastore --reporter=dot</automated>
  </verify>
  <done>Blank-clusterName vSAN/host-local datastores attributed via Hosts→cluster join; NAA dedupe preserved per cluster; non-blank path unchanged; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: StretchedPill + stretchedClusters store slice + ClusterColumn wiring + str i18n</name>
  <files>src/components/stretched/StretchedPill.tsx, src/components/dashboard/ClusterColumn.tsx, src/store/snapshotStore.ts, src/hooks/useEstateView.ts, src/i18n/index.ts, src/i18n/locales/en/str.json, src/i18n/locales/fr/str.json</files>
  <action>Add inputs-only `stretchedClusters: Set<string>` slice + replace-never-mutate setter + selector to `snapshotStore.ts` (Set-replace idiom :39-44; NO persist/localStorage). Thread it into `useEstateView` (still the ONLY useMemo — add to the existing memo dep array, NO second memo) → `buildEstateView(merged, mode, { stretchedClusters })` → `aggregateClusters`. Create `StretchedPill.tsx` copying the `AccountingModeToggle.tsx:52-82` `<fieldset role="group"> + <legend className="sr-only"> + button aria-pressed` idiom VERBATIM (keep the `biome-ignore lint/a11y/noRedundantRoles` + literal `role="group"` — CI grep gate asserts it); 2-state (pressed = cluster in `stretchedClusters` Set); controlled `value`/`onChange` lifted like `AccountingModeToggle`; active `bg-primary-600 text-white`, focus ring `ring-2 ring-primary-500`; NO bespoke toggle (DRY-locked). Low-confidence chip = neutral grey `bg-surface-100 text-slate-600 dark:bg-surface-700 dark:text-slate-300` + inline info SVG (`stroke="currentColor"`, ThemeToggle :6-68 glyph pattern), shown ONLY when `stretchedConfidence === 'low'` — never red, never editorial (UI-SPEC §Color). In `ClusterColumn.tsx`, ABOVE the reserved footer band (:122), render `<StretchedPill>` + per-site `Row`s (label left 16/600, value right mono `tabular-nums`, reuse :17-24 Row) showing per-site GHz+RAM + a "Reservation" row (locale `%` via `fmtPercent`, em-dash `—` sentinel when a site is indeterminate — NO pre-formatted number in the string) + a Caption `Confidence:` row (localized enum word only, neutral, no traffic-light) + the conditional low-confidence chip; column stays `min-w-[200px]` (no widening). i18n: add `'str'` to `NAMESPACES` + register both locales; create `locales/{en,fr}/str.json` with the UI-SPEC §i18n keys (`str.pill.label`="Étendu / Stretched", `str.pill.group`, `str.site.a/b`, `str.reservation`, `str.confidence.label`, `str.confidence.high|medium|low` (FR `élevée/moyenne/faible`), `str.lowConfidence.chip`) — `{{placeholder}}` only, NO editorial verbs, EN/FR parity.</action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/rvtui && npx tsc --noEmit && npx vitest run --reporter=dot && grep -rn 'useMemo' src --include='*.ts' --include='*.tsx' | grep -v '\.test\.'</automated>
  </verify>
  <done>Pill toggles `stretchedClusters` (in-memory only); per-site rows + neutral confidence + conditional grey chip render in ClusterColumn within the 200px floor; pill reuses the aria-pressed idiom (no bespoke toggle); useEstateView still the ONLY useMemo; str namespace EN/FR parity; typecheck + suite green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stretchedClusters Set → engine | In-memory inputs-only store slice; never persisted |
| fault-domain metadata absence | A confidence/correctness concern, surfaced factually — not security |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | flat-50% applied to asymmetric cluster (wrong DR numbers — project #1 risk) | mitigate | Per-site `maxSiteCap/totalCap`; confidence enum surfaces metadata absence; full 4+4/6+4/8+0/2+2 test matrix (Task 1) |
| T-04-06 | Information Disclosure | stretched flags persisted to localStorage | mitigate | In-memory store slice only, Set replaced never mutated; NO persist middleware (verified by grep gate) |
| T-04-07 | Information Disclosure | confidence/verdict rendered with editorial/alarm framing | mitigate | Neutral enum words, grey chip (never red), denylist lint; UI-SPEC §Color enforced |
| T-04-08 | Information Disclosure | datastore Hosts list leaked in an error message | accept | Region ErrorBoundary reads `error.message` only (shipped Critical-2 idiom); low risk |
</threat_model>

<verification>
From `/Users/fjacquet/Projects/rvtui`:
1. `npx tsc --noEmit`
2. `npx @biomejs/biome check .` (NOT `npm run lint`)
3. `npx vitest run --coverage --reporter=dot` — `aggregateClusters` ≥75%; full matrix 4+4/6+4/8+0/2+2 present
4. `node scripts/check-bundle-size.mjs` — gates green (no new dep/chunk)
5. `npm run check:supply-chain` — no new deps (no math/stats library — locked)
6. Single-useMemo grep gate: exactly one `useMemo` (useEstateView.ts)
7. REAL-FILE UAT (mandatory per feedback_real_file_uat): browser/throwaway-script against `~/Library/CloudStorage/OneDrive-Home/*allvCenters*.xlsx` → `CL_VXB1K_CORE` (the only real stretched cluster, 4+4 Secondary/UNI) when pinned stretched yields reservedFraction 0.5 + confidence 'high'; a non-tagged stretched cluster yields 'low' + the grey chip; vSAN datastores with blank Cluster name now show an owning cluster (storage no longer under-counted)
8. ROADMAP reconcile note per project_roadmap_reconcile (manual checkbox/Progress reconcile at end of execute-phase).
</verification>

<success_criteria>
Mirrors ROADMAP Phase-4 Success Criterion 2:

- A user toggles the Étendu/Stretched pill on a 6+4 asymmetric cluster and sees a per-site reservation (NOT flat 50%) with a `low` confidence chip when site/fault-domain metadata was inferred.
- A symmetric 4+4 (real `CL_VXB1K_CORE`) reserves exactly 50% at confidence `high`.
- vSAN/host-local datastores with blank `Cluster name` are attributed via Hosts→cluster (storage no longer under-counted).
- Confidence + chip are factual/neutral (no traffic-light, no red, no editorial verb).
- `aggregateClusters` ≥75% coverage including the full 4+4/6+4/8+0/2+2 matrix.
</success_criteria>

<output>
After completion, create `.planning/phases/04-multi-vcenter-stretched-allocation-dr/04-02-SUMMARY.md` (note the A3 datastore-attribution inclusion).
</output>
