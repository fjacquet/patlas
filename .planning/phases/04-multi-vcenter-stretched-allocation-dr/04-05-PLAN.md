---
phase: 04-multi-vcenter-stretched-allocation-dr
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/estate.ts
  - src/engines/aggregation/aggregateClusters.ts
  - src/engines/aggregation/aggregateClusters.test.ts
  - src/engines/aggregation/globals.test.ts
  - src/components/stretched/StretchedPill.tsx
  - src/components/dashboard/ClusterColumn.tsx
  - src/components/dashboard/GlobalDashboard.tsx
  - src/i18n/locales/en/str.json
  - src/i18n/locales/fr/str.json
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [STR-01, STR-02, STR-03]
user_setup: []

must_haves:
  truths:
    - "A stretched cluster WITH fault-domain data shows Site A/B GHz+RAM rows + Reservation % + a neutral 'site data: detected' caption — NO high/medium/low verdict, NO chip"
    - "A stretched cluster WITHOUT fault-domain data shows Reservation % + 'symmetric split assumed (no site data)' caption and NO Site A/B rows"
    - "The estate/global level shows a neutral factual line 'N clusters marked stretched'"
    - "Toggling stretched on one collision-suffixed cluster does NOT affect another suffixed cluster of the same base name (independent per real cluster)"
    - "Per-site reservation MATH is unchanged from the kept engine (real per-resource maxSiteCap/totalCap when tagged; symmetric 0.5 when inferred)"
    - "REQUIREMENTS.md has a single factual STR-03 and no STR-04 id"
  artifacts:
    - path: "src/types/estate.ts"
      provides: "ClusterAggregate.siteData: 'detected' | 'assumed' replacing stretchedConfidence; StretchedConfidence type removed"
      contains: "siteData"
    - path: "src/engines/aggregation/aggregateClusters.ts"
      provides: "reservationFor returns factual siteData (detected/assumed); same per-resource fraction math"
      contains: "siteData"
    - path: "src/components/dashboard/ClusterColumn.tsx"
      provides: "factual site-data caption replacing the Confidence row + LowConfidenceChip"
      contains: "site data"
  key_links:
    - from: "src/components/dashboard/ClusterColumn.tsx"
      to: "src/types/estate.ts ClusterAggregate.siteData"
      via: "factual caption rendered from siteData, no verdict"
      pattern: "siteData"
    - from: "src/components/dashboard/GlobalDashboard.tsx"
      to: "src/store/snapshotStore.ts selectStretchedClusters"
      via: "estate-level 'N clusters marked stretched' count line"
      pattern: "marked stretched"
---

<objective>
Re-derived Phase 4 (analytics-core replan). The multi-vCenter merge engine
spine + per-site reservation MATH are already shipped and real-file-validated
(`04-01`) and are KEPT UNCHANGED. This plan implements ONLY the UAT-G1
stretched rework: replace the rejected auto `stretchedConfidence` verdict +
`LowConfidenceChip` with a FACTUAL site-data indicator (the stretched flag is
the user's declaration; the engine never judges it), add a neutral estate
count line, and reword the stale STR-03/STR-04 requirements.

Allocation (G2) and DR modes/metric (G3) are explicitly OUT — they are
re-derived Phase 6. The vSAN blank-cluster datastore relink is deferred to
Phase 9. See `04-CONTEXT.md` (authoritative) — the original
`04-RESEARCH.md`/`04-PATTERNS.md`/`04-UI-SPEC.md` are PRE-REPLAN and stale.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-multi-vcenter-stretched-allocation-dr/04-CONTEXT.md
@.planning/ANALYTICS-CORE-REPLAN.md
@.planning/phases/04-multi-vcenter-stretched-allocation-dr/04-UAT.md
@.planning/phases/04-multi-vcenter-stretched-allocation-dr/04-02-SUMMARY.md
~/.claude/projects/-Users-fjacquet-Projects-vatlas/memory/project_stretched_is_user_decision.md

# Self-extend targets (read before modifying)

@src/types/estate.ts
@src/engines/aggregation/aggregateClusters.ts
@src/components/stretched/StretchedPill.tsx
@src/components/dashboard/ClusterColumn.tsx
@src/components/dashboard/GlobalDashboard.tsx
@src/i18n/locales/en/str.json
@.planning/REQUIREMENTS.md

<interfaces>
<!-- CURRENT (to change): ClusterAggregate has
       stretchedConfidence: 'high'|'medium'|'low'
       reservedFraction: number
       siteACapacityGhz/siteBCapacityGhz/siteACapacityRamMib/siteBCapacityRamMib: GHz|MiB|null
     plus StretchedConfidence type. aggregateClusters.reservationFor()
     returns { confidence } among the fraction fields.

     TARGET:
       - Remove StretchedConfidence type + stretchedConfidence field.
       - Add  siteData: 'detected' | 'assumed'   (factual, no judgement):
           'detected' = all hosts tagged + ≥2 distinct fault domains
                        (real per-site split computed — Site A/B real)
           'assumed'  = partial or no fault-domain coverage
                        (symmetric 0.5 reservation; no real per-site values)
       - reservedFraction + per-site capacity fields: KEEP (math unchanged).
         Per-site capacities stay null in the 'assumed' case (already true).
     The reservation fraction computation in reservationFor() is UNCHANGED;
     only the returned discriminator changes from confidence→siteData and
     the 'medium' vs 'low' distinction collapses to the single 'assumed'. -->

<!-- ClusterColumn current renders a "Confidence:" Row + conditional
     <LowConfidenceChip/>. Replace with:
       cluster.stretched && siteData==='detected'
         → Site A/B rows + Reservation % + caption tStr('siteData.detected')
       cluster.stretched && siteData==='assumed'
         → Reservation % + caption tStr('siteData.assumed'); NO Site A/B rows
     Pill placement unchanged (footer band). LowConfidenceChip deleted from
     StretchedPill.tsx (its only consumer is ClusterColumn). -->

<!-- str.json (EN/FR) currently has confidence.label/high/medium/low +
     lowConfidence.chip. Replace those keys with:
       siteData.detected = "site data: detected" / "données de site : détectées"
       siteData.assumed  = "symmetric split assumed (no site data)"
                         / "répartition symétrique supposée (aucune donnée de site)"
       stretched.estateCount = "{{count}} clusters marked stretched"
                         / "{{count}} clusters marqués étendus"
     Keep str.pill.* / str.site.* / str.reservation. EN/FR key parity gate. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: estate.ts + aggregateClusters — factual siteData replaces the confidence verdict (math unchanged)</name>
  <files>src/types/estate.ts, src/engines/aggregation/aggregateClusters.ts, src/engines/aggregation/aggregateClusters.test.ts, src/engines/aggregation/globals.test.ts</files>
  <behavior>
    - `ClusterAggregate` no longer has `stretchedConfidence`; `StretchedConfidence` type is removed. It has `siteData: 'detected' | 'assumed'`.
    - All hosts tagged + ≥2 distinct fault domains → `siteData: 'detected'`, real per-resource `reservedFraction` (e.g. 6+4 → 0.6), real `siteA/siteB` capacities.
    - Partial OR no fault-domain coverage → `siteData: 'assumed'`, `reservedFraction` = 0.5 symmetric, per-site capacities `null`.
    - Non-stretched cluster → unchanged failback (no reservation; siteData value is irrelevant but must be a valid enum — use `'assumed'`).
    - The numeric reservation/factor math is byte-for-byte the same as the shipped engine for every existing matrix case (4+4→0.5, 6+4 tagged→0.6, 6+4 untagged→0.5, 2+2→0.5, 8+0 failback). Only the discriminator label changes.
  </behavior>
  <action>In `estate.ts`: delete `export type StretchedConfidence`; in `ClusterAggregate` replace `stretchedConfidence: StretchedConfidence` with `siteData: 'detected' | 'assumed'`. Keep `reservedFraction`, `siteACapacityGhz/siteBCapacityGhz/siteACapacityRamMib/siteBCapacityRamMib`. In `aggregateClusters.ts` `reservationFor()`: keep the fraction/site math EXACTLY; change the returned `confidence` to `siteData` where `'high'`→`'detected'`, and BOTH `'medium'` and `'low'`→`'assumed'` (the partial/none distinction is dropped — UAT G1: no judgement). Update the non-stretched failback object's `confidence` field to `siteData: 'assumed'`. Update the return mapping (`stretchedConfidence: res.confidence` → `siteData: res.siteData`). Rework `aggregateClusters.test.ts`: replace every `stretchedConfidence` assertion — `expect(c.siteData).toBe('detected')` for 4+4/6+4-tagged/2+2/CL_VXB1K_CORE; `'assumed'` for 6+4-untagged + partial; keep all `reservedFraction`/`drReservedGhz`/per-site assertions unchanged (proves math intact). Fix `globals.test.ts` ClusterAggregate fixture: `stretchedConfidence: 'high'` → `siteData: 'assumed'` (fixture is non-stretched). Branded units / pure-engine rules unchanged.</action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/engines/aggregation/aggregateClusters --coverage --reporter=dot</automated>
  </verify>
  <done>StretchedConfidence type gone; siteData enum present; full reservation matrix passes with identical numbers; aggregateClusters ≥75% coverage; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: ClusterColumn factual caption + drop chip + estate count + str i18n + REQUIREMENTS reword</name>
  <files>src/components/stretched/StretchedPill.tsx, src/components/dashboard/ClusterColumn.tsx, src/components/dashboard/GlobalDashboard.tsx, src/i18n/locales/en/str.json, src/i18n/locales/fr/str.json, .planning/REQUIREMENTS.md</files>
  <action>`StretchedPill.tsx`: delete the `LowConfidenceChip` export entirely (ClusterColumn is its only consumer); keep `StretchedPill` (pill, aria-pressed idiom, placement) UNCHANGED. `ClusterColumn.tsx`: remove the `LowConfidenceChip` import + the `Confidence:` Row + the `{cluster.stretchedConfidence === 'low' && <LowConfidenceChip/>}` block. New stretched block (pill placement unchanged — footer band): when `cluster.stretched`: render the `StretchedPill` always; when `siteData === 'detected'` render Site A / Site B GHz rows (existing `siteGhz` helper + `siteACapacityGhz/siteBCapacityGhz`) + the Reservation % Row (`fmtPercentWhole(cluster.reservedFraction, loc)`) + a neutral caption Row showing `tStr('siteData.detected')`; when `siteData === 'assumed'` render ONLY the Reservation % Row + caption `tStr('siteData.assumed')` (NO Site A/B rows). Caption uses the existing neutral 12px slate text style (NOT the chip styling, no icon). `GlobalDashboard.tsx`: add a neutral estate-level line near the dashboard summary showing `tStr('stretched.estateCount', { count: stretchedClusters.size })` (read via the existing `selectStretchedClusters` store selector already imported; do NOT add a second `useMemo` — derive inline from the Set size). `str.json` EN+FR: remove `confidence.*` and `lowConfidence.chip`; add `siteData.detected`, `siteData.assumed`, `stretched.estateCount` (placeholders only, no pre-formatted numbers, no editorial verbs, EN/FR key parity). `REQUIREMENTS.md`: replace the STR-03 line with the single factual requirement — `**STR-03**: User sees, factually, whether a stretched cluster's per-site split comes from real fault-domain data (Site A/B shown) or an assumed symmetric split — no high/medium/low verdict, no warning chip` — and DELETE the STR-04 line and its row in the requirement-tracking table (do not renumber other IDs). Add a one-line note under STR that vSAN blank-cluster datastore attribution + networks inventory are deferred to Phase 9 (forward pointer, not a P4 requirement).</action>
  <verify>
    <automated>npx tsc --noEmit && npx @biomejs/biome check src/ && npx vitest run --reporter=dot && node -e "const e=require('./src/i18n/locales/en/str.json'),f=require('./src/i18n/locales/fr/str.json');const fl=o=>Object.entries(o).flatMap(([k,v])=>typeof v==='object'?fl(v).map(s=>k+'.'+s):[k]);if(JSON.stringify(fl(e).sort())!==JSON.stringify(fl(f).sort()))throw new Error('str EN/FR key mismatch')" && grep -c 'STR-04' .planning/REQUIREMENTS.md | grep -qx 0 && echo OK</automated>
  </verify>
  <done>LowConfidenceChip gone; ClusterColumn shows factual caption (detected→rows+caption / assumed→caption no rows), no Confidence verdict; estate "N clusters marked stretched" renders; str EN/FR parity, no confidence keys; REQUIREMENTS.md has one factual STR-03 and zero STR-04; single useMemo invariant intact; typecheck/biome/suite green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stretched declaration → engine | In-memory inputs-only Set; user's sole decision; never persisted |
| siteData label → user | A factual discriminator, never a judgement of the user's call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-04R-01 | Tampering | reservation math regression while relabelling | mitigate | Task 1 keeps fraction math byte-identical; full matrix asserts unchanged numbers |
| T-04R-02 | Information Disclosure | re-introducing an editorial/judgement framing | mitigate | siteData is a neutral enum; no chip; PROJECT.md denylist + factual captions; UAT G1 |
| T-04R-03 | Information Disclosure | stretched set persisted | accept | Unchanged shipped in-memory Set slice; no persist (privacy invariant) |
</threat_model>

<verification>
From `/Users/fjacquet/Projects/vatlas`:
1. `npx tsc --noEmit` — clean (app + test config)
2. `npx @biomejs/biome check .` — clean (NOT `npm run lint`; RTK intercept)
3. `npx vitest run --coverage --reporter=dot` — full suite green; `aggregateClusters` ≥75%; reservation matrix numbers identical to pre-change
4. Single-useMemo grep gate: actual `useMemo(` call sites remain exactly 2 (useEstateView + pre-existing P1 SnapshotListSidebar) — no new memo
5. i18n EN/FR parity for `str` (and unchanged mvc/alloc/dr) — key sets identical
6. `npm run check:supply-chain` + `node scripts/check-bundle-size.mjs` — green (no new dep/chunk)
7. REQUIREMENTS.md: exactly one STR-03 (factual wording), zero `STR-04`
8. REAL-FILE/browser UAT (mandatory per feedback_real_file_uat): on the real allvCenters estate, toggle stretched on a fault-domain-tagged cluster → Site A/B rows + Reservation % + "site data: detected"; toggle stretched on an untagged cluster → Reservation % + "symmetric split assumed (no site data)", NO Site A/B, NO chip, NO verdict; estate shows "N clusters marked stretched"; toggling one collision-suffixed cluster does not flip another
9. ROADMAP reconcile (project_roadmap_reconcile): flip the 04-05 plan checkbox + Progress row; Phase 4 status reflects re-derived plan complete
</verification>

<success_criteria>

- The auto confidence verdict + LowConfidenceChip are GONE; a factual `siteData` ('detected'/'assumed') drives a neutral caption (UAT G1).
- Per-site reservation math is provably unchanged (matrix numbers identical).
- Tagged → Site A/B rows + Reservation % + "site data: detected"; inferred → Reservation % + "symmetric split assumed (no site data)", no Site A/B.
- Estate "N clusters marked stretched" factual line renders.
- Independent stretched per collision-suffixed cluster (MVC-02) — unchanged, confirmed.
- REQUIREMENTS.md: one factual STR-03, no STR-04; vSAN + networks documented as Phase-9 deferrals.
- Single-useMemo invariant intact; typecheck/biome/coverage/supply-chain/bundle green; real-file UAT passes.
</success_criteria>

<output>
After completion, create `.planning/phases/04-multi-vcenter-stretched-allocation-dr/04-05-SUMMARY.md`. Then reconcile ROADMAP (flip 04-05 + Progress row; Phase 4 = re-derived plan complete).
</output>
