---
phase: 04-multi-vcenter-stretched-allocation-dr
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/hooks/useAllocationHash.ts
  - src/hooks/useAllocationHash.test.ts
  - src/hooks/useEstateView.ts
  - src/engines/aggregation/estateView.ts
  - src/engines/aggregation/aggregateClusters.ts
  - src/engines/aggregation/aggregateClusters.test.ts
  - src/types/estate.ts
  - src/components/allocation/AllocationSliders.tsx
  - src/components/dashboard/GlobalDashboard.tsx
  - src/i18n/index.ts
  - src/i18n/locales/en/alloc.json
  - src/i18n/locales/fr/alloc.json
autonomous: true
requirements: [ALC-01, ALC-02, ALC-03, ALC-04]
user_setup: []

must_haves:
  truths:
    - "User moves the CPU allocation slider 4:1 → 8:1 and the consolidation/headroom verdict recomputes against physical cores (not threads) with NO localStorage write"
    - "The active ratios are reflected in the URL hash (#alloc=cpu:8,ram:1) and survive a page reload from that URL"
    - "On first load defaults are CPU 4:1 / RAM 1:1"
    - "Named presets (1:1, 4:1, 8:1, VDI 10:1) set both sliders; moving a slider off a preset clears the active preset"
    - "A malformed/oversized #alloc hash falls back to the 4:1/1:1 defaults (no eval/JSON.parse of the raw hash)"
  artifacts:
    - path: "src/hooks/useAllocationHash.ts"
      provides: "URL-hash codec — bounded-regex parse, clamp, history.replaceState write; NOT a useMemo"
      contains: "history.replaceState"
    - path: "src/components/allocation/AllocationSliders.tsx"
      provides: "Two native range sliders + preset segmented group (aria-pressed idiom)"
      contains: "type=\"range\""
    - path: "src/hooks/useAllocationHash.test.ts"
      provides: "Parse/clamp/fallback + round-trip tests"
      contains: "describe"
  key_links:
    - from: "src/components/dashboard/GlobalDashboard.tsx"
      to: "src/hooks/useAllocationHash.ts"
      via: "ratios threaded into useEstateView like mode"
      pattern: "useAllocationHash"
    - from: "src/hooks/useEstateView.ts"
      to: "src/engines/aggregation/aggregateClusters.ts"
      via: "allocRatios passed as a scalar param (like AccountingMode)"
      pattern: "allocRatios|ratios"
---

<objective>
Ship the CPU/RAM allocation sliders + named presets with URL-hash-ONLY
persistence. The slider changes the headroom verdict only — `vcpuPerPcpu` is
already physical-core-based (ALC-04 satisfied structurally since Phase 2); this
plan adds a guard test confirming that and threads `{cpuRatio,ramRatio}` into
`useEstateView`/`aggregateClusters` exactly like the shipped `mode` param.

Purpose: Operators need to model consolidation at configurable ratios without
any cross-session persistence (privacy invariant) — the URL hash is the only
sanctioned, shareable state surface.
Output: `useAllocationHash` codec hook, `AllocationSliders` toolbar component,
the ratio param threaded through the single memo, the ALC-04 guard test, the
`alloc` i18n namespace.

This plan depends on 04-01 (merged estate / single-memo contract) but is
INDEPENDENT of 04-02 and 04-04 — it can execute in parallel with 04-02 in
principle; sequenced after for review pacing (no file overlap with 04-02 except
the shared single-memo files, which 04-02 also touches → run after 04-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/04-multi-vcenter-stretched-allocation-dr/04-RESEARCH.md

# ^ §Pattern 4 (ratios threaded like AccountingMode; slider changes the headroom

# verdict, NOT vcpuPerPcpu), §Pattern 5 (URL-hash codec — NOT a useMemo

# bounded regex, clamp, history.replaceState, fallback {4,1})

# §Code Examples "Allocation headroom verdict", §Security V5 (defensive hash

# parse — no eval/JSON.parse of attacker hash), Open Question 3 (pass as arg)

@.planning/phases/04-multi-vcenter-stretched-allocation-dr/04-PATTERNS.md

# ^ useAllocationHash section (invert src/hooks/useTheme.ts localStorage-sync to

# window.location.hash — READ useTheme.ts for the sync skeleton)

# AllocationSliders section (AccountingModeToggle preset group + native range

# fmtRatio format.ts:92-99 "X.X : 1"; aria-label+aria-valuetext)

# GlobalDashboard section (:48-87 lifted useState + single useEstateView caller

# — add ratios source like `mode`, NOT a memo), Shared Patterns

@.planning/phases/04-multi-vcenter-stretched-allocation-dr/04-UI-SPEC.md

# ^ §Layout (single allocation toolbar below the Phase-2 dashboard toolbar

# full-width .panel, Dashboard view only), §Interaction "Allocation sliders +

# presets" (ranges CPU 1–16 / RAM 0.5–4, default 4:1/1:1, preset clears on

# manual move, URL-hash only), §Copywriting (alloc.* keys), §i18n

@src/hooks/useTheme.ts
@src/hooks/useEstateView.ts
@src/engines/aggregation/estateView.ts
@src/engines/aggregation/aggregateClusters.ts
@src/types/estate.ts
@src/components/dashboard/GlobalDashboard.tsx
@src/components/dashboard/AccountingModeToggle.tsx
@src/utils/format.ts

<interfaces>
<!-- ALC-04 already satisfied structurally (aggregateClusters.ts:68):
     vcpuPerPcpu = vcpuAllocated / usablePhysicalCores  (physical cores;
     VHostRow has no threads field — Moderate-4 type-prevented, confirmed 02-02).
     This plan adds a GUARD TEST asserting that, and the headroom verdict:
       capacityVcpu  = usablePhysicalCores * cpuRatio   (default 4)
       capacityRamMib= physicalRamMib * ramRatio        (default 1)
     The slider changes the verdict ONLY, never vcpuPerPcpu. -->

<!-- Hash format (RESEARCH Pattern 5): #alloc=cpu:8,ram:1.25
     bounded regex, clamp to slider bounds, fallback {cpu:4,ram:1}.
     history.replaceState(null,'',`#alloc=cpu:${c},ram:${r}`). Returns
     [ratios, setRatios]; event-driven sync — NOT a render memo. -->

<!-- useTheme.ts is the project's ONLY state-sync hook precedent — invert its
     localStorage idiom to window.location.hash. Read it for the skeleton. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useAllocationHash codec hook (bounded-regex parse, clamp, replaceState)</name>
  <files>src/hooks/useAllocationHash.ts, src/hooks/useAllocationHash.test.ts</files>
  <behavior>
    - Empty/absent hash → returns defaults `{cpu:4, ram:1}` (ALC-02).
    - `#alloc=cpu:8,ram:1.25` → `{cpu:8, ram:1.25}`.
    - Out-of-bounds (`cpu:999`, `ram:-3`) → clamped to slider bounds (CPU 1–16, RAM 0.5–4).
    - Malformed / oversized / injection-y hash (`#alloc=<script>`, a 10kB string) → falls back to `{cpu:4, ram:1}`; NO `eval`, NO `JSON.parse` of the raw hash; the bounded regex has no catastrophic-backtracking quantifier (ReDoS-safe).
    - `setRatios({cpu,ram})` writes `#alloc=cpu:..,ram:..` via `history.replaceState` (NOT pushState — no history spam) and does NOT touch localStorage/sessionStorage.
  </behavior>
  <action>Create `useAllocationHash.ts` by inverting the `src/hooks/useTheme.ts` state-sync skeleton (its localStorage read/write) to `window.location.hash`. Parse with a STRICT bounded regex (e.g. `^#?alloc=cpu:(\d{1,2}(?:\.\d{1,2})?),ram:(\d{1,2}(?:\.\d{1,2})?)$` — bounded quantifiers, no `.*`, RESEARCH Security V5 / STRIDE Tampering-DoS); on any non-match or NaN → defaults `{cpu:4,ram:1}`. Clamp parsed values to CPU [1,16] / RAM [0.5,4]. `setRatios` → `history.replaceState(null,'',`#alloc=cpu:${c},ram:${r}`)`. NEVER `eval`/`JSON.parse` the raw hash. Return `[ratios, setRatios]`. This is event-driven sync — explicitly NOT a `useMemo` (does not count against the single-useMemo invariant; RESEARCH Pattern 5). NO localStorage/sessionStorage/Zustand-persist (ALC-03 / privacy invariant). Tests cover every behavior bullet incl. the ReDoS/oversized/injection fallback and the localStorage-untouched assertion.</action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/rvtui && npx vitest run src/hooks/useAllocationHash --reporter=dot</automated>
  </verify>
  <done>Parse/clamp/default/round-trip + malformed-hash fallback all pass; no eval/JSON.parse of raw hash; localStorage never written; hook is not a useMemo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread allocRatios through the single memo + ALC-04 guard test + headroom verdict</name>
  <files>src/hooks/useEstateView.ts, src/engines/aggregation/estateView.ts, src/engines/aggregation/aggregateClusters.ts, src/types/estate.ts, src/engines/aggregation/aggregateClusters.test.ts</files>
  <behavior>
    - `aggregateClusters` with `{cpuRatio:4,ramRatio:1}` vs `{cpuRatio:8,ramRatio:1}` → the headroom/capacity figures change; `vcpuPerPcpu` is IDENTICAL between the two (the slider changes the verdict ONLY, never vcpuPerPcpu — ALC-04 guard).
    - `vcpuPerPcpu` is computed against `usablePhysicalCores` (physical cores), provably not threads (the guard test asserts the formula basis; VHostRow has no threads field).
    - Default ratios CPU 4:1 / RAM 1:1 when not supplied.
    - Threading the ratios does NOT add a second `useMemo` (grep gate).
  </behavior>
  <action>Add `allocRatios: { cpuRatio: number; ramRatio: number }` as a scalar param to `aggregateClusters` and `buildEstateView`, threaded exactly like the shipped `mode` param (RESEARCH Pattern 4 / Open-Q 3 — pass as an argument, NOT a store mirror). Compute capacity/headroom: `capacityVcpu = usablePhysicalCores * cpuRatio`, `capacityRamMib = physicalRamMib * ramRatio`, expose on `ClusterAggregate` (extend `estate.ts`); branded unwrap/rewrap (`(x as number)` → compute → `cores()`/`mib()`). DO NOT change `vcpuPerPcpu` — it is already physical-core-based (aggregateClusters.ts:68). Thread ratios into `useEstateView` by adding to the existing single `useMemo` dependency array (signature `useEstateView(mode, ratios)`); NO second memo. Add the ALC-04 GUARD TEST to `aggregateClusters.test.ts`: assert vcpuPerPcpu is invariant across cpuRatio 4 vs 8 (slider affects verdict only) and that the formula divides by `usablePhysicalCores` (physical cores, Moderate-4 type-prevented).</action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/rvtui && npx tsc --noEmit && npx vitest run src/engines/aggregation --reporter=dot && grep -rn 'useMemo' src --include='*.ts' --include='*.tsx' | grep -v '\.test\.'</automated>
  </verify>
  <done>allocRatios threaded like `mode`; headroom recomputes with the ratio; vcpuPerPcpu invariant guard test passes (physical cores, not threads); default 4:1/1:1; exactly one useMemo (useEstateView.ts); typecheck green.</done>
</task>

<task type="auto">
  <name>Task 3: AllocationSliders toolbar (native ranges + preset segmented group) + dashboard wiring + alloc i18n</name>
  <files>src/components/allocation/AllocationSliders.tsx, src/components/dashboard/GlobalDashboard.tsx, src/i18n/index.ts, src/i18n/locales/en/alloc.json, src/i18n/locales/fr/alloc.json</files>
  <action>Create `AllocationSliders.tsx`: two native `<input type="range">` (CPU 1–16, RAM 0.5–4 — keyboard-operable by construction) with `aria-label` + `aria-valuetext` carrying the localized "n : 1" via `fmtRatio` (format.ts:92-99); thumb/fill `primary-600`. Named preset buttons **1:1 / 4:1 / 8:1 / VDI 10:1** = ONE mutually-exclusive group reusing the `AccountingModeToggle` `<fieldset role="group"> + aria-pressed` idiom VERBATIM (active navy-fill `bg-primary-600 text-white`; NOT bespoke). Selecting a preset sets both sliders; manually moving a slider off a preset clears the active preset (no preset pressed — factual, no error). A Caption (12/400) echo line states the active ratios via `fmtRatio` (parallels the Phase-2 accounting-mode echo). State source = `useAllocationHash` (URL-hash only). In `GlobalDashboard.tsx` (:48-87): call `useAllocationHash`, thread `ratios` into `useEstateView(mode, ratios)` (it stays the SINGLE caller), and insert `<AllocationSliders>` as a full-width `.panel` toolbar directly BELOW the existing Phase-2 dashboard toolbar (the `AccountingModeToggle` row, :72-78), Dashboard view only (UI-SPEC §Layout). Single instance, governs the whole estate. i18n: add `'alloc'` to `NAMESPACES` + register both locales; create `locales/{en,fr}/alloc.json` with UI-SPEC §i18n keys (`alloc.title`, `alloc.cpu.label`="CPU ratio (vCPU per physical core)", `alloc.ram.label`, `alloc.preset.1to1|4to1|8to1|vdi10to1`, `alloc.echo`="CPU {{cpu}} · RAM {{ram}}") — `{{placeholder}}` only, NO pre-formatted numbers, NO editorial verbs, EN/FR parity.</action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/rvtui && npx tsc --noEmit && npx vitest run --reporter=dot && npx @biomejs/biome check src/components/allocation src/components/dashboard/GlobalDashboard.tsx</automated>
  </verify>
  <done>Two keyboard-operable range sliders + preset segmented group (aria-pressed idiom, not bespoke); default 4:1/1:1 echo; ratios round-trip through the URL hash only (no localStorage); toolbar below the Phase-2 toolbar, Dashboard-only; alloc namespace EN/FR parity; typecheck + suite + biome green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL hash `#alloc=` → app | UNTRUSTED: a shared/tampered link can carry a malformed/oversized hash |
| ratios → engine | Scalar inputs threaded like `mode`; never persisted to storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Tampering / DoS | malicious `#alloc=` (ReDoS / huge-number / injection) | mitigate | Strict bounded-quantifier regex (no `.*`), numeric clamp to slider min/max, default-on-failure, no `eval`/`JSON.parse` — Task 1 fallback tests + semgrep gate |
| T-04-10 | Information Disclosure | ratio/slider state persisted to localStorage | mitigate | URL hash ONLY via `history.replaceState`; localStorage-never-written assertion in Task 1 tests; only `vatlas-theme`/`vatlas-lang` keys remain |
| T-04-11 | Information Disclosure | dynamic code execution from hash | mitigate | No `eval`/`Function`/dynamic import of hash content; semgrep on generated code (in <verification>) |
</threat_model>

<verification>
From `/Users/fjacquet/Projects/rvtui`:
1. `npx tsc --noEmit`
2. `npx @biomejs/biome check .` (NOT `npm run lint`)
3. `npx vitest run --coverage --reporter=dot` — includes the ALC-04 vcpuPerPcpu-invariant guard test + the malformed-hash fallback test
4. `node scripts/check-bundle-size.mjs` — gates green (no new dep)
5. `npm run check:supply-chain` — no new deps
6. semgrep the generated code — no `eval`/`Function`/dynamic-import sink on hash input
7. Single-useMemo grep gate: exactly one `useMemo` (useEstateView.ts)
8. REAL-FILE/browser UAT: load a real workbook, move CPU slider 4:1→8:1 → headroom recomputes, URL hash updates to `#alloc=cpu:8,ram:1`, DevTools Application tab shows NO new localStorage key; reload from that URL → sliders restore to 8:1/1:1; paste a junk `#alloc=zzz` → falls back to 4:1/1:1
9. ROADMAP reconcile note per project_roadmap_reconcile (manual reconcile at end of execute-phase).
</verification>

<success_criteria>
Mirrors ROADMAP Phase-4 Success Criterion 3:

- A user adjusts the CPU allocation slider 4:1 → 8:1 and sees the consolidation/headroom recompute against physical cores (not threads) with NO `localStorage` write, the new ratio reflected in the URL hash, and surviving a reload from that URL.
- First-load defaults are CPU 4:1 / RAM 1:1; presets (1:1/4:1/8:1/VDI 10:1) reuse the aria-pressed idiom (no bespoke control).
- A malformed/oversized `#alloc` hash safely falls back to defaults (no eval/JSON.parse).
- `useEstateView` remains the project's ONLY `useMemo`.
</success_criteria>

<output>
After completion, create `.planning/phases/04-multi-vcenter-stretched-allocation-dr/04-03-SUMMARY.md`
</output>
