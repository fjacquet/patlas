---
phase: 04-multi-vcenter-stretched-allocation-dr
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - src/engines/drSim/allocate.ts
  - src/engines/drSim/runScenario.ts
  - src/engines/drSim/index.ts
  - src/engines/drSim/allocate.test.ts
  - src/engines/drSim/runScenario.test.ts
  - src/types/estate.ts
  - src/store/snapshotStore.ts
  - src/hooks/useEstateView.ts
  - src/engines/aggregation/estateView.ts
  - src/components/dr/DrSimPanel.tsx
  - src/components/dashboard/GlobalDashboard.tsx
  - src/i18n/index.ts
  - src/i18n/locales/en/dr.json
  - src/i18n/locales/fr/dr.json
autonomous: true
requirements: [DRS-01, DRS-02, DRS-03, DRS-04, DRS-05, DRS-06]

must_haves:
  truths:
    - "User selects Host-loss / Cluster-loss / vCenter-loss mode and marks component(s) failed; before/after estate numbers + evacuee total + per-survivor verdict render"
    - "Failed components render struck-through + dimmed with a neutral grey 'simulated failed' chip — reversible, NO confirmation dialog, NO red"
    - "vCenter-loss removes all rows of a viSdkUuid from the merged estate (depends on the merge)"
    - "DR result subtracts the stretched per-site reservation computed in 04-02"
    - "Every DR result carries a confidence enum + caveats[] (i18n keys); an assumptions panel lists what is and is not modeled"
  artifacts:
    - path: "src/engines/drSim/runScenario.ts"
      provides: "Pure (merged,scenario,stretched,ratios) -> DrSimResult re-running shipped aggregation on survivors"
      contains: "export const runScenario"
    - path: "src/engines/drSim/allocate.ts"
      provides: "Pure per-survivor verdict absorbs|tight|overflows (no library)"
      contains: "absorbs"
    - path: "src/components/dr/DrSimPanel.tsx"
      provides: "3-mode segmented selector + fail-select + before/after + assumptions + caveats"
      contains: "aria-pressed"
  key_links:
    - from: "src/engines/drSim/runScenario.ts"
      to: "src/engines/aggregation (aggregateClusters + aggregateGlobals)"
      via: "re-run shipped engines on the survivor row subset"
      pattern: "aggregateClusters|aggregateGlobals"
    - from: "src/hooks/useEstateView.ts"
      to: "src/engines/drSim/runScenario.ts"
      via: "called inside the single useMemo"
      pattern: "runScenario"
    - from: "src/components/dr/DrSimPanel.tsx"
      to: "useEstateView().drSim"
      via: "pure presenter, plain props, no useMemo"
      pattern: "drSim"
---

<objective>
Ship the three DR simulation modes (host loss, cluster loss, vCenter loss) as a
pure engine composing the shipped aggregation on a survivor row subset, with the
stretched per-site reservation (04-02) subtracted, plus the UI panel: 3-mode
segmented selector, neutral reversible simulated-failed selection (no confirm),
before/after + evacuee totals + per-survivor verdict, an explicit assumptions
panel, and a factual `caveats[]`.

Purpose: Wrong DR numbers are the project's #1 risk — the structural mitigation
is `confidence` + `caveats[]` on every result and an always-visible assumptions
panel disclosing what is NOT modeled (HA admission control, anti-affinity,
restart priority, split-brain). Numbers only, never editorial.
Output: `src/engines/drSim/` module, the `scenario` store slice, the single-memo
DR integration, `DrSimPanel`, the `dr` i18n namespace.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/04-multi-vcenter-stretched-allocation-dr/04-RESEARCH.md

# ^ §Pitfall 5 (DR trust — confidence + caveats[] + assumptions panel

# surface vCluster.AdmissionControlEnabled/Failover Level as factual context

# reservation-vs-capacity >80% caveat, respect the 3 accounting modes)

# §Code Examples "Allocation headroom verdict", Assumptions A5 (caveats =

# string[] of i18n keys, confidence high|medium|low), Deferred Ideas

# (DR presets OUT, HA hard-enforcement OUT — caveat only)

@.planning/phases/04-multi-vcenter-stretched-allocation-dr/04-PATTERNS.md

# ^ drSim/runScenario section (estateView.ts:33-92 buildEstateView compose

# pattern — re-run aggregateClusters+aggregateGlobals on a filtered subset

# estate.ts trends:null idiom :318-319 for drSim:DrSimResult|null)

# drSim/allocate section (aggregateClusters derivation style + headroom

# verdict, factual enum no color), DrSimPanel section (CpuReadyPanel +

# AccountingModeToggle 3-option + ColumnPicker checkbox multi-select +

# GlobalDashboard DashboardError :19-30 error.message-only)

# snapshotStore section (scenario slice — Sets replaced never mutated)

# GlobalDashboard section (:48-87 append DrSimPanel after CpuReadyPanel

# 2xl break), Shared Patterns (segmented idiom, region error fallback

# pure-engine barrel, locale format em-dash)

@.planning/phases/04-multi-vcenter-stretched-allocation-dr/04-UI-SPEC.md

# ^ §Color (simulated-failed = neutral grey strike/dim, NO red, NO confirm

# verdict neutral enum, evacuee total = the ONE gold accent figure)

# §Layout (DR panel = full-width .panel appended after CpuReadyPanel, 2xl

# 48px break, NOT a new view), §Interaction "DR-mode selector" +

# "Marking failed" + "Before/after + evacuee + verdict" + "Assumptions panel"

# §Copywriting (dr.* keys, assumptions copy verbatim FR+EN), §i18n

@src/engines/aggregation/estateView.ts
@src/engines/aggregation/aggregateClusters.ts
@src/engines/aggregation/globals.ts
@src/types/estate.ts
@src/store/snapshotStore.ts
@src/hooks/useEstateView.ts
@src/components/dashboard/GlobalDashboard.tsx
@src/components/dashboard/AccountingModeToggle.tsx
@src/components/dashboard/CpuReadyPanel.tsx
@src/components/inventory/ColumnPicker.tsx
@src/utils/format.ts

<interfaces>
<!-- estateView.ts:40-52 — the compose pattern to copy -->
  const clusters = aggregateClusters({ vinfo, vhost, mode, stretchedClusters, datastoreCountByCluster })
  const globals  = aggregateGlobals(clusters, datastoreCount, totalStorageMib)
<!-- DR = the SAME composition on a row subset (failed rows filtered out). -->

<!-- DrSimResult (define in estate.ts; mirror the trends:null|T idiom :318-319):
  interface DrSimResult {
    mode: 'host'|'cluster'|'vcenter'
    before: GlobalSummary
    after: GlobalSummary
    evacueeVcpu: Cores            // gold accent figure
    evacueeVramMib: MiB
    perSurvivor: { cluster: string; verdict: 'absorbs'|'tight'|'overflows' }[]
    confidence: 'high'|'medium'|'low'
    caveats: string[]             // i18n KEYS, never free text, never numbers
  }
  EstateView.drSim: DrSimResult | null   // null when no failed component selected
-->

<!-- allocate verdict (RESEARCH headroom-verdict example, factual enum, no color):
  capacity = usablePhysicalCores * cpuRatio / physicalRamMib * ramRatio
  verdict = allocated <= 0.8*cap ? 'absorbs' : allocated <= cap ? 'tight' : 'overflows'
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: drSim/allocate.ts — pure per-survivor verdict (no library)</name>
  <files>src/engines/drSim/allocate.ts, src/engines/drSim/allocate.test.ts</files>
  <behavior>
    - allocated ≤ 0.8·capacity → 'absorbs'; ≤ capacity → 'tight'; > capacity → 'overflows'.
    - capacity = `usablePhysicalCores * cpuRatio` (CPU) / `physicalRamMib * ramRatio` (RAM); default ratios CPU 4:1 / RAM 1:1.
    - The combined per-survivor verdict is the worse of the CPU and RAM verdicts (a survivor 'tight' on RAM but 'absorbs' on CPU → 'tight').
    - Branded units round-trip correctly (`(x as number)` compute → `cores()`/`mib()`); no NaN on zero-capacity (degenerate cluster → 'overflows' if any allocated, else 'absorbs').
  </behavior>
  <action>Create `allocate.ts` in the `aggregateClusters.ts` derivation style (pure scalar arithmetic, branded unwrap/rewrap). Implement the RESEARCH "Allocation headroom verdict" exactly: factual enum `'absorbs'|'tight'|'overflows'` — NO color, NO "good/poor" (UI-SPEC §Color, PROJECT.md line 39 denylist). Combine CPU+RAM verdicts as the worse of the two. Default ratios CPU 4:1 / RAM 1:1 (ALC-02). NO external math/stats library (locked decision — grade-school arithmetic). Tests cover all three verdict bands per resource + the worse-of combination + zero-capacity edge.</action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/rvtui && npx vitest run src/engines/drSim/allocate --coverage --reporter=dot</automated>
  </verify>
  <done>absorbs/tight/overflows bands correct per resource; worse-of combination correct; branded round-trip; zero-capacity safe; no library import; ≥75% coverage on the file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: drSim/runScenario.ts — 3 modes, survivor re-aggregation, confidence + caveats</name>
  <files>src/engines/drSim/runScenario.ts, src/engines/drSim/index.ts, src/types/estate.ts, src/engines/drSim/runScenario.test.ts</files>
  <behavior>
    - Host-loss (DRS-01): selected hosts removed from `merged.vhost` (+ their VMs reassigned/excluded per accounting mode); before/after GlobalSummary differ; evacuee = Σ failed-side vCPU/vRAM.
    - Cluster-loss (DRS-02): all hosts of the selected cluster(s) removed; evacuee = that cluster's VM vCPU/vRAM sum.
    - vCenter-loss (DRS-03): ALL rows of the selected `viSdkUuid` removed from the MERGED estate (depends on Plan 04-01 merge) — survivors are the other vCenters.
    - Stretched reservation from 04-02 is subtracted on survivors (a stretched survivor reserves its per-site fraction before absorbing evacuees).
    - DRS-06: result carries before/after GlobalSummary, evacuee totals, per-survivor verdict (via allocate.ts).
    - DRS-05: result carries `confidence: 'high'|'medium'|'low'` + `caveats: string[]` (i18n KEYS). A reservation-vs-capacity check emits a caveat when survivor memory reservation > 80% survivor RAM (Moderate-10). vCluster AdmissionControlEnabled/Failover Level surfaced as factual caveat context, never enforced. Empty scenario → `EstateView.drSim` is `null` (no failed component selected).
  </behavior>
  <action>Create `runScenario(merged, scenario, stretchedFlags, allocRatios) → DrSimResult` copying the `estateView.ts:33-92` compose pattern: (1) filter failed host/cluster/vCenter rows out of `merged.vhost`/`vinfo` (NEVER mutate — fresh arrays); for vCenter-loss filter by `row.viSdkUuid`; (2) re-run the SHIPPED `aggregateClusters` + `aggregateGlobals` on survivors (Don't-Hand-Roll — reuse, don't re-derive), passing `stretchedClusters` + `allocRatios` so the per-site reservation (04-02) is subtracted; (3) compute evacuee totals (Σ failed-side vCPU/vRAM, respecting the active accounting mode — powered-off/template handling via the shipped 3 modes), before/after `GlobalSummary`, per-survivor verdict via `allocate.ts`; (4) emit `confidence` + `caveats: string[]` of i18n KEYS (never free text, never a pre-formatted number, never an editorial verb — RESEARCH A5 / PROJECT.md line 39): reservation-vs-capacity >80% caveat (Moderate-10), surface parsed `vCluster.AdmissionControlEnabled`/`Failover Level` as factual context caveats (disclose, never enforce). Add `DrSimResult` + `EstateView.drSim: DrSimResult | null` to `estate.ts` (mirror the `trends: TimelinePoint[] | null` idiom :318-319). Add the `index.ts` barrel mirroring `engines/aggregation/index.ts`. Pure: no React/Zustand/Zod. Tests: host/cluster/vCenter modes (vCenter-loss over a 3-viSdkUuid merged fixture), evacuee totals, a stretched survivor subtracting its reservation, the >80% reservation caveat, empty-scenario → null.</action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/rvtui && npx vitest run src/engines/drSim --coverage --reporter=dot</automated>
  </verify>
  <done>3 modes correct (vCenter-loss filters by viSdkUuid on the merged estate); stretched reservation subtracted on survivors; before/after + evacuee + per-survivor verdict + confidence + caveats[] present; empty scenario → drSim null; `engines/drSim/` ≥75% coverage.</done>
</task>

<task type="auto">
  <name>Task 3: scenario store slice + single-memo DR integration + DrSimPanel + dr i18n</name>
  <files>src/store/snapshotStore.ts, src/hooks/useEstateView.ts, src/engines/aggregation/estateView.ts, src/components/dr/DrSimPanel.tsx, src/components/dashboard/GlobalDashboard.tsx, src/i18n/index.ts, src/i18n/locales/en/dr.json, src/i18n/locales/fr/dr.json</files>
  <action>Add inputs-only `scenario: { failedHosts: Set<string>; failedClusters: Set<string>; failedVCenters: Set<string> }` slice + replace-never-mutate setters + selector to `snapshotStore.ts` (Set-replace idiom :39-44; NO persist/localStorage — in-memory only). Thread `scenario` into the SINGLE `useMemo` in `useEstateView` (add to its dep array; call `runScenario(...)` INSIDE the same memo after `buildEstateView`; set `EstateView.drSim`); NO second memo (grep-gated). `buildEstateView` returns `drSim` (null when scenario empty). Create `DrSimPanel.tsx` (pure presenter, plain props from `useEstateView().drSim`, NO useMemo): a full-width `.panel` with a 3-option segmented mode selector (Host/Cluster/vCenter loss) reusing the `AccountingModeToggle` aria-pressed idiom + Arrow-key `onKeyDown` VERBATIM (no bespoke control); fail-selection = checkbox multi-select reusing the Phase-3 `ColumnPicker.tsx` pattern; a selected component renders `line-through` + `text-slate-400 dark:text-slate-500` + a neutral grey "simulated failed" chip (NO red, NO alarm icon); unchecking immediately restores — explicitly NO confirmation dialog (reversible what-if; locked constraint — record the rationale comment for the checker). Before/After = two side-by-side `.panel`-inset stat blocks (xl 32px gap) reusing the `ClusterColumn` `Row` (mono `tabular-nums`, locale formatter); evacuee total = the SINGLE gold `--color-accent-500` figure (the one closed-list accent use); per-survivor verdict list = localized factual enum `absorbs/tight/overflows` in neutral Body text + adjacent number (no color verdict). Always-visible assumptions panel (`.panel`-inset): "Models:" + "Does not model:" factual ADR-style copy (i18n verbatim from UI-SPEC §Copywriting), `Confidence:` Caption row (neutral enum), `Caveats:` `<ul>` from `drSim.caveats[]` i18n keys (empty array → omit the list, no filler); parsed `vCluster.AdmissionControlEnabled`/`Failover Level` as factual context lines (disclosed, never judged). Region errors via the shipped `DashboardError`/`error.message`-only idiom (Critical-2). In `GlobalDashboard.tsx`: append `<DrSimPanel>` AFTER `<CpuReadyPanel>` (:82) separated by a `2xl` (48px) break (UI-SPEC §Layout); GlobalDashboard stays the SINGLE `useEstateView` caller (children get derived props); add lifted `useState` for DR-mode/fail UI selection (component state like `mode`, NOT a memo). i18n: add `'dr'` to `NAMESPACES` + register both locales; create `locales/{en,fr}/dr.json` with all UI-SPEC §i18n `dr.*` keys (incl. `dr.assumptions.models`/`doesNotModel` verbatim EN+FR from §Copywriting, `dr.verdict.absorbs|tight|overflows` FR `absorbe/juste/dépassé`, `dr.mode.host|cluster|vcenter`, `dr.failed.chip`, `dr.evacueeTotal`, `dr.empty.noSelection`, and one `dr.caveats.<key>` per engine-emitted caveat) — `{{placeholder}}` only, NO pre-formatted numbers, NO editorial verbs (denylist + CI FR↔EN key-diff gate apply).</action>
  <verify>
    <automated>cd /Users/fjacquet/Projects/rvtui && npx tsc --noEmit && npx vitest run --reporter=dot && grep -rn 'useMemo' src --include='*.ts' --include='*.tsx' | grep -v '\.test\.'</automated>
  </verify>
  <done>3-mode selector + checkbox fail-select (neutral grey strike/dim chip, reversible, NO confirm/red); before/after + gold evacuee total + neutral verdict list; always-visible assumptions panel + confidence + caveats list (omitted when empty); DrSimPanel appended after CpuReadyPanel with 2xl break; scenario is in-memory store only; useEstateView still the ONLY useMemo; dr namespace EN/FR parity; typecheck + suite green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scenario Sets → engine | In-memory inputs-only store slice; never persisted (no hash, no localStorage) |
| DR numbers → operator budget decisions | Wrong numbers are the project's #1 risk — structural disclosure mitigation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12 | Tampering | DR sim under-states risk (no HA/anti-affinity/restart-priority modeling) | mitigate | Every result carries `confidence` + `caveats[]`; always-visible assumptions panel discloses what is NOT modeled; reservation-vs-capacity >80% caveat (Moderate-10) |
| T-04-13 | Information Disclosure | scenario persisted across sessions | mitigate | In-memory store slice only, Sets replaced never mutated; NO persist/localStorage (grep gate) |
| T-04-14 | Information Disclosure | host/VM names leaked via DR error or editorial verdict | mitigate | Region ErrorBoundary `error.message`-only (Critical-2); verdict is a factual enum, denylist lint; caveats are i18n keys not free text |
| T-04-15 | Tampering | simulated-failed framed as a destructive action | accept | Reversible what-if input (no delete, no confirm, no red) by explicit locked design — documented rationale comment for the checker |
</threat_model>

<verification>
From `/Users/fjacquet/Projects/rvtui`:
1. `npx tsc --noEmit`
2. `npx @biomejs/biome check .` (NOT `npm run lint`)
3. `npx vitest run --coverage --reporter=dot` — `engines/drSim/` ≥75%; full Phase-4 suite green
4. `node scripts/check-bundle-size.mjs` — gates green (no new dep/chunk)
5. `npm run check:supply-chain` — no new deps (no math/stats library)
6. semgrep the generated code
7. Single-useMemo grep gate: exactly one `useMemo` (useEstateView.ts)
8. i18n FR↔EN key-diff gate: `dr`/`mvc`/`str`/`alloc` namespaces have identical key sets EN vs FR; no editorial verb in any value (denylist)
9. REAL-FILE/browser UAT (mandatory per feedback_real_file_uat): load the real `allvCenters` merged estate, simulate vCenter-loss of one `viSdkUuid` → before/after numbers + evacuee total + per-survivor verdict + assumptions panel + a non-empty caveats list render; toggle a failed component off → it restores with no confirm dialog
10. ROADMAP reconcile (per project_roadmap_reconcile memory): at end of execute-phase, manually flip the top-of-file `- [ ] **Phase 4 …**` → `- [x]`, update the `## Progress` row `| 4. … | 4/4 | Complete | <today ISO> |`, commit `chore: reconcile ROADMAP — phase 4 complete`.
</verification>

<success_criteria>
Mirrors ROADMAP Phase-4 Success Criteria 4 & 5:

- A user simulates the loss of one entire vCenter (over the merged estate) and sees before/after per-survivor numbers, an evacuee total, and an assumptions panel listing what the sim does and does not model (HA admission control, anti-affinity, restart priority).
- Every DR result carries a `confidence` indicator (high/medium/low) and a `caveats` array, displayed as a chip/list.
- Failed components are a reversible neutral-grey simulation input (NO red, NO confirmation dialog).
- DR subtracts the stretched per-site reservation from 04-02.
- `engines/drSim/` ≥75% Vitest coverage (3-mode + reservation-vs-capacity).
- `useEstateView` remains the project's ONLY `useMemo`.
</success_criteria>

<output>
After completion, create `.planning/phases/04-multi-vcenter-stretched-allocation-dr/04-04-SUMMARY.md`. Then perform the ROADMAP reconcile (step 10) since this is the final Phase-4 plan.
</output>
