---
phase: 4
slug: multi-vcenter-stretched-allocation-dr
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-16
---

# Phase 4 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

**Visual continuity (one explicit sentence, per `<must_specify>`):** Phase 4 inherits the APPROVED Phase-2/Phase-3 UI-SPEC **verbatim** — identical Midnight Executive `@theme` tokens, the identical spacing scale `4 / 8 / 16 / 24 / 32 / 48 / 64`, the identical EXACTLY-4 type sizes `12 / 14 / 16 / 20` and EXACTLY-2 weights `400 / 600` (Label at 600 via `.label`), the identical `.panel` / `.label` utilities, the identical light/dark dark-pair classes, the identical ported `utils/format.ts` locale formatter, and the identical `ThemeToggle`/`AccountingModeToggle` `<fieldset> + <legend className="sr-only"> + map(button aria-pressed)` segmented idiom — **zero new design primitives, zero new tokens, zero new chart families** are introduced by this phase. This continuity is the explicit reason Phase 3 passed 6/6 first try; Phase 4 replicates that posture exactly.

Scope: the analytics-core surfaces — multi-vCenter merge labelling (cluster-collision suffix, per-snapshot vCenter/RVTools-version disambiguation), the Étendu/Stretched pill with per-site reservation + confidence chip, the CPU/RAM allocation sliders + presets (URL-hash only), and the 3-mode DR simulation panel with assumptions + caveats. All of it attaches to the SHIPPED `SnapshotListSidebar` + `Dashboard ⟷ Inventory` `ViewToggle`; no new top-level view is added.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (shadcn gate **not applicable** — no `components.json`, no `tailwind.config.*`; the manual Tailwind v4 `@theme` design system was shipped Phase 1 and frozen-approved through Phases 2–3) |
| Preset | not applicable |
| Component library | none (Tailwind v4 CSS-first `@theme` + hand-rolled `.panel`/`.label` utilities in `src/index.css`; inherited verbatim) |
| Icon library | inherited from Phases 1–3 — inline SVG only (matches `ThemeToggle` precedent). Phase 4 adds **no** new icon dependency; the only new glyphs are inline-SVG status ticks/chips drawn with `stroke="currentColor"` |
| Font | inherited from Phase 2 — UI `system-ui` stack; numerics `--font-mono` (`JetBrains Mono`…), `tabular-nums`, right-aligned in metric columns |
| Theme strategy | inherited — class-strategy dark (`@custom-variant dark`), light + dark BOTH mandatory, every color utility carries its `dark:` twin |
| Charting | inherited `<Chart>` wrapper (`echarts-for-react/lib/core`, `{ renderer: 'svg' }`, `midnight-executive` ECharts theme). **Phase-4 decision: NUMBERS-FIRST, NO new chart.** DR before/after is rendered as paired numeric figures + the EXISTING gauge family only (KISS, resolves RESEARCH Open-Question 4). No treemap/heatmap/sunburst/calendar — those are later-phase families and are explicitly out of this contract. |

**Source of truth:** `src/index.css` Midnight Executive `@theme` tokens (shipped Phase 1, mapped into `src/theme/echartsTheme.ts` Phase 2). Phase 4 adds **no** palette tokens and **no** ECharts series mappings.

---

## Spacing Scale

Declared values (multiples of 4 — the inherited Phase-2/3 scale, unchanged):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Chip icon-to-text gap, pill inner gap, slider tick gap, label-to-value inline gap |
| sm | 8px | Compact stat-row spacing, chip padding, segmented-control inner padding, snapshot-card meta-line spacing |
| md | 16px | Default — `.panel` padding, allocation-toolbar gaps, DR-panel internal padding, grid gap between cluster columns |
| lg | 24px | Section vertical rhythm (allocation toolbar → dashboard → DR panel), assumptions-panel block spacing |
| xl | 32px | Dashboard outer horizontal padding on wide viewports; DR before/after column gap |
| 2xl | 48px | Major break between dashboard region and the DR-simulation region |
| 3xl | 64px | Reserved; not used Phase 4 |

Exceptions:
- Interactive controls (Stretched pill, DR-mode segmented selector, allocation preset buttons, slider thumb) keep the inherited keyboard/hit-target floor: minimum 32px (`h-8`) hit height, 40px (`h-10`) for the segmented selector — this is the SAME `AccountingModeToggle` `h-10` rule, **not** a new spacing token, and MUST NOT be imported into the scale above.
- Per-cluster column min-width `min-w-[200px]` is unchanged from Phase 2 (a min-width floor for the added Stretched pill + confidence chip; not a spacing token). The pill + chip must fit inside the existing 200px floor without widening it.
- Range-slider track length is a fluid layout dimension, not a spacing step; it does not enter the scale.

---

## Typography

EXACTLY 4 sizes, EXACTLY 2 weights — inherited from Phase 3 **verbatim** (this is the block that must NOT regress to 5 sizes / 3 weights; the Phase-2 5/3 block is permanently superseded).

| Role | Size | Weight | Line Height | Phase-4 Usage |
|------|------|--------|-------------|---------------|
| Caption | 12px | 400 | 1.5 | RVTools-version + vCenter `Server` provenance line in snapshot card; allocation hash echo; assumptions-panel footnotes; `—` sentinel captions; the "(Active)" accounting-mode echo |
| Body | 14px | 400 | 1.5 | DR before/after metric values, evacuee-total figures, slider current-value readout, per-site reservation values, caveat list items |
| Label | 16px | 600 | 1.5 | Stat labels ("Reservation", "Site A", "Confidence"), pill text ("Étendu / Stretched"), DR-mode option labels, allocation preset labels, section sub-headers, slider field labels |
| Heading | 20px | 600 | 1.2 | Region titles ("DR simulation", "Allocation", "Stretched cluster"), DR-panel header |

Notes:
- Numeric/tabular values use `--font-mono` `tabular-nums` at the **Body (14/400)** size, right-aligned in their column — identical posture to the shipped `ClusterColumn` `Row`. No new numeric role is introduced.
- The `.label` utility's 600 weight is the only Label treatment; the Phase-2 deviation note (override the shipped `.label` 500 → 600) still applies anywhere a raw `.label` class is reused.
- No italic, no letter-spacing overrides, no Display size. Any DR headline number collapses onto Body-mono or Heading — never a new size.

---

## Color

60/30/10 split, stated explicitly against the inherited Midnight Executive tokens. Light value given; dark counterpart is the shipped `dark:` twin (never re-chosen).

| Role | Token / Value | Usage |
|------|---------------|-------|
| Dominant (~60%) | `bg-slate-50` (light) / `bg-surface-900` (dark) | App background, DR-panel canvas, allocation-toolbar negative space |
| Secondary (~30%) | `.panel` = `bg-white` border `slate-200` (light) / `bg-surface-800` border `surface-700` (dark) | Cluster card (Stretched pill host), snapshot card, allocation toolbar, DR panel, assumptions panel, every chip/pill background |
| Accent (~10%) | `--color-accent-500` gold (`oklch(78% 0.16 75)` ≈ `#F9B935`) | RESERVED — explicit closed list below |
| Primary brand | `--color-primary-*` navy scale | Active segment of DR-mode segmented selector + active allocation preset (`bg-primary-600 text-white`, same as `AccountingModeToggle`); slider thumb/fill (`primary-600`); focus ring `ring-2 ring-primary-500` |
| Status — neutral muted | `--color-surface-*` grey scale (NOT util-low/mid/high, NOT red) | The **simulated-failed** treatment: a failed host/cluster/vCenter renders struck-through (`line-through`) + dimmed (`text-slate-400 dark:text-slate-500`) on its existing card, with a neutral grey "simulated failed" status chip. This is a reversible what-if input, not a destructive event — see Interaction Contract. |
| Status — low/mid/high | `--color-util-low/mid/high` | UNCHANGED Phase-2 usage only (CPU%/RAM% gauges). **NOT** repurposed for DR verdicts or stretched confidence — those are neutral (see below). |
| Destructive | `--color-util-high` red | **NOT used Phase 4.** Phase 4 has no destructive action: DR "failed" is a reversible simulation toggle, not a delete; there is NO confirmation dialog and NO red framing. Declared only for token-table completeness. |

Accent (gold `--color-accent-500`) reserved for — explicit closed list (nothing else; gold is a factual attention marker, never an editorial flourish, never "anything interactive"):
- The active segment of the **DR-mode segmented selector** IF the navy-fill active treatment is not used (pick exactly ONE active treatment in implementation — navy-fill OR gold-underline — consistent with the Phase-2 toggle rule; default: navy-fill, matching `AccountingModeToggle`).
- The single **evacuee-total** figure in the DR result (the one "this much vCPU/vRAM must be re-homed" headline number) — a factual highlight number, never a label or background.
- The active **allocation preset** marker IF navy-fill is not used (same one-treatment rule; default: navy-fill).

Accent is NOT used for: the Stretched pill (uses navy-primary aria-pressed like the shipped toggles), the low-confidence chip (neutral — see below), DR verdicts, links, hovers, borders, or chart series.

**Stretched confidence + low-confidence chip + DR verdict — neutral, factual, NO verdict color (per locked constraint):**
- Confidence indicator `high` / `medium` / `low` (STR-03): a small Caption-size text tag on the cluster card reading the localized word only ("Confidence: high/medium/low") — **no** green/amber/red, **no** traffic-light. Neutral slate text on `.panel`.
- Low-confidence warning chip (STR-04): a neutral grey chip (`bg-surface-100 text-slate-600 dark:bg-surface-700 dark:text-slate-300`) with an inline-SVG outline info-glyph (`stroke="currentColor"`), shown only when `stretchedConfidence === 'low'`. Factual copy, no "you should", no alarm color (NOT red).
- DR per-survivor verdict `absorbs` / `tight` / `overflows` (DRS-06): rendered as the localized **factual enum word** in neutral Body text — NOT colored red/amber/green, NOT "good/bad/poor". The number sits adjacent in body color.

Contrast: all text/chip combinations reuse Phase-2-verified slate-on-panel pairings (≥4.5:1 both themes). No text is ever set on a colored fill.

---

## Layout Contract

Desktop-first. Phase 4 attaches to the SHIPPED structure — **no third view**. The simplest coherent model (resolves the `<must_specify>` "where does this attach" + RESEARCH Open-Question 4):

- **MVC-02 cluster-collision suffix:** purely a data transform (`snapshotMerge` rewrites `cluster` → `"<cluster> (<vCenterLabel>)"`). The UI renders the suffixed string in the EXISTING `ClusterColumn` heading and the EXISTING Inventory tree node — **no new component, no badge**. The vCenter label in the suffix is text-escaped React children (XSS-safe, shipped P3 idiom).
- **MVC-04 per-snapshot vCenter + RVTools version:** added as a Caption-size meta line inside the EXISTING `SnapshotListSidebar` snapshot card, **below** the existing capture-date line. A single workbook carrying N vCenters (RVTools 4.x — the real `allvCenters.xlsx` is 1 file / 3 vCenters) renders **one snapshot card** that lists "3 vCenters" (count + the per-`viSdkUuid` `Server` labels stacked, Caption size) plus the parsed `RVTools version` — the card MUST convey N-vCenters-from-one-snapshot, never imply 1 file = 1 vCenter.
- **STR-01/03/04 Stretched pill + confidence:** the pill + confidence tag + low-confidence chip live **inside the existing per-cluster `ClusterColumn` `.panel`**, in the cluster card footer region (the Phase-2 spec already reserved vertical room there for Phase-6 sparklines; the pill sits above that reserved band). No layout reflow; column stays `min-w-[200px]`.
- **ALC allocation sliders + presets:** a single horizontal **allocation toolbar** added to the Dashboard region, directly **below** the existing Phase-2 dashboard toolbar (which holds the `AccountingModeToggle`), full width, `.panel`. One instance, governs the whole estate (parallels the single accounting-mode toggle). Visible only in the Dashboard view (it is a dashboard analytical lens, like accounting mode).
- **DR simulation panel:** a new full-width `.panel` region **appended to the bottom of the Dashboard view**, after the Phase-2 `CpuReadyPanel`, separated by a `2xl` (48px) major break. It is NOT a new top-level view — it is a panel within Dashboard (KISS; the DR sim consumes the same merged estate the dashboard already shows).

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ Header (shipped: vatlas · ViewToggle · LanguageToggle · ThemeToggle)          │
├───────────────┬───────────────────────────────────────────────────────────────┤
│ SnapshotList  │  Dashboard toolbar  [Accounting mode ⟨segmented⟩]   (Phase 2)  │
│ Sidebar       │ ┌── Allocation toolbar (ALC) — .panel, full width ───────────┐ │
│ (shipped)     │ │ CPU ratio ⟨slider⟩  RAM ratio ⟨slider⟩  Presets:           │ │
│  + MVC-04     │ │   [1:1][4:1][8:1][VDI 10:1]    echo: CPU 4:1 · RAM 1:1     │ │
│  meta line:   │ └─────────────────────────────────────────────────────────────┘ │
│  vCenters(n)  │  GlobalSummaryCard · OsBreakdownDonut          (Phase 2, as-is) │
│  Server×n     │ ┌── PerClusterColumns (Phase 2) ─────────────────────────────┐ │
│  RVTools v    │ │ ▤col ▤col … each .panel min-w-200; footer adds:            │ │
│               │ │   [⟨Étendu/Stretched pill, aria-pressed⟩]                  │ │
│               │ │   Site A: <ghz/ram>  Site B: <ghz/ram>  Reservation: <n%>  │ │
│               │ │   Confidence: <high|med|low>  [⚠ low-confidence chip]      │ │
│               │ └─────────────────────────────────────────────────────────────┘ │
│               │  CpuReadyPanel (Phase 2, as-is)                                 │
│               │  ── 48px major break ──                                         │
│               │ ┌── DrSimPanel (DRS) — .panel, full width ───────────────────┐ │
│               │ │ "DR simulation"   Mode: ⟨Host│Cluster│vCenter segmented⟩   │ │
│               │ │ Pick component(s) to fail → struck/dimmed + grey chip       │ │
│               │ │ ┌ Before ──────┐  ┌ After ───────┐  Evacuee total: <gold>  │ │
│               │ │ │ vCPU/vRAM/GHz │ │ vCPU/vRAM/GHz │  per-survivor verdict   │ │
│               │ │ └───────────────┘ └───────────────┘   list (absorbs/…)     │ │
│               │ │ ┌ Assumptions (DRS-04) ─ factual list ─────────────────────┐ │ │
│               │ │ │ Models: capacity subtraction. Does not model: HA adm…    │ │ │
│               │ │ └───────────────────────────────────────────────────────────┘ │ │
│               │ │ Confidence: <high|med|low>   Caveats: • … • …  (DRS-05)    │ │
│               │ └─────────────────────────────────────────────────────────────┘ │
│               │  Footer (shipped)                                               │
└───────────────┴───────────────────────────────────────────────────────────────┘
```

Layout rules:
- Region vertical rhythm `lg` (24px) within the dashboard; `2xl` (48px) only at the Dashboard↔DR-panel break.
- Allocation toolbar mirrors the Phase-2 dashboard-toolbar height/idiom; sliders + presets sit on one row, wrapping below `min-w` like the cluster grid does.
- The Stretched pill block stays inside the 200px cluster-column floor; per-site values are mono `tabular-nums` right-aligned exactly like the existing `Row`.
- DR before/after are two side-by-side `.panel`-inset blocks (`xl` 32px gap), each a stat list reusing the Phase-2 stat-tile pattern; no chart.
- Single-snapshot / single-vCenter remains valid: all Phase-4 surfaces degrade (see Copywriting state scope) without breaking the Phase-2 dashboard.

---

## Interaction Contract

**Étendu / Stretched pill (STR-01)**
- A 2-state toggle on each cluster card, reusing the shipped `ThemeToggle`/`AccountingModeToggle` `<fieldset> + <legend className="sr-only"> + button aria-pressed` idiom — **no bespoke toggle** (DRY, locked constraint). `aria-pressed="true"` = cluster is in the `stretchedClusters` Set.
- Active treatment: navy-fill `bg-primary-600 text-white` (identical to `AccountingModeToggle`). Focus ring `ring-2 ring-primary-500`. Fully keyboard-operable (Tab in, Space/Enter toggles).
- It is an **input** (in-memory store Set, replaced not mutated — RESEARCH §store-holds-inputs); ephemeral, never persisted (no localStorage, no URL hash).
- Toggling on reveals the per-site reservation rows + confidence tag + (conditionally) the low-confidence chip on that card.

**Per-site reservation display (STR-02)**
- Below the pill: localized "Site A / Site B" rows (label left 16/600, value right mono Body) showing per-site GHz + RAM, then a "Reservation" row showing the computed `maxSiteCap/totalCap` as a locale percentage via the inherited formatter (NO pre-formatted number in the string). Em-dash `—` sentinel when a site cannot be determined.

**Confidence indicator + low-confidence chip (STR-03 / STR-04)**
- `Confidence:` Caption row with the localized enum word (`high`/`medium`/`low`) — neutral text, no traffic-light color.
- When `low`: a neutral grey chip with an inline info-glyph and factual copy (e.g. "Site metadata absent — symmetric split assumed"). Never red, never "you should", never a verdict. Chip is `aria`-labelled.

**Allocation sliders + presets (ALC-01/02/03/04)**
- Two native `<input type="range">` sliders (CPU ratio, RAM ratio) — keyboard-operable by construction (Arrow keys, Home/End, Page Up/Down), `aria-label` + `aria-valuetext` carrying the localized "n : 1" via the formatter. Slider thumb/fill use `primary-600`.
- CPU range covers at least 1–16 (presets land inside), RAM at least 0.5–4; values clamped on parse.
- Named preset buttons: **1:1**, **4:1**, **8:1**, **VDI 10:1** — a single mutually-exclusive group reusing the segmented `aria-pressed` idiom (NOT bespoke). Active preset = navy-fill; selecting a preset sets both sliders. Manually moving a slider off a preset clears the active preset (no preset pressed) — factual, no error.
- **Default echo:** on first load CPU = 4:1, RAM = 1:1 (ALC-02); a Caption echo line states the active ratios via the formatter so the lens is always visible (parallels the Phase-2 accounting-mode echo).
- **Persistence = URL hash ONLY (ALC-03):** ratios round-trip through `window.location.hash` (e.g. `#alloc=cpu:8,ram:1.25`) via `history.replaceState`. NO `localStorage`, NO `sessionStorage`, NO Zustand-persist. A shared/tampered hash is parsed defensively with a bounded regex, clamped to slider bounds, and falls back to the 4:1/1:1 defaults on any parse failure (never `eval`/`JSON.parse` of the raw hash). Only `vatlas-theme` + `vatlas-lang` remain the sole localStorage keys.
- The slider changes the **headroom verdict only**, not `vcpuPerPcpu` (which is already physical-core-based — ALC-04 satisfied structurally); copy reflects "capacity at ratio R", never threads.

**DR-mode selector (DRS-01/02/03)**
- A 3-option mutually-exclusive segmented control (Host loss · Cluster loss · vCenter loss) reusing the shipped `aria-pressed` segmented idiom — **no bespoke control**. Active = navy-fill, focus ring `ring-2 ring-primary-500`, Arrow-key navigable (mirrors `AccountingModeToggle`'s `onKeyDown`).
- Scenario is an **in-memory store input** (Sets of failed host/cluster/vCenter ids), replaced not mutated; never persisted (no hash, no localStorage).

**Marking component(s) "failed" — simulated-failed treatment (locked constraint)**
- Selection affordance: the user clicks/keyboard-activates a host/cluster/vCenter row within the DR panel's scope list (a checkbox-style multi-select reusing the shipped checkbox pattern from the Phase-3 column-picker — keyboard-operable, Space toggles, Escape clears focus).
- Selected-as-failed visual: the component's existing label renders `line-through` + dimmed (`text-slate-400 dark:text-slate-500`) with a **neutral grey "simulated failed" status chip** — NOT destructive red, NOT an alarm icon.
- **Fully reversible, NO confirmation dialog:** un-checking immediately restores the component (it is a what-if, never a delete). There is explicitly no "Are you sure?" modal — documented rationale: DR failure here is a non-destructive analytical input, identical in spirit to toggling accounting mode; a confirm dialog would falsely imply data loss. (Locked-constraint reasoning, recorded verbatim for the checker.)

**Before/after + evacuee total + per-survivor verdict (DRS-06)**
- Two side-by-side stat blocks: **Before** (current estate `GlobalSummary`) and **After** (survivors), each listing vCPU / vRAM / GHz / usable cores via the inherited formatter, mono `tabular-nums`.
- **Evacuee total**: the single gold accent figure — total vCPU/vRAM that must be re-homed (the one closed-list accent use).
- **Per-survivor verdict list**: each surviving cluster with its localized factual enum verdict `absorbs` / `tight` / `overflows` in neutral Body text + the adjacent capacity number. No color verdict, no editorial verb.

**Assumptions panel (DRS-04) + confidence + caveats (DRS-05)**
- A persistent `.panel`-inset block inside `DrSimPanel`, always visible beside/below the result (never hidden behind a tooltip-only affordance). Two factual lists: "Models:" (capacity subtraction, accounting-mode-respecting) and "Does not model:" (HA admission control, anti-affinity, restart priority, stretched split-brain) — copy is ADR-style factual, NEVER "you should"/"recommend".
- `Confidence:` row (Caption, neutral enum word, same posture as stretched confidence).
- `Caveats`: a bulleted list rendered from the engine's `caveats[]` (each item an i18n key, never free text, never a pre-formatted number, never an editorial verb). Empty array → the list is omitted (no "no caveats" filler).
- Parsed `vCluster.AdmissionControlEnabled` / `Failover Level` are surfaced as factual context lines only — disclosed, never enforced, never judged.

State location: every Phase-4 control is an inputs-only store slice or a URL-hash codec; `useEstateView` (the project's ONLY `useMemo`) re-derives — no second memo, no cached aggregate, consistent with the locked architecture.

---

## Number & Locale Formatting (inherited — surfaced for completeness)

All Phase-4 numbers render through the SHIPPED `utils/format.ts` (Phase-2 contract, unchanged): counts with locale thousands separators, GHz 1-decimal, GiB (never "GB" — RVTools MB is MiB), `%` 1-decimal, allocation ratio as locale `n : 1`, em-dash `—` sentinel for absent/indeterminate (never `0`, never `N/A`). i18n strings carry `{{placeholders}}` only — **no pre-formatted numbers, ever**. Unit suffixes are part of value rendering, not the i18n label.

---

## Copywriting Contract

All strings i18n-keyed in NEW namespaces — recommend **three** small namespaces `mvc`, `stretched`/`str`, `alloc`, `dr` added beside the shipped `common`/`upload`/`dashboard`/`inventory` (FR + EN; the CI FR↔EN key-diff gate applies). No editorial verbs anywhere ("recommend/should/poor/good/healthy/critical" denylisted — D12 / ADR-0003). No pre-formatted numbers.

| Element | Copy (EN) | Copy (FR) |
|---------|-----------|-----------|
| Primary CTA | (none — Phase 4 surfaces are read-only analytical lenses; the only interactions are the Stretched pill, allocation sliders/presets, DR-mode selector, and fail-selection — all toggles/inputs, no primary action button. CTA row intentionally empty.) | — |
| Stretched pill label | "Étendu / Stretched" | "Étendu / Stretched" |
| Stretched pill group label (sr-only) | "Stretched-cluster status" | "Statut cluster étendu" |
| Site labels | "Site A" / "Site B" | "Site A" / "Site B" |
| Reservation row label | "Reservation" | "Réservation" |
| Confidence label | "Confidence" | "Confiance" |
| Confidence values | "high" / "medium" / "low" | "élevée" / "moyenne" / "faible" |
| Low-confidence chip | "Site metadata absent — symmetric split assumed" | "Métadonnées de site absentes — répartition symétrique supposée" |
| Allocation section title | "Allocation" | "Allocation" |
| CPU slider label | "CPU ratio (vCPU per physical core)" | "Ratio CPU (vCPU par cœur physique)" |
| RAM slider label | "RAM ratio (vRAM per physical RAM)" | "Ratio RAM (vRAM par RAM physique)" |
| Preset labels | "1:1" / "4:1" / "8:1" / "VDI 10:1" | "1:1" / "4:1" / "8:1" / "VDI 10:1" |
| Allocation echo caption | "CPU {{cpu}} · RAM {{ram}}" | "CPU {{cpu}} · RAM {{ram}}" |
| Snapshot vCenter line (MVC-04) | "{{count}} vCenters" / "1 vCenter" | "{{count}} vCenters" / "1 vCenter" |
| Snapshot RVTools version line | "RVTools {{version}}" | "RVTools {{version}}" |
| DR section title | "DR simulation" | "Simulation PRA" |
| DR mode group label | "Loss scenario" | "Scénario de perte" |
| DR mode options | "Host loss" / "Cluster loss" / "vCenter loss" | "Perte d'hôte" / "Perte de cluster" / "Perte de vCenter" |
| Simulated-failed chip | "simulated failed" | "panne simulée" |
| Before / After headers | "Before" / "After" | "Avant" / "Après" |
| Evacuee total label | "Evacuee total" | "Total à réhéberger" |
| Per-survivor verdict values | "absorbs" / "tight" / "overflows" | "absorbe" / "juste" / "dépassé" |
| Assumptions panel title | "Assumptions" | "Hypothèses" |
| Assumptions — models | "Models: capacity subtraction; respects the active accounting mode." | "Modélise : soustraction de capacité ; respecte le mode de comptabilisation actif." |
| Assumptions — does not model | "Does not model: HA admission control, anti-affinity rules, VM restart priority, stretched-cluster split-brain." | "Ne modélise pas : le contrôle d'admission HA, les règles d'anti-affinité, la priorité de redémarrage des VM, le split-brain de cluster étendu." |
| Caveats list label | "Caveats" | "Réserves" |
| Empty — no snapshot | "No snapshot loaded" | "Aucun instantané chargé" |
| Empty body — no snapshot | "Drop an RVTools .xlsx export in the sidebar to run the analysis." | "Déposez un export RVTools .xlsx dans la barre latérale pour lancer l'analyse." |
| Merge-in-progress (loading) | "Merging the estate…" | "Fusion du parc en cours…" |
| Single-vCenter (no merge applicable) | "Single vCenter — no cross-vCenter merge applied." | "vCenter unique — aucune fusion inter-vCenter appliquée." |
| DR — no failed component selected | "Select one or more components to simulate a loss." | "Sélectionnez un ou plusieurs composants pour simuler une panne." |
| Error state | "This estate could not be analysed: {{message}}. Try re-dropping the workbook(s) in the sidebar." | "Ce parc n'a pas pu être analysé : {{message}}. Réessayez en redéposant le(s) classeur(s) dans la barre latérale." |
| Destructive confirmation | (none — Phase 4 has ZERO destructive actions; DR "failed" is a reversible simulation input with NO confirmation dialog, by explicit locked-constraint design) | — |

State scope: empty / loading / single-vCenter / no-DR-selection / error states replace **only** the affected Phase-4 region (allocation toolbar, Stretched block, or DR panel) — the shipped Phase-1 sidebar, Phase-2 dashboard, and header stay interactive. A merge/DR failure renders inline using `error.message` ONLY (never the error object — shipped Critical-2 idiom); the Phase-1 `ErrorBoundary`/`FallbackError` still wraps `App`.

---

## i18n

New namespaces (FR + EN, both mandatory; CI key-diff gate applies). No pre-formatted numbers; no editorial verbs. Approximate keys:

```
mvc.snapshot.vcenters         mvc.snapshot.vcenter.one
mvc.snapshot.rvtoolsVersion   mvc.merge.loading
mvc.merge.singleVCenter
str.pill.label                str.pill.group
str.site.a                    str.site.b
str.reservation               str.confidence.label
str.confidence.high|medium|low
str.lowConfidence.chip
alloc.title                   alloc.cpu.label
alloc.ram.label               alloc.preset.1to1|4to1|8to1|vdi10to1
alloc.echo
dr.title                      dr.mode.group
dr.mode.host|cluster|vcenter
dr.failed.chip                dr.before                dr.after
dr.evacueeTotal               dr.verdict.absorbs|tight|overflows
dr.assumptions.title          dr.assumptions.models
dr.assumptions.doesNotModel   dr.caveats.label
dr.caveats.<key>              (one key per engine-emitted caveat)
dr.empty.noSelection
common.empty.noSnapshot       common.error.analysis
```

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| none (manual Tailwind v4 `@theme` design system, shipped P1) | none | not applicable |
| shadcn official | none — shadcn not initialized (no `components.json`); gate not applicable | not applicable |
| third-party | none declared | not applicable |

No registry components enter this phase. Phase 4 adds **zero new runtime dependencies** (RESEARCH §Standard Stack — all math is hand-rolled pure arithmetic; no `mathjs`/`d3-array`/`simple-statistics`). All UI is hand-built React over the shipped Tailwind v4 `@theme` tokens and the shipped `<Chart>` wrapper (no new chart family). The registry vetting gate does not apply (no `shadcn view` to run). Existing bundle gates (echarts ≤300 KB gz) stay green — no new chunk.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
