d---
phase: 02-aggregation-global-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/theme/echartsTheme.ts
  - src/theme/echartsTheme.test.ts
  - src/components/Chart.tsx
  - src/components/Chart.test.tsx
  - scripts/check-bundle-size.mjs
requirements: [VIZ-01, VIZ-02, VIZ-03]
autonomous: true

must_haves:
  truths:
    - "A developer can pass any ECharts option object to <Chart> and it renders with the Midnight Executive theme"
    - "Every chart rendered through <Chart> emits inline <svg>, never <canvas> (DevTools-verifiable)"
    - "The production ECharts-bearing chunk is ≤300 KB gzipped, enforced in CI"
    - "The midnight-executive theme exposes both a light and a dark variant resolved from the shipped index.css tokens"
  artifacts:
    - path: "src/components/Chart.tsx"
      provides: "Tree-shaken ECharts wrapper: single option prop, SVG renderer injected centrally, midnight-executive theme, React.memo reference comparator"
      contains: "echarts/core"
    - path: "src/theme/echartsTheme.ts"
      provides: "Midnight Executive ECharts theme object (light + dark) mapped from index.css tokens per UI-SPEC chart-series table"
      exports: ["MIDNIGHT_EXECUTIVE_LIGHT", "MIDNIGHT_EXECUTIVE_DARK"]
    - path: "scripts/check-bundle-size.mjs"
      provides: "CI gate: fails when the echarts chunk exceeds 307200 bytes gzipped"
  key_links:
    - from: "src/components/Chart.tsx"
      to: "echarts/core + echarts.use([...]) + SVGRenderer"
      via: "module-scope registration before first render"
      pattern: "echarts\\.use\\(\\["
    - from: "src/components/Chart.tsx"
      to: "echarts-for-react/lib/core ReactEChartsCore"
      via: "opts={{ renderer: 'svg' }} + theme prop injected by the wrapper"
      pattern: "renderer:\\s*'svg'"
    - from: "scripts/check-bundle-size.mjs"
      to: "dist/assets/*.js echarts chunk"
      via: "gzipSync + 307200-byte hard gate"
      pattern: "307200"
---

<objective>
Land the ECharts SVG-renderer infrastructure: the tree-shaken `<Chart>` wrapper (single `option` prop, `{ renderer: 'svg' }` injected centrally, `React.memo` reference comparator), the Midnight Executive ECharts theme (light + dark, mapped from the shipped `index.css` tokens), and the CI bundle-size gate (echarts chunk ≤300 KB gz). This is the chart primitive every later phase consumes.

Purpose: VIZ-01/02/03 — every chart in vatlas (this phase and all later phases) renders as crisp inline SVG through one wrapper, themed consistently, with tree-shaking enforced by CI so the bundle never bloats.
Output: `src/components/Chart.tsx`, `src/theme/echartsTheme.ts`, `scripts/check-bundle-size.mjs`, `package.json` (echarts deps + `check:bundle-size` script), unit tests for the wrapper and theme.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 2 source artifacts
@.planning/phases/02-aggregation-global-dashboard/02-RESEARCH.md   # §Pattern 1 (tree-shaken registry, VERIFIED import paths), §Pattern 2 (<Chart> recommended API verbatim), §Standard Stack (echarts@^6.0.0 / echarts-for-react@^3.0.6, re-verify commands), Pitfall 1 (v6 default-theme moot), Pitfall 3 (SVG verification — jsdom opts-level), Pitfall 4 (bundle gate value 307200), Open Question 2
@.planning/phases/02-aggregation-global-dashboard/02-PATTERNS.md   # §Chart.tsx (RESEARCH Pattern 1+2 spec; FallbackError.tsx component-shape conventions), §echartsTheme.ts (UI-SPEC chart-series token map; light+dark mandate), §check-bundle-size.mjs (check-supply-chain.mjs is the exact template)
@.planning/phases/02-aggregation-global-dashboard/02-UI-SPEC.md    # §Color "Chart series → token mapping" table (the authoritative token→series map; light + dark columns), §Design System Charting row

<interfaces>
<!-- Shipped contracts the executor builds against — no codebase exploration needed. -->

package.json (current — add to dependencies, then `npm install`):
  "xlsx": "https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz"  (do NOT touch — supply-chain-pinned)
  scripts: { "prebuild": "node scripts/check-supply-chain.mjs", "check:supply-chain": "...", "build": "tsc -b && vite build", "test:run": "vitest run" }
  ADD: "echarts": "^6.0.0", "echarts-for-react": "^3.0.6"  (RESEARCH §Standard Stack — re-run `npm view echarts version` / `npm view echarts-for-react peerDependencies` at execution; if 6.0.0 is no longer latest GA, pin the current GA major and note it in the SUMMARY)
  ADD script: "check:bundle-size": "node scripts/check-bundle-size.mjs"

scripts/check-supply-chain.mjs (the EXACT template for the new gate — same shape):
  - bare Node, no deps, `import { readFileSync } from 'node:fs'`
  - clear `console.error(...)` + `process.exit(1)` on violation
  - `console.log('check-supply-chain: OK')` + exit 0 on pass
  (the new script mirrors this shape but uses `node:zlib` gzipSync + globs dist/assets/*.js)

src/index.css @theme tokens (the theme's source of truth — resolve to concrete oklch values found here):
  --color-primary-500: oklch(45% 0.18 270)   --color-primary-300: oklch(70% 0.12 270)   --color-primary-200: oklch(82% 0.08 270)
  --color-surface-200: oklch(88% 0.01 260)   --color-surface-700: oklch(28% 0.02 260)   --color-surface-800: oklch(20% 0.02 260)   --color-surface-900: oklch(15% 0.02 260)
  --color-util-low: oklch(64% 0.16 142)  --color-util-mid: oklch(72% 0.18 65)  --color-util-high: oklch(58% 0.22 25)
  (slate-500/slate-200/slate-400 are Tailwind defaults; resolve to their hex/oklch at theme-build time)

src/components/FallbackError.tsx — component-shape convention to mirror: named export, typed props interface, NO default export, no `JSX.Element` return annotation (TS infers; this tsconfig has no global JSX namespace under React 19 — see 01-05-SUMMARY decision).

Test-file typecheck contract (from 01-04/01-05 SUMMARYs): `*.test.tsx`/`*.test.ts` are EXCLUDED from `tsconfig.app.json` (the `tsc -b` build) and INCLUDED in `tsconfig.test.json`. New `*.test.*` files are already covered by the existing globs — do NOT add new tsconfig excludes unless `npm run build` fails on a test file (then mirror the 01-05 fix exactly).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ECharts deps + the bundle-size CI gate</name>
  <files>package.json, package-lock.json, scripts/check-bundle-size.mjs</files>
  <action>
Re-verify versions first (RESEARCH §Standard Stack mandates this — versions move): run `npm view echarts version`, `npm view echarts-for-react version`, `npm view echarts-for-react peerDependencies`. If `echarts` latest GA is still `6.0.0` (or any `6.x` GA, NOT an rc), declare `"echarts": "^6.0.0"` and `"echarts-for-react": "^3.0.6"` in `package.json` `dependencies`; if latest GA has moved to a newer major still satisfying `echarts-for-react`'s peer range, pin that major and record the exact resolved versions + rationale in the SUMMARY. NEVER pin an `-rc`/`-beta` (stack discipline — GA only).

Add the dependency lines to `package.json` `dependencies` (keep the `xlsx` CDN-tarball pin byte-identical — it is supply-chain-gated), then run a plain manifest-driven `npm install` (no URL argument — installs the repo's own declared deps; this is the Phase-1-proven non-bypass path, see 01-03-SUMMARY deviation #1). Add `"check:bundle-size": "node scripts/check-bundle-size.mjs"` to `package.json` `scripts`.

Create `scripts/check-bundle-size.mjs` modeled EXACTLY on `scripts/check-supply-chain.mjs`'s shape (bare Node, no deps, clear `console.error` + `process.exit(1)` on violation, `console.log('check-bundle-size: OK')` + exit 0 on pass). Logic per RESEARCH Pitfall 4 / 02-PATTERNS §check-bundle-size.mjs: read `dist/assets/` (use `node:fs` `readdirSync`), select the `.js` chunk(s) whose content contains an echarts marker (e.g. the string `echarts` / a known echarts export token — pick a stable substring; document the chosen marker in a comment), `gzipSync` (`node:zlib`) the chunk bytes, and `process.exit(1)` with a clear message if the gzipped size `> 307200` (300 KiB — ROADMAP success #5 authoritative value, RESEARCH Pitfall 4). If `dist/assets/` does not exist, exit 1 with a message instructing to run `npm run build` first (the gate is a post-build CI step, not a prebuild hook — do NOT wire it into `prebuild`; keep `prebuild` as the supply-chain gate only, per 01-03-SUMMARY). Keep the Phase-1 700 KB raw Vite chunk warning untouched (orthogonal metric — do not modify vite.config).

Do NOT inline a fenced echarts registration here — registration lives in Task 2's Chart.tsx.
  </action>
  <verify>
    <automated>npm run check:supply-chain && node -e "const p=require('./package.json'); if(!p.dependencies.echarts||!p.dependencies['echarts-for-react'])process.exit(1); if(!p.scripts['check:bundle-size'])process.exit(1); if(p.dependencies.xlsx!=='https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz')process.exit(1)" && grep -q "307200" scripts/check-bundle-size.mjs && grep -q "gzipSync\|createGzip\|node:zlib" scripts/check-bundle-size.mjs && node --check scripts/check-bundle-size.mjs && (test ! -d dist/assets && node scripts/check-bundle-size.mjs; test $? -eq 1)</automated>
  </verify>
  <done>echarts@^6 + echarts-for-react@^3.0.6 declared and installed (xlsx pin untouched, supply-chain green); `check:bundle-size` npm script wired; `scripts/check-bundle-size.mjs` is valid Node, references the 307200-byte gz gate via node:zlib, exits 1 when dist/assets is absent (build-first guard), and is NOT in prebuild.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Midnight Executive ECharts theme (light + dark) + the &lt;Chart&gt; wrapper</name>
  <files>src/theme/echartsTheme.ts, src/theme/echartsTheme.test.ts, src/components/Chart.tsx, src/components/Chart.test.tsx</files>
  <behavior>
    - echartsTheme.test.ts: `MIDNIGHT_EXECUTIVE_LIGHT` and `MIDNIGHT_EXECUTIVE_DARK` are exported objects; each has a `color` array whose first three entries are the OS-donut Windows/Linux/Other colors from the UI-SPEC token map (light: primary-500 / primary-300 / surface-200; dark: primary-300 / primary-200 / surface-700) resolved to concrete color strings.
    - echartsTheme.test.ts: light and dark variants are distinct objects (deep-inequality on the `color`/axis entries) — proves the dark variant is genuinely resolved, not aliased.
    - echartsTheme.test.ts: axis/grid label color is the slate token (not pure black/white) in both variants.
    - Chart.test.tsx: rendering `<Chart option={someBarOption} />` produces DOM containing an `<svg>` element and NO `<canvas>` element (Pitfall 3 / VIZ-01 — opts-level/DOM assertion via @testing-library/react in jsdom, per RESEARCH Open Question 2 recommendation: jsdom DOM-presence assertion; if jsdom cannot mount ReactEChartsCore at all, assert the `opts.renderer` passed to a mocked `ReactEChartsCore` equals `'svg'` instead — document which path was used in the SUMMARY).
    - Chart.test.tsx: the `React.memo` comparator returns true (skip re-render) when the same `option` reference + same other props are passed twice, and false when `option` reference changes.
  </behavior>
  <action>
Create `src/theme/echartsTheme.ts` exporting `MIDNIGHT_EXECUTIVE_LIGHT` and `MIDNIGHT_EXECUTIVE_DARK` ECharts theme objects. Map the shipped `src/index.css` `@theme` tokens to ECharts theme keys per the UI-SPEC §Color "Chart series → token mapping" table EXACTLY: donut/stacked-bar series colors `[Windows, Linux, Other]` = light `[primary-500, primary-300, surface-200]` / dark `[primary-300, primary-200, surface-700]`; gauge utilization bands `util-low/util-mid/util-high` at 0–60/60–80/80–100; vCPU-allocation gauge single `primary-500` (light) / `primary-300` (dark) with NO banding (it is a ratio, not a verdict — Moderate-4); axis/grid/label `slate-500`/`slate-200` (light) and `slate-400`/`surface-700` (dark); tooltip surface = `.panel` equivalent per variant. Resolve each token to a concrete color string from the oklch values in `index.css` (the tokens are oklch — emit the same oklch string; ECharts SVG renderer accepts CSS color strings). Add NO new palette tokens (UI-SPEC §Design System: Phase 2 adds none — this is a pure mapping). Pure module, no React/DOM/Zustand/Zod imports.

Create `src/components/Chart.tsx` per RESEARCH Pattern 1 + Pattern 2 (the verbatim spec). Module scope, ONCE: `import * as echarts from 'echarts/core'`; `import { BarChart, PieChart, GaugeChart } from 'echarts/charts'`; `GridComponent, TooltipComponent, LegendComponent, DatasetComponent` from `echarts/components`; `SVGRenderer` from `echarts/renderers`; then `echarts.use([...all of those...])` and `echarts.registerTheme('midnight-executive', MIDNIGHT_EXECUTIVE_LIGHT)` + `echarts.registerTheme('midnight-executive-dark', MIDNIGHT_EXECUTIVE_DARK)`. FORBIDDEN: `import * as echarts from 'echarts'` (pulls ~1 MB — the bundle gate would catch it but never write it); importing `CanvasRenderer` (defeats SVG-only — RESEARCH Anti-Patterns / Pitfall 3). Wrap `echarts-for-react/lib/core`'s `ReactEChartsCore` (the tree-shaking entry — NOT the default `echarts-for-react`). Props: `option: EChartsCoreOption` (the single universal surface — do NOT add per-chart-family props; that is the premature abstraction PROJECT.md forbids), optional `style`/`className`/`ariaLabel`/`notMerge`. Inject `opts={{ renderer: 'svg' }}` and `theme` centrally so no caller can pick canvas (VIZ-01 structurally enforced). Theme selection: read the active theme (light/dark) — since dark is class-strategy (`.dark` on `<html>`, shipped Phase 1) and ECharts theme is fixed at mount, choose the theme name from a `document.documentElement.classList.contains('dark')` check at render (KISS; a later phase can make this reactive — Phase 2 dashboards remount on snapshot change anyway). Default `style` min-height 320 (RESEARCH Pattern 2 / UI-SPEC §Layout: donut ≤320). Export as `React.memo(Chart)` with the reference-equality comparator `a.option === b.option && a.className === b.className && a.ariaLabel === b.ariaLabel && a.style === b.style` (Moderate-9 — option objects come from useEstateView-derived selectors so reference equality short-circuits correctly; RESEARCH Pattern 2 rationale). Named export `Chart`, no default export, no `JSX.Element` annotation (mirror FallbackError.tsx + 01-05-SUMMARY decision).

Write RED tests first (this task is `tdd="true"`): create `src/theme/echartsTheme.test.ts` and `src/components/Chart.test.tsx` asserting the `<behavior>` block; confirm they FAIL (modules absent) before writing the implementation; then GREEN. Use `@testing-library/react` (installed Phase 1) for the Chart DOM assertion. Do NOT place fenced ECharts config in this action — it lives in the source file.
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run src/theme/echartsTheme.test.ts src/components/Chart.test.tsx && grep -q "echarts/core" src/components/Chart.tsx && grep -q "echarts-for-react/lib/core" src/components/Chart.tsx && grep -q "renderer:\s*'svg'\|renderer: 'svg'" src/components/Chart.tsx && ! grep -q "from 'echarts'" src/components/Chart.tsx && ! grep -q "CanvasRenderer" src/components/Chart.tsx && grep -q "MIDNIGHT_EXECUTIVE_LIGHT\|MIDNIGHT_EXECUTIVE_DARK" src/theme/echartsTheme.ts</automated>
  </verify>
  <done>Theme module exports distinct light + dark Midnight Executive ECharts objects mapped from index.css tokens per the UI-SPEC table; `<Chart>` is a tree-shaken (`echarts/core` + `echarts.use`, no `from 'echarts'`, no CanvasRenderer) memoized wrapper around `echarts-for-react/lib/core` that injects SVG renderer + theme centrally; RED→GREEN observed; all behavior tests pass; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| new npm dep → app bundle | `echarts`/`echarts-for-react` enter the supply chain; render-only, but a transitive telemetry path would be an exfiltration vector for workbook-derived chart data |
| app → network | The Phase-1 runtime fetch/XHR/WS/Beacon guard + CSP `connect-src 'self'` already throw on any non-same-origin request; ECharts is render-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information disclosure | echarts/echarts-for-react deps | mitigate | Phase-1 fetch/XHR/WS/Beacon guard + CSP `connect-src 'self'` make any outbound call throw (detectable, not silent); `check:supply-chain` telemetry denylist re-verified green this plan; ECharts is render-only (no network code path) |
| T-02-02 | Tampering | dist bundle integrity | mitigate | `check-bundle-size.mjs` fails CI if the echarts chunk exceeds 300 KB gz — catches an accidental/malicious `import * as echarts from 'echarts'` (≈1 MB) that would pull untree-shaken code |
| T-02-03 | Information disclosure | Chart render errors | mitigate | `<Chart>` passes only the caller-supplied `option`; it never interpolates `Snapshot`/`VInfoRow` objects into messages. Render-error surfacing is the consumer's concern (handled in 02-03 per the Critical-2 contract); this wrapper introduces no error string |
| T-02-04 | Denial of service | render storm | accept | `React.memo` reference comparator (Moderate-9) bounds re-renders; option objects originate from memoized selectors (02-02). Accepted: no untrusted input drives chart count at this layer |
</threat_model>

<verification>
- `npm run typecheck` (app + tsconfig.test.json) exit 0
- `npm run lint` (biome) clean on the new files
- `npx vitest run src/theme/echartsTheme.test.ts src/components/Chart.test.tsx` all green
- `npm run check:supply-chain` → `check-supply-chain: OK` (xlsx pin intact; no telemetry dep introduced)
- `node --check scripts/check-bundle-size.mjs` (valid syntax) and the script exits 1 with a build-first message when `dist/assets/` is absent
- Bundle-gate end-to-end (informational this plan; the live gate runs in 02-03 once charts are imported): `npm run build && npm run check:bundle-size` — if the echarts chunk has not yet been pulled into the bundle (no chart imported anywhere outside tests), the script reports no echarts chunk found; the authoritative ≤300 KB gz assertion is exercised by 02-03's verification once the dashboard imports `<Chart>`
- Tree-shaking source gates: `grep -q "echarts/core"` present, `! grep "from 'echarts'"`, `! grep "CanvasRenderer"` in `Chart.tsx`
</verification>

<success_criteria>
Mirrors ROADMAP Phase-2 success criteria #4 and #5:
- (#4 infra half) Every chart routed through `<Chart>` renders as inline `<svg>` and never `<canvas>` — the wrapper injects `opts={{renderer:'svg'}}` centrally and `CanvasRenderer` is never imported; the jsdom DOM/opts test proves the renderer choice (full DevTools confirmation lands in 02-03 against the real dashboard).
- (#5) The CI bundle-size gate (`check:bundle-size`) enforces the ECharts chunk ≤300 KB gzipped, proving tree-shaking is effective; it becomes a live failing gate the moment any non-tree-shaken echarts import slips in.
- (VIZ-03) The `midnight-executive` theme is registered once at module load (light + dark), so every chart in this and every later phase is consistently themed.
</success_criteria>

<output>
After completion, create `.planning/phases/02-aggregation-global-dashboard/02-01-SUMMARY.md` (use the summary template). Record: the exact resolved echarts/echarts-for-react versions installed (re-verification result), which SVG-assertion path Chart.test.tsx used (jsdom DOM-presence vs mocked-opts), and the chosen echarts-chunk marker substring in check-bundle-size.mjs.
</output>
