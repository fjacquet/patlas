---
phase: 02-aggregation-global-dashboard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/estate.ts
  - src/types/index.ts
  - src/utils/format.ts
  - src/utils/format.test.ts
  - src/engines/aggregation/ghz.ts
  - src/engines/aggregation/perCluster.ts
  - src/engines/aggregation/vinfoMerge.ts
  - src/engines/aggregation/aggregateClusters.ts
  - src/engines/aggregation/globals.ts
  - src/engines/aggregation/contention.ts
  - src/engines/aggregation/osFamily.ts
  - src/engines/aggregation/perDatastore.ts
  - src/engines/aggregation/perEsx.ts
  - src/engines/aggregation/estateView.ts
  - src/engines/aggregation/index.ts
  - src/engines/aggregation/ghz.test.ts
  - src/engines/aggregation/perCluster.test.ts
  - src/engines/aggregation/vinfoMerge.test.ts
  - src/engines/aggregation/aggregateClusters.test.ts
  - src/engines/aggregation/globals.test.ts
  - src/engines/aggregation/contention.test.ts
  - src/engines/aggregation/osFamily.test.ts
  - src/engines/aggregation/perDatastore.test.ts
  - src/engines/aggregation/perEsx.test.ts
  - src/engines/aggregation/estateView.test.ts
  - src/hooks/useEstateView.ts
  - src/hooks/useEstateView.test.ts
requirements: [DSH-01, DSH-02, DSH-03, DSH-04, DSH-05, DSH-06]
autonomous: true

must_haves:
  truths:
    - "buildEstateView(snapshot, mode) returns per-cluster aggregates (physical/consumed GHz, mean CPU%, mean RAM%, vCPU-per-physical-core, ESX count, VM-by-OS), global estate totals, datastores, hosts, and OS breakdown — purely, with no React/Zustand/Zod"
    - "Configured / Active / Storage-realistic produce three distinct totals on a ~50/50 powered-on fixture"
    - "Shared LUNs across clusters are NAA-deduped — total datastore capacity never double-counts (Moderate-11)"
    - "vcpuPerPcpu is computed against physical cores, never threads (Moderate-4) — and 2 sockets × 12 cores × 2600 MHz composes to 62.4 GHz"
    - "CPU Ready mean is arithmetic, max via reduce, count>5% via CONTENTION_THRESHOLDS; absence yields readinessAvailable=false (never 0)"
    - "useEstateView(mode) is the project's only useMemo site; it returns a stable EstateView and a frozen EMPTY_VIEW when no snapshot is active"
  artifacts:
    - path: "src/engines/aggregation/estateView.ts"
      provides: "Pure buildEstateView(snapshot, mode) → EstateView assembler + frozen EMPTY_VIEW"
      contains: "buildEstateView"
    - path: "src/engines/aggregation/index.ts"
      provides: "Public barrel: buildEstateView, EMPTY_VIEW, classifyOsFamily, CONTENTION_THRESHOLDS"
      exports: ["buildEstateView", "EMPTY_VIEW", "classifyOsFamily"]
    - path: "src/types/estate.ts"
      provides: "AccountingMode, OsFamily, ClusterAggregate, GlobalSummary, DatastoreAggregate, EsxAggregate, EstateView (branded, activeMemMb dropped)"
      contains: "AccountingMode"
    - path: "src/hooks/useEstateView.ts"
      provides: "The ONE useMemo bridge: store → buildEstateView, keyed on (snapshot, mode)"
      contains: "useMemo"
    - path: "src/utils/format.ts"
      provides: "Ported vsizer locale-aware formatters (em-dash sentinel, GiB/TiB suffixes per ADR-0010)"
      exports: ["fmtInt", "fmtGhzValue", "fmtPercentValue", "fmtRatio"]
  key_links:
    - from: "src/hooks/useEstateView.ts"
      to: "buildEstateView from @/engines/aggregation"
      via: "useMemo([snapshot, mode]) over selectActiveSnapshot"
      pattern: "buildEstateView\\(snapshot, mode\\)"
    - from: "src/engines/aggregation/ghz.ts"
      to: "@/engines/units mhzToGhz"
      via: "import (DRY — never re-declare mhzToGhz; Pitfall 7)"
      pattern: "from '@/engines/units'"
    - from: "src/engines/aggregation/perDatastore.ts"
      to: "VDatastoreRow grouped by naa ?? name"
      via: "first-row capacity, never summed (Moderate-11)"
      pattern: "naa\\s*\\?\\?\\s*"
    - from: "src/engines/aggregation/vinfoMerge.ts"
      to: "AccountingMode powered-off filter"
      via: "if (mode !== 'configured' && !row.poweredOn) continue"
      pattern: "mode !== 'configured'"
---

<objective>
Port + retrofit the six vsizer aggregation engines to branded units, add the three net-new pure engines (`osFamily`, `perDatastore` NAA-keyed, `perEsx`), thread the three accounting modes through `vinfoMerge`, assemble it all in a pure `buildEstateView(snapshot, mode)`, port vsizer's locale-aware formatters, and expose the project's single sanctioned `useMemo` bridge `useEstateView`.

Purpose: DSH-01..06 — turn one parsed `Snapshot` into the headline estate view (per-cluster columns, global totals, OS breakdown, CPU Ready, three accounting modes) through pure Vitest-gated engines and the one store→UI bridge every downstream UI/export consumes.
Output: `src/engines/aggregation/*` (10 engine modules + barrel), `src/types/estate.ts`, `src/utils/format.ts`, `src/hooks/useEstateView.ts`, ≥75% engine coverage, the three ROADMAP-mandated math tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 2 source artifacts
@.planning/phases/02-aggregation-global-dashboard/02-RESEARCH.md   # §vsizer Port Map (per-file retrofit verdict), §perDatastore (full DatastoreAggregate interface + Moderate-11 rule), §perEsx (full EsxAggregate interface + DRY readinessStats mandate), §Three Accounting Modes (Critical-6 table + the mode-driven filter), §Physical-Cores Consolidation (Moderate-4), §CPU Ready (ADR-0012), §Pattern 3 (useEstateView exact shape + forward-compat trends:null), Pitfall 2 (recompute not precompute), Pitfall 5/6/7, A1/A4, Open Question 1
@.planning/phases/02-aggregation-global-dashboard/02-PATTERNS.md   # The ONE worked brand-retrofit example (ghz.ts §, lines 60-83); per-engine "Required deviations" lists (perCluster/vinfoMerge/aggregateClusters/globals §); osFamily verbatim; perDatastore/perEsx pattern source; estateView assembler; §Shared Patterns "The Brand Retrofit" + "Locale-aware Formatting"; §useEstateView.ts idiom (shipped); engine *.test.ts port deviations + the 3 ROADMAP tests
@.planning/phases/01-foundation-invariants/01-03-SUMMARY.md        # @engines/units exports: mib/gib/tib/bytes/mhz/ghz/cores/sockets constructors + mhzToGhz/mibToGib converters; the 62.4 GHz canary pattern; manifest-then-install non-bypass path
@.planning/phases/01-foundation-invariants/01-04-SUMMARY.md        # Snapshot/VInfoRow/VHostRow/VDatastoreRow shapes; column alias coverage; tsconfig.test.json test-typecheck contract; canary fixture exact contents
@.planning/phases/01-foundation-invariants/01-05-SUMMARY.md        # store contract FINAL (Map<id,Snapshot> + activeSnapshotId); selectActiveSnapshot is referentially stable; useEstateView is the ONLY useMemo site; canary fixture reference values

<interfaces>
<!-- Shipped contracts the executor builds against. Port source is the sibling vsizer repo. -->

PORT SOURCE (read in full at execution — bare `number`, retrofit to branded):
  /Users/fjacquet/Projects/vsizer/src/engines/aggregation/{ghz,perCluster,vinfoMerge,aggregateClusters,globals,contention,index}.ts
  /Users/fjacquet/Projects/vsizer/src/utils/format.ts
  /Users/fjacquet/Projects/vsizer/src/types/{cluster,global}.ts   (ClusterAggregate / GlobalSummary interfaces to port + brand)
  NOTE: vsizer aggregation dir has NO perDatastore/perEsx/osFamily (confirmed) — those are net-new (patterns in 02-RESEARCH/02-PATTERNS).
  NOTE: vsizer format.ts ACTUAL exports (verified): fmtInt, fmtGhz, fmtGhzNumber, fmtGhzValue, fmtMhzValue, fmtPercent, fmtPercentWhole, fmtPercentValue, fmtRatio, fmtMemMb — port the file; the dashboard (02-03) uses fmtInt/fmtGhzValue/fmtPercentValue/fmtRatio/fmtMemMb. (RESEARCH listed slightly different names — defer to the ACTUAL vsizer exports above.)

@/engines/units (Phase 1, shipped — import, do NOT re-implement):
  brand types: MiB, GiB, TiB, Bytes, MHz, GHz, Cores, Sockets
  constructors: mib(n), gib(n), tib(n), bytes(n), mhz(n), ghz(n), cores(n), sockets(n)
  converters: mhzToGhz(MHz):GHz, mibToGib(MiB):GiB, mibToBytes, ... (Zod-free)
  → ghz.ts MUST `import { mhzToGhz } from '@/engines/units'` and NOT re-declare it (Pitfall 7, the DRY trap).

VInfoRow (src/types/vinfo.ts, shipped): vmName, cluster, host, vcpu:Cores, vramMib:MiB, cpuReadinessPercent:number|null, poweredOn:boolean, osConfig:string, osTools:string, vmBiosUuid, vmInstanceUuid, viSdkUuid, viSdkServer, provisionedMib:MiB, inUseMib:MiB
  → NO activeMemMb field. Every vsizer activeMemMb / sumActiveMem reference is DEAD CODE in the port — DROP it everywhere (vinfoMerge/aggregateClusters/globals/estate.ts), per 02-PATTERNS cross-cutting finding #3.

VHostRow (src/types/vhost.ts, shipped): hostName, cluster, sockets:Sockets, cores:Cores, speedMhz:MHz, memoryMib:MiB, cpuRatio:number, ramRatio:number
  → vsizer's `h.memoryMb` → vatlas `h.memoryMib`. `cpuRatio`/`ramRatio` stay bare number (0..1) — do NOT brand. NO threads field (Moderate-4 structurally prevented).

VDatastoreRow (src/types/snapshot.ts, shipped): name:string, capacityMib:MiB, freeMib:MiB, provisionedMib:MiB, naa:string|null, type:string
  → perDatastore groups by `naa ?? name`; capacity/free from the FIRST row in a group, NEVER summed (Pitfall 5).

Store (src/store/snapshotStore.ts, shipped FINAL): `useSnapshotStore`, `selectActiveSnapshot` (returns referentially-stable Snapshot|null — perfect useMemo dep; snapshots are frozen/replaced not mutated).

useEstateView target (02-PATTERNS §useEstateView.ts — the idiom is shipped via SnapshotListSidebar's useMemo-off-store pattern):
  import { useMemo } from 'react'; import { selectActiveSnapshot, useSnapshotStore } from '@/store/snapshotStore'
  import { buildEstateView, EMPTY_VIEW } from '@/engines/aggregation'; import type { AccountingMode, EstateView } from '@/types/estate'
  export function useEstateView(mode: AccountingMode): EstateView {
    const snapshot = useSnapshotStore(selectActiveSnapshot)
    return useMemo(() => (snapshot ? buildEstateView(snapshot, mode) : EMPTY_VIEW), [snapshot, mode])
  }
  This is the ONLY useMemo in the project (PROJECT.md / 01-05-SUMMARY). No other useMemo anywhere.

A1 resolution (fixture-verified during planning): the MOM-vCenter fixture's vDatastore sheet DOES carry a `Cluster name` column, BUT Phase-1's VDatastoreRow does NOT parse it (no cluster field on the row). Per-cluster datastore attribution is therefore NOT derivable in Phase 2 without re-parsing — out of scope (Phase 3 datastore-table concern, RESEARCH §perDatastore). EstateView exposes a GLOBAL datastoreCount only; ClusterAggregate carries NO datastore count (02-03 renders em-dash `—` per cluster). This is a scoped simplification, not a gap.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Estate types + ported math engines (ghz/perCluster/vinfoMerge/aggregateClusters/globals/contention) + format.ts</name>
  <files>src/types/estate.ts, src/types/index.ts, src/utils/format.ts, src/utils/format.test.ts, src/engines/aggregation/ghz.ts, src/engines/aggregation/perCluster.ts, src/engines/aggregation/vinfoMerge.ts, src/engines/aggregation/aggregateClusters.ts, src/engines/aggregation/globals.ts, src/engines/aggregation/contention.ts, src/engines/aggregation/ghz.test.ts, src/engines/aggregation/perCluster.test.ts, src/engines/aggregation/vinfoMerge.test.ts, src/engines/aggregation/aggregateClusters.test.ts, src/engines/aggregation/globals.test.ts, src/engines/aggregation/contention.test.ts</files>
  <behavior>
    - ghz.test.ts: `physicalGhz(mhz(2600), cores(12))` returns `ghz(31.2)`; `2 (sockets) × physicalGhz(mhz(2600), cores(12))` composition equals `62.4` GHz (ROADMAP success #6 canary, branded-fixture style per converters.test.ts:42-50). `consumedGhz(speedMhz, cores, cpuRatio)` = physicalGhz × ratio.
    - perCluster.test.ts: capacity-weighted mean CPU ratio (ADR-0011) ports green on a 2-host cluster; `memoryMib` field used (not `memoryMb`); empty-cluster rows dropped.
    - vinfoMerge.test.ts: `readinessStats` arithmetic mean + reduce-not-spread max + count>5%; `readinessAvailable=false` & mean=null when no VM reports CPU Ready (ADR-0012 — never 0). The mode-driven powered-off filter: `configured` keeps powered-off VMs in vCPU/vRAM sums; `active` excludes them; readinessStats is ALWAYS powered-on-only regardless of mode.
    - aggregateClusters.test.ts: `vcpuPerPcpu = vcpuAllocated / usablePhysicalCores` uses physical cores (Moderate-4 — assert it explicitly; structurally there is no threads field); stretched math present but dormant with `stretchedClusters = new Set()`; cluster sort stable by `localeCompare`.
    - globals.test.ts: every cluster field summed; `emptySummary` frozen constant with the null-not-zero readiness contract; `datastoreCount`/`totalStorageMib` fields exist on GlobalSummary (default 0 / mib(0) in emptySummary).
    - format.test.ts: `fmtInt`/`fmtGhzValue`/`fmtPercentValue`/`fmtRatio` return locale-aware strings; non-finite input returns the em-dash sentinel `—` (never 0/N/A); `fmtMemMb` renders `GiB`/`TiB` suffixes (NOT `GB`/`TB` — ADR-0010) with the base-2 division math unchanged; locale is passed in (not hardcoded `fr-FR`).
  </behavior>
  <action>
Create `src/types/estate.ts`: port vsizer `types/cluster.ts` `ClusterAggregate` + `types/global.ts` `GlobalSummary`, then apply the brand retrofit per 02-PATTERNS §estate.ts — brand GHz fields (`physicalGhz`/`consumedGhz`/`availableGhz`/`drReservedGhz` → `GHz`), rename+brand `physicalRamMb`→`physicalRamMib:MiB`, `vramAllocatedMb`→`vramAllocatedMib:MiB`, `physicalCores`/`usablePhysicalCores`/`vcpuAllocated`→`Cores`. DROP `activeMemMb` from both interfaces (VInfoRow lacks the source field — 02-PATTERNS finding #3). Add `export type AccountingMode = 'configured' | 'active' | 'storage-realistic'`, `OsFamily` (re-export from osFamily.ts in Task 2 or declare here), `DatastoreAggregate` (full shape from 02-RESEARCH §perDatastore), `EsxAggregate` (full shape from 02-RESEARCH §perEsx), and `EstateView { globals; clusters: ClusterAggregate[]; hosts: EsxAggregate[]; datastores: DatastoreAggregate[]; vmsByCluster; osBreakdown; accountingMode: AccountingMode; trends: null }` (`trends: null` is Phase-4 forward-compat — RESEARCH Pattern 3; the field exists, typed `TimelinePoint[] | null`, null in Phase 2). Add `datastoreCount: number` + `totalStorageMib: MiB` to `GlobalSummary` and its `emptySummary`. Header JSDoc in the vatlas `src/types/vhost.ts:3-12` tone (explain the vsizer→vatlas rename rationale). Re-export the new types from `src/types/index.ts` (extend the existing `export type {...}` block).

Port `src/utils/format.ts` from vsizer `src/utils/format.ts` (create `src/utils/` — does not exist yet; KISS, follow this location consistently). Functions take bare `number` and format display values (RESEARCH A4: zero-change port) EXCEPT: change `fmtMemMb`'s suffix strings `GB`/`TB`→`GiB`/`TiB` (ADR-0010 — adjust ONLY the suffix literals; the `/1024/1024` base-2 math is already correct, do NOT touch arithmetic); make `locale` a passed parameter (callers will pass `i18n.language` in 02-03) rather than the vsizer hardcoded `'fr-FR'` default — keep `'fr-FR'` as the default param value for the port's own tests. The em-dash sentinel on non-finite input is mandatory and ports unchanged.

Port the six math engines applying the brand retrofit (the ONE worked example is 02-PATTERNS §ghz.ts lines 60-83 — apply mechanically: brand on the boundary, `x as number` for arithmetic, re-wrap with `mib()`/`ghz()`/`cores()` on outputs; `cpuRatio`/`ramRatio` stay bare number):
- `ghz.ts`: port `physicalGhz`/`consumedGhz` ONLY; `import { mhzToGhz } from '@/engines/units'` — do NOT port/re-declare vsizer's own `mhzToGhz` (Pitfall 7 DRY trap; two `mhzToGhz` definitions in src/ is a violation).
- `perCluster.ts`: port `groupByCluster`/`sum`/`mean`/`aggregateHostsPerCluster` verbatim; deviations: `h.memoryMb`→`h.memoryMib`; unwrap `physicalGhzOf`/`consumedGhzOf` GHz returns with `as number` inside `sum(...)`; keep `Math.max/min` on the small per-cluster host `cpuRatio`/`ramRatio` arrays (V8-spread concern is VM-scale only); `ClusterHostStats` moves to `src/types/estate.ts` branded.
- `vinfoMerge.ts`: port `readinessStats` (the reduce-not-spread max loop + `v != null && Number.isFinite(v)` narrowing — ADR-0012, verbatim) and `topReadinessVmsByCluster` verbatim; the ONE logic change (Critical-6) — `groupByCluster(rows, mode)` filter becomes `if (mode !== 'configured' && !row.poweredOn) continue`; thread `mode` through `aggregateVmsPerCluster(vinfo, mode)`; rename `vramAllocatedMb`→`vramAllocatedMib:MiB`; DROP `activeMemMb`/`sumActiveMem` (dead). **`readinessStats` is ALWAYS powered-on-only regardless of mode** — do NOT make it mode-conditional. Export `readinessStats` (perEsx imports it — DRY, Task 2).
- `aggregateClusters.ts`: port verbatim incl. the dormant `stretchedClusters?: ReadonlySet<string>` param + 0.5 DR-reservation + `vcpuPerPcpu`/`computeMhzPerVcpu` + stable sort; deviations: callers pass `stretchedClusters = new Set()`; thread `mode` into `aggregateVmsPerCluster`; drop `activeMemMb` refs; `ClusterAggregate` resolves to `@/types/estate`.
- `globals.ts`: port `emptySummary` + `aggregateGlobals` verbatim incl. capacity-weighted DR-aware means + reportingReadiness null-not-zero; ADD `datastoreCount`/`totalStorageMib` (sourced from perDatastore output in Task 2's estateView wiring — the field is summed there; globals.ts just carries the field on GlobalSummary/emptySummary); drop `activeMemMb`.
- `contention.ts`: copy `CONTENTION_THRESHOLDS = { warning: 5, serious: 10 } as const` + `TOP_N_DEFAULT = 10` verbatim; strip the PPTX doc lines (no PPTX in Phase 2); keep the ADR-0003 status-not-verdict note.

Engines stay Zod-free, no React/DOM/Zustand imports (PROJECT.md binding). Write RED tests first (port vsizer's `*.test.ts` regression suites, wrap all numeric fixtures in brand constructors `mib()`/`mhz()`/`cores()`/`sockets()`, adjust field names) — confirm FAIL (modules absent) before GREEN. Include the ROADMAP-mandated tests: the 62.4 GHz `physicalGhz` composition (ghz.test.ts) and the explicit `vcpuPerPcpu`-uses-cores assertion (aggregateClusters.test.ts). No fenced code in this action — port source is in the vsizer repo, brand pattern in 02-PATTERNS.
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run src/engines/aggregation/ghz.test.ts src/engines/aggregation/perCluster.test.ts src/engines/aggregation/vinfoMerge.test.ts src/engines/aggregation/aggregateClusters.test.ts src/engines/aggregation/globals.test.ts src/engines/aggregation/contention.test.ts src/utils/format.test.ts && grep -q "from '@/engines/units'" src/engines/aggregation/ghz.ts && [ "$(grep -rc 'const mhzToGhz\|function mhzToGhz' src/engines/aggregation/ 2>/dev/null | grep -v ':0$' | wc -l | tr -d ' ')" = "0" ] && grep -q "mode !== 'configured'" src/engines/aggregation/vinfoMerge.ts && ! grep -rn "activeMemMb\|sumActiveMem" src/engines/aggregation/ src/types/estate.ts && grep -q "GiB\|TiB" src/utils/format.ts && ! grep -qE "'GB'|\"GB\"|'TB'|\"TB\"" src/utils/format.ts</automated>
  </verify>
  <done>estate.ts ports the branded ClusterAggregate/GlobalSummary (+ AccountingMode/EstateView/DatastoreAggregate/EsxAggregate, activeMemMb dropped, trends:null); the six math engines port with the mechanical brand retrofit; ghz.ts imports mhzToGhz from units (zero re-declaration); vinfoMerge has the mode-driven filter with readinessStats mode-independent; format.ts ported with GiB/TiB suffixes (base-2 math intact); RED→GREEN observed; 62.4 GHz + vcpuPerPcpu-uses-cores tests pass; typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: New engines (osFamily/perDatastore/perEsx) + estateView assembler + barrel + useEstateView</name>
  <files>src/engines/aggregation/osFamily.ts, src/engines/aggregation/perDatastore.ts, src/engines/aggregation/perEsx.ts, src/engines/aggregation/estateView.ts, src/engines/aggregation/index.ts, src/engines/aggregation/osFamily.test.ts, src/engines/aggregation/perDatastore.test.ts, src/engines/aggregation/perEsx.test.ts, src/engines/aggregation/estateView.test.ts, src/hooks/useEstateView.ts, src/hooks/useEstateView.test.ts</files>
  <behavior>
    - osFamily.test.ts: `classifyOsFamily(osConfig, osTools)` returns `'windows'` for Microsoft/Windows strings, `'linux'` for the rhel/ubuntu/suse/... bank, `'other'` otherwise; prefers `osConfig`, falls back to `osTools`; `other` is a real returned bucket (never throws, never empty-string-special-cased away).
    - perDatastore.test.ts: rows sharing a `naa` collapse to one `DatastoreAggregate` keyed `naa ?? name`; `capacityMib`/`freeMib` taken from the FIRST row in the group (NEVER summed — two clusters sharing a LUN must NOT double capacity, Pitfall 5); `usedMib = capacity - free`; `sharedDuplicateCount` = group size (>1 ⇒ shared LUN); `naa === null` falls back to `name` key.
    - perEsx.test.ts: hosts grouped by `hostName`, VMs attached via `VInfoRow.host === hostName`; `vmCount`/`vcpuAllocated` honor the accounting `mode`; CPU Ready stats reuse the SAME exported `readinessStats` from vinfoMerge (DRY — no copied reduce loop); `cores` is physical cores (Moderate-4).
    - estateView.test.ts: `buildEstateView(canarySnapshot, 'active')` returns a populated EstateView (clusters[], globals, hosts[], datastores[], osBreakdown global+per-cluster, accountingMode echoed, trends:null); on the ~50/50 powered-on fixture the three modes (`configured`/`active`/`storage-realistic`) produce THREE DISTINCT global totals (Critical-6, ROADMAP success #2/#6); `EMPTY_VIEW` is a frozen constant with the empty-but-valid shape.
    - useEstateView.test.ts: seeding the store then `renderHook(() => useEstateView('active'))` returns a stable EstateView reference across re-render with unchanged (snapshot, mode), a NEW reference when mode flips, and `EMPTY_VIEW` when no snapshot is active.
  </behavior>
  <action>
Create `src/engines/aggregation/osFamily.ts` — the verbatim-ready classifier from 02-PATTERNS §osFamily.ts (pure, no deps, Zod-free): `export type OsFamily = 'windows'|'linux'|'other'`; `classifyOsFamily(osConfig, osTools)` lowercases `osConfig || osTools`, regex bank `windows|microsoft` → windows, `linux|rhel|red hat|centos|ubuntu|debian|suse|sles|oracle|rocky|alma|photon|coreos` → linux, else other. NOT the Phase-5 EOS normalizer — 3-way only; `other` is a real visible bucket.

Create `src/engines/aggregation/perDatastore.ts` (Moderate-11) — reuse vsizer `perCluster.ts:41-50` `groupByCluster` Map-accumulate SHAPE, key on `naa ?? name`. Produce `DatastoreAggregate[]` per the 02-RESEARCH §perDatastore interface: within a key-group take `capacityMib`/`freeMib`/`provisionedMib` from the FIRST row (shared LUN identical across cluster views — NEVER sum capacity, Pitfall 5), `usedMib = capacity - free` (brand: unwrap → compute → `mib()`), `usedRatio` (0 when capacity 0), `overProvisionRatio = provisioned/capacity`, `sharedDuplicateCount` = group size. The Moderate-11 `provisioned ≤ capacity × 10` check is a `// TODO(diagnostics, Phase 3+)` comment ONLY — do NOT build a panel (YAGNI).

Create `src/engines/aggregation/perEsx.ts` — group `VHostRow` by `hostName`, attach VMs via `VInfoRow.host === hostName`. Produce `EsxAggregate[]` per 02-RESEARCH §perEsx interface (branded `Sockets`/`Cores`/`MHz`/`GHz`/`MiB`); `vmCount`/`vcpuAllocated` honor the same `mode` param as vinfoMerge; CPU Ready fields MUST call the SAME `readinessStats` exported from `vinfoMerge.ts` (DRY mandate — import it, do NOT copy-paste the reduce loop); `physicalGhz` via `ghz.physicalGhz(speedMhz, cores)`; `cores` is physical cores (Moderate-4 — no threads field exists).

Create `src/engines/aggregation/estateView.ts` — pure `buildEstateView(snapshot: Snapshot, mode: AccountingMode): EstateView` composing: `aggregateClusters({ vinfo, vhost, stretchedClusters: new Set() })` threading `mode`, `aggregateGlobals(...)`, `aggregateHostsPerCluster`, `perDatastore(vdatastore)`, `perEsx(vhost, vinfo, mode)`, and `classifyOsFamily` reduced per-VM to global + per-cluster `osBreakdown` (Windows/Linux/Other counts; `other` always present even at 0). Wire `datastoreCount` = distinct datastore keys count and `totalStorageMib` = sum of per-key `capacityMib` (the dedupe already prevents double-count) into `globals`. Return `EstateView` with `accountingMode: mode` echoed and `trends: null`. Export a frozen `EMPTY_VIEW` constant (model on vsizer `globals.ts` `emptySummary` frozen-constant idiom) — the valid empty shape `useEstateView` returns when snapshot is null. No React/Zustand/Zod.

Create `src/engines/aggregation/index.ts` barrel re-exporting `buildEstateView`, `EMPTY_VIEW`, `classifyOsFamily`, `CONTENTION_THRESHOLDS`, `readinessStats`, and the aggregate types (port the vsizer index.ts barrel shape).

Create `src/hooks/useEstateView.ts` EXACTLY per the `<interfaces>` target (the shipped SnapshotListSidebar useMemo-off-store idiom): read `useSnapshotStore(selectActiveSnapshot)`, `useMemo(() => snapshot ? buildEstateView(snapshot, mode) : EMPTY_VIEW, [snapshot, mode])`. This is the ONLY `useMemo` in the entire project — confirm no other `useMemo` is introduced anywhere in this plan.

RED first (this task is `tdd="true"`): write the four `*.test.ts` + `useEstateView.test.ts` (use `@testing-library/react` `renderHook`, seed the store via `useSnapshotStore.getState().addSnapshot(...)` per the e2e-smoke pattern; for estateView's three-modes-distinct test use the canary or a hand-built ~50/50 powered-on snapshot fixture). Confirm FAIL before GREEN. After GREEN, run the full engine coverage gate. No fenced code here — interfaces are in 02-RESEARCH/02-PATTERNS and the `<interfaces>` block.
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run src/engines/aggregation/ src/hooks/useEstateView.test.ts && npx vitest run --coverage --coverage.include='src/engines/aggregation/**' --coverage.thresholds.statements=75 --coverage.thresholds.branches=75 --coverage.thresholds.functions=75 --coverage.thresholds.lines=75 && grep -q "naa ?? name\|naa\s*??\s*name" src/engines/aggregation/perDatastore.ts && grep -q "import.*readinessStats.*from.*vinfoMerge\|from './vinfoMerge'" src/engines/aggregation/perEsx.ts && grep -q "trends: null\|trends:null" src/engines/aggregation/estateView.ts && [ "$(grep -rl 'useMemo' src/ --include='*.ts' --include='*.tsx' | grep -v test | wc -l | tr -d ' ')" = "1" ] && grep -q "useMemo" src/hooks/useEstateView.ts</automated>
  </verify>
  <done>osFamily/perDatastore (NAA-deduped, first-row capacity)/perEsx (DRY readinessStats reuse) created; buildEstateView composes the full EstateView with trends:null and three distinct accounting-mode totals proven; barrel exports the public surface; useEstateView is the SOLE useMemo in src/ (verified count == 1); engine coverage ≥75% on all four metrics; RED→GREEN observed; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validated Snapshot rows → engines | Rows are already Zod-validated + branded at the Phase-1 parser boundary; engines consume them directly and stay Zod-free (re-validation forbidden — 01-RESEARCH A8) |
| engine output → memoized view | `buildEstateView` output is the single source feeding every dashboard/export; wrong numbers are the real integrity threat for an operator tool |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information disclosure | engine error paths | mitigate | Engines are pure transforms with no error strings that interpolate VM names/hostnames; any thrown error carries message/name only (consumed by 02-03's Critical-2 inline error). No `console`, no network (Phase-1 fetch guard + CSP still active) |
| T-02-06 | Tampering (report integrity) | aggregation math correctness | mitigate | Ported vsizer ADRs (0010 MiB, 0011 capacity-weighted, 0012 CPU Ready arithmetic-mean/reduce-max, 0007 stretched) preserved verbatim; branded units make MiB/MHz unit bugs unrepresentable; ≥75% engine coverage gate incl. the 62.4 GHz, vcpuPerPcpu-uses-cores, NAA-dedupe, and three-modes-distinct regression tests |
| T-02-07 | Tampering | NAA datastore double-count (Moderate-11) | mitigate | `perDatastore` keys on `naa ?? name`, capacity taken from first row in a group never summed; `sharedDuplicateCount` surfaced; regression test asserts a shared LUN across two clusters does not double total capacity |
| T-02-08 | Information disclosure | engines stay Zod-free | accept | V5 input-validation already enforced at the parser boundary (Phase 1); engines re-validating would be redundant and is explicitly forbidden — accepted per 01-RESEARCH A8 |
</threat_model>

<verification>
- `npm run typecheck` (app + tsconfig.test.json) exit 0
- `npm run lint` clean on all new files
- `npx vitest run src/engines/aggregation/ src/hooks/useEstateView.test.ts src/utils/format.test.ts` all green
- `npx vitest run --coverage --coverage.include='src/engines/aggregation/**'` ≥75% statements/branches/functions/lines (ROADMAP success #6 gate; Phase-1 parser hit 93.5% for reference)
- ROADMAP-mandated math tests present and green: 62.4 GHz `physicalGhz` composition; `vcpuPerPcpu` uses `cores` not threads (explicit assertion); three-accounting-modes-distinct-totals on a ~50/50 powered-on fixture
- DRY/FP gates: `grep "from '@/engines/units'"` in ghz.ts AND zero `mhzToGhz` re-declaration in aggregation/; exactly ONE `useMemo` in non-test src/ (it is in useEstateView.ts); zero `activeMemMb`/`sumActiveMem` in aggregation/ + estate.ts; zero React/Zustand/Zod imports in `src/engines/aggregation/**` and `src/utils/format.ts`
- `npm run check:supply-chain` → OK (no new deps this plan)
</verification>

<success_criteria>
Mirrors ROADMAP Phase-2 success criteria #2, #3, #6 (engine half):
- (#2) Toggling Configured / Active / Storage-realistic yields three distinct estate totals — proven by the three-modes-distinct test; the powered-off-VM trap is surfaced by the mode-driven `vinfoMerge` filter (Critical-6).
- (#3, engine) `osBreakdown` provides Windows / Linux / Other counts at both global and per-cluster level (`other` always a visible bucket) — consumed by 02-03's donut.
- (#6) `engines/aggregation/` ships ≥75% Vitest coverage including the hyperthreads-vs-physical-cores test and the 2-socket × 12-core × 2600 MHz = 62.4 GHz unit test.
- (DSH-01/02/03/05 engine) per-cluster physical/consumed GHz, mean CPU%/RAM%, vCPU-per-physical-core, ESX count, datastore totals (global, NAA-deduped), and CPU Ready (mean/max/count>5%, absence ≠ 0) all available through `buildEstateView` / `useEstateView`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-aggregation-global-dashboard/02-02-SUMMARY.md`. Record: final engine coverage numbers (all four metrics), the EstateView/ClusterAggregate/EsxAggregate/DatastoreAggregate/GlobalSummary field layouts as the contract 02-03 consumes, the exported `readinessStats` signature (perEsx + future phases reuse it), the A1 decision as implemented (global datastoreCount only; no per-cluster datastore field), and the canary `buildEstateView` reference values for 02-03's integration test.
</output>
