---
milestone: "1.0"
audited: 2026-05-18
status: gaps_found
scores:
  requirements_in_app: 93/95      # all but PLN-03/PLN-04 functional in the running app
  requirements_in_deliverable: 70/95  # reach the HTML report / PPTX deck ("report is the product" DoD)
  phases: 10/10                   # all shipped & deployed (v1.0.0 live)
  integration: 10/11              # one phase-boundary partial (P9→export)
  flows: 5/5                      # all on-screen E2E flows complete
verification_artifacts: 0/10      # workflow.verifier:false — no VERIFICATION.md by config choice
gaps:
  requirements:
    - id: "STG-01..05, NET-01..05, ALR-01..05, DTL-01..03, VSR-01..05"
      status: "partial"
      phase: "09-storage-network-detailed-views-threshold-alerting"
      claimed_by_plans: ["09-01..09-05-SUMMARY.md (requirements_completed frontmatter lists all 23)"]
      completed_by_plans: ["all P9 plans — on-screen WIRED (StorageView/NetworkView consume view.storage/network/flags/vsan/*Detail)"]
      verification_status: "missing (no VERIFICATION.md; validated via 09-UAT + STR-04 real-file gate + live deploy)"
      evidence: "Integration checker F-2: renderReport.tsx and pptx/builder.ts read NO P9 view-model; chartBundle.ts builds no storage/network chart. P9 intelligence is fully on-screen but absent from both shareable artifacts. Core value is 'the report is the product' → DoD gap."
    - id: "PLN-03, PLN-04"
      status: "unsatisfied"
      phase: "06-allocation-dr-re-derived"
      claimed_by_plans: ["06-02-SUMMARY.md (PLN-* planning surface)"]
      completed_by_plans: ["none — view.plannedView has zero consumers"]
      verification_status: "missing"
      evidence: "Integration checker F-1: EstateView.plannedView (types/estate.ts:494) computed every buildEstateView pass, zero readers in non-test src/ (PlanningView/PlannedRatiosControl write ratios only; not in any export). Planned-ratio re-aggregated estate surface never reaches screen or report. (view.plannedDrSim — the planned-DR half — IS wired; only the planned-estate half is dead.)"
  integration:
    - boundary: "P9 (storage/network/threshold) → P10 (export)"
      issue: "On-screen path and export path are each internally sound and use the same EstateView, but no HTML section / PPTX slide / chartBundle entry reads view.storage|network|flags|vsan|datastoreDetail|vmDetail. Cross-phase coverage gap, not broken wiring."
  flows: []   # all 5 on-screen E2E flows COMPLETE (drop→dashboard, navigate, threshold→flags, HTML export, PPTX export)
tech_debt:
  - phase: "10-html-pptx-exports-deploy"
    items:
      - "HTML report + PPTX deck omit every Phase 9 surface (storage by cluster/ESX/VM/datastore, network rollup, threshold flags, vSAN relink, datastore/VM detail). Largest material gap — product decision required (F-2)."
  - phase: "06-allocation-dr-re-derived"
    items:
      - "view.plannedView is a dead projection — recomputed every pass, consumed nowhere. Render it in PlanningView or remove from the EstateView contract (DRY/KISS) (F-1)."
  - phase: "01-foundation-invariants"
    items:
      - "Stale doc-comment UploadZone.tsx:20-22 references a removed Plan-01 console.warn stub; wiring is correct, comment is drift (F-3)."
  - phase: "milestone-wide"
    items:
      - "workflow.verifier:false ⇒ 0/10 phases have a GSD VERIFICATION.md. Substantive validation exists via UAT (05/07/09), the STR-04 real-file relink gate, CodeRabbit PR reviews #1–#4, and a green production deploy of v1.0.0 — but the formal GSD verification artifact is absent for every phase by config choice."
      - "REQUIREMENTS.md traceability is stale: Phases 4/5/9/10 rows still 'Pending' despite shipping (~46 rows); HTM/PPT/DEP rows mislabeled 'Phase 7' (they are Phase 10). SUMMARY requirements_completed frontmatter is empty for P3 (INV-*) and P6 (PLN/DRX) though both are traceability-Complete and integration-WIRED."
---

# Milestone v1.0 — Audit Report

**Audited:** 2026-05-18 · **Status:** `gaps_found` · **Scope:** Phases 1–10 (all shipped; v1.0.0 released & deployed to fjacquet.github.io/vatlas/)

## Summary

The v1.0 product is **built, deployed, and functional** — every on-screen end-to-end flow completes, the parser→store→`useEstateView`→views→export spine is correctly and consistently wired (single-`useMemo` invariant intact, privacy invariant intact), and 93/95 requirements are functional in the running app. The audit returns `gaps_found` for two substantive reasons plus a process caveat:

1. **F-2 — Phase 9 absent from the deliverable (material).** All 23 P9 requirements (storage-by-X, network rollup, threshold alerting, vSAN relink, datastore/VM detail) render on screen but reach **neither the HTML report nor the PPTX deck**. The project's stated core value is *"the report is the product"* — an entire shipped phase's analytics is invisible in the shareable artifact. This is a definition-of-done gap, not stale bookkeeping.
2. **F-1 — PLN-03/PLN-04 dead view-model.** `view.plannedView` is computed on every pass and consumed nowhere (screen or export). The planned-ratio re-aggregated estate surface was specified but never surfaced.
3. **Process caveat.** `workflow.verifier:false` means no phase has a GSD `VERIFICATION.md`; validation was done via UAT, the STR-04 real-file gate, CodeRabbit, and live deploy. Traceability bookkeeping (REQUIREMENTS.md) is stale for Phases 4/5/9/10.

## Phase Status

| Phase | Shipped | On-screen | In deliverable | VERIFICATION.md |
|------|---------|-----------|----------------|-----------------|
| 1 Foundation/Invariants | ✓ | ✓ | n/a (infra) | missing (verifier:false) |
| 2 Aggregation/Dashboard | ✓ | ✓ | ✓ | missing |
| 3 Inventory Navigation | ✓ | ✓ | ✓ (PPTX inventory slide) | missing |
| 4 Multi-vCenter Merge | ✓ | ✓ | ✓ | missing |
| 5 Cluster/Host/ESX | ✓ | ✓ | ✓ | missing |
| 6 Allocation/DR | ✓ | ✓ (PLN-01/02, DRX) | ✓ (drSim) | missing — **PLN-03/04 dead (F-1)** |
| 7 EOS Forecast | ✓ | ✓ | ✓ | missing |
| 8 In-Session Trends | ✓ | ✓ | ✓ (D-09 honored) | missing |
| 9 Storage/Network/Thresholds | ✓ | ✓ | **✗ (F-2)** | missing |
| 10 HTML+PPTX+Deploy | ✓ | ✓ | ✓ (but omits P9) | missing |

## Requirements Coverage (3-source cross-reference)

- **Source A — REQUIREMENTS.md traceability:** 95 rows. Stale: P4/P5/P9/P10 still "Pending" (~46 rows) despite shipping; P10 rows mislabeled "Phase 7".
- **Source B — phase VERIFICATION.md tables:** none exist (0/10, `workflow.verifier:false`).
- **Source C — SUMMARY `requirements_completed` frontmatter:** P1/P2/P4/P5/P7/P8/P9/P10 list their reqs; P3 (INV-*) and P6 (PLN/DRX) frontmatter empty though traceability-Complete and integration-WIRED.
- **Integration checker (independent 4th trace):** confirms wiring sound except F-1/F-2.

**Determination:** with no VERIFICATION.md the strict status matrix would mark everything "partial (verification gap)"; the independent integration trace + UAT + live deploy upgrade the on-screen-wired set to **functionally satisfied (93/95)**. The two true gaps are PLN-03/04 (F-1, unsatisfied) and the P9-in-deliverable set (F-2, partial — satisfied in-app, absent from the shareable report/deck).

## Recommended Closure

Two options, your call (see routing below):

- **Accept as v1.0 + close inline:** mark P9 export-coverage and PLN-03/04 as documented tech-debt / v1.1 scope (the in-app product is complete and deployed), reconcile the stale traceability, then `/gsd-complete-milestone 1.0`.
- **Close the gaps first:** insert a closure phase for F-2 (add P9 storage/network/threshold sections to `renderReport.tsx` + `pptx/builder.ts` + `chartBundle.ts`) and decide F-1 (render or remove `plannedView`), via `/gsd-phase --insert` → discuss → plan → execute, then re-audit.
