# Dependencies & build output
node_modules
dist
dist-ssr
coverage

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Caches & local overrides
tmp/
*.local
# tsc -b (composite projects) incremental build cache
*.tsbuildinfo

# Environment / secrets
.env
.env.*
!.env.example

# RTK tooling
.rtk/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Tooling scratch dirs (Playwright MCP captures, Serena cache) — never commit
.playwright-mcp/
.serena/

# SDD orchestration scratch (briefs, reports, review diffs, ledger) — not part of the project
.superpowers/

# Privacy invariant (PAR-05): NEVER commit dataset bytes. RVTools/Proxmox
# report spreadsheets (real drops AND the synthetic stress fixture) are loaded
# at runtime/test time only — never tracked. Regenerate the synthetic fixture
# with `npm run generate-inventory-10k`. Force-add a verified non-dataset
# spreadsheet with `git add -f` if ever needed.
*.xlsx
*.xls
*.csv
# Proxmox report bundle side-car (network topology SVG export — dataset-derived)
src/engines/parser/__fixtures__/network-diagram.svg
